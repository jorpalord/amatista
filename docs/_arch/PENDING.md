# PENDING.md — Backlog de Fases

> Tareas identificadas pero no ejecutadas todavía. El arquitecto las prioriza.

## Sin priorizar, prioridad baja — LSP para Vue (`@vue/language-server`) descartado tal cual está

Investigado real (`docs/_arch/verify_6_lsp_servers.md`), junto con YAML/ESLint/Terraform/Lua/Bash (esos 5 sí se agregaron, ver `docs/_arch/CONTRACT.md` → "Fix real — workspace/configuration + pull-diagnostics..."). El handshake LSP de `@vue/language-server` funciona perfecto (`initialize`/`initialized`/`didOpen`), pero el diagnóstico real sobre errores de tipo/sintaxis en `.vue` depende de un protocolo propietario no-LSP (`tsserver/request`/`tsserver/response`, ~17 comandos custom `_vue:*`) donde el servidor espera que el CLIENTE corra su propio `tsserver` con `@vue/typescript-plugin` cargado y le responda — confirmado real en 6 intentos distintos (2 archivos de prueba, 2 estrategias de bridge fabricado a mano, 1 archivo `.ts` puro de control) que sin eso, `publishDiagnostics` llega real pero con `diagnostics:[]` siempre, incluso para un error de tipo TS puro sin nada de Vue de por medio.

**Camino real si se decide construirlo después**: implementar el bridge completo (`tsserver` real + proxy de los ~17 comandos `_vue:*`) — trabajo de integración serio y específico de este servidor, no reusable para ningún otro (el mecanismo nuevo de `configResponses`/pull-diagnostics de esta fase no ayuda acá, el problema de Vue es otro). Alternativa no verificada: una versión pre-v2 de `@vue/language-server` ("take-over mode", sin bridge) podría no tener este problema — quedaría pendiente de investigar si hay interés real en soporte Vue.

## RESUELTO — 10 candidatos LSP investigados (Astro, Clojure, Dart, Deno, Gleam, Nix, Prisma, Svelte, Typst, Zig): 9 integrados, 1 descartado sin camino real (Nix)

Investigado real (`docs/_arch/verify_lsp_10_remaining.md`, misma metodología en tandas que Vue/YAML/ESLint/Terraform/Lua/Bash) e integrado real (`docs/_arch/CONTRACT.md` → "Fix real — 8 de 10 lenguajes LSP nuevos integrados" + "Feature real — LSP para Deno con detección condicional por `deno.json`"). **Cierra por completo esta investigación** — no queda ningún candidato de esta tanda pendiente de decisión.

- **8 integrados a `LANGUAGE_SERVERS`**: Dart, Gleam, Clojure, Typst, Zig (sin ajustes de esquema — binarios externos por PATH, mismo patrón que rust-analyzer/gopls/clangd/terraform-ls/lua-language-server), Svelte (bundleado vía npm, sin ajustes reales), Astro (bundleado vía npm, necesitó un campo nuevo real en el esquema, `initializationOptions`, para el `typescript.tsdk` obligatorio que exige Volar), Prisma (bundleado vía npm, necesitó `configResponses:{prisma:{}}` — sin esto crashea el proceso real al abrir el primer archivo, confirmado). Los 8 con diagnóstico real verificado contra un error puesto a propósito, `LspClient`/`languageServerConfigFor()` reales, cero mocks.
- **Clojure** particularmente: quedó "LISTO CON AJUSTES" en la investigación por el bug de enrutamiento de `handleChunk()` que disparaba (ver el fix separado de hoy, commit `1c66b27`, y la entrada de HISTORY.md correspondiente) — con ese fix ya en el repo, se integró sin ningún ajuste adicional propio.
- **RESUELTO — Deno agregado con detección condicional por `deno.json`**: cerraba como "descartado, no agregado" por el bloqueo de arbitraje ya descrito (colisión de extensión `.ts`/`.js` con `typescript-language-server`/`eslint-language-server`) — resuelto real, ver `docs/_arch/CONTRACT.md` → "Feature real — LSP para Deno con detección condicional por `deno.json` (cierra el último candidato LSP pendiente)". Era el último de los 10 candidatos de esta investigación sin resolución definitiva — con esto, la investigación queda 100% cerrada (9 integrados, 1 descartado sin camino real — Nix).
- **Nix — descartado, NO agregado**: `nixd` y `nil` (los 2 language servers reales de Nix) no tienen NINGÚN binario publicado para Windows — 46 releases combinadas revisadas vía GitHub API, `assets:[]` en las 46. Ambos dependen funcionalmente del propio Nix (POSIX-only, requiere WSL en Windows). Sin build real que integrar — no hay camino futuro sin WSL/Docker, fuera de alcance explícito de Amatista.

## RESUELTO — `removeProjectRoot`/`projects:removeRoot` comparaba pertenencia de carpeta con `startsWith` (substring, no ruta real)

Hallazgo de la 3ra revisión externa, confirmado real e implementado (`docs/_arch/CONTRACT.md` → "Fix real — `isWithinFolder()`..."). `ipc-projects-workspace.ts:60` usaba `session.activeWorkspace.startsWith(root.path)` — comparación de **string**, no de pertenencia real de ruta. Reproducido real ANTES del fix: borrar el `projectRoot` `...\Proyecto` desconectaba por error una sesión conectada a `...\ProyectoExtra`, carpeta hermana distinta que solo comparte el prefijo de texto (`collision_desconectada: true`, el bug tal cual se especulaba). Confirmado además un segundo bug en el mismo `startsWith`, en sentido inverso: si `activeWorkspace` llegaba con un casing distinto (mismo directorio real de Windows) al de `root.path`, la comparación daba **falso negativo** (`casemixed_desconectada: false` antes del fix) — no desconectaba una sesión que sí correspondía a la carpeta borrada.

**Fix real**: `isWithinFolder(parent, candidate)` nueva, exportada en `runtime-state.ts` junto a `assertInsideWorkspace()` (que ahora la reusa, cero cambio de comportamiento ahí) — `path.relative()` + chequeo de que no empiece en `..` ni sea ruta absoluta, SIN volver a llamar `realpathSync()` (root.path/activeWorkspace ya vienen canonicalizados una vez al guardarse; forzar un `realpathSync` nuevo en la comparación rompería el caso legítimo de una carpeta ya borrada del disco). Confirmado real que `path.win32.relative()` (el que corre en runtime en Windows) ya es case-insensitive por sí solo — no hizo falta normalizar a lowercase a mano.

**Verificado real, 5/5 casos** (código de producción real vía `registerProjectsAndWorkspaceIpc()`/`sessionRegistry` reales, cero mocks del código bajo prueba, mismo patrón de harness que Hallazgo 1/2/3): legítima (subcarpeta real) sigue desconectando ✔ sin regresión; colisión de prefijo (`ProyectoExtra`) YA NO desconecta ✔ (bug resuelto); no relacionada sigue sin verse afectada ✔; raíz exacta sigue desconectando ✔; casing mezclado real de Windows para la MISMA carpeta ahora SÍ desconecta ✔ (segundo bug también resuelto). `npm run typecheck`/`npm run build` limpios.

## RESUELTO (parcial) — verificación end-to-end de `web_search`/`web_fetch` con una key real de Tavily

Implementación real completa (`docs/_arch/CONTRACT.md` → "Fix real — `web_search`/`web_fetch` reales vía Tavily", `docs/_arch/HISTORY.md`): credencial (`AppSettings.integrations.tavily.apiKey`, cifrada igual que `provider.apiKey`), las 2 tools reales, gating real en `toolCatalog()`, aprobación incondicional (mismo precedente que `generate_image`), y manejo de error 401/403 verificado real y en vivo contra `api.tavily.com` con una key deliberadamente inválida (nunca una credencial real obtenida por el agente).

**Ítem 2 (turno real completo) ya CERRADO**: confirmado real, desde un panel real conectado (Foundry/`gpt-5.6-sol`, runtime API — se confirmó antes en esta misma sesión que los runtimes CLI no pueden usar estas tools), con la key real del usuario que el agente nunca tocó ni vio: `web_search x2` + `web_fetch x2` reales en un solo turno, título+URL real citados (`https://openai.com/index/gpt-5-6`), contenido real de la página traído y resumido por el modelo, con el modelo aclarando por su cuenta que era la publicación oficial del fabricante (no una evaluación independiente) — sin fabricar nada cuando un intento anterior, con una query anclada al día exacto, no encontró resultados relevantes (el modelo dijo explícitamente que no podía citar sin inventar, en vez de alucinar una fuente).

**Ítem 1 (camino de error `429`) sigue sin disparar en vivo** — el "excede los límites" que el usuario reportó en el camino resultó ser un límite del lado de Foundry/el modelo (no un 429 de Tavily; resuelto por el usuario agregando `gpt-5.6-sol` como alternativa), así que no cuenta como verificación real de esa rama. Queda pendiente de que la cuenta real de Tavily del usuario agote su cuota de uso en el curso normal de uso — no hay plan de forzarlo a propósito.

## RESUELTO — `callCompactionModel()` no tenía rama propia para `openai-chat`

Hallazgo lateral real, confirmado en `docs/_arch/verify_claude_cli_compaction_design.md` (Tarea 3) durante la investigación de la compactación de respaldo para claude-cli/antigravity-cli — **separado del fix implementado ahí a propósito, cerrado aparte después**. `callCompactionModel()` (`compaction-engine.ts`) tiene ramas explícitas para `runtime==='foundry'` y `runtime==='gemini-api'`, pero todo lo demás (incluido `runtime==='openai-chat'`, OpenRouter/Chat-Completions) caía en el fallback final, que asume el formato de la Anthropic Messages API (`anthropicMessagesUrl()`, headers `x-api-key`/`anthropic-version`). Ya afectaba a cualquier usuario que configurara un modelo OpenRouter como `compactionProviderId`/`compactionModelId` dedicado — la llamada de compactación real fallaba contra el endpoint equivocado.

**Resuelto real** (`docs/_arch/verify_compaction_openai_chat_branch.md`, `docs/_arch/CONTRACT.md` → "Fix real — rama `openai-chat` en `callCompactionModel()`"): investigación previa confirmó real, con un servidor Chat-Completions real y local, que el bug producía un `404` real (`"Compactacion Claude API fallo 404: ..."`, mensaje engañoso) atrapado en silencio por el `try/catch` de fire-and-forget — compactación que nunca compactaba, sin ningún aviso visible. Fix: rama nueva `runtime==='openai-chat'`, mismo patrón exacto que `foundry`/`gemini-api` — `openAiChatCompletionsUrl()` (ya exportada) para la URL, `Authorization: Bearer` real, body `messages`/`role`-`content` real (formato Chat Completions, no Anthropic), `resolveMaxOutputTokens(_, 'openai')` (ya soportaba ese kind). La detección `o1/o3/o4/gpt-5.x → max_completion_tokens` se duplicó (2 líneas, función pura) en vez de exportar el método privado equivalente de `ApiAgentRuntime` — mismo criterio ya establecido en este codebase para no acoplar módulos por algo tan chico. Verificado real, mismo escenario exacto que reprodujo el bug: la request ahora sale a `/v1/chat/completions` con `Authorization: Bearer` real, el servidor real responde `200`, y el resumen queda persistido real (`getChatSummaryState()` ya no es `null`).

## NO APLICA — `run_command` fuera del fix de TOCTOU de `read_file`/`write_file`/`apply_patch`

Fix real de TOCTOU implementado (`docs/_arch/verify_toctou_fix_design.md`, `docs/_arch/CONTRACT.md` → "Fix real — TOCTOU real de `write_file`/`apply_patch` cerrado"): `read_file` registra un hash por sesión de lo que el modelo vio; `write_file`/`apply_patch` lo auditan antes de escribir. **`run_command` quedó fuera a propósito** — también toca archivos reales del workspace (puede correr cualquier comando de shell, incluida edición de archivos por vías ajenas a `write_file`/`apply_patch`) sin registrar ni consultar ningún hash de sesión. Pregunta real sin resolver: ¿debería tener el mismo chequeo de staleness?

**Investigado real, cerrado como "no aplica"** (`docs/_arch/verify_run_command_revert_file_staleness.md`, Tarea 1): el mecanismo de hash-por-sesión funciona porque `write_file`/`apply_patch` reciben un path explícito + contenido completo — permite saber DE ANTEMANO qué archivo hashear. `run_command` solo recibe un string de shell opaco (`runShellCommand()`, `tool-registry.ts`) — ni tool-registry.ts ni ningún llamador parsean qué archivos toca (podría ser 0, 1 o N, desconocidos de antemano; parsear shell arbitrario para derivarlos es inviable). La única forma de aplicar el mismo mecanismo sería un diff completo del workspace antes/después — ya no es TOCTOU (prevenir una escritura basada en creencia vieja) sino auditoría post-hoc costosa que no puede prevenir nada, porque `exec()` ya corrió antes de que exista cualquier comparación posible. Además, en `workspace-write` el usuario ya ve el comando literal vía `confirm()` antes de que corra (gate de intención real, categoría distinta de "staleness"). Cerrado sin cambio de código — categoría de riesgo estructuralmente distinta, no un caso pendiente de este mecanismo.

## RESUELTO — `revert_file` sin chequeo de staleness (gap real, mismo bug que TOCTOU de `write_file`/`apply_patch`)

Fix real de TOCTOU implementado (`docs/_arch/verify_toctou_fix_design.md`): `write_file`/`apply_patch` re-leen el disco después de `resolveApproval()` y comparan contra el hash tomado antes de pedir la aprobación. **`revert_file` quedó fuera** — restaura una versión vieja del VCS oculto sobre el archivo real, sin ningún re-chequeo post-aprobación.

**Investigado real** (`docs/_arch/verify_run_command_revert_file_staleness.md`, Tarea 2): se confirmó que "restaurar contenido viejo a propósito" (la función de la tool) y "pisar en silencio un cambio nuevo de OTRA sesión hecho mientras la aprobación estaba pendiente" son dos cosas distintas — la segunda SÍ es el mismo bug de TOCTOU ya cerrado para `write_file`/`apply_patch` (`d3a8fe4`), simplemente nunca portado a `revert_file`. Confirmado en el código real: `revert_file` leía `currentContent` una sola vez, armaba el diff para `resolveApproval()`, y escribía directo tras la aprobación sin volver a leer el disco.

**Fix real implementado**: `hashFileContent(currentContent)` capturado en el mismo instante en que ya se leía `currentContent` (antes de `resolveApproval()`); justo antes de `writeFileSync()`, re-lectura real del disco + comparación de hash — si no coincide, rechaza con el mismo estilo de mensaje que `write_file`/`apply_patch` ("cambió en disco... volvé a llamar list_file_history y revert_file de nuevo"). NO usa `sessionFileHashes` (confirmado en la Tarea 3 de la investigación que no aporta nada útil en este flujo: `revert_file` se llama tras `list_file_history`, no tras `read_file`, así que casi nunca habría una entrada de sesión que consultar) — mismo primitivo `hashFileContent` ya en uso, mismo criterio que el chequeo #1 (`existingHash`) de `write_file`/`apply_patch`.

**Verificado real** con `ToolRegistry` real (bundle esbuild, sin reimplementar nada), workspace real en disco temporal, VCS oculto real (git real vía `local-vcs.ts`): Caso A (carrera real — `write_file` real de "otra sesión" corriendo mientras el `confirm()` de un `revert_file` real estaba pausado 300ms simulando aprobación humana lenta) → `revert_file` rechazado (`"cambio en disco despues de que se genero la vista previa..."`), el contenido de la otra sesión sobrevivió intacto en disco. Caso B (caso feliz, sin interferencia) → `revert_file` sigue funcionando igual, archivo restaurado correctamente. Ver `docs/_arch/CONTRACT.md` → "Fix real — staleness en `revert_file`".

## RESUELTO — carpeta de datos seleccionable en el instalador

**Historial real de esta entrada, 2 pasadas**: (1) investigación original marcándola como pendiente, riesgo real de electron-builder/NSIS sin confirmar contra la versión exacta de este proyecto. (2) Fase 0 (`docs/_arch/verify_installer_data_folder_design.md`): investigación real confirmó viabilidad técnica (compilación + render + interacción, sin problemas) contra la versión real vendorizada, Y un hallazgo no buscado — la feature **ya había existido y se retiró deliberadamente en v0.4.7** (comentarios reales sobrevivientes en `build/installer.nsh`, canal viejo `AMATISTA_DATA_DIR` ya muerto en el código). (3) **Esta pasada, Fase 1, implementación real**: página `nsDialogs` reintroducida en `build/installer.nsh`, escribiendo al canal REAL que `app-paths.ts` ya lee (`AMATISTA_STORAGE_ROOT`, nunca el nombre viejo). Ver `docs/_arch/CONTRACT.md` → "Feature real — carpeta de datos seleccionable en el instalador (Fase 1, cierra la entrada de PENDING.md)".

**Verificado real, ambos escenarios (ruta custom y checkbox "misma carpeta"), con el instalador real completo (`npm run dist`)**: registro real de Windows (`HKCU\Environment\AMATISTA_STORAGE_ROOT`) confirmado con el valor correcto en cada caso; app real, lanzada desde el acceso directo real que el instalador crea, usando la carpeta correcta en ambos casos; `D:\AMATISTA\data` real del usuario confirmado sin ningún cambio. 2 bugs reales de scoping NSIS encontrados y corregidos en el camino (detalle en `docs/_arch/verify_installer_data_folder_phase1.md`) — ninguno de los reportados en los issues de electron-builder que motivaron la investigación original. `app-paths.ts` sin cambios.

## RESUELTO — gestión de modelos individuales para Foundry/OpenAI/Anthropic/Google/DeepSeek

**Historial de esta entrada, 3 pasadas reales**: (1) hallazgo original, gestión de modelos solo para `openrouter`/`openai-compatible` (después corregido a incluir `openai`, ya lo tenía desde antes). (2) Actualizada a PARCIAL tras el capítulo de Foundry/`model-discovery.ts` — Foundry llegó a 3/4, Claude/Antigravity/Codex a "parcial" (1/4, mecanismo distinto). (3) **Esta pasada, cierre real**: el rediseño de `ConnectionModelsPanel` (fusión de los 3 bloques + gate `type==='anthropic'||'antigravity'||'openai-codex'||'google'` **sin filtrar por `authMode`**, ver `docs/_arch/CONTRACT.md` → "Fix real — rediseño de gestión de modelos por conexión") le dio manual-add/toggle/eliminar a TODOS los tipos restantes de una sola vez — efecto colateral real y bienvenido, no buscado explícitamente en ese momento pero exactamente lo que decía la Tarea 4 de `verify_individual_model_management_design.md` ("gratis" al extender el gate). Sumado a `gemini-catalog.ts` (esta misma pasada, Fase B) para Google/Gemini.

**Tabla real final, 4 capacidades × 9 combinaciones tipo/authMode, confirmada contra el código y verificada en vivo donde aplicaba:**

| Tipo (authMode) | 1. Sincronizar catálogo | 2. Agregar manual | 3. Buscar en catálogo | 4. Activar/Desactivar/Eliminar puntual |
|---|---|---|---|---|
| **Foundry** (api-key) | ✅ SÍ | ✅ SÍ | ❌ NO (a propósito, catálogos chicos) | ✅ SÍ |
| **OpenAI/OpenRouter/Compatible** (api-key) | ✅ SÍ | ✅ SÍ | ✅ SÍ | ✅ SÍ |
| **Anthropic — Claude Pro** (subscription) | 🟡 solo-agrega (`Actualizar modelos`, fila) | ✅ SÍ (nuevo) | ❌ NO (a propósito) | ✅ SÍ (nuevo) |
| **Anthropic — api-key genérico** (api-key) | ❌ NO | ✅ SÍ (nuevo) | ❌ NO | ✅ SÍ (nuevo) |
| **DeepSeek** (`type:anthropic`, api-key) | ❌ NO | ✅ SÍ (nuevo) | ❌ NO | ✅ SÍ (nuevo) |
| **Antigravity** (subscription) | 🟡 solo-agrega (fila) | ✅ SÍ (nuevo) | ❌ NO (a propósito) | ✅ SÍ (nuevo) |
| **Antigravity** (api-key) | ❌ NO | ✅ SÍ (nuevo) | ❌ NO | ✅ SÍ (nuevo) |
| **Google/Gemini** (cualquier authMode) | ✅ SÍ (nuevo, `gemini-catalog.ts`, con verificación real por candidato) | ✅ SÍ (nuevo) | ❌ NO (a propósito, ver diseño) | ✅ SÍ (nuevo) |
| **Codex (ChatGPT)** (subscription) | ✅ SÍ ×2 (solo-agrega + reemplazo completo, consolidados en la fila) | ✅ SÍ (nuevo) | ❌ NO (a propósito) | ✅ SÍ (nuevo) |

**Lo único que queda deliberadamente distinto, no un gap real**: Claude/Antigravity/Codex no tienen un catálogo-completo-para-buscar como OpenRouter — su descubrimiento es "encontrar candidatos nuevos reales" (Claude/Antigravity vía CLI, Codex vía RPC), no "listar todo el catálogo del proveedor para elegir" — decisión de diseño ya confirmada (Tarea 2 de `verify_individual_model_management_design.md`: catálogos reales chicos, sin necesidad de buscador). El ejemplo real que motivó esta entrada (duplicado de Claude Haiku 4.5, corregible solo editando `settings.json` a mano) **ya no aplica** — `toggleModel()`/`deleteModel()` están disponibles para las 9 combinaciones, cualquier modelo agregado por error se puede eliminar desde la UI hoy.

**Fase B (Google/Gemini) — verificación real, `gemini-catalog.ts`**: con la conexión real del usuario (key nunca vista por el agente), 40 candidatos reales pasaron el filtro barato (`supportedGenerationMethods.includes('generateContent')`), 26 sobrevivieron la verificación real por candidato (`generateContent` mínimo, 200=usable), 14 descartados (404/error real, confirma el hallazgo real de la investigación: el catálogo estático de Google mezcla modelos deprecados sin campo de estado). ~15s reales medidos, mismo orden de magnitud que Claude.

**Sin investigar ni priorizar todavía**: cuando se aborde el cierre completo, decidir si conviene (a) extender manual-add/búsqueda/toggle-eliminar a Foundry (le falta solo búsqueda) y a los 3 tipos de suscripción (les falta las 3), o (b) diseñar un mecanismo distinto para las suscripciones — dado que ahí "agregar modelo manual" tiene menos sentido (no hay un endpoint propio del usuario para escribir un id arbitrario, a diferencia de Foundry/OpenAI).

## CERRADA — Reportar a Google: --add-dir no confina bajo --dangerously-skip-permissions

> Hallazgo real de seguridad encontrado en la integración de Antigravity CLI (documentado en `docs/_arch/CONTRACT.md` → "Integración completa de Antigravity CLI"): bajo `--mode plan`/`accept-edits`, un intento de leer un archivo fuera del `--add-dir` especificado es auto-denegado real por `agy` (mensaje real: *"a tool required the \"read_file\" permission that headless mode cannot prompt for, so it was auto-denied."*). Bajo `--dangerously-skip-permissions`, el MISMO intento tiene éxito real — `--add-dir` es una lista de permitidos que se suma, no un límite duro, y ese límite desaparece por completo en el modo más permisivo. Reproducido en vivo con 2 proyectos reales distintos. Reportado real a Google vía AI VRP, **issue 556011794**.

**Resultado real del reporte**: Google cerró el issue el **1 de septiembre de 2026** como **Duplicate** — ya existía un reporte previo de otra persona sobre el mismo problema. No calificó para recompensa por ese motivo (duplicado), **no** porque el hallazgo fuera inválido — un cierre como Duplicate implica que Google reconoce el comportamiento reportado como real (contra un reporte previo independiente del propio).

**Nota real sobre la discrepancia previa, no reinvestigada a fondo**: la reproducción independiente propia (`docs/_arch/verify_antigravity_bug_report.md`) no había logrado confirmar el mismo contraste con un setup 100% sintético — quedó documentada como `SIN CONFIRMAR` en su momento, y esta entrada llegó a decir explícito "NO reportar esto a Google con la redacción actual". El reporte se envió de todas formas (fuera del alcance de esta bitácora saber exactamente con qué evidencia/redacción final) y el cierre como Duplicate de Google es la confirmación externa que la reproducción sintética propia no había podido dar. Por qué esa reproducción propia no lo reprodujo (defecto metodológico no detectado, o diferencia real entre el escenario sintético y el original) queda sin resolver — ya no es necesario para el objetivo del reporte, que se cerró.

## RESUELTO — Pre-aprobación de las 4 tools de LSP (MCP) en sandbox "Workspace"

> Hallazgo real encontrado en la implementación del servidor MCP de LSP: con sandbox mode "Workspace" (`--permission-mode acceptEdits` en Claude), un turno real que intentó `mcp__amatista-lsp__list_symbols` era denegado por el propio Claude, sin preguntarle a nadie — `acceptEdits` auto-aprueba las tools nativas de edición de Claude, pero no las tools MCP de terceros, dejando las 4 tools de LSP inutilizables en el sandbox mode por defecto de un panel nuevo pese a ser de solo lectura. Investigado real (`docs/_arch/verify_claude_permission_allowlist.md`) y arreglado (commit `ceb327f`, `docs/_arch/CONTRACT.md` → "Fix del gate de permisos — `--allowedTools` para las 4 tools MCP de LSP"): `permissionArgs()` agrega `--allowedTools` con los 4 nombres EXACTOS (`mcp__amatista-lsp__find_definition`/`find_references`/`list_symbols`/`get_diagnostics`) fuera de `danger-full-access` — nunca wildcard, descartado por evidencia real de bugs documentados en `anthropics/claude-code`. Verificado real en los 3 sandbox modes: default/workspace-write confirmado arreglado (`permission_denials` vacío, `permissionDecisionMs=0`); `danger-full-access` confirmado sin cambios.

**1 caveat real heredado, no bloqueante, documentado — no resuelto por este fix**:

- **`read-only` (`--permission-mode plan`) sigue sin poder usar las 4 tools**: confirmado real que `--allowedTools` no tiene efecto bajo `plan` mode — es un comportamiento estructural de Claude Code, ese modo está diseñado para bloquear TODA ejecución de tools hasta que el usuario apruebe un plan explícito, ningún allowlist lo puede overridear (confirmado con el propio mensaje del modelo explicándolo). El código de este fix se aplica igual en esa rama pero es un no-op inofensivo ahí — sin regresión, pero las 4 tools de LSP siguen sin estar disponibles en sandbox "Solo lectura".

## RESUELTO — Integración con Antigravity CLI (Google)

> Investigación completa (`docs/_arch/verify_antigravity_cli.md` + `verify_antigravity_integration.md`) e implementación completa (`docs/_arch/CONTRACT.md` → "Integración completa de Antigravity CLI"): `agy` integrado como runtime real (`CliAgentKind`/`RuntimeKind`/`ProviderType` ganan `'antigravity'`), `sendAntigravity()` real, siembra automática, UI completa. 4 bugs reales encontrados y corregidos en la propia verificación en vivo — ninguno anticipado en el diseño original (el envelope JSON válido con exit code 1, la carrera de `settings.json` compartido entre conexiones de distinto `authMode`, el hallazgo crítico de que `--add-dir` no confina `danger-full-access`, y la detección de instalación con PATH stale). Verificado real en 2 niveles (runtime directo + UI en la app empaquetada e instalada), carpeta real del usuario confirmada sin cambio neto.

**1 caveat real heredado de la implementación, no bloqueante, sin resolver**:

- **`danger-full-access` sin confinamiento real posible** (hallazgo del bug 3, no un pendiente de arreglar — es el comportamiento esperado del propio flag, documentado explícito en el código y en CONTRACT.md): a diferencia de `read-only`/`workspace-write` (confinados real, confirmado que `agy` auto-deniega accesos fuera de `--add-dir` en modo headless), no existe ningún flag real que confine `danger-full-access` — mismo perfil de riesgo que el equivalente de Claude/Gemini. Documentado, no oculto — el usuario que elige ese sandbox mode ya está aceptando full access explícito.

### RESUELTO — HOME de Antigravity por conexión, ya no compartido entre toda la app

> Investigación real (`docs/_arch/verify_antigravity_authmode_race.md`) confirmó el mecanismo de la carrera (2 conexiones distintas SÍ podían pisarse el mismo `settings.json` compartido, sin ningún error ni aviso), sin poder confirmar el síntoma final en un turno completo por un bloqueo de cuota real no relacionado. Fix implementado y verificado (`docs/_arch/verify_antigravity_home_per_connection.md`, `docs/_arch/CONTRACT.md` → "Fix real — HOME de Antigravity por conexión"): `getAntigravityHomeDir(connectionId)` ahora recibe el `provider.id` real de la conexión y devuelve una subcarpeta distinta por conexión, bajo la misma carpeta aislada de siempre — elimina la carrera de raíz (ya no hay ningún archivo compartido que pisar), no solo la mitiga. `clearAntigravityHomeDir()` sin cambio de lógica (sigue barriendo la carpeta padre, que ahora contiene una subcarpeta por conexión en vez de los archivos directos de una sola). Verificado real con 2 conexiones Antigravity reales del usuario, turnos reales completos (`--model claude-sonnet-4-6`, cuota de Gemini evitada a propósito), y repitiendo el mismo mecanismo real de la carrera original (ensanchamiento + 2 procesos de Node reales concurrentes) — confirmado que la colisión ya no es reproducible, cada conexión escribe en su propio archivo real.

## RESUELTO — LSP para C, C++ y Java (jdtls) vía rediseño config-driven — los 7 lenguajes de SWE-Bench ProMax ya cubiertos

**Encontrado en el piloto real de Fase 2/3 del benchmark** (`davidesantangelo/krep`, instancia real de SWE-Bench ProMax, en C): `find_definition`/`find_references`/`list_symbols` no pudieron aplicarse en absoluto en esa tarea — **no existía language server de C integrado**, era una limitación real de cobertura. SWE-Bench ProMax cubre 7 lenguajes reales (Python, Java, TypeScript, Go, C, C++, Rust); Amatista soportaba LSP para 4 (Fases 20/Python/Rust/Go). Faltaban **C**, **C++** y **Java**.

**C y C++ — RESUELTO real** (`docs/_arch/verify_clangd_integration_scope.md`, commit `f1b14de`): `clangd` (mismo binario para ambos lenguajes) — ver detalle completo en `docs/_arch/CONTRACT.md`.

**Java — RESUELTO real** (`docs/_arch/verify_jdtls_integration_scope.md` + `docs/_arch/verify_lsp_config_redesign.md`): la investigación de ayer confirmó que jdtls rompía las 3 asunciones del `LANGUAGE_SERVERS` de entonces (entry único, args estáticos, sin validación de runtime previo) — en vez de agregar un 3er `kind` ad-hoc, se rediseñó todo el mecanismo hacia un esquema **config-driven confirmado contra el código fuente real de OpenCode** (sst/opencode): cada lenguaje resuelve a una forma final única `{command: string[], extensions, env?}`, y esa complejidad (glob del jar versionado, `-data` único por workspace, validación real de Java ≥21, flags condicionales por versión de JVM) vive en una función real de resolución (`jdtlsResolveCommand()`), exactamente como OpenCode mismo lo resuelve para su propio jdtls built-in — sin ningún wrapper script externo. Detalle completo de la implementación, con los 5 hallazgos reales de la investigación de jdtls y el diseño confirmado contra OpenCode, en `docs/_arch/CONTRACT.md`.

**Bonus real del rediseño, más allá de Java**: `D:\AMATISTA\data\config\lsp.json` (global) + `.lsp.json` opcional por workspace permiten que el usuario agregue SUS PROPIOS language servers (o sobreescriba/desactive un built-in) sin tocar el código fuente de Amatista — mismo espíritu que `.mcp.json`, verificado real con un lenguaje inventado de punta a punta.

Los **7 de 7** lenguajes de SWE-Bench ProMax están cubiertos: TypeScript, Python, Rust, Go, C, C++, Java.

## RESUELTO — `closePanel()` no pasaba por el ref-espejo de `openChatInPanel()`

**No es un bug confirmado — riesgo documentado por transparencia, sin reproducir ni investigar a fondo.** El fix real de `openChatInPanel()` (ver `docs/_arch/CONTRACT.md` → "Fix bug real — `openChatInPanel()` devolvía `null` cuando el caller era un callback de IPC") introdujo `openPanelsRef` para que el `panelId` devuelto sea sincrónico de verdad, sin depender de si React ya corrió el updater de `setOpenPanels()`. Ese ref se mantiene al día de 2 formas: el `useEffect` general (corre en cada render, con el retraso normal de React) y una mutación manual dentro de la propia `openChatInPanel()` (para que el mismo tick ya vea el cambio).

`closePanel()` (`App.tsx`) llama a `setOpenPanels(current => {...})` directo — **nunca toca `openPanelsRef.current`**, ni por mutación manual ni indirectamente antes de que el `useEffect` corra. Si `closePanel(panelId)` se dispara y, en el mismo tick, una llamada a `openChatInPanel()` originada por IPC (auto-open de `send_to_window`, mismo mecanismo que causó el bug original) lee `openPanelsRef.current` ANTES de que el `useEffect` resincronice el ref tras el cierre, esa lectura sigue reflejando el snapshot de ANTES del cierre — un panel recién cerrado podría seguir contando para `overCap`/`targetIsOpen` en `openChatInPanel()`, una version angosta del mismo bug (decisión tomada sobre datos desactualizados, aunque en sentido inverso: de-sincronización por un cierre no reflejado, no por una apertura no reflejada).

**No reproducido, no investigado a fondo** — requeriría forzar el mismo tipo de carrera de timing que el bug original (cierre de panel + auto-open IPC en la misma vuelta de reloj), fuera del alcance de esta ronda. Revisar si se prioriza: la opción más directa es que `closePanel()` mute `openPanelsRef.current` manualmente, mismo patrón ya usado en `openChatInPanel()`.

**Resuelto real (`docs/_arch/verify_closepanel_race.md`, commit `3ff0805`):** investigado y reproducido real — no solo el riesgo especulado (falso límite de `MAX_PANELS`), sino un síntoma real más serio: resurrección de identidad de panel, un panel cerrado por el usuario "resucitando" con el MISMO `panelId`, arrastrando estado viejo. Gap real medido de 4.1ms entre `closePanel()`/`setOpenPanels()` y que el `useEffect` sincroniza `openPanelsRef.current`. Fix: `closePanel()` muta `openPanelsRef.current` manualmente y de forma SINCRÓNICA, mismo patrón de ref-espejo que `openChatInPanel()` ya usaba para el caso inverso (abrir). Re-verificado con el mismo escenario exacto, esta vez con el ensanchamiento artificial eliminado por completo — timing 100% natural (de 30s a 0s), el fix se sostuvo igual.

### RESUELTO — `settings:save`: merge por campo en vez de reemplazo total

> Encontrado durante el análisis de la carrera de Fase Paneles-2a (ver `docs/_arch/CONTRACT.md` → "Fase Paneles-2a"), agravado por Paneles-2b (paneles reales en paralelo). Investigación completa (`docs/_arch/verify_settings_race.md`, 5 tareas) confirmó que no era una carrera de milisegundos: el renderer nunca refresca `settings` tras el arranque, así que cualquiera de 12 acciones reales que disparan `settings:save` podía revertir en silencio un cambio autónomo de main (`activeProviderId`/`activeModelId`/`activeProjectPath`, `withSettingsLock()`) bajo el orden de eventos MÁS COMÚN, no uno raro. `settings:save` (`ipc-settings.ts`) fusiona ahora campo por campo — toma del payload solo `providers`/`compactionProviderId`/`compactionModelId`/`turnWatchdogSeconds` (confirmado renderer-owned, main nunca los toca en vivo), ignora por completo `activeProviderId`/`activeModelId`/`activeProjectPath`/`projectRoots` del payload (main siempre gana con su propio valor vivo). `addProjectRoot()` (`App.tsx`) pierde su guardado redundante (Canal 1, `projects:addRoot`, ya persiste el root real antes de que corra). Verificado real repitiendo el mismo escenario exacto que reprodujo la pérdida antes del fix — confirmado que el cambio de main sobrevive ahora. Detalle completo en `docs/_arch/CONTRACT.md`.

### RESUELTO — Fase Paneles-3: auto-open real, main pide y el renderer abre+conecta

> Cierra por completo el árbol Paneles-2/3/4 original. `sendToWindowByTitle()` ya no falla limpio de una cuando el chat destino no tiene panel conectado en vivo — dispara un handshake real (`pendingPanelOpenRequests`, mapa global en `cross-window-messaging.ts`, distinto de `requestSessionToolApproval()` por diseño, confirmado en la investigación que no era reusable tal cual) pidiéndole al SHELL (`App()`, vía `sendToShell()` nuevo en `runtime-state.ts`) que abra (`openChatInPanel()` real, nunca un camino paralelo) y conecte (`autoConnectRequestId`, nuevo prop de `ChatPanel`) el chat, con un timeout real de 8s (justificado sobre el mismo número base que ya justificó el timeout de LSP, Fase 20). De paso, se corrigió un bug real preexistente e independiente: `window.alert()` al tope de `MAX_PANELS` era bloqueante y congelaba TODA la app — reemplazado por `setNotice()` para ambos flujos (humano y automático). Verificado real con CDP + Foundry real: auto-open completo con turno real y entrega real (CASO A), guard de "nunca usado" sin intentar nada (CASO B, 2ms), tope de 4 paneles sin colgar la app (CASO C, 674ms). Detalle completo en `docs/_arch/CONTRACT.md`.
>
> **Caveat heredado, sigue sin resolver, fuera del alcance de esta fase**: `session.activeChatId` (`SessionRuntimeState`, main) solo se actualiza en `agent:connect`, no en cada cambio de chat en pantalla — no bloqueó Paneles-3 (el auto-open siempre crea/confirma una conexión fresca con el chatId correcto), pero sigue siendo una fuente potencial de inconsistencia si un panel YA conectado cambia de chat en pantalla sin reconectar, y `findConnectedPanelForChat()` lo reporta como "conectado a X" cuando en realidad el usuario está mirando otro chat en ese panel.

### RESUELTO — Fase Paneles-2b: `<ChatPanel>` real, layout dinámico 1-4, "Agregar panel"/cerrar panel

> `App.tsx` deja de ser "el único panel implícito" — contenedor real (`App()`, shell: sidebar/topbar/Settings/overlays compartidos) + `<ChatPanel panelId chatId>` real, 1 a `MAX_PANELS=4` instancias dinámicas según `openPanels` (nuevo, en memoria, NO persistido). Mismo chat en 2 paneles PROHIBIDO, aplicado en un único punto (`openChatInPanel()`). `focusedPanelId` alimenta lo que sidebar/topbar muestran (`panelStatuses`, reportado por cada panel). Layout con CSS Grid, columnas/filas calculadas en JS según cantidad de paneles. "Agregar panel" (menú contextual de chat) y cerrar panel (nunca el último) cubren el reemplazo de "Abrir en ventana nueva" que Paneles-1 había retirado — Paneles-4 (UI de layout 1/2/3/4) queda absorbido acá, no es una fase aparte. Verificado real con CDP: layout 1→2→3 confirmado por `getComputedStyle()`, no-duplicado confirmado, cierre de panel sin afectar al resto, 3 paneles reales conectados simultáneo a Foundry/Codex/DeepSeek con colores de marca verificados. Detalle completo en `docs/_arch/CONTRACT.md`. Lo único que queda del árbol Paneles-2/3/4 original: Paneles-3 (ver arriba).

### RESUELTO — Fase Paneles-1: backend, `panelId` reemplaza `windowId`

> Cambio de tipo puro (`windowId: number` → `panelId: string`, 13 firmas + 2 `Map<number,...>`) en `runtime-state.ts`/`ipc-agent.ts`, confirmado sin lógica interna a tocar (`npm run typecheck` limpio en el primer intento). Los 16 handlers reales (5 archivos: `ipc-agent.ts`, `ipc-window.ts`, `ipc-agents-md.ts`, `ipc-mcp.ts`, `ipc-projects-workspace.ts`) leen `panelId` del payload, nunca más vía `event.sender`/`BrowserWindow.fromWebContents`. `windowRegistry`/`registerWindow`/`setWindowChatId`/`isWindowUsable` (Fase 22a) retirados por completo — con ellos, `window:openInNewWindow` y **el entregable íntegro de Mensajería Paso 1** (`window:setActiveChatId`, ver nota en esa entrada más abajo). `forPanel(panelId)` nuevo en preload — wrapper que inyecta `panelId` en cada invoke y filtra cada evento entrante antes de llamar al callback del panel, migrando los 58 call sites de `App.tsx` (15 a `api.*`, 2 retirados, 41 sin tocar). `sendToWindow()` simplificado a una sola `mainWindow` real, sin lookup. CASO ESPECIAL documentado: `send_to_window` a un chat sin panel conectado en vivo falla limpio (regresión temporal, Paneles-3 la resuelve). Verificado real con CDP + Foundry real. Detalle completo en `docs/_arch/CONTRACT.md`. Lo que falta para paneles reales (UI, apertura async desde main) queda arriba, sección "Paneles-2/3/4".

### RESUELTO — Paso 3: tool `send_to_window`, auto-open/auto-connect, aprobación siempre-on, distinción visual

> Cierra por completo la mensajería entre ventanas (Pasos 1-3). Tool nueva `send_to_window(destino, mensaje)` (`tool-registry.ts`) — `destino` es el TÍTULO del chat, no un id técnico. Resolución completa (`cross-window-messaging.ts`, `sendToWindowByTitle()`): título → chatId (`findChatSessionByTitle()`, `chat-store.ts`, `COLLATE NOCASE`, duplicados resueltos al más reciente sin avisar por decisión explícita) → windowId (busca en `windowRegistry`, abre una ventana nueva vía `createAppWindow()` si ninguna la muestra). Auto-conecta la ventana destino SOLO si `chat_sessions.provider_id`/`model_id` no son `NULL` (ese chat ya tuvo un turno real) — si nunca se usó, error claro sin adivinar con qué conectarlo. Aprobación SIEMPRE incondicional en la ventana de origen (`ctx.confirm()` directo en el caso `send_to_window` de `tool-registry.ts`, deliberadamente sin pasar por `resolveApproval()` — el sandbox de la conexión actual no tiene relación con la seguridad de mandar un mensaje a otra sesión). `connectSessionForWindow(windowId, payload)` factorizada de `agent:connect` (mismo patrón que `runTurnForWindow()` de Paso 2). Distinción visual: `CrossWindowMeta` (`shared/types.ts`, `direction`/`windowLabel`/`providerType`) persistido en `chat_messages.cross_window` (columna nueva), consumido real en `App.tsx` (`onIncomingMessage` con listener real por primera vez, badge + borde coloreado con `PROVIDER_BRAND` real de Fase 21, ningún color nuevo). Verificado end-to-end real (Foundry real, `chat_sessions` reales, DOM real) — detalle completo en `docs/_arch/CONTRACT.md`.

### RESUELTO — Paso 1: `WindowEntry.chatId` deja de ser código muerto

> Se seteaba una sola vez al crear la ventana (Fase 22a) y nunca se actualizaba — `setWindowChatId()` existía pero no se llamaba desde ningún lado. Nuevo canal IPC `window:setActiveChatId` (dedicado, no reusado sobre `disconnect()` — mezclaría responsabilidades), disparado por un `useEffect` nuevo en `App.tsx` cada vez que `activeChatId` cambia. Verificado real vía CDP: 3 cambios de chat consecutivos dentro de la misma ventana, cada uno reflejado en el registro real del proceso main, sin quedar pegado en el valor anterior. Detalle completo en `docs/_arch/CONTRACT.md`.
>
> **Retirado por completo en Fase Paneles-1** (registro histórico, no editado — ver sección de arriba): `window:setActiveChatId`/`setWindowChatId`/`WindowEntry.chatId`/`windowRegistry` ya no existen. "Qué chat muestra esta ventana" dejó de tener sentido al pasar a paneles múltiples dentro de una sola ventana.

### RESUELTO — Paso 2: el motor (turno real en otra ventana + entrega cross-session)

> `runTurnForWindow(windowId, payload)` (factorizada de `agent:send`, sin `IpcMainInvokeEvent`) + `deliverResultToOriginWindow()`/`sendMessageToWindow()` (`cross-window-messaging.ts`, nuevo). Canal `chat:incomingMessage`, deliberadamente separado de `agent:event` (justificación completa en `docs/_arch/CONTRACT.md`). Hallazgo real durante la verificación: `CodexClient.sendTurn()` resolvía apenas el app-server aceptaba el turno, no cuando terminaba — corregido esperando `turn/completed`/`turn/cancelled` de verdad, con efecto colateral real y documentado sobre `agent:send` normal para chats Codex de una sola ventana (bajo riesgo confirmado). Verificado real: 2 ventanas, Codex real, texto real del modelo entregado y persistido, confirmado leyendo la base de vuelta. Sin aprobación, sin tool, sin distinción visual todavía — eso es Paso 3 (ver arriba). Detalle completo en `docs/_arch/CONTRACT.md`.

## RESUELTO (Fase 22b) — concurrencia real de conexión, indexada por ventana

> `agent:connect` ya NO mata la conexión de otra ventana — cada `BrowserWindow.id` tiene su propio `SessionRuntimeState` real (`sessionRegistry`, `runtime-state.ts`), con `codexClient`/`cliRuntime`/`apiRuntime`/`mcpManager`/`lspManager`/`activeRuntime`/`activeWorkspace`/`activeThreadId`/`activeChatId`/`activeContextSeeded`/`currentTurnAbort`/`isDisconnecting`/`toolTrustSession` independientes por sesión (mismas 9+4 identificadas en Fase 22 Tarea 0), más `pendingToolApprovals` (agregado esta fase, no estaba en la lista original — ver justificación en `docs/_arch/CONTRACT.md` → Fase 22b). `activeConnectionWindowId` (Fase 22a, singular a propósito) se eliminó por completo — ya no hace falta, cada sesión sabe su propia ventana.
>
> Alcance deliberadamente acotado a conexión/turno — quedó pendiente para **Fase 22c**:
> - **RESUELTO — Los sitios de `disconnect()` en `App.tsx`** (Parte A.1 de `verify_fase22_scope.md`, clasificación vieja confirmada obsoleta — el análisis fresco vigente es `docs/_arch/verify_fase22c_disconnect_scope_2026.md`). Reclasificación real contra la arquitectura de sesiones-por-panel ya vigente (`ChatPanel` por instancia): quedan 5 sitios reales hoy, no 13/18. Los (c) YA desaparecieron solos con Paneles-2b (el `useEffect` por `chatId` sigue siendo necesario, pero es interno a 1 panel — nunca afecta a otros). Los (a)/(b) quedaron consolidados en 1 solo mecanismo (`catalogChangeSignal`/`disconnectAllPanels()`), confirmado indiscriminado por diseño explícito ("broadcast crudo-pero-seguro", desconectaba TODOS los paneles sin importar el proveedor real afectado). **Fix real implementado**: `catalogChangeNonce: number` → `catalogChangeSignal: {seq, providerId: string | null}` — `providerId` real acota la desconexión a los paneles cuyo `activeChat.providerId` coincide (11 call sites actualizados); `providerId: null` explícito para los 3 casos genuinamente globales (`logoutCodex`/`importQConfig`/`removeProjectRoot`, sin cambio de comportamiento — el 3ro por ser otra dimensión de filtro, workspace en vez de provider, fuera de alcance de este fix). Verificado real con la app corriendo (2 paneles reales, proveedores distintos, sin API keys): editar la conexión Anthropic real que usaba el Panel 2 (`claude`) lo desconectó real; el Panel 1 (`antigravity`, proveedor distinto) siguió conectado sin interrupción. Detalle completo en `docs/_arch/CONTRACT.md`.
> - **RESUELTO — Parte D de `verify_fase22_scope.md`** (staleness de `write_file`/`apply_patch` entre lectura y escritura) sigue sin ningún mecanismo de detección — ahora sí relevante en la práctica, porque 2 sesiones reales pueden tocar el mismo proyecto a la vez. **Cerrado en 2 pasadas reales**: `d3a8fe4` implementó el primer chequeo (huella de contenido SHA-256, cubriendo la ventana "entrar a la tool → fin de la aprobación"); una revisión arquitectónica externa posterior encontró que esa ventana no cubría el gap real más común — cuando el modelo leyó el archivo turnos antes (vía `read_file`) y otra sesión lo modificó en el medio, el chequeo de `d3a8fe4` no lo detectaba — cerrado por completo en `7d6ff8a` (chequeo ADICIONAL por sesión, huella de lo que el modelo realmente vio, auditando la ventana completa "cuando el modelo leyó → cuando escribe", sin reemplazar el chequeo de `d3a8fe4`).
> - **RESUELTO — `sessionRegistry` no se limpiaba al cerrar un panel** (`runtime-state.ts`). **Corrección real sobre el enunciado original**: decía que el disparador era "cerrar una ventana" vía `window.on('closed', ...)` en `window-manager.ts` — confirmado con código real que ese handler **ya no existe** (Paneles-1 lo retiró, `window-manager.ts` documenta su propio retiro en un comentario explícito: bajo el modelo de paneles hay una única ventana física siempre, `registerWindow()`/`windowRegistry` fueron reemplazados por `setMainWindow()`). El disparador real hoy es **cerrar un panel** (`closePanel()` → `agent:disconnect`), un evento mucho más frecuente que "cerrar una ventana" — el leak en sí (la entrada vacía queda en el `Map` para siempre) seguía siendo real y cierto, no obsoleto.
> 
>   **Confirmado real antes de implementar** (`docs/_arch/verify_sessionregistry_leak_2026.md`): tocar `disconnectSession()` directamente (compartida por 4 call sites) rompería 2 de ellos (`ipc-projects-workspace.ts`, `workspace:open`/`projects:removeRoot`), que mutan una referencia local a `session` justo después de llamarla, esperando que siga siendo el objeto vivo del Map. Y `agent:disconnect` tampoco podía asumir "cierre genuino" sin más — 4 de los 5 disparadores reales de `disconnect()` en `App.tsx` (cambio de `chatId`/config/proveedor/modelo/sandbox) dejan el panel vivo y reconectan enseguida, usando el mismo canal IPC.
> 
>   **Fix real implementado**: `agent:disconnect` gana `payload.panelClosing?: boolean` (sin tocar `disconnectSession()` en sí) — el handler llama `sessionRegistry.delete(panelId)` SOLO si `panelClosing===true`. `closePanel()` (el único cierre genuino real — `deleteChat()` lo reusa) manda `disconnectAgent(true)`; los otros 3 call sites reales no pasan el campo, sin cambio de comportamiento. Verificado real con la app corriendo: cerrar un panel real bajó `sessionRegistry.size` en 1 (la entrada desapareció, no solo se vació); cambiar de proveedor en un panel que sigue abierto no tocó el tamaño del Map en ningún momento, y la reconexión funcionó normal, sin errores nuevos. Detalle completo en `docs/_arch/CONTRACT.md`.

## CERRADA (sin fix necesario) — fallback de `handleAgentEvent()`, riesgo de Fase 21.5

> Investigación real (`docs/_arch/verify_handleagentevent_fallback.md`): el riesgo residual que esta nota dejó pendiente de revisar antes de Fase 22 quedó **cerrado en la práctica por trabajo de fases posteriores** — ninguno escrito pensando en esta nota puntual. `handleAgentEvent()` (`App.tsx`) sigue armando su variable local (`workspace`, nombre engañoso — en la práctica funciona como "el chat id de este evento") con el mismo fallback de 2 términos que esta nota describía (hoy en realidad son 3: `asString(event.chatId) || asString(event.workspace) || activeChatIdRef.current`, el 3ro agregado después sin conexión documentada a esta nota) — pero la condición real que lo haría peligroso (`session.activeChatId` nulo en el proceso main en el momento exacto de un evento) está bloqueada por 2 mecanismos reales:

- **Asignación sincrónica, antes de enganchar listeners** (`ipc-agent.ts`, `connectSessionForWindow()`): `session.activeChatId` se asigna sin ningún `await` en el medio, ANTES de que se llame a `wireCodex()`/`wireApi()`/el equivalente CLI — los únicos puntos que enganchan los listeners (`client.on('raw'/'notification'/...)`) que terminan mandando eventos sin `chatId` explícito. Estructuralmente, por orden de ejecución (mismo hilo, sin punto de suspensión entre medio), ningún evento puede dispararse antes de que `session.activeChatId` ya esté seteado.
- **`chatId` explícito en los 5 puntos que llevan texto real** (`ipc-agent.ts`, dentro de `agent:send`): cada evento con contenido real del modelo (`item/agentMessage/delta`, `turn/completed`, `turn/cancelled`) manda `chatId: requestChatId` directo en su propio payload — que pisa el fallback interno de `sendSessionEvent()` (`runtime-state.ts`) sin importar el estado de `session.activeChatId` en ese instante. `requestChatId` siempre prioriza `payload.chatId`, que el renderer (`connectAgent()`/`runTurn()`) siempre manda poblado.

**Reproducción real intentada** (mismo criterio que los hallazgos reales de esta bitácora, no solo análisis de código): turno real de punta a punta con Codex por suscripción — el runtime más expuesto al riesgo, sus eventos `raw`/`notification` son justo los que dependen del fallback sin `chatId` explícito — vía el mismo mecanismo del orquestador (auto-open + auto-connect + turno real). **70+ eventos reales capturados con instrumentación temporal, `sessionChatId` nunca nulo, ni uno solo**; margen real medido de 242ms entre que `connectSessionForWindow()` asigna `activeChatId` y el primer evento real. A diferencia de otros hallazgos de esta bitácora (`closePanel`, staleness de `write_file`/`apply_patch`), acá la evidencia real consistentemente NO reprodujo el riesgo — reportado así, sin forzarlo.

**No hace falta ningún fix** — el fallback de 3 términos sigue existiendo tal cual en el código (no se tocó nada), pero el camino real para activarlo está bloqueado por diseño de Fase 22b/22c/Paneles/el orquestador del panel 1, sin relación directa con esta nota.

## RESUELTO (Fase 3) — Pérdida real de contexto

> Confirmado con evidencia en Fase 1 Tarea C, diseño fijado en la ronda de decisiones previa a Fase 3, implementado en Fase 3. El detalle completo del bug original (tres constantes triplicadas, fórmula exacta del hueco `[0, N-26)`, alcance por runtime) queda preservado en [CONTRACT.md](./CONTRACT.md) → "Contrato de memoria/contexto — v1 (DEPRECATED, ver v2)" — no se repite acá. El diseño vigente está en la v2 de esa misma sección.
>
> Resuelto para runtimes API (`anthropic-api`, `foundry`, `gemini-api`): compactación real vía LLM, persistida en SQLite por chat (`summary` + `summary_watermark_id`, watermark por id de mensaje — inmune a ediciones/borrados), disparada asíncronamente después de cada turno sin agregar latencia. De paso se corrigió un segundo bug real encontrado durante la implementación: `foundry` y `gemini-api` nunca leían `compactSummary` en absoluto (ni siquiera el truncado viejo) — solo `anthropic-api` lo usaba.
>
> **Caveat documentado, no oculto** (ver CONTRACT.md v2): en un chat viejo retomado con backlog grande nunca compactado, puede haber un hueco transitorio entre resumen y ventana verbatim en los primeros turnos post-actualización — se cierra en pasadas sucesivas de compactación, nunca de una sola vez. Ningún dato se pierde nunca (SQLite persiste todo siempre); el hueco, cuando existe, es solo de lo que un turno puntual le manda al modelo.

## RESUELTO — el botón "Compatible" (`type:'openai-compatible'`) ignoraba el endpoint que muestra

**Bug real, confirmado leyendo el código.** `type:'openai-compatible'` resolvía a `runtime:'codex-api'` (`runtimeFor()`, `App.tsx`), que corre por `codex-client.ts` — spawnea el binario `codex` real vía JSON-RPC. Ese archivo **nunca lee `provider.endpoint`** (`grep` sobre `OPENAI_BASE_URL|baseUrl|provider.endpoint` en `codex-client.ts` → cero resultados): `start()` solo seteaba `env.OPENAI_API_KEY = apiKey` y arrancaba `codex app-server --stdio`, sin pasarle ningún endpoint custom.

**Efecto real:** el campo "Endpoint" que la UI mostraba para este botón era un placebo — cualquier URL que el usuario cargara ahí (servidor local, proxy propio, cualquier backend Chat-Completions-compatible) se ignoraba en silencio, y la conexión seguía yendo contra la OpenAI real con la API key que el usuario cargó pensando que era para su propio endpoint.

**Camino 1 investigado y descartado con evidencia real** (`docs/_arch/verify_compatible_button_fix_options.md`): probado en vivo contra `codex-cli` 0.147.0 real (`-c` overrides efímeros, nunca tocó el `config.toml` real, sin costo real) — `model_providers.<id>.base_url` sí es una clave real y funcional, PERO codex-cli **solo habla el formato Responses API para un provider custom, nunca Chat Completions** (`wire_api:"chat"` explícitamente discontinuado, confirmado con el error real del binario). Ni siquiera resolviendo la dependencia del proceso externo, este camino serviría para el caso de uso real del botón.

**Resuelto real — Camino 2, migración a `openai-chat`** (`docs/_arch/verify_compatible_migration_scope.md`, `docs/_arch/verify_compatible_migration_implementation.md`, `docs/_arch/CONTRACT.md` → "Fix real — migración de `openai-compatible`/`openai` a `openai-chat`"): `runtimeFor()` cambiado de `'codex-api'` a `'openai-chat'` para estos 2 types — conexiones existentes se autocorrigen solas en el próximo reinicio (mismo mecanismo que ya resolvió el Hallazgo 2). Bug bloqueante encontrado y corregido en el mismo fix: `readiness()` exigía Codex CLI instalado para estos 2 types, que ya no lo necesitan — sacados de ese chequeo. Pérdida de funcionalidad real identificada y resuelta en la misma pasada: el selector de nivel de esfuerzo/razonamiento (antes exclusivo de runtimes CLI) ahora también funciona para `openai-chat` vía `reasoning_effort` real en `sendOpenAiApi()`, con la regla real de forzar `'none'` explícito cuando hay tools activas (conflicto real y documentado entre `reasoning_effort` y `tools` en la Chat Completions API de OpenAI). Verificado real en los 3 frentes (auto-corrección de runtime, `readiness()` ya no bloquea, `reasoning_effort` correcto con y sin tools) — detalle completo en los docs referenciados.

## RESUELTO — ¿`codex-subscription`/`codex-api` necesitan un mecanismo de compactación propio?

Pregunta abierta desde el cierre de Fase 3, sin investigar hasta ahora. **Corrección real sobre el enunciado
original**: decía que Codex "depende de `--resume <sessionId>` del binario externo para memoria de turnos
posteriores al primero" — **esto ya no es así** (y no está claro que lo haya sido para la integración vía
`app-server` real: `codex-client.ts:113` usa `ephemeral: true` en `thread/start`, confirmado en código — nunca
`--resume`/persistencia a disco para esto). La continuidad entre turnos vive enteramente en la memoria del
proceso `app-server` real (mismo proceso, mismo `threadId`, mientras la sesión de Amatista siga conectada).

**Investigado real** (`docs/_arch/verify_codex_compaction_need.md`): 3 hallazgos reales, confirmados con
evidencia, no solo documentación. (1) Codex nunca estuvo en el branch CLI que se extendió con compactación de
respaldo (`f54cda8`) — usa `session.codexClient` (JSON-RPC), estructuralmente distinto de `session.cliRuntime`
(`claude-cli`/`antigravity-cli`), con `return` temprano propio en `runTurnForWindow()`. (2) SÍ hay un límite
real de contexto (272 000 tokens para `gpt-5.x`, confirmado con `codex debug models` real) — y Codex tiene su
**propia compactación automática nativa, real y ya activa por default** (`model_auto_compact_token_limit`,
ítems `contextCompaction` reales en el protocolo JSON-RPC), confirmada empíricamente con una conversación real
de 6 turnos (ventana forzada a 3000 tokens vía `-c` para no gastar cuota real de más): 5 compactaciones
automáticas reales ocurrieron solas, Codex respondió correctamente el número de turno las 6 veces pese a ellas
— no hace falta ningún mecanismo de Amatista para el proceso vivo, ya está resuelto del lado del binario. (3)
El único gap real y aplicable: como `maybeCompactChatInBackground()` nunca se disparaba para turnos de Codex,
un chat 100% Codex nunca generaba el resumen de respaldo que sí tienen claude-cli/antigravity-cli/API desde
`f54cda8` — al reconectar (thread NUEVO, `ephemeral:true`, sin nada de la memoria en proceso del thread viejo,
que murió con el proceso anterior), lo único disponible era el recorte duro de `normalizeHistory()`, sin ningún
resumen de respaldo.

**Fix real implementado**: mismo patrón fire-and-forget de una línea que ya tienen los branches API y CLI —
`void maybeCompactChatInBackground({...})` agregado al branch de Codex en `runTurnForWindow()`
(`ipc-agent.ts`), justo antes del `return` que ya existía. NO toca la compactación nativa de Codex
(`thread/compact/start`, corre sola) ni el thread vivo — solo mantiene fresco el resumen que se usaría en el
PRÓXIMO reconnect de ese chat. Ver `docs/_arch/CONTRACT.md` → "Fix real — resumen de respaldo para reconexión
de Codex".

## CERRADA (obsoleta por retiro completo de gemini-cli) — falla pre-existente en Gemini CLI

**Ya no aplica — la función que tenía el bug fue retirada entera, no arreglada.** Historial completo de la investigación (preservado para contexto):

**No es un bug de esta limpieza — confirmado que `sendGemini()` (`cli-agent-runtime.ts`) quedó copiado byte-a-byte, sin ningún cambio.** Durante la verificación real (Tarea 7) de la limpieza de `claude-cli`, un `sendMessage()` real contra `qcfg-gemini-subscription` (conexión real, `connectAgent()` exitoso) falló con `"Cannot use both a positional prompt and the --prompt (-p) flag together"` — un error de parseo de argumentos del binario `gemini` real instalado en la máquina de desarrollo. Sospecha más probable, no confirmada: desajuste entre la versión de `gemini` CLI instalada hoy y el formato de flags (`-p <prompt>`) que `sendGemini()` construye. Sin investigar más allá — fuera de alcance por la restricción explícita de esa fase de no tocar nada de Gemini. Revisar la versión real del binario instalado (`gemini --version`) contra cuándo se escribió/probó por última vez `sendGemini()` (Fase 17 Parte 2) cuando se priorice.
>
> **Reproducido de nuevo, sin cambios de código** durante la verificación real de la reintegración de claude-cli (`docs/_arch/CONTRACT.md` → "Reintegración completa de claude-cli") — mismo error exacto, confirmado con `git diff` que `sendGemini()`/`geminiCommand()` no cambiaron ni una línea. Dato nuevo real que descarta parte de la sospecha anterior: `geminiCommand()` (el fix de `a39e0d8` contra el bug de `shell:true`) SÍ resuelve correctamente en esta máquina (`%APPDATA%\npm\node_modules\@google\gemini-cli\package.json` existe, `bin.gemini` presente) — el camino `process.execPath` + `shell:false` se está tomando, no el fallback viejo, y el error persiste igual. Descarta que sea "el fallback inseguro reapareciendo" — la causa real sigue sin confirmar, pero ya no es esa hipótesis.
>
> **Cierre real (docs/_arch/verify_gemini_cli_removal_scope.md, docs/_arch/verify_gemini_cli_removal.md):** confirmado en investigación previa que `gemini-cli` standalone quedó discontinuado para cuentas individuales (`IneligibleTierError` real, Google redirige a Antigravity, que ya cubre el mismo terreno) — decisión del usuario de retirar el feature entero en vez de seguir investigando este bug de parseo. `sendGemini()`/`geminiCommand()` (y todo `CliAgentKind:'gemini'`) se borraron de `cli-agent-runtime.ts` — no queda ninguna función real con este bug. El camino HTTP de Gemini (`authMode:'api-key'`, `RuntimeKind` renombrado de `'gemini-cli'` a `'gemini-api'`) es un mecanismo completamente distinto (fetch HTTP directo, sin subproceso, sin parseo de argv) y nunca tuvo este bug.

## RESUELTO (Fase 14, seguimiento) — `compactionProviderId`/`compactionModelId` nunca se persistían

> Encontrado como hallazgo colateral al cerrar Fase 14, confirmado con evidencia real (`docs/_arch/verify_compaction_settings.md`) y corregido en un fix de seguimiento el mismo día, después de que el bug causara un error real y visible en uso activo (`"explore falló: No hay modelo de compactación configurado"`, justo tras un reinicio de la app). Detalle completo del mecanismo confirmado y el fix en `docs/_arch/CONTRACT.md` → "Timeout del watchdog de turno configurable (Fase 14)".
>
> Resumen: `AppSettings.compactionProviderId`/`compactionModelId` (Fase 3) nunca se habían agregado a `StoredSettings`/`loadSettings()`/`saveSettings()` en `settings-store.ts` — se perdían en cada reinicio de la app (dentro de la misma sesión andaban bien). Fix: agregados a `StoredSettings` y threadeados en `loadSettings()`/`saveSettings()`, mismo patrón que `turnWatchdogSeconds`, sin guard de validez (son IDs de string, `resolveConfiguredCompactionModel()` ya descarta ids inválidos). Verificado con un roundtrip real a disco (sembrado → releído → cambiado → releído tras un restart simulado).

## Sin priorizar (originado en el cierre de Fase 11)

- **Recuperación selectiva por tema.** La inyección de memoria por tema (Fase 11) sigue siendo completa — todos los temas, siempre — igual que Fase 6 lo era para las 3 listas planas. Con muchos temas acumulados en un chat muy largo, esto puede volver a acercarse al mismo tipo de presión de contexto que motivó Fase 3, ahora en la memoria estructurada en vez del historial verbatim. Filtrar/priorizar temas relevantes al turno actual (en vez de mandarlos todos) fue evaluado y descartado explícitamente como fuera de alcance de esta fase — decisión ya tomada, no un olvido — pero queda anotado para priorizar si algún chat real llega a acumular suficientes temas como para que importe.

  **Confirmado real (`docs/_arch/verify_topic_memory_pressure.md`)**: el mecanismo sin filtro es real (todos los temas siempre, sin techo de tokens — y para los 4 runtimes API viaja en CADA turno, no solo al reconectar), pero **hoy no hay presión real**. Lectura de solo lectura de la base real del usuario (`D:\AMATISTA\data\config\amatista.db`, 15 chats reales): el bloque "Memoria por tema" está vacío en el 100% de los casos — ningún chat real tiene `structured_memory` poblado, la compactación nunca se disparó todavía para ninguno (el chat más cargado tiene ~21 tokens estimados de historial total, 0.35% de `CONTEXT_TOKEN_BUDGET=6000`). Sin ningún tema real acumulado, no puede haber ningún síntoma real tampoco — confirmado sin necesitar un turno de prueba costoso, la propia base ya lo descarta. **Sigue siendo apropiadamente teórico, sin costo real medible hoy** — la decisión de no filtrar sigue en pie, sin fix. Reabrir cuando algún chat real acumule varios temas, no antes.

## RESUELTO (Fase 12) — Race condition de `activeWorkspace` en `agent:connect`

> Originado como caveat sin confirmar en "Encontrado durante refactor (Fase 2)" (mismatch de tipos `activeProjectPath` en `ipc-agent.ts`) — el fix de tipos de esa fase (`activeProjectPath: payload.workspace?.trim() ? activeWorkspace ?? undefined : settings.activeProjectPath`, `ipc-agent.ts`) quedó sin confirmar si la carrera que lo motivaba era alcanzable en la práctica. Investigado y blindado en Fase 12.
>
> **Tarea 1 — Alcanzabilidad, confirmada con el código real, no asumida:** el botón "Quitar" de una carpeta raíz (`App.tsx`, sidebar "PROYECTOS") **no tiene ningún `disabled` ligado a `agentState`** — a diferencia del botón "Conectar agente" (`disabled={... || agentState === 'connecting'}`) y el botón de enviar (`disabled={agentState === 'connecting'}`), que sí lo tienen. Un click en "Quitar" mientras `connectAgent()` sigue en vuelo dispara `projects:removeRoot` sin ningún bloqueo de UI. Si el root removido matchea el workspace activo, ese handler (`ipc-projects-workspace.ts`) llama `disconnectAgent()` (para procesos, resetea `apiRuntime`/`cliRuntime`/`codexClient`/`mcpManager`/`activeRuntime` a `null`) y luego `setActiveWorkspace(null)` — mientras `agent:connect` puede seguir en cualquiera de sus 3 awaits (`client.start()`, `detectClaude()`/`detectGemini()`, `mcpManagerForConnection.startAll()`). **Confirmado alcanzable, no solo teórico.**
>
> **Hallazgo real durante la investigación, más severo que el caveat original:** el riesgo no era solo `null` vs `undefined` en `settings.json` — sin blindaje, la conexión en vuelo seguía de largo tras la carrera y terminaba **resucitando** `apiRuntime`/`mcpManager`/`activeRuntime` (via `setApiRuntime`/`setMcpManager`/`setActiveRuntime`) que el `disconnectAgent()` concurrente ya había parado, dejando al renderer creyendo "conectado" (`agentState = 'connected'`) mientras el proceso principal quedaba en un estado inconsistente (o, en el otro orden posible, con el `mcpManager`/`client` recién arrancados por la conexión en vuelo parados a mitad de arranque por el `disconnectAgent()` concurrente, sin que `agent:connect` se enterara).
>
> **Fix (Tarea 2 — blindaje mínimo, `ipc-agent.ts`):** `assertWorkspaceStillActive(connectingWorkspace, cleanup?)`, un guard que compara `activeWorkspace` contra el valor capturado **antes** de los 3 awaits de `agent:connect` — si cambió (la carrera ocurrió), para explícitamente lo que esa conexión ya había arrancado (`client.stop()` / `mcpManagerForConnection.stopAll()`, ambos idempotentes — no rompen si `disconnectAgent()` ya los paró) y aborta con un `Error` claro en vez de continuar. Se invoca justo después de cada uno de los 3 awaits relevantes (`client.start()`, `detectClaude()`/`detectGemini()`, `mcpManagerForConnection.startAll()`), antes de cualquier `setActiveThreadId`/`setActiveRuntime`/`runtime.configure()` posterior. La rama CLI no necesita `cleanup` — no arranca ningún proceso antes de su único await.
>
> El fix de tipos original de Fase 2 (`activeProjectPath: ... ?? undefined`) queda sin cambios — sigue siendo correcto y necesario, el blindaje de esta fase es una capa adicional que hace que, en el escenario de carrera, `agent:connect` ni siquiera llegue a esa línea.
>
> `npm run typecheck` y `npm run build` en verde.

## RESUELTO PARCIALMENTE — Orquestador multi-modelo en paralelo, con costo heterogéneo por panel

**Motivada por una comparación real con "Agent Teams" y "Claude Research" de Anthropic.** Anthropic publicó que su patrón de investigación multi-agente (líder + 3-5 subagentes en PARALELO simultáneo, todos el MISMO modelo caro) consume ~15x más tokens que un chat normal — y la propia Anthropic advierte que ese patrón "no es una buena opción para dominios que requieren que todos los agentes compartan el mismo contexto o involucren muchas dependencias entre agentes", y que es "menos efectivo para tareas fuertemente interdependientes, como programar".

El orquestador actual de Amatista (`send_to_window`, un panel dispara un turno en OTRO panel puntual, de a uno) **no incurre en ese costo** — es arquitectónicamente más parecido al "subagente" barato de Claude Code (resultado resumido de vuelta al que lo pidió) que al "equipo" caro de instancias simultáneas.

**La idea nueva, genuinamente más grande, que queda pendiente de diseñar**: paneles genuinamente SIMULTÁNEOS (no secuenciales/uno-a-la-vez como hoy) trabajando en paralelo real — pero con **costo heterogéneo por panel**: un modelo barato (tipo DeepSeek) explorando en paralelo, mientras un modelo caro (tipo Claude) hace la síntesis/razonamiento difícil — en vez de clonar el mismo modelo caro N veces como hace el patrón de Anthropic. Esto evitaría el multiplicador de ~15x porque no se paga precio completo N veces — se distribuye el trabajo según costo real, no se clona el gasto.

**Diferencial real confirmado**: ni "Agent Teams" de Claude Code ni "Research" de Anthropic pueden hacer esto por diseño — están atados a un solo proveedor (todos los "teammates"/subagentes son del mismo proveedor). Amatista, al ser multi-proveedor desde su arquitectura base, es la única posición real desde la que se podría construir paralelismo con costo heterogéneo real entre proveedores distintos.

**Lo que haría falta diseñar, genuinamente más grande que el orquestador de hoy**:
1. Mecanismo real de paneles SIMULTÁNEOS (no uno-a-la-vez como `send_to_window` hoy).
2. Algún criterio real para decidir qué parte de una tarea le conviene a qué modelo/proveedor según costo vs. capacidad (exploración barata vs. síntesis cara).

**Marco de vocabulario investigado (deepseek-harness / "dsh", developer preview) — adoptado como DISEÑO, no como código ni API**: dsh separa a propósito 3 modos de delegación, y esa separación mapea limpio sobre lo que Amatista ya tiene y lo que le falta:
- **Subagente** — pedir y esperar el resultado completo, síncrono desde el punto de vista de quien pide. **Ya lo tenemos**: `send_to_window` es exactamente esto. Confirmado que ya cumple el principio central de dsh — *"un ID prueba admisión, no finalización"* (el ID real que devuelve una llamada confirma que la tarea fue aceptada, nunca que ya terminó — quien pide sigue teniendo que esperar/consultar el resultado real, no asumirlo del ID en sí).
- **Job** — mandar a segundo plano y volver más tarde a revisar/cancelar, sin bloquear a quien lo pidió mientras tanto. **No lo tenemos** — `send_to_window` siempre bloquea hasta el resultado.
- **Workflow** — abanico paralelo real (varias ramas simultáneas) que converge en UN resultado único. **Es la pieza que le falta exactamente a esta idea** — "paneles simultáneos con costo heterogéneo" es, en este vocabulario, un Workflow con selección de proveedor por rama según costo/capacidad, no un Subagente ni un Job.

**Tensión real a resolver a propósito, no a ignorar, si esto se construye**: el default de seguridad real de dsh es explícitamente *sin selección dinámica de proveedor por llamada* — lo OPUESTO exacto a la premisa central de esta idea (elegir modelo barato vs. caro según la parte de la tarea). Antes de diseñar el Workflow real, hay que decidir cómo mantener autoridad predecible sobre qué proveedor/modelo puede terminar ejecutando qué, en vez de heredar sin más el default de dsh (que asume un solo proveedor fijo por diseño) ni descartar la protección sin reemplazo.

**Decisión de esta sesión**: adoptar el marco (Subagente/Job/Workflow) como vocabulario de diseño para cuando esto se priorice — no se construye nada todavía, ni Job ni Workflow.

No investigado a nivel de implementación, no diseñado en detalle, no implementado — decisión explícita del usuario de dejarlo para otra sesión.

**Actualización real (`docs/_arch/verify_parallel_orchestrator_design.md`, tool `parallel_ask`)**: el punto 1 de "lo que haría falta diseñar" ("mecanismo real de paneles SIMULTÁNEOS") está RESUELTO — `parallel_ask` es exactamente el "Workflow" del vocabulario dsh adoptado acá (abanico paralelo real que converge en un único resultado, ver `runParallelAsk()`/`parallel-orchestrator.ts`), verificado real con paneles genuinamente concurrentes (tiempo total ≈1x el delay de la sub-tarea más lenta, nunca ≈Nx). El **costo heterogéneo real entre proveedores** también se logra en la práctica — el reparto usa `sessionRegistry` tal cual, así que si el usuario tiene paneles conectados a proveedores distintos (DeepSeek barato + Claude caro, por ejemplo), `parallel_ask` los reparte a ambos sin distinguirlos.

**Punto 2 SIGUE sin resolver, a propósito, fuera de alcance de esta fase**: no hay ningún "criterio real para decidir qué parte de una tarea le conviene a qué modelo/proveedor según costo vs. capacidad" — el reparto es puramente round-robin, CIEGO a cuál panel es el barato y cuál el caro. Es el modelo (o el usuario) quien decide manualmente qué paneles conectar y con qué sub-tareas redactarlas para explotar el costo heterogéneo — Amatista no asigna inteligentemente. La "tensión" de seguridad ya anotada (selección dinámica de proveedor por llamada) tampoco aplica: `parallel_ask` NUNCA elige proveedor por su cuenta, solo reparte entre lo que el usuario ya conectó a mano.

**Actualización — cancelación en cascada de `parallel_ask`: AHORA REAL para los 3 runtimes** (`docs/_arch/verify_parallel_idle_detection_design.md`, fix del Hallazgo 1). Antes, la cascada de cancelación de `runParallelAsk()` (`onOriginAbort` → `cancelSessionTurn()` por panel destino activo) era no-op para destinos CLI/Codex (`cancelSessionTurn()` solo manejaba `currentTurnAbort`, que solo setea API). Ahora `cancelSessionTurn()` cubre los 3 (API vía `currentTurnAbort`, CLI/Codex vía el hook `cancelCurrentTurn`), y la detección de idle usa `turnInFlight` (fiable para los 3) — así que ya no reparte a un panel CLI/Codex ocupado, y si se cancela el turno de origen, los sub-turnos hijos CLI/Codex sí se frenan (CLI: kill preservando sessionId; Codex: kill destructivo + waiter desbloqueado, requiere reconexión).

**RESUELTO — gap del `originSignal`**: la limitación residual de arriba (el `originSignal` era `session.currentTurnAbort?.signal`, solo API) está cerrada — ver `docs/_arch/CONTRACT.md` → "Fix real — señal de cancelación uniforme para el origen de `parallel_ask` (`turnAbortSignal`)". Investigación previa (`docs/_arch/verify_origin_signal_design.md`) confirmó, no buscado, que el escenario descrito (origen CLI/Codex) no era alcanzable por ningún camino de código real hoy (`parallel_ask` solo está wireado para `ApiAgentRuntime`) — el fix igual generaliza el mecanismo para dejar la base lista para cuando `parallel_ask` se conecte a CLI/Codex.

## Sin priorizar, prioridad baja — Hooks de extensión para el usuario

Inspirado en deepseek-harness ("dsh")/Claude Code: 8 puntos reales de ciclo de vida (`PreToolUse`/`PostToolUse`/etc.) donde el usuario podría engancharse con su propio script — interceptar, loguear, o incluso bloquear una acción antes/después de que corra. **Amatista no tiene ningún mecanismo de este tipo hoy.**

**Decisión**: no construir. Es superficie de ataque real (un hook de usuario mal escrito, o comprometido, corre con el mismo nivel de acceso que el resto de Amatista) sin ninguna necesidad concreta identificada que lo justifique todavía. Documentado únicamente como referencia futura, sin urgencia ni caso de uso real pendiente.

## RESUELTO — Formalizar el patrón de "guardias monótonas"

Documentado en `docs/_arch/CONTRACT.md` → "Principio de diseño — guardias monótonas (un chequeo que solo puede rechazar, nunca aprobar por su cuenta)", con los 3 ejemplos reales ya existentes citados (staleness de `write_file`/`apply_patch`, staleness de `revert_file`, `sessionFileHashes`/TOCTOU) y un criterio concreto de auto-chequeo para guards futuros ("si este chequeo se comentara por completo, ¿el sistema queda igual o peor que sin él?").

## Confirmado y descartado explícitamente — no re-investigar

- **Separar sandbox mode de aprobación en 2 controles independientes**: confirmado con código real (`resolveApproval()`, `tool-registry.ts`) que hoy están acoplados a propósito — `sandbox: SandboxMode` es el único input que decide si `confirm()` se llama, sin ninguna variable de aprobación separada (ver confirmación puntual de esta sesión). **Decisión: NO desacoplar** — la simplificación actual (3 ramas fijas: `read-only` bloquea sin preguntar, `workspace-write` pregunta, `danger-full-access` aprueba sin preguntar) cubre bien el uso real; no hay caso real que necesite, por ejemplo, `danger-full-access` con aprobación igual, o `workspace-write` sin preguntar nunca (más allá de `toolTrustSession`, que ya es un mecanismo aparte, session-wide, dentro de la rama `workspace-write`).
- **Reescritura post-ejecución de resultados de tools**: descartado — sin ningún caso real que lo motive hoy.

## RESUELTO — Compactación podía guardar un resumen sobre historial ya descartado (TOCTOU de `maybeCompactChatInBackground`)

Hallazgo de la 3ra revisión externa, investigado (`docs/_arch/verify_compaction_toctou.md`) e implementado real (`docs/_arch/CONTRACT.md` → "Fix real — TOCTOU de compactación..."). **Confirmado alcanzable con un solo panel** — el `fire-and-forget` de la compactación libera `turnInFlight` antes de que su llamada HTTP real resuelva, y `chats:deleteMessagesFrom` (editar/regenerar) no tiene ningún guard — 2 modos de falla reales medidos: (A) watermark colgante — una edición concurrente borra el mensaje que sería el nuevo watermark, `getMessagesAfter()` queda devolviendo `0` para siempre (compactación atascada en silencio); (B) resurrección — una edición legítima invalida el resumen (`null` correcto vía `invalidateSummaryIfWatermarkMissing()`), pero una compactación vieja ya en vuelo lo sobreescribe al resolver, describiendo contenido que el usuario ya borró. **Severidad confirmada baja/media, no comparable al clobber de archivos**: el historial crudo (`chat_messages`, fuente de verdad) nunca se corrompe ni se pierde, y `normalizeHistory()` nunca depende del watermark — solo se degrada un bloque de memoria auxiliar de solo-contexto.

**Fix real**: `getLastMessageId(chatId)` nueva en `chat-store.ts` (firma barata — id del último mensaje real de la cola), capturada al arrancar `maybeCompactChatInBackground()` y re-consultada SINCRÓNICAMENTE justo antes de `setChatSummaryState()` — si no coincide, el resultado se descarta por completo, sin persistir nada. Fire-and-forget preservado, sin errores visibles.

**Verificado real, reproduciendo los 2 casos exactos de la investigación**: Caso A (watermark colgante) → con el fix, estado final `null`, descartado por completo. Caso B (resurrección) → con el fix, estado final `null`, la invalidación de la edición sobrevive intacta. No-regresión: 2 pasadas normales consecutivas sin concurrencia persistieron watermark+summary reales, comportamiento idéntico al de antes. `npm run typecheck`/`npm run build` limpios.

**Prioridad**: media — plausible pero no confirmado, y el pipeline de compactación es "mejor esfuerzo" por diseño (nunca bloquea el turno, un resumen corrupto es recuperable en la próxima pasada) — menor severidad que un TOCTOU sobre el archivo real del usuario.

## RESUELTO — `parallel_ask` + `danger-full-access` reabría el clobber silencioso de `write_file`/`apply_patch`/`revert_file`

Hueco de la 3ra revisión externa, investigado (`docs/_arch/verify_parallel_staleness_reopened.md`), diseñado (`docs/_arch/verify_toctou_parallel_fix_design.md`) e implementado real (`docs/_arch/CONTRACT.md` → "Fix real — TOCTOU de `write_file`/`apply_patch`/`revert_file` bajo concurrencia real de `parallel_ask`..."). La conclusión de `verify_staleness_realistic_gap.md` ("estructuralmente inalcanzable") era correcta solo para un runtime SECUENCIAL (1 panel) — no modeló la concurrencia genuina entre paneles de `parallel_ask`. Con 2 paneles reales en `danger-full-access` sobre el mismo archivo: clobber silencioso 25/25 en simultaneidad genuina, medido antes del fix.

**Fix real (Opción A2 del diseño, elegida sobre lock-por-archivo y write atómico)**: el re-chequeo de hash quedó pegado, sin ningún `await` de por medio, al `writeFileSync` — el `snapshotFile()` viejo (un solo commit "original"+"newContent" ANTES del write, con un `await` real de por medio que abría la ventana) se partió en `snapshotOriginalIfNeeded()` (antes del re-chequeo, solo archiva lo que YA está en disco) y `commitVersion()` (después del write real, nunca se alcanza si el re-chequeo rechazó — cero commits huérfanos). Aplicado a las 3 rutas de escritura. Más un fix separado para el hallazgo secundario (contención real de git entre archivos DISTINTOS del mismo workspace, mismo repo VCS oculto): cola de ejecución por-repo + timeout real en `runGit()` (no tenía ninguno).

**Verificado real, 5/5 casos**: clobber cerrado en TODO el rango 0-800ms (antes clobber pleno en 0-200ms); cero commits huérfanos confirmado leyendo el historial VCS real; contención de git resuelta (0/20 fallos, antes 10/20); timeout real confirmado con un hook `pre-commit` real que cuelga (`write_file` resolvió en 15397ms, nunca colgado, escritura real siempre completa); no-regresión con 1 solo panel en las 3 tools. `npm run typecheck`/`npm run build` limpios.

## Notas breves — 3ra revisión externa (adicionales, menor prioridad)

- **RESUELTO — Transparencia real vs. simulado en la documentación de verificaciones**: convención agregada en `docs/_arch/CONTRACT.md` → "Convención de documentación — transparencia real vs. simulado en la verificación de cada fix" (hacia adelante, sin reescribir entradas viejas): distingue explícitamente "100% real" (binario/proceso/servidor real efectivamente ejecutado) de "stand-in controlable" (mock/harness que emula la forma real sin serlo), con el criterio de que un destino de prueba aislado (carpeta/`%TEMP%`) no invalida "real" si el mecanismo/binario/protocolo sí lo es.
- **Carpeta de datos seleccionable en el instalador**: RESUELTO — ver `## RESUELTO — carpeta de datos seleccionable en el instalador` más arriba en este mismo archivo.

## RESUELTO — `parallel_ask` no re-validaba identidad completa del destino, solo ocupación

Hallazgo 2 de la 4ta revisión arquitectónica externa (5/5 confirmados), investigado (`docs/_arch/verify_external_review_4_findings.md`), diseñado (`docs/_arch/verify_parallel_ask_identity_guard_design.md`) e implementado real (`docs/_arch/CONTRACT.md` → "Fix real — guard de identidad completa para `parallel_ask`..."). El re-chequeo del Hallazgo 1 (`docs/_arch/verify_parallel_idle_detection_design.md`) validaba conexión+ocupación antes de despachar, pero nunca comparaba que el panel destino siguiera siendo el MISMO chat/workspace/proveedor/modelo que cuando `planParallelAsk()` armó la asignación aprobada — reproducido real: reconectar un panel destino en la ventana de aprobación humana (`ctx.confirm()`) hacía que la sub-tarea se despachara igual, contra la identidad NUEVA (no aprobada), etiquetada con la VIEJA.

**Fix real**: 2 campos nuevos (`approvedChatId`/`approvedWorkspace`) capturados en `planParallelAsk()` junto a `providerId`/`modelId` (ya existían, ahora también se comparan, no solo se muestran) — firma de identidad completa. `runParallelAsk()` gana un 2do chequeo secuencial, DESPUÉS del de ocupación ya existente (mensaje distinguible): si `activeChatId`/`activeWorkspace`/`provider.id`/`model.id` de la sesión VIVA no coinciden exacto con lo aprobado, esa sub-tarea puntual se rechaza — guardia monótona (mismo principio ya documentado en `CONTRACT.md`), nunca aborta las demás.

**Verificado real, 4 casos** (harness standalone con esbuild, código real de `planParallelAsk()`/`runParallelAsk()` sin mocks): reconexión real de un panel destino → rechazado con el mensaje nuevo, distinto del de ocupación; el resto del mismo batch (panel sin cambios) siguió despachándose normal; confirmado con evidencia real que "nivel de esfuerzo" nunca participa del guard (campo que ni existe en `SessionRuntimeState`, confirmado por grep — el panel de control sin ningún cambio de identidad se despachó normal); no-regresión, batch normal sin ningún cambio, idéntico a antes. `npm run typecheck`/`npm run build` limpios.

**Mejora futura bien definida, NO implementada, para cuando haya interés real**: "reautorización rápida" — hoy, si el guard rechaza una sub-tarea por identidad cambiada, esa sub-tarea se pierde por completo (el modelo tendría que volver a llamar `parallel_ask` desde cero). Una mejora real sería, en el mensaje de rechazo devuelto al modelo, incluir explícitamente la NUEVA identidad del panel (chat/proveedor/modelo actual) para que el modelo pueda decidir si vale la pena reintentar esa sub-tarea puntual contra el destino nuevo (una 2da llamada a `parallel_ask`, con aprobación humana de nuevo) — sin automatizar ningún re-despacho silencioso (rompería la misma garantía que este fix protege). Alcance chico, aislado, no bloqueante — anotado para no perderlo, no priorizado.

## RESUELTO — carpeta de datos dentro de `$INSTDIR` se borraba al desinstalar (Hallazgo 1 de la 4ta revisión externa)

Confirmado real que `RMDir /r $INSTDIR` (desinstalador estándar de electron-builder) borra sin ninguna excepción, y que esto se dispara no solo al desinstalar manualmente sino en **cada actualización normal**, en silencio (`uninstallOldVersion`) — investigado (`docs/_arch/verify_installer_data_preservation_design.md`, diseño original: exclusión post-hoc en el desinstalador). **Cambio de dirección real del usuario**: en vez de mitigar después, eliminar el estado peligroso en el origen — ver `docs/_arch/CONTRACT.md` → "Fix real — validación en el origen de la carpeta de datos del instalador...". El checkbox "misma carpeta que la instalación" se retira por completo; la página real de `nsDialogs` rechaza activamente cualquier intento de elegir `$INSTDIR` o una subcarpeta suya como carpeta de datos, con `${StrCase}` (real, `StrFunc.nsh`) para la comparación case-insensitive — mismo principio que `isWithinFolder()` (TypeScript, 3ra revisión externa). Verificado real, 4 casos, con el instalador completo generado y `computer-use`: exacto → rechazado; subcarpeta → rechazado (mensaje distinto); fuera de `$INSTDIR` → aceptado, instalación real completa, registro confirmado; mayúsculas/minúsculas mezcladas → detectado igual. Cero lógica nueva en el desinstalador — `uninstaller.nsh` sin tocar.

## RESUELTO — la cola de git serializaba comandos individuales, no la operación add+commit completa (Hallazgo 4 de la 4ta revisión externa)

Hallazgo 4 de la 4ta revisión arquitectónica externa (5/5 confirmados), investigado (`docs/_arch/verify_external_review_4_findings.md`), diseñado (`docs/_arch/verify_git_atomic_operation_design.md`) e implementado real (`docs/_arch/CONTRACT.md` → "Fix real — `runInRepoQueue()`, la cola de git pasa a envolver la operación lógica completa..."). `repoQueues` (la cola por-repo del fix de TOCTOU) serializaba cada llamada individual a `runGit()`, no la operación lógica completa — `commitPath()` cedía el event loop entre su `add` y su `commit`, permitiendo que otro `commitPath()` concurrente encolara su propio `add` en el medio. Reproducido real: 10 escrituras concurrentes al mismo repo oculto colapsaron en 1 solo commit real, mal atribuido (9 caían en "nothing to commit").

**Fix real**: `runInRepoQueue<T>(cwd, task)` nueva — generaliza el mismo tail-chaining de promesas ya existente para encolar una operación lógica completa (potencialmente varias llamadas a `runGit()`) como unidad indivisible. `runGit()` pasa a ser el primitivo puro de ejecución, sin cola propia. `commitPath()` envuelta ENTERA (el fix real); `ensureVcsRepo()`/`listHistoryInRepo()`/`readFileVersion()` migrados al mismo primitivo, sin cambio de comportamiento real. `GIT_TIMEOUT_MS` sin tocar.

**Verificado real, 3/3 casos, git real, subprocesos reales**: mismo escenario exacto del Hallazgo 4 (10 escrituras concurrentes) → 10 commits reales, cada uno con su propio archivo y atribución correcta, cero colapso; no-regresión del timeout real de git colgado (hook `pre-commit` real con `sleep 30`) → corte real en ~15s, cola recuperada para la siguiente operación; repos DISTINTOS en paralelo real (2 workspaces, 5 escrituras cada uno) → tiempo real con ambos en paralelo MENOR que uno solo (razón 0.87x, no ~2x) — confirma cola por-repo, no global, atomicidad intacta en ambos. Bug real propio del harness (no del código bajo prueba) encontrado y corregido durante la propia verificación: el helper de disambiguación de repos devolvía el repo equivocado con varios workspaces bajo la misma raíz — fix, disambiguación real por contenido de `seed.txt` vía `git show HEAD:seed.txt`. `npm run typecheck`/`npm run build` limpios.

## RESUELTO — descubrimiento de Gemini trataba 429/503 igual que 404 (Hallazgo 5, el último de la 4ta revisión externa)

Hallazgo 5 de la 4ta revisión arquitectónica externa (5/5 confirmados), investigado (`docs/_arch/verify_external_review_4_findings.md`), diseñado (`docs/_arch/verify_gemini_inconclusive_states_design.md`) e implementado real (`docs/_arch/CONTRACT.md` → "Fix real — tri-estado `confirmed`/`unavailable`/`inconclusive`..."). `verifyGeminiModel()` usaba `response.ok` puro — 404 (retiro genuino), 429/503 (transitorio), 403/400 (permiso/request) y timeout/red quedaban todos indistinguibles, descartados en silencio del catálogo como si fueran el mismo 404 real. **Con este fix, los 5 hallazgos de la 4ta revisión externa quedan cerrados.**

**Fix real**: tri-estado real por status — `200`→`confirmed`, `404` exacto→`unavailable` (única señal real y determinista de retiro), cualquier otro no-200→`inconclusive`. Reintento real con backoff acotado a las 3 clases que Google documenta como reintentables (`429`/`500`/`503`+timeout/red, hasta 3 intentos totales, backoff 800ms/2000ms) — `403`/`400` nunca se reintentan. Los inconclusos que sobreviven al reintento se reportan aparte (`GeminiCatalogResult.inconclusive`, con su `status` real), nunca se agregan al catálogo ni se descartan en silencio. `GEMINI_VERIFY_CONCURRENCY` sin cambio (5) — sin evidencia real medida de que el paralelismo fuera el driver del riesgo original. `syncGemini()` arma el mensaje tri-estado en el feedback local a la fila.

**Verificado real, 4/4 casos con servidor HTTP local real controlable** (cero cuota real gastada para esa parte): 200→confirmed; 404→unavailable; 429 con 2do intento 200→rescatado real por el backoff (gap real 815ms); 503 persistente→agota los 3 intentos reales, sigue inconclusive; 403/400→inconclusive con exactamente 1 request real cada uno, nunca reintentado. **Corrida real final contra la conexión Google real del usuario** (key real nunca vista): mensaje real `"26 confirmados, 1 retirados, 13 sin verificar por limite de cuota -- reintenta mas tarde."`. Hallazgo real adicional (no bug, observación honesta): de los 13 inconclusos reales ninguno fue 429 — la mayoría 400 (modelos TTS/audio/computer-use/deep-research/Lyria, cuyo request mínimo de texto plano no coincide con la forma real que esperan) y 3 fueron 500 (familia Lyria) — el texto "sin verificar por limite de cuota" sigue siendo el mejor default (nunca miente sobre retiro) pero la causa real más común no fue cuota; anotado para una redacción más genérica futura, no priorizado. `npm run typecheck`/`npm run build` limpios.

## RESUELTO — infraestructura real de tests de regresión (`tests/regression/`), 8 harnesses reales conservados como tests versionados

Investigado (`docs/_arch/verify_regression_test_infra_design.md`) e implementado real (`docs/_arch/CONTRACT.md` → "Infraestructura real de tests de regresión..."). Sugerencia real de la revisión externa: en vez de borrar siempre cada harness real de verificación al terminar, conservar los de mayor valor como tests de regresión versionados. `node:test` nativo + `esbuild` (cero dependencias nuevas), `npm run test:regression`.

**Hallazgo real más serio de esta pasada, en la propia infraestructura de tests, no en el código de producción**: la primera versión de los tests seteaba `AMATISTA_STORAGE_ROOT` DENTRO de cada archivo, antes de su propio `import` — no alcanza (los `import` se evalúan antes del resto del módulo). 2 corridas de prueba reales escribieron repos VCS de prueba dentro de `D:\AMATISTA\data\vcs\` real — detectado, confirmado que `amatista.db` (chats reales) nunca se escribió, limpiado a mano. Fix real: `tests/regression/_support/run.cjs`, wrapper que setea el storage root aislado en el PROCESO PADRE antes de lanzar `node --test`.

Los 8 candidatos: TOCTOU de `write_file` bajo concurrencia, `runInRepoQueue()`/atomicidad de git, `resolveJsonSchemaRefs()`, guard de identidad de `parallel_ask`, `isWithinFolder()`, `runtimeFor()`, tri-estado de Gemini, límite de `sessionRegistry`. Verificado real, 12/12 aserciones en verde; validado con revert-temporal-y-restauración real para 3 de los 8 (`resolveJsonSchemaRefs()`, `runInRepoQueue()`, staleness de `write_file`) — los 3 tests fallaron real al revertir el fix correspondiente, restaurados a byte-idéntico después. `npm run typecheck`/`npm run build` (la app normal) sin cambio de comportamiento — `tests/` fuera de los `tsconfig.*.json` y del `files` allowlist de `electron-builder`.

## RESUELTO — `npm run bench:bundle` estaba roto, cierra el Ítem 1 del Punto 7 de la 4ta revisión externa (benchmark SWE-Bench ProMax)

Investigado (`docs/_arch/verify_benchmark_completion_scope.md`) y confirmado real: `npm run bench:bundle` fallaba (`No loader is configured for ".node" files`, `@napi-rs/canvas` nativo) desde que `read_document` (`document-reader.ts`) se agregó a `tool-registry.ts` ~2 horas después del último commit real del benchmark (mismo día, 2026-08-31) — nadie lo notó porque nadie volvió a correr el benchmark desde entonces. **Fix aplicado**: `--external:@napi-rs/canvas --external:pdfjs-dist` agregado al script `bench:bundle` en `package.json` — mismos externals que ya usa `tests/regression/`. Confirmado real: `npm run bench:bundle` bundlea limpio (cero errores, cero warnings, `benchmark/dist/run-instance.cjs` producido, ~2MB).

**Ítems 2 (script de agregación de `summary.jsonl`/`final_result.json`) y 3 (mecanismo de reintentos) del mismo Punto 7 — DESCARTADOS por costo, decisión explícita del usuario, no un olvido**: el benchmark en sí no es algo que se planee correr activamente ahora — construir infraestructura real para tandas futuras que no se van a generar sería trabajo especulativo. Ambos quedan documentados en `verify_benchmark_completion_scope.md` (diseño ya investigado: qué haría falta, alcance estimado) para retomar si algún día se prioriza correr el benchmark de nuevo. "Intervención humana" (el 3er punto original de la revisión) no necesitaba ningún trabajo — ya es 0 por diseño del harness (`ctx.confirm` fijo en `true`, `sandbox:'danger-full-access'` fijo), documentado así, sin acción pendiente.

## Sin priorizar, prioridad baja — guard de compactación (`getLastMessageId()`) descarta un turno nuevo igual que una edición/borrado, sin presión real que lo justifique medir todavía

Observación de la 4ta revisión arquitectónica externa: el guard de TOCTOU de compactación (`maybeCompactChatInBackground()`, `compaction-engine.ts`) pide medir con qué frecuencia real descarta un resumen recién calculado — en particular cuando el chat solo recibió mensajes nuevos mientras la llamada de compactación seguía en vuelo, sin que nadie editara ni borrara nada. Decisión del usuario, confirmada: **no tocar código todavía**, solo investigar y documentar.

**Hallazgo real, no solo sospecha**: el guard agrupa a propósito "turno nuevo agregado" en la MISMA categoría que "mensaje borrado/editado" — confirmado en el propio comentario del código (`compaction-engine.ts:427`, y en `CONTRACT.md` → "Fix real — TOCTOU de compactación..."), decisión de diseño original, no un efecto colateral no anticipado. La verificación original de ese fix (Caso C, no-regresión) solo probó 2 pasadas de compactación **secuenciales sin ninguna edición concurrente** — nunca probó específicamente el caso más común en la práctica (uso activo normal del chat mientras una compactación de fondo sigue en vuelo).

**Confirmado real que hoy no hay datos retroactivos para medir esto**: el único punto donde el descarte deja rastro es un `console.log()` gateado por `AMATISTA_DEBUG_TOOLS === '1'` (`compaction-engine.ts:439`) — nunca escribe a la base ni a ningún archivo, y esa variable de entorno de desarrollo nunca estuvo seteada en uso real. Las columnas reales de `chat_sessions` (`summary`/`summary_watermark_id`/`structured_memory`) solo reflejan el estado ACTUAL, nunca un historial de intentos descartados.

**Confirmado real, mismo patrón exacto que "Recuperación selectiva por tema" de arriba (memoria dormida)**: lectura de solo lectura de una copia aislada de la base real del usuario (`D:\AMATISTA\data\config\amatista.db`, nunca tocado el archivo real — confirmado mismo tamaño/mtime antes y después) — **0 de 19 chats reales tienen compactación activa hoy**: `summary`/`summary_watermark_id`/`structured_memory` vacíos en el 100% de los casos. El trigger real (`backlogTokens > CONTEXT_TOKEN_BUDGET=6000`) nunca se cruzó ni una sola vez — el chat real más activo tiene 17 mensajes cortos, muy por debajo del umbral. `maybeCompactChatInBackground()` hace `early return` antes de intentar la llamada al modelo en el 100% de los chats reales.

**Sin presión real medible hoy** — el problema de fondo (compactación disparándose con uso concurrente activo) no existe todavía porque la compactación en sí nunca se disparó ni una vez. Sin fix, sin logging nuevo agregado. Reabrir si algún chat real acumula suficiente historial como para que la agresividad del guard empiece a importar en la práctica — recién ahí valdría la pena instrumentar la frecuencia real de descarte.

## Sin priorizar, prioridad baja — "Restaurar vista de paneles de una orquestación"

Idea del usuario. `openPanels` (qué paneles están abiertos) **no se persiste entre sesiones** — confirmado varias veces en esta sesión (es estado de React puro, `useState`, sin ningún roundtrip a disco) — cerrar la app pierde esa lista por completo. Tampoco existe ningún registro real de "estos paneles trabajaron juntos como grupo" — ej. los que un orquestador (`send_to_window`/`parallel_ask`) abrió para un proyecto real. Hoy, cerrar los paneles dependientes de una orquestación (dejando solo el principal) no tiene forma real de "restaurarse" — hay que volver a pedirle al modelo que los reabra, o abrirlos a mano.

**Propuesta del usuario**: un ítem de menú contextual (click derecho) en el panel/chat PRINCIPAL, "Restaurar vista de paneles", que reabra los paneles dependientes que trabajaron con él la última vez.

**Necesitaría diseñarse** (nada de esto investigado ni decidido todavía):
1. Algún mecanismo real de registro de qué paneles se abrieron como parte de una orquestación de un chat principal dado — hoy no existe ningún tracking así.
2. Dónde persistir eso (¿por chat principal, en la DB real, `chat_sessions` o una tabla nueva?).
3. El propio ítem de menú + la lógica de reapertura (reusando `openChatInPanel()` ya existente).

No investigado, no diseñado en detalle, no implementado — prioridad baja, para otra sesión.
