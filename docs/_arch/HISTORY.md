# HISTORY.md — Bitácora de Fases

> Log append-only. Una entrada por fase ejecutada.

| Fase | Fecha | Resumen | Archivos tocados | Estado |
|---|---|---|---|---|
| 19 | 2026-08-26 | Log de actividad en vivo del turno pasa a mostrar el archivo/comando especifico + diff de lineas (+X/-Y) en vez de solo "tool completado" generico. Tarea 0 (investigacion real contra codex app-server): los eventos nativos de Codex YA traen detalle rico (item.type:'commandExecution' -> item.command real; item.type:'fileChange' -> item.changes[].path + .diff), mucho mas que el generico name/phase de ApiAgentRuntime -- wireCodex() no necesito ningun cambio (reenvia raw/notification sin sintetizar nada). Hallazgo real colateral: summarizeCodexItem() (App.tsx) ya intentaba leer item.path/item.file para items de archivo, campos que NUNCA existen en el shape real (es item.changes[].path) -- nunca matcheaba antes de este fix, corregido para leer el campo real; el verbo (Creando/Editando/Borrando) solo se etiqueta con certeza para kind.type:'add' (unico confirmado en vivo, el helper de sandbox de Windows no esta instalado en la maquina de investigacion y bloqueo probar 'modify'/'delete'). Camino API: toolStatusArgDetail() (api-agent-runtime.ts, nueva) extrae path/command/pattern segun la tool desde los args que runTool() ya recibe, sin cambiar que se ejecuta. ToolExecutionResult gana lineDiff opcional; formatWriteFileDiff() (tool-registry.ts) paso de devolver solo el string del dialogo de aprobacion a devolver {preview, added, removed} del MISMO DiffLine[] de computeLineDiff() ya calculado ahi (countLineChanges(), un tabulado simple, confirmado con grep que computeLineDiff( tiene un unico call site -- nunca se recalcula). write_file/apply_patch adjuntan lineDiff a su resultado exitoso; revert_file solo ajustado a la nueva firma (.preview), sin ganar lineDiff (fuera del alcance pedido). UI: toolCallTargetLabel()/lineDiffLabel() arman "Ejecutando: write_file (src/App.tsx)" y "write_file completado (src/App.tsx, +100 -20)" -- antes mostraba el workspace conectado (siempre el mismo valor durante todo el turno) en vez del argumento real de cada llamada. | `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; Tarea 0 verificada en vivo contra el transporte real de Codex; conteo +X/-Y confirmado como reuso del mismo computeLineDiff(), no un calculo nuevo |
| Fix | 2026-08-26 | Watchdog stale: TURN_WATCHDOG_MS (Fase 14) se leia congelado en el closure del primer render dentro de startTurnWatch(), porque handleAgentEvent (unica que la llama) se suscribe una sola vez via useEffect(...,[]) (confirmado con grep antes de tocar codigo, linea ~873, deps vacias) -- si el usuario cambiaba el timeout en Settings a mitad de sesion, el cambio nunca se aplicaba hasta recargar la app. Mismo mecanismo exacto que ya resolvia turnStepsRef para el log de pasos del turno: turnWatchdogMsRef = useRef(TURN_WATCHDOG_MS) + useEffect(() => {turnWatchdogMsRef.current = TURN_WATCHDOG_MS}, [TURN_WATCHDOG_MS]) -- el efecto si se re-ejecuta en cada render donde cambia el valor (a diferencia del useEffect de suscripcion, que tiene deps vacias), asi que el ref queda sincronizado con Settings en vivo aunque startTurnWatch() siga en el closure congelado. Las dos referencias directas dentro de la funcion (el setTimeout y el string del mensaje de error) pasan a leer turnWatchdogMsRef.current. Verificado por lectura directa del codigo resultante (mismo criterio ya usado para sendClaude() en Fase 17 Parte 2): grep confirma cero referencias directas a TURN_WATCHDOG_MS dentro de startTurnWatch(). | `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; verificado por lectura, sin test end-to-end (no hacia falta) |
| 18 | 2026-08-26 | Sincronizacion real de catalogo para conexiones openai-chat (OpenRouter y cualquier backend Chat-Completions-compatible), mismo patron que syncCodexProvider()/listCodexModels() de Codex pero SIN reemplazo completo del array de modelos -- con 400+ modelos posibles eso seria inmanejable, en su lugar un panel buscable donde el usuario agrega modelos puntuales. Fetch real GET <endpoint>/models (modulo nuevo openai-chat-catalog.ts), confirmado en vivo contra OpenRouter real sin necesitar key (endpoint publico): 417 modelos reales, shape {data:[{id,name,context_length,supported_parameters,architecture:{input_modalities},top_provider:{max_completion_tokens}}]}. Parseo defensivo de 4 senales: supportsTools ("tools" exacto en supported_parameters, 348/417 lo traen), supportsVision ("image" en architecture.input_modalities), maxOutputTokens (top_provider.max_completion_tokens, confirmado 131072 real para stealth/ox-alpha, coincide con lo documentado a mano en Fase 15), contextLength (solo informativo). IPC nuevo dedicado (ipc-openai-chat-catalog.ts, no sumado a ipc-cli.ts por no tener relacion con CLIs) recibe {endpoint,apiKey} directo del provider activo en memoria del renderer. UI: boton "Sincronizar modelos" solo para type:'openrouter', panel con input de busqueda (filtra por id/displayName, nunca un select gigante ni scroll infinito), checkbox "Mostrar todos" para destapar el filtro-por-default de tools, lista con scroll acotado y boton "Agregar" por fila que suma ESE modelo puntual con los campos precargados desde datos reales (addCatalogModel(), no addManualModel() vacio). Verificado real contra la funcion real (bundle esbuild standalone, --external:electron + stub): 417 modelos parseados sin error, stealth/ox-alpha con los 4 campos correctos, busqueda "ox" lo encuentra. | `src/main/openai-chat-catalog.ts`, `src/main/ipc-openai-chat-catalog.ts` (nuevos), `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real contra OpenRouter sin key (417 modelos, ox-alpha, busqueda); test con key transitoria real pendiente de que el usuario la provea |
| 17 (Parte 2) | 2026-08-26 | Vision real en los 2 runtimes CLI (claude-cli/codex), cierra Fase 17 completa junto con Parte 1. Investigacion previa (3 hallazgos empiricos, ninguno asumido): Codex localImage NO respeta el cwd del spawn (a diferencia de Read de Claude Code, que si lo rechaza fuera de cwd); Codex {type:'image', url:'data:...'} acepta un data URI embebido sin filesystem (probado con logoamatista.png real -- la 1x1 de ejemplo de Anthropic dio un falso negativo, limitacion conocida de imagenes <200px); claude -p SI tiene mecanismo de imagen embebida pero no es un flag, es --input-format stream-json --output-format stream-json con el mensaje por stdin en shape Messages API real -- corrigio la hipotesis inicial de necesitar un archivo temporal. Riesgo identificado antes de tocar codigo: sendClaude() actual hace un JSON.parse(stdout) unico al final asumiendo un blob, pero stream-json emite varias lineas JSON por turno -- migrar TODO sendClaude() habria expuesto una regresion silenciosa (el try/catch absorbe el parse roto y devuelve lineas crudas como respuesta) al 100% de los turnos, no solo a los que tienen imagen. Decision: bifurcacion condicional, no migracion completa -- turno sin imagenes usa sendClaude() sin cambio de codigo ejecutado, turno con imagenes delega a sendClaudeWithImages() nuevo con parseo linea-por-linea (mismo patron readline que sendGemini()). Codex: un solo cambio en sendTurn(), input suma {type:'image', url:attachment.preview} por cada imagen del turno actual. Claude: parseDataUrl()/claudeImageBlocks() duplicados localmente en cli-agent-runtime.ts a proposito (no reusados de api-agent-runtime.ts, restriccion de la fase de no tocar ese archivo + ninguna de las dos funciones esta exportada). Bug real encontrado en la Tarea 3 (no en la investigacion): --print + --output-format=stream-json sin --verbose es rechazado por el CLI, y el intento de escritura a stdin igual generaba un 'error' sin manejar que tumbaba el proceso Node entero -- fix de dos partes, --verbose agregado a los args + handler defensivo en child.stdin. Verificado real contra CliAgentRuntime/CodexClient reales (bundle esbuild standalone): 3 turnos Claude en la misma sesion (sin imagen establece sessionId -> con imagen +--resume +--effort low, descripcion correcta -> sin imagen de nuevo, recuerda el turno 1, cero regresion); Codex con imagen real via sendTurn()+notificacion turn/completed, descripcion correcta. | `src/main/codex-client.ts`, `src/main/cli-agent-runtime.ts` | Completa (Parte 2, Fase 17 completa) — `npm run typecheck` y `npm run build` en verde; verificado end-to-end contra las clases reales de produccion (bundle esbuild), 3 casos con imagen real (Claude x1 + Codex x1) mas continuidad de sesion confirmada en Claude, no solo scripts de investigacion standalone |
| 17 (Parte 1) | 2026-08-25 | Vision real en los 4 runtimes API (foundry/anthropic-api/gemini-api/openai-chat) -- hasta ahora ninguno mandaba bytes de imagen pese a capabilities.vision:true, runtimeAttachments() descartaba ChatAttachment.preview y solo se inyectaba una referencia de texto. Tarea 0 (investigacion, sin tocar codigo): shape openai-chat confirmado en vivo contra OpenRouter real (stealth/ox-alpha, HTTP 200, descripcion coincide con la imagen real de prueba); Codex (LocalImageUserInput en el schema real, probado en vivo) y claude-cli (Read nativo multimodal, confirmado empiricamente) quedan fuera de esta Parte 1, pendientes de la ronda de attachment storage; Anthropic/Foundry/Gemini verificados solo contra documentacion oficial, sin key en texto plano disponible (keys cifradas con safeStorage, desencriptar headless bloqueado por el clasificador de seguridad). Tarea 1: preview dejaba de descartarse en DOS chokepoints independientes (runtimeAttachments() en App.tsx Y runtimeAttachmentView() en attachments.ts -- este ultimo es el real, buildRuntimeContext() lo llama sobre payload.attachments, no sobre el array del renderer directamente), solo para kind:'image'. Tarea 2: helper parseDataUrl() compartido separa (mimeType, base64Puro) una sola vez; Foundry/openai-chat mandan el data URL completo (preview tal cual), Anthropic/Gemini el base64 sin prefijo; imagen solo en el turno ACTUAL (context.attachments es siempre el turno actual por construccion), historial sigue como referencia de texto, nunca se re-manda una imagen vieja. Tarea 3: guard de tamano antes de armar el payload (assertImageAttachmentsWithinLimit(), chokepoint unico en send()), limites reales verificados contra documentacion oficial: Anthropic 10MB base64-encoded (exacto), Foundry 20MB (exacto), Gemini 20MB del request completo (exacto), openai-chat sin numero exacto documentado por OpenAI -> se uso el mas conservador de los confirmados (10MB), por instruccion explicita del usuario. | `src/renderer/src/App.tsx`, `src/main/attachments.ts`, `src/main/api-agent-runtime.ts` | Completa (Parte 1) — `npm run typecheck` y `npm run build` en verde; 1 de 4 shapes API verificado en vivo (openai-chat), 3 verificados solo contra documentacion (sin key transitoria disponible para Anthropic/Foundry/Gemini en esta ronda); Parte 2 (CLI: context-envelope.ts/cli-agent-runtime.ts/codex-client.ts) pendiente de la investigacion de attachment storage |
| 16 | 2026-08-25 | Tool nueva search_files: busqueda de texto real en el workspace (archivo:linea:contenido por match), la brecha mas grande frente a Grep/Glob de Claude Code/Codex nativos. Dos capas "intentar lo rapido, caer a lo generico" (mismo criterio que Fase 14): git grep -n -I --untracked (respeta .gitignore, incluye archivos nuevos sin git add) via execFile (NO exec -- decision de seguridad real, search_files no pide aprobacion asi que pattern/path del modelo no pasan por revision humana antes de ejecutarse, execFile evita inyeccion de shell al no invocar ninguna). Exit codes de git grep manejados correctamente: 0=matches, 1=sin matches (valido, no error), otro=fallback manual. Fallback manual reusa ignoredDirectories/MAX_TEXT_FILE_BYTES ya exportados de workspace-tree.ts (una sola lista de exclusion). Tope explicito SEARCH_FILES_MAX_MATCHES=200. path pasa por resolveWithinWorkspace como todas las demas tools. Sumada a EXPLORE_TOOL_NAMES (Fase 4) sin necesitar tocar runReadOnlyTool(), ya filtra generico por nombre. Verificado real con git de verdad: archivos commiteados encontrados con linea correcta, archivo untracked incluido (--untracked) sin que node_modules/ignorado por .gitignore aparezca nunca, patron inexistente da ok:true "Sin resultados." (no error), workspace sin git cae al fallback manual y encuentra igual, mas case-insensitive y path traversal bloqueado. | `src/main/tool-registry.ts`, `src/main/explore-tool.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con git real (commiteado + untracked + sin git) y ToolRegistry.execute() real, no reimplementado |
| 15 | 2026-08-24 | Runtime HTTP nuevo 'openai-chat' (Chat Completions estilo OpenAI) para type:'openrouter', origen: usuario comparte openrouter.ai/stealth/ox-alpha (modelo gratis, 1M contexto, 128K output, tool-calling real) y pide analizar y decidir. Investigado ANTES de codear: el boton "Compatible" existente (type:'openai-compatible') no sirve -- resuelve a runtime:'codex-api', que corre por codex-client.ts, el cual NUNCA lee provider.endpoint (confirmado con grep, cero resultados) y siempre le pega a la OpenAI real. Los otros 3 runtimes HTTP (foundry/anthropic-api/gemini-api) tampoco encajan -- cada uno habla el formato nativo de su proveedor, ninguno es Chat Completions. Decision confirmada con el usuario via AskUserQuestion: runtime nuevo en vez de forzar un mal ajuste. sendOpenAiApi() en api-agent-runtime.ts implementa el loop completo (tool_calls en choices[0].message, resultados como mensajes role:'tool', rol system nativo sin el hack de tag [system] que usan foundry/gemini). Wiring completo: shared/types.ts, model-capabilities.ts, ipc-agent.ts, runtime-state.ts, App.tsx (boton nuevo, modelo default stealth/ox-alpha editable, endpoint default openrouter.ai/api/v1). Verificado real con una API key real de OpenRouter provista por el usuario (usada solo en memoria, nunca escrita a archivo ni committeada): turno simple real ("OK", cost:0 real) y turno CON tool-calling real completo -- el modelo llamo read_file de verdad, el resultado simulado volvio como mensaje tool, y la respuesta final cito el contenido correctamente. | `src/shared/types.ts`, `src/shared/model-capabilities.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/main/runtime-state.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; verificado end-to-end contra el servicio real de OpenRouter, turno simple y tool-calling completo, no solo contra un mock |
| 14 (seguimiento) | 2026-08-24 | Fix del hallazgo colateral de Fase 14: compactionProviderId/compactionModelId (Fase 3) nunca se agregaron a StoredSettings/loadSettings()/saveSettings() en settings-store.ts, se perdian en cada reinicio de la app. Investigado ANTES de escribir el fix (docs/_arch/verify_compaction_settings.md): confirmado que dentro de la misma sesion andaban bien (sanitizeSettings() los preserva via spread, loadSettings() se llama una sola vez al arrancar), la perdida era solo en el roundtrip a disco -- explica un error real reportado en uso activo ("explore fallo: No hay modelo de compactacion configurado") justo despues de un reinicio de la app. Fix: mismo patron exacto que turnWatchdogSeconds, sin guard de validez (son strings, resolveConfiguredCompactionModel() ya descarta ids invalidos con find()). Verificado con bundle real: sembrado, releido, cambiado, releido tras un restart simulado -- el cambio sobrevivio el roundtrip completo. | `src/main/settings-store.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con roundtrip real a disco, sin tocar el settings.json real |
| 14 | 2026-08-23 | Timeout del watchdog de turno configurable: AppSettings.turnWatchdogSeconds (segundos) reemplaza la constante fija TURN_WATCHDOG_MS=90000, mismo criterio que maxOutputTokens (Fase 6). Default 90s si no esta configurado o el valor es invalido (0/negativo/no numerico), guard en dos puntos (UI al guardar, settings-store.ts al leer/guardar) para que nunca dispare casi instantaneo. Sin techo maximo -- el boton Detener ya cubre el caso de turno colgado. UI: seccion nueva "GENERAL" en Settings, junto a "MEMORIA". Hallazgo colateral documentado y NO corregido: compactionProviderId/compactionModelId (Fase 3) nunca se persistian en settings-store.ts, se pierden en cada reinicio -- turnWatchdogSeconds se implemento evitando ese mismo error. Ademas, fix chico separado: la descripcion de la tool run_command ahora aclara que la shell real es Windows/cmd.exe, instruye usar dir/type/del/copy en vez de ls/cat/rm/cp -- solo texto, sin cambio de logica. | `src/shared/types.ts`, `src/main/settings-store.ts`, `src/main/tool-registry.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde |
| 13 | 2026-08-23 | Nivel de esfuerzo/razonamiento configurable por turno, selector nuevo en el composer (mismo patron que sandbox mode), visible solo para claude-cli (5 niveles fijos del CLI, --effort) y codex-subscription/codex-api (catalogo real ya sincronizado, model.reasoningLevels — no hardcodeado). Investigacion previa confirmo: Claude CLI headless -p soporta --effort con efecto medible en thinking_tokens; Codex app-server usa el campo real "effort" (no "reasoningEffort") en TurnStartParams de turn/start, NO existe en thread/start (property por turno, no de conexion) — confirmado contra el schema oficial del protocolo (codex app-server generate-json-schema) y el transporte real. Sin seleccion = no se manda ningun flag/campo, mismo criterio que maxOutputTokens (Fase 6). Se agrego tambien "Claude Haiku" a defaultModels(). Verificacion end-to-end real: Claude vía la clase de produccion real dio thinking_tokens 939 (sin seleccion) / 0 (low) / 1029 (high) — confirma que "sin seleccion" es una ausencia real, no un default oculto; Codex completo turnos reales sin error con y sin effort, pero el efecto conductual cuantitativo sigue sin poder medirse (sin campo de usage/thinking en la respuesta) — documentado explicito como mitigacion con soporte estructural confirmado, no como correccion medida. | `src/renderer/src/App.tsx`, `src/main/cli-agent-runtime.ts`, `src/main/codex-client.ts`, `src/main/ipc-agent.ts`, `src/preload/index.ts`, `src/preload/index.d.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado end-to-end contra las clases reales de produccion (bundle esbuild), no scripts de investigacion standalone |
| Fix | 2026-08-23 | Fallback de deteccion de CLI (detectClaude/detectGemini/detectCodex, cli-status.ts): si el intento normal por PATH falla y es Windows, reintenta contra %APPDATA%\npm\<comando>.cmd (misma ubicacion donde installClaudeCli()/installGeminiCli() ya instalan via npm install -g), con existsSync() antes de ejecutar para no gastar el timeout de 12s en una ruta inexistente. Mitigacion para una sospecha SIN CONFIRMAR EMPIRICAMENTE (Electron instalado, lanzado desde Explorer, puede heredar un PATH de usuario desactualizado) — el usuario prefirio no correr el diagnostico manual. Si ambos caminos fallan, el detail menciona los dos intentos explicitamente. Sin regresion para quien ya detecta bien por PATH (mismo shape de retorno exacto). | `src/main/cli-status.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con bundle real contra binarios instalados de verdad en la maquina de desarrollo (4 escenarios: PATH ok, PATH roto+fallback real, ambos fallan sin archivo candidato, ambos fallan con archivo candidato roto) |
| 12 | 2026-08-23 | Fix de seguridad: el sandbox mode (read-only/workspace-write/danger-full-access) se aplicaba solo en runtimes CLI, nunca en los 3 runtimes API (foundry/anthropic-api/gemini-api) — confirmado con evidencia mecánica (grep) antes de tocar código: ExecuteContext no tenía campo sandbox, ningún caso del switch lo consultaba. Los 3 modos eran indistinguibles en runtimes API (siempre se comportaban como workspace-write). Fix: resolveApproval() centralizado en tool-registry.ts (read-only bloquea sin mostrar dialogo, danger-full-access saltea el dialogo, workspace-write sin cambios), reusado por write_file/apply_patch/run_command/revert_file y por el dispatch de tools MCP en api-agent-runtime.ts. sandbox conectado hasta ExecuteContext vía ipc-agent.ts. Test end-to-end real con conteos de invocaciones a confirm por modo: read-only=0, workspace-write=1 por accion, danger-full-access=0. | `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con bundle real y conteos concretos de confirm por modo |
| 11 | 2026-08-23 | Memoria jerárquica por tema, extiende compaction-engine.ts (Fase 3/6), no crea módulo nuevo. decisions/constraints/nextSteps pasan de 3 listas planas a agrupadas por tema (Record<nombreDeTema, MemoryTopic>); summary sigue siendo un único string sin temas. Prompt instruye reusar un tema existente si un hecho encaja ahí, crear uno nuevo solo si genuinamente no encaja en ninguno. Migración automática y sin pérdida: structured_memory en formato plano viejo se lee como tema "General" la primera vez que se toca, sin reescribir la fila hasta la próxima compactación natural. Inyección agrupada con un heading por tema en context-envelope.ts y api-agent-runtime.ts. Test end-to-end real (bundle esbuild de compaction-engine.ts+chat-store.ts, SQLite real, solo fetch mockeado): pasada 1 crea 2 temas, pasada 2 fusiona un hecho nuevo en un tema existente sin duplicarlo y crea un tema nuevo aparte, migración confirmada sin pérdida de entradas. | `src/shared/types.ts`, `src/main/chat-store.ts`, `src/main/compaction-engine.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/runtime-state.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con SQLite real de producción, cero residuos tras la corrida |
| 10 | 2026-08-23 | Cliente MCP propio (solo stdio) para los 3 runtimes API — claude-cli/codex ya tienen MCP nativo, fuera de alcance. Investigación confirmó que RpcStdioClient (Fase 9) sirve de base con un solo ajuste aditivo (envelopeExtras() para jsonrpc:"2.0"). .mcp.json mismo formato que Claude Code CLI. Tools namespaced mcp__servidor__tool, aprobación sin excepción, descubrimiento cacheado al conectar, servidor roto nunca bloquea la conexión. Test end-to-end real contra @modelcontextprotocol/server-everything vía npx: 13 tools descubiertas, tools/call real exitoso. | `src/main/mcp-client.ts`, `src/main/ipc-mcp.ts` (nuevos), `src/main/rpc-stdio-client.ts`, `src/main/api-agent-runtime.ts`, `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con servidor MCP real, no simulado |
| 9 | 2026-08-23 | Housekeeping: investigación confirmó que CodexClient y CodexAccountBridge deben seguir en procesos separados (CODEX_HOME distinto, ciclos de vida independientes, uso concurrente real) — se extrajo solo el framing JSON-RPC compartido a rpc-stdio-client.ts, texto de error preservado exacto por clase vía métodos abstractos. Borrado claude-subscription-runtime.ts (stub muerto, sin referencias vivas confirmado con grep antes de borrar). | `src/main/rpc-stdio-client.ts` (nuevo), `src/main/codex-client.ts`, `src/main/codex-account-bridge.ts`, `src/main/claude-subscription-runtime.ts` (borrado) | Completa — `npm run typecheck` y `npm run build` en verde; cero cambio de comportamiento observable |
| 8 | 2026-08-23 | VCS local oculto: git como motor interno invisible, repo privado por workspace en D:\AMATISTA\data\vcs\<id> (nunca en el proyecto real). Snapshot síncrono en write_file/apply_patch (commit del original en el primer toque, sin bloquear la escritura si falla). Tools list_file_history (sin aprobación) y revert_file (con aprobación, mismo diálogo de diff). Retención ilimitada, sin UI de línea de tiempo en esta fase. | `src/main/local-vcs.ts` (nuevo), `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; test end-to-end contra el módulo real (bundleado standalone con esbuild): 3 versiones tras 2 ediciones, revert restauró el original byte a byte, 4 versiones tras el revert (nunca borra), git status del repo real del proyecto de prueba limpio sin rastro del repo oculto |
| 7 | 2026-08-23 | Soporte AGENTS.md por proyecto (estándar real, texto plano). Tarea 0 confirmó empíricamente que claude-cli NO lo lee solo (codex-cli SÍ) — inyección manual solo donde hace falta: claude-cli, gemini-cli, y los 3 runtimes API; codex-subscription/codex-api sin tocar. Lectura cacheada una vez por conexión. Aviso (nunca truncado) si supera ~200 líneas. Botón en topbar para crear/abrir en el editor del sistema. | `src/main/agents-md.ts`, `src/main/ipc-agents-md.ts` (nuevos), `src/main/runtime-state.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `src/shared/types.ts` | Completa — `npm run typecheck` y `npm run build` en verde; Tarea 0 verificada con los binarios reales (claude 2.1.226, codex-cli 0.147.0), no asumida de la documentación |
| 6 | 2026-08-19 | Extracción estructurada en compaction-engine.ts: la misma llamada de compactación (Fase 3) ahora pide/devuelve JSON con summary + decisions/constraints/nextSteps acumulativos. Fallback a texto plano si el JSON es inválido, sin vaciar las listas. Persistido en chat_sessions.structured_memory. Inyectado en el contexto de los 3 runtimes API (no solo context-envelope.ts, extensión necesaria — ver CONTRACT.md). Extiende el contrato v2, no crea v3. | `src/main/chat-store.ts`, `src/main/compaction-engine.ts`, `src/main/context-envelope.ts`, `src/main/runtime-state.ts`, `src/main/api-agent-runtime.ts`, `src/shared/types.ts` | Completa — `npm run typecheck` y `npm run build` en verde; parseo JSON/fallback verificado con script standalone |
| 5 | 2026-08-19 | Tool "apply_patch": edición search/replace exacto (old_str/new_str, ocurrencia única obligatoria, comparación normalizada \r\n↔\n, preserva estilo de salto de línea original al escribir) para bajar tokens de salida vs. regenerar el archivo completo con write_file. write_file sin cambios, coexisten. | `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; lógica de match/CRLF verificada con script standalone |
| 4 | 2026-08-19 | Tool "explore": delega búsqueda/lectura repetitiva (read_file/list_dir/git_status/git_diff, nunca write_file/run_command) al modelo barato de Fase 3, en un mini-loop propio acotado (10 iteraciones), sin paralelismo, con resultado condensado y fallback a error claro si no hay modelo configurado. | `src/main/explore-tool.ts` (nuevo), `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/main/compaction-engine.ts`, `src/main/ipc-agent.ts` | Completa — `npm run typecheck` y `npm run build` en verde |
| 3 | 2026-08-19 | Memoria/contexto real: compactación asíncrona vía LLM para runtimes API, persistida en SQLite (summary + watermark por id de mensaje), reemplaza el truncado de App.tsx. Constante de presupuesto de contexto centralizada en src/shared/. Modelo de compactación configurable en Settings. | `src/shared/context-budget.ts`, `src/shared/model-capabilities.ts` (nuevos), `src/main/compaction-engine.ts` (nuevo), `src/shared/types.ts`, `src/main/chat-store.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; ver PENDING.md por el caveat de hueco transitorio en backfill y el ítem CLI sin priorizar |
| 2 | 2026-08-19 | Refactor de index.ts en 10 módulos por responsabilidad, sin cambio de comportamiento salvo el ítem anotado en PENDING | `src/main/runtime-state.ts`, `src/main/settings-provisioning.ts`, `src/main/attachments.ts`, `src/main/workspace-tree.ts`, `src/main/ipc-window.ts`, `src/main/ipc-settings.ts`, `src/main/ipc-chats.ts`, `src/main/ipc-cli.ts`, `src/main/ipc-projects-workspace.ts`, `src/main/ipc-attachments.ts`, `src/main/ipc-agent.ts` (nuevos), `src/main/index.ts` (reducido a bootstrap) | Completa — `npm run build` (typecheck + electron-vite build) en verde; ver PENDING.md por el caso límite no 100% verificado |
| 1 (Tarea C) | 2026-08-19 | Fase 1 Tarea C: confirmado con evidencia que compactSummary no genera resumen real, pérdida de contexto más allá de ~26 mensajes en runtimes API | `docs/_arch/PENDING.md`, `docs/_arch/CONTRACT.md`, `docs/_arch/HISTORY.md` | hallazgo documentado, fix pendiente de diseño |
