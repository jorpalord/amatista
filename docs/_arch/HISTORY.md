# HISTORY.md — Bitácora de Fases

> Log append-only. Una entrada por fase ejecutada.

| Fase | Fecha | Resumen | Archivos tocados | Estado |
|---|---|---|---|---|
| Fix real -- tool todo_write (lista de tareas propia del modelo) | 2026-09-05 | Basado en verify_todo_write_design.md, patron externo convergente (Claude Code/Qwen Code/GitLab Duo): reemplazo TOTAL de la lista en cada llamada, {content,status,priority?}, a lo sumo 1 in_progress a la vez. Persistencia: columna nueva chat_sessions.todos TEXT (mismo ALTER TABLE+try/catch que structured_memory), getTodos()/setTodos() mismo shape de reemplazo total que getChatSummaryState()/setChatSummaryState(), sin watermark. Inyeccion: buildRuntimeContext() lee getTodos() junto a getChatSummaryState(), una lectura mas. Hallazgo real no anticipado y decisivo: formatContextEnvelope() (context-envelope.ts) NO es el unico punto de render -- los 4 runtimes API tienen su propia funcion duplicada (memoryBlockText(), api-agent-runtime.ts, documentada ahi mismo porque esos 4 runtimes arman su payload directo sin pasar por formatContextEnvelope(), que solo consume el runtime CLI) -- la primera verificacion real (turno 2 de un chat con tareas guardadas) confirmo el gap en vivo: el bloque llegaba al CLI pero no al runtime API real probado, corregido agregando el mismo bloque tambien ahi. Registro: mismo mecanismo de siempre (TOOL_DEFINITIONS+case), primera tool con parametro array de objetos -- ToolDefinition.parameters.properties extendido de {type,description} plano a ToolPropertySchema recursivo (opcional, no rompe ninguna entrada existente), confirmado que los 4 conversores de schema por runtime pasan def.parameters completo sin reconstruirlo. Acceso al chatId: ninguna tool tenia esto en ExecuteContext -- resuelto sin threadear un campo nuevo por runTurnForWindow(), writeTodos nuevo es una closure inyectada por ipc-agent.ts cerrada sobre `session` (mutable), leyendo session.activeChatId fresco en el momento de la llamada, mismo patron exacto que listWindows. Sin aprobacion en ningun sandbox (mismo criterio que list_windows, no toca filesystem/red/dinero). Validacion fail-closed: 2+ in_progress se rechaza con error claro nombrando las tareas en conflicto, nunca auto-corrige en silencio -- decision deliberada mas estricta que el precedente externo (confirmado con evidencia real, system prompt de Claude Code + docs oficiales del Agent SDK, que ahi "exactamente 1 in_progress" es autodisciplina de prompt del modelo, no una validacion de servidor). Verificado real (ApiAgentRuntime/ToolRegistry/chat-store.ts/buildRuntimeContext() reales, storage aislado, servidor HTTP local real de "modelo"): persistencia real confirmada en SQLite real; inyeccion real confirmada en el turno siguiente tras corregir el hallazgo de memoryBlockText(); rechazo real con el mensaje exacto devuelto como tool result, chat_sessions.todos sin cambios tras el intento invalido; chat sin todo_write nunca llamado no renderiza nada y getTodos() da []. | `src/shared/types.ts`, `src/main/chat-store.ts`, `src/main/runtime-state.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/tool-registry.ts`, `src/main/ipc-agent.ts` | Completa -- npm run typecheck y npm run build (con mcp:lsp:bundle) en verde; verificado real con clases de produccion reales, storage aislado, 4 casos reales (persistencia+inyeccion, rechazo, chat sin todos); sin commit, pendiente de que el usuario lo pida |
| Fix real -- workspace/configuration + pull-diagnostics en LspClient, 5 lenguajes LSP nuevos (12 en total) | 2026-09-05 | Basado en verify_lsp_pull_config_support.md + verify_lsp_pull_config_implementation_design.md, evidencia real reunida antes de implementar (mismo scratchpad de investigacion de hoy, verify_6_lsp_servers.md). Pieza 1: handleChunk() ganaba una rama nueva para requests SERVIDOR->cliente (method+id a la vez, ej. workspace/configuration) -- antes se descartaban en silencio, LspFramer los parseaba bien pero nada los contestaba, dejando al servidor esperando para siempre; respond() nuevo (distinto de request()/notify()), buildConfigurationResponse() arma un array de la misma longitud que items[] (confirmado real que puede traer 1 a N elementos), section vacia o con nombre segun config.configResponses. Pieza 2: capabilities de initialize() ampliadas (workspace.configuration:true, textDocument.diagnostic) sin afectar a los 6 servidores existentes (confirmado que ninguno pide ninguna de las 2 cosas). Pieza 3: configResponses?:Record<string,unknown> nuevo en LanguageServerConfig (codigo) y CustomLspEntry (lsp.json, mismo patron de reemplazo total por clave) -- ESLINT_CONFIG_RESPONSE es el objeto MINIMO real confirmado hoy (nodePath:null, experimental.useFlatConfig, problems.shortenToSingleLine, rulesCustomizations -- cada campo arreglando un crash real distinto, documentado con el mensaje de error real de cada uno). Pieza 4: pullDiagnostics() nuevo en LspClient (textDocument/diagnostic real, escribe en el MISMO cache con el MISMO shape que onPublishDiagnostics() via parseDiagnosticItems() extraido) -- LspManager.diagnosticsFromClient() decide con supportsCapability('diagnosticProvider') (mecanismo ya existente, capability nueva) si usa pull o el push pasivo de siempre; tool-registry.ts sin cambios de formato. Pieza 5: Terraform (terraform-ls serve, nativo, sin npm real, solo PATH) y Lua (lua-language-server, nativo, sin npm real, solo PATH) listos tal cual sin necesitar nada de las piezas 1-4 (push); YAML/ESLint(vscode-langservers-extracted)/Bash bundleados como paquetes npm reales (mismo patron que typescript-language-server/pyright, dependencies+files+asarUnpack en package.json) -- ESLint con extensions .js/.jsx/.mjs/.cjs a proposito, NUNCA .ts/.tsx (esas ya son de typescript-language-server, dar chequeo de tipos real; languageServerConfigFor() resuelve un solo config por extension, soportar 2 servidores por archivo es cambio de arquitectura mayor, fuera de alcance, documentado); Bash con diagnosticos de lint dependientes 100% de shellcheck (binario externo que el usuario provee, Amatista no lo autodetecta ni inyecta, mismo espiritu que el JRE de jdtls). Vue investigado y descartado tal cual esta (PENDING.md) -- diagnostico real depende de un bridge tsserver propietario no estandar, confirmado en 6 intentos reales distintos. Verificado real contra LspManager real de produccion (bundle esbuild): Terraform/Lua/YAML/ESLint(pull real)/Bash(shellcheck real via PATH) dieron diagnosticos reales y correctos sobre errores puestos a proposito; no regresion confirmada en Python (push, bundleado) y Rust (push, nativo externo, rust-analyzer real ya instalado en la maquina -- diagnostico real tras ~24s de indexacion real de un Cargo nuevo, latencia esperada, no regresion); prueba decisiva del override de 2 niveles con un .customjs custom en lsp.json con configResponses propio e INCOMPLETO a proposito, que reprodujo el error real EXACTO ya documentado, mientras la entrada built-in con el config completo funciono normal en el mismo turno -- confirma que .lsp.json llega de punta a punta. | `src/main/lsp-client.ts`, `src/main/lsp-manager.ts`, `src/main/tool-registry.ts`, `package.json`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build (con mcp:lsp:bundle) en verde; npm install real de yaml-language-server/vscode-langservers-extracted/bash-language-server; verificado real con LspManager real de produccion, 5 lenguajes nuevos + 2 no-regresion + 1 caso custom de lsp.json; sin commit, pendiente de que el usuario lo pida |
| Fix real -- web_search/web_fetch reales via Tavily | 2026-09-05 | Basado en verify_web_search_design.md, ya confirmado antes de implementar (registro de tools generico via TOOL_DEFINITIONS + case en execute(), generate_image como precedente PARCIAL -- reusa apiKey de un provider de modelo existente, no aplica directo porque Tavily no tiene ningun LLM). Credencial nueva: AppSettings.integrations.tavily.apiKey -- primer campo top-level que NO es referencia a un provider, mismo mecanismo de cifrado exacto (encryptSecret/decryptSecret sobre safeStorage, sin crypto nueva) cableado en 2 puntos nuevos de settings-store.ts; UI con patron staged (estado local + boton Guardar, no guardado por keystroke). Tools nuevas en web-search.ts (modulo aislado, paralelo a compaction-engine.ts/image-generation.ts): web_search->POST api.tavily.com/search, web_fetch->POST api.tavily.com/extract (body {urls:[url], format:'markdown'}), registradas con el mecanismo generico de siempre (TOOL_DEFINITIONS + case en ToolRegistry.execute()). Gating real: hasWebSearchIntegration nuevo en la config de ApiAgentRuntime (calculado en ipc-agent.ts via hasTavilyIntegration()), toolCatalog() filtra ambas tools con el mismo patron exacto que orchestratorToolNames/hideOrchestratorTools -- sin key, ni aparecen en el catalogo mandado al modelo; confirmado con grep que TOOL_DEFINITIONS nunca se importa en cli-agent-runtime.ts, CLI (claude-cli/antigravity-cli/codex) no las ve por este camino. Aprobacion incondicional (ctx.confirm() directo en el case, bypass total de resolveApproval()/sandbox), mismo precedente exacto que generate_image ("gasta dinero real"). Manejo de errores propio (readTavilyErrorBody()/tavilyErrorMessage(), NO reusa readErrorBody() de api-agent-runtime.ts -- forma de error distinta confirmada real para Tavily) -- 401/403 mensaje de key invalida, 429 mensaje de limite, web_fetch revisa failed_results[] explicito (una URL fallida en Tavily sigue siendo HTTP 200). Verificado real: gating confirmado con ApiAgentRuntime real + servidor HTTP local real capturando el body real de tools enviado (sin key: 17 tools, ninguna de las 2 nuevas; con key: 19 tools, ambas presentes); error 401/403 confirmado con una llamada REAL y en vivo a api.tavily.com usando una key deliberadamente invalida (nunca una credencial real obtenida por el agente) contra los 2 endpoints -- hallazgo real no anticipado: la forma real del error es {"detail":{"error":"..."}} , un nivel mas anidado que lo asumido originalmente (documentacion oficial + reportes de comunidad); readTavilyErrorBody() corregido con la evidencia real, mensaje final limpio confirmado sin JSON crudo. 429 y turno real con resultados no verificados -- ninguna key real de Tavily fue provista ni solicitada al usuario en este turno, respetando la restriccion de no obtener credenciales reales por cuenta propia; queda pendiente de una key de prueba real del usuario. Sin entrada previa en PENDING.md que cerrar (no existia ninguna sobre web_search/Tavily). | `src/main/web-search.ts` (nuevo), `src/shared/types.ts`, `src/main/settings-store.ts`, `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/renderer/src/App.tsx` | Completa -- npm run typecheck y npm run build (con mcp:lsp:bundle) en verde; gating y error 401/403 verificados reales (servidor local real + api.tavily.com real con key invalida real); 429 y turno con resultados reales pendientes de una key de prueba; sin commit, pendiente de que el usuario lo pida |
| Fix real -- LANGUAGE_SERVERS rediseñado config-driven + LSP para Java via jdtls (7mo lenguaje, los 7 de SWE-Bench ProMax completos) | 2026-09-05 | Basado en verify_jdtls_integration_scope.md + verify_lsp_config_redesign.md, confirmado contra el codigo fuente real de OpenCode (sst/opencode) antes de implementar. jdtls rompia las 3 asunciones del LANGUAGE_SERVERS de ayer (kind:'node'|'native', resolveEntry() de un solo string, args estatico) -- en vez de un 3er kind ad-hoc, se redisenio el mecanismo entero. Pieza 1: LanguageServerConfig colapsa kind/resolveEntry/args/extraPathDirs en un solo resolveCommand(workspace):Promise<{command,env?}|null> -- LspClient.start() se simplifica a un unico spawn(), sin ningun if por tipo de servidor. Los 5 resolveXEntry() existentes no se tocan por dentro, se envuelven en funciones xResolveCommand() nuevas (TypeScript/Python arman [process.execPath, entry, '--stdio'] con env:ELECTRON_RUN_AS_NODE calculado por Amatista; Go pliega extraPathDirs en env.PATH; Rust/clangd quedan {command:[entry]}). Pieza 2: jdtlsResolveCommand() -- confirmado con el codigo fuente real de OpenCode (server.ts) que su propio built-in de jdtls resuelve los mismos 3 puntos duros (glob del jar versionado con el MISMO regex, directorio de datos unico, validacion real de Java) sin ningun wrapper script, todo en una funcion real -- mismo patron implementado en Amatista: jdtlsHomeCandidate() (JDTLS_HOME o convencion propia bajo getAppDataSubdir('jdtls'), sin instalador oficial en Windows a diferencia de LLVM/Go), resolveEquinoxLauncherJar() (glob real), jdtlsConfigDir() (config_win/linux/mac por plataforma), jdtlsDataDir() (ESTABLE por workspace via hash sha256, a diferencia del mkdtemp() de OpenCode -- evita re-indexar en cada reconexion), resolveJavaRuntime() (JAVA_HOME o PATH, parsea version real), flags condicionales si Java>=24. Pieza 3: D:\AMATISTA\data\config\lsp.json (global) + .lsp.json opcional por workspace (mismo criterio que .mcp.json) -- cada clave es un languageId con {command,extensions,env?,disabled?} siempre estatico (confirmado real que OpenCode nunca permite algo dinamico en lo que el usuario edita a mano); merge por CLAVE no por archivo completo (mismo principio confirmado en la documentacion real de OpenCode); cache real por mtime, sin reiniciar la app. Pieza 4: LspClient/LspManager confirmados sin friccion (ya genericos) -- unico ajuste real, los 8 call sites de languageServerConfigFor() en lsp-manager.ts pasan this.workspace como segundo argumento. Verificado real: ToolRegistry/LspManager reales, storage aislado, jdtls real (mismo tarball oficial de ayer) + clangd real (binario aislado) + un lenguaje CUSTOM real (mini language server propio) agregado a mano en lsp.json -- Java: find_definition/find_references/list_symbols reales y correctos, get_diagnostics real (aislado, 15s de espera -- JVM arranca mas lento) encontro el error real puesto a proposito; no regresion confirmada en Python (Node, aislado) y C (clangd, nativo); custom del usuario funciono end-to-end con un diagnostico real fijo del mini-server. Nota honesta: un primer intento combinado (los 4 casos en un solo proceso, solo 1.5s de espera) hizo fallar Java y Python -- confirmado que es timing/concurrencia del harness sintetico (jdtls arranca mas lento que los otros 5, y 3 resoluciones casi simultaneas en un proceso Node plano fuera de Electron), no una regresion real, aislando cada caso funciono perfecto. PENDING.md: entrada de C/Java/C++ cerrada definitivamente -- los 7 de 7 lenguajes de SWE-Bench ProMax cubiertos. | `src/main/lsp-client.ts`, `src/main/lsp-manager.ts`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build (con mcp:lsp:bundle) en verde; verificado real con ToolRegistry/LspManager reales, jdtls real, clangd real, custom real; sin commit, pendiente de que el usuario lo pida |
| Fix real -- LSP para C/C++ via clangd (5to lenguaje) | 2026-09-05 | Basado en verify_clangd_integration_scope.md. LspClient/LspManager confirmados genericos por diseno (el propio comentario de LspClient.start() ya decia que no sabe nada de TypeScript/Python/Rust/Go, solo habla el protocolo generico) -- agregar clangd fue una entrada nueva en LANGUAGE_SERVERS, sin tocar ninguna de las 2 clases. resolveClangdEntry(): mismo patron de 2 niveles exacto que Rust/Go (PATH primero via 'clangd --version', fallback a C:\Program Files\LLVM\bin\clangd.exe, ruta real del instalador oficial de LLVM), kind:'native', args:[] (confirmado real con handshake LSP completo que clangd tambien usa stdio por defecto, sin flags). Confirmado con npm view/npm pack que el paquete npm "clangd" es un placeholder de 391 bytes sin binario, misma categoria que gopls, nunca bundleable. Hallazgo real no anticipado, decisivo: languageId:'cpp' cubre TODAS las extensiones (.c/.h/.cpp/.cc/.cxx/.hpp/.hh/.hxx) sin el caso especial que si necesita TypeScript (.tsx) -- confirmado real con un mismatch deliberado (.c con contenido C real pero languageId:'cpp' declarado) que clangd decide C vs C++ por la EXTENSION real del archivo (su propio driver de clang interno), nunca por el languageId que el cliente LSP declara; confirmado tambien que un solo proceso clangd sirve un .c y un .cpp simultaneamente sin confusion cruzada (diagnosticos reales correctos y distintos para cada uno). Limitacion conocida documentada (comentario real + CONTRACT.md), no un bug: sin compile_commands.json, clangd cae a un comando de fallback razonable (confirmado real en los logs), funciona para diagnosticos basicos pero con menos precision en proyectos con dependencias reales -- comportamiento esperado de clangd mismo. Limpieza menor: descriptions de get_diagnostics/find_definition/find_references (tool-registry.ts) actualizadas mencionando C/C++, texto plano sin cambio de logica. Verificado real: ToolRegistry/LspManager reales, binario real de clangd 22.1.1 (aislado, nunca instalado en el sistema), workspace real con .c y .cpp reales -- get_diagnostics/find_definition/find_references/list_symbols funcionando correctamente en ambos, con diagnosticos reales exactos (error de sintaxis real en C, tipo real mal asignado detectado en C++). TypeScript/Python/Rust/Go confirmados sin regresion en el mismo turno (Python probado explicito, incluido get_diagnostics real con el error real puesto a proposito). Nota honesta: un intento inicial de probar los 3 lenguajes juntos via 3 write_file() casi simultaneos en el harness standalone hizo fallar la resolucion de pyright -- confirmado que es un artefacto de concurrencia del propio harness fuera de Electron (reproducido 2 veces, pero Python aislado funciono perfecto a la primera), no una regresion real ni relacionada con el fix. PENDING.md: entrada "LSP para C, Java y C++" actualizada -- C/C++ RESUELTO, Java (jdtls) queda como el unico pendiente real. | `src/main/lsp-client.ts`, `src/main/tool-registry.ts`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con ToolRegistry/LspManager reales y clangd real; sin commit, pendiente de que el usuario lo pida |
| Fix real -- sessionRegistry deja de crecer sin limite al cerrar un panel | 2026-09-05 | Basado en verify_sessionregistry_leak_2026.md, sobre una nota de PENDING.md cuyo disparador citado ("cerrar una ventana", window.on('closed', ...) en window-manager.ts) se confirmo obsoleto -- ese handler ya no existe (Paneles-1: una unica ventana fisica siempre, registerWindow()/windowRegistry reemplazados por setMainWindow(), el propio archivo documenta su retiro). El leak en si (disconnectSession() nunca hace sessionRegistry.delete(), solo vacia los campos) seguia siendo real, confirmado con grep (cero .delete()/.clear() sobre sessionRegistry en todo src/main/). El disparador real hoy es cerrar un panel (closePanel() -> agent:disconnect), mucho mas frecuente que cerrar una ventana. Confirmacion previa critica para el diseno: tocar disconnectSession() directamente (compartida por 4 call sites reales) hubiera roto 2 de ellos -- ipc-projects-workspace.ts (workspace:open/projects:removeRoot) capturan una referencia local a session ANTES de llamar disconnectSession(panelId) y la siguen mutando DESPUES esperando que sea el objeto vivo del Map (bug real en workspace:open, inofensivo solo por coincidencia en projects:removeRoot). Tampoco alcanzaba con asumir que agent:disconnect siempre es cierre genuino -- el mismo canal IPC lo dispara disconnect() en ChatPanel, usado por 4 de los 5 disparadores reales (chatId/catalogChangeSignal/selectProvider/selectModel/sandbox), donde el panel sigue vivo y reconecta enseguida. Fix: agent:disconnect gana payload.panelClosing?:boolean -- disconnectSession() en si no se toca, sigue llamandose igual; el handler agrega sessionRegistry.delete(panelId) DESPUES, solo si panelClosing===true. closePanel() (unico cierre genuino real, deleteChat() lo reusa) manda disconnectAgent(true); los otros 3 call sites reales no pasan el campo, sin cambio de comportamiento. preload/index.ts/index.d.ts actualizados con la firma opcional. Verificado real con la app corriendo (CDP + instrumentacion temporal para leer sessionRegistry.size del proceso main, revertida al 100% al terminar, confirmado con grep y git diff --stat): cerrar un panel real bajo el size en 1 (entrada realmente eliminada, no solo vaciada); cambiar de proveedor en un panel que sigue abierto no toco el size en ningun momento del ciclo, y la reconexion funciono normal, sin errores nuevos en los logs. PENDING.md: entrada corregida (disparador "cerrar ventana" -> "cerrar panel") y marcada RESUELTO. | `src/main/ipc-agent.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con la app corriendo, instrumentacion temporal revertida al 100%; sin commit, pendiente de que el usuario lo pida |
| Fix real -- disconnectAllPanels() acotado al proveedor/modelo afectado (Fase 22c) | 2026-09-05 | Basado en verify_fase22c_disconnect_scope_2026.md (reclasificacion fresca, verify_fase22_scope.md paragraph A.1 confirmado obsoleto -- la arquitectura de sesion-unica que describia ya no existe). Con sesiones-por-panel ya vigentes (ChatPanel por instancia, sessionRegistry por panelId en main) solo quedan 5 sitios reales de disconnect() en App.tsx, no 13/18. 3 (selectProvider/selectModel/sandbox) ya eran (a) reales acotados por construccion. El useEffect por chatId sigue siendo necesario (SessionRuntimeState.provider/model se resuelven una sola vez en agent:connect, cambiar de chat sin desconectar dejaria la sesion hablando con el proveedor/workspace viejo) pero acotado a 1 panel, sin cambios. El gap real: catalogChangeNonce/disconnectAllPanels() (Paneles-2b) -- confirmado indiscriminado por diseno explicito en su propio comentario ("broadcast crudo-pero-seguro... CADA panel abierto reacciona desconectandose"), pese a que 9 de 11 call sites reales eran (a) genuinos. Fix: catalogChangeNonce:number -> catalogChangeSignal:{seq, providerId:string|null} (mismo useState, shape ampliado, no un estado paralelo nuevo); disconnectAllPanels(providerId=null) gana el parametro; el useEffect consumidor gana un guard (if providerId!==null && providerId!==activeChat.providerId return) ANTES de desconectar -- comparacion deliberada contra activeChat.providerId (campo persistido y estable, ya usado con el mismo proposito en deleteProvider()), NO contra activeProvider?.id (useMemo derivado de settings que para cuando el efecto corre ya puede reflejar el fallback post-cambio, dando falso negativo). Los 11 call sites pasan el providerId que ya tenian en scope sin buscarlo (addProvider/addDeepSeekProvider/syncCodexModels/edicion de conexion: provider.id; deleteProvider/toggleProvider/deleteModel/toggleModel: su propio parametro); logoutCodex/importQConfig/removeProjectRoot pasan null explicito (genuinamente globales los 2 primeros, sin cambio de comportamiento; removeProjectRoot fuera de alcance de este fix -- su dimension real es workspace, no providerId). No se toco sessionRegistry ni nada de main, confirmado innecesario. Verificado real con la app corriendo (CDP, 2 paneles reales conectados a proveedores distintos por suscripcion real, sin ninguna API key manipulada): editar la conexion Anthropic real que usaba el Panel 2 (claude) lo desconecto real (paso a "Agente sin iniciar"); el Panel 1 (antigravity, proveedor distinto) siguio conectado sin interrupcion, sin ningun cambio. Caso global (logoutCodex/importQConfig) verificado por equivalencia real de codigo, no por accion en vivo (logout real requeriria relogin manual del usuario, import necesita un archivo real via dialogo nativo no accionable por CDP) -- confirmado que con providerId===null el guard nuevo es un no-op exacto, ejecuta las mismas 4 lineas de siempre sin ninguna rama nueva. Entorno de prueba (panel agregado + electron-vite dev lanzado para la verificacion) cerrado y limpiado al terminar; settings.providers real confirmado sin cambios de datos. PENDING.md: entrada de Fase 22c sobre los sitios de disconnect() marcada RESUELTO. | `src/renderer/src/App.tsx`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con la app corriendo, 2 paneles reales, proveedores reales distintos; sin commit, pendiente de que el usuario lo pida |
| Fix real -- resumen de respaldo para reconexion de Codex | 2026-09-05 | Basado en verify_codex_compaction_need.md, pregunta sin evaluar desde el cierre de Fase 3. 3 hallazgos reales: (1) Codex nunca estuvo en el branch CLI extendido con compactacion de respaldo en f54cda8 -- runTurnForWindow() (ipc-agent.ts) resuelve activeRuntime==='codex' en su propia rama con return temprano, antes de llegar al branch API o al branch CLI (session.cliRuntime); Codex usa session.codexClient (JSON-RPC), estructuralmente distinto. (2) Codex SI tiene un limite real de contexto (272000 tokens para gpt-5.x, confirmado con codex debug models real) y SU PROPIA compactacion automatica nativa, ya activa por default del lado del app-server real (model_auto_compact_token_limit, items contextCompaction reales en el protocolo JSON-RPC) -- confirmada empiricamente con una conversacion real de 6 turnos (ventana forzada a 3000 tokens via -c para no gastar cuota real de mas): 5 compactaciones automaticas reales, coherencia preservada las 6 veces -- nada que hacer ahi, ya resuelto del lado del binario. (3) Unico gap real: maybeCompactChatInBackground() nunca se disparaba para turnos de Codex, asi que un chat 100% Codex nunca generaba el resumen de respaldo que si tienen claude-cli/antigravity-cli/API desde f54cda8 -- al reconectar (thread NUEVO, ephemeral:true, sin nada de la memoria en proceso del thread viejo), solo quedaba el recorte duro de normalizeHistory(), sin resumen de respaldo. Fix: mismo patron fire-and-forget de una linea ya usado en los branches API/CLI, agregado al branch de Codex justo antes del return existente -- resolveCompactionTarget()/findAnyApiCapableConnection() (ya construidos en f54cda8) cubren el caso sin modelo dedicado exactamente igual que para claude-cli/antigravity-cli (codex-subscription es subscription/sin apiKey/no api-capable, mismo shape). NO toca la compactacion nativa de Codex (thread/compact/start sigue corriendo sola) ni el thread vivo -- exclusivo para el momento de reconexion. Corregida ademas la entrada de PENDING.md (Fase 3) que decia que Codex dependia de --resume -- ya no es asi (ephemeral:true, confirmado en codigo). Verificado real: bundle compilado paso de 2 a 3 call sites reales de maybeCompactChatInBackground(); misma metodologia exacta de f54cda8 (storage aislado real, chat-store real SQLite, servidor HTTP local real como otra conexion API-capable, fallbackProvider/fallbackModel con forma real de codex-subscription) -- chat 100% Codex con backlog real >6000 tokens: antes del fix getChatSummaryState() siempre null, con el fix se persiste un resumen real via el fallback de 2do nivel, y buildRuntimeContext() (funcion real que arma el seedContext de un reconnect) lo inyecta correctamente. | `src/main/ipc-agent.ts`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con storage aislado, chat-store real, servidor HTTP local real; sin commit, pendiente de que el usuario lo pida |
| Fix real -- staleness en revert_file + run_command cerrado como no aplica | 2026-09-05 | Basado en verify_run_command_revert_file_staleness.md, investigacion motivada por la pregunta abierta en PENDING.md desde el fix de TOCTOU original (d3a8fe4): run_command/revert_file quedaron fuera a proposito del mecanismo de sessionFileHashes. Tarea 1 (run_command): cerrado como NO APLICA, sin cambio de codigo -- el mecanismo de hash-por-sesion necesita un path conocido de antemano (write_file/apply_patch lo reciben como argumento explicito), run_command solo recibe un string de shell opaco pasado a exec(), sin forma confiable de derivar que archivos toca sin parsear shell arbitrario; la unica forma de extender el mismo mecanismo seria un diff completo del workspace antes/despues, que ya no es TOCTOU (prevenir) sino auditoria post-hoc (solo puede reportar despues del hecho); ademas resolveApproval() ya muestra el comando literal al usuario en workspace-write, un gate de intencion de categoria distinta. Tarea 2 (revert_file): gap real confirmado -- a diferencia de write_file/apply_patch (que releen el disco despues de resolveApproval() y comparan contra un hash tomado antes de pedir la aprobacion, d3a8fe4), revert_file leia currentContent una sola vez para armar el diff aprobado y escribia directo tras la aprobacion, sin ningun re-chequeo -- si otra sesion editaba el mismo archivo real mientras la aprobacion humana estaba pendiente, lo pisaba en silencio. Distincion real aplicada: "restaurar contenido viejo a proposito" (la funcion de la tool, no necesita proteccion) es distinto de "pisar sin avisar un cambio nuevo hecho DURANTE la ventana de aprobacion" (mismo bug de TOCTOU, nunca portado a esta tool). Fix: hashFileContent(currentContent) capturado antes de resolveApproval(), re-lectura real del disco + comparacion de hash justo antes de writeFileSync() -- si no coincide, rechaza con el mismo estilo de mensaje que write_file/apply_patch. NO usa sessionFileHashes (confirmado en la Tarea 3 de la investigacion que no aporta nada util en este flujo -- revert_file se llama tras list_file_history, no tras read_file). write_file/apply_patch sin ningun cambio. Verificado real: ToolRegistry real (bundle esbuild), workspace real en disco temporal, VCS oculto real (git real). Caso A (carrera real: write_file real de "otra sesion" corriendo mientras el confirm() de un revert_file real esta pausado 300ms simulando aprobacion humana lenta) -- revert_file rechazado por staleness, contenido de la otra sesion intacto en disco. Caso B (caso feliz, sin interferencia) -- revert_file sigue funcionando igual, archivo restaurado correctamente. PENDING.md: entrada dividida en 2 -- run_command marcada NO APLICA, revert_file marcada RESUELTO. | `src/main/tool-registry.ts`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con los 2 casos exactos pedidos, ToolRegistry real, VCS oculto real; sin commit, pendiente de que el usuario lo pida |
| Fix real -- migracion de openai-compatible/openai a openai-chat + reasoning_effort | 2026-09-05 | Basado en verify_compatible_button_fix_options.md/verify_compatible_migration_scope.md: el boton "Compatible" (type:'openai-compatible') ignoraba en silencio cualquier endpoint custom (runtime:'codex-api', spawnea codex app-server, nunca lee provider.endpoint). Camino 1 (model_providers.<id>.base_url de codex-cli) investigado y descartado con evidencia real en vivo: existe y funciona, pero solo habla Responses API para un provider custom, nunca Chat Completions. Fix, 3 piezas: runtimeFor() cambia estos 2 types de 'codex-api' a 'openai-chat' (conexiones existentes se autocorrigen solas en el proximo loadSettings(), mismo mecanismo del Hallazgo 2); readiness() (App.tsx) deja de exigir Codex CLI instalado para estos 2 types (bug bloqueante real que habria impedido conectar tras la migracion, openai-codex sigue exigiendolo sin cambios); reasoning_effort real agregado a sendOpenAiApi() (ApiAgentRuntime.send() gana effort? opcional, ignorado por los otros 3 kinds) -- openAiReasoningEffort() nueva fuerza 'none' EXPLICITO (nunca omitido) cuando hay tools activas en un modelo reasoning (o1/o3/o4/gpt-5.x, mismo regex que openAiMaxTokensField()), conflicto real y documentado entre reasoning_effort y tools en la Chat Completions API (omitir no es neutral, los modelos reasoning tienen default propio distinto de apagado); el valor real elegido viaja si no hay tools. Limpieza menor: 2 builtins de q_config.yaml (Groq/Ollama) actualizados de 'codex-api' a 'openai-chat'; panel de "Sincronizar catalogo" suma 'openai' a su gate (confirmado directo, listOpenAiChatModels() ya generico). Verificado real: conexion forzada a runtime:'codex-api' se autocorrigio a 'openai-chat' en reinicio real (incidente real de BOM durante la manipulacion manual, corregido de inmediato, 6 conexiones reales del usuario confirmadas intactas); logica de readiness() confirmada con cliStatus sintetico en el motor V8 real de la app (Codex CLI genuinamente instalado en esta maquina, no se pudo reproducir el bloqueo real sin desinstalarlo); reasoning_effort confirmado real con ApiAgentRuntime real + servidor HTTP local real -- con tools "none" explicito pese a effort="high" elegido, sin tools el valor real "high" elegido viaja tal cual. PENDING.md: entrada del bug "Compatible" marcada RESUELTO. | `src/shared/runtime-for.ts`, `src/renderer/src/App.tsx`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/main/settings-provisioning.ts`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con la app corriendo, reinicio real, servidor HTTP local real; sin commit, pendiente de que el usuario lo pida |
| Fix real -- rama openai-chat en callCompactionModel() | 2026-09-04 | Hallazgo lateral confirmado durante el fix anterior (compactacion de respaldo para claude-cli) e investigado aparte (verify_compaction_openai_chat_branch.md): callCompactionModel() tenia ramas explicitas para foundry/gemini-api, pero runtime==='openai-chat' (OpenRouter/Chat-Completions) caia en el fallback final (formato Anthropic Messages API). Reproducido real con un servidor Chat-Completions real y local (nunca una credencial externa real): la request salia como POST /v1/messages con headers de Anthropic, el servidor real (que solo entiende /v1/chat/completions) respondia 404 real, y ese error (mensaje enganoso, "Claude API") quedaba atrapado en silencio por el try/catch de fire-and-forget -- compactacion que nunca compactaba, sin ningun aviso visible. Fix: rama nueva para runtime==='openai-chat' en callCompactionModel(), mismo patron exacto que foundry/gemini-api -- valida apiKey/modelId con throw explicito, URL via openAiChatCompletionsUrl() (ya exportada, recien importada), fetchWithTimeout con Authorization: Bearer real (no x-api-key), body real de Chat Completions (messages, sin tools), resolveMaxOutputTokens(_, 'openai') (ya soportaba ese kind sin cambios), parseo con collectText/asRecord igual que las demas ramas. La deteccion de familia o1/o3/o4/gpt-5.x -> max_completion_tokens (metodo privado de ApiAgentRuntime) se duplico (2 lineas, funcion pura) en vez de exportarse -- mismo criterio ya establecido en este codebase para no acoplar modulos por algo tan chico. Ramas de foundry/gemini-api sin ningun cambio. Verificado real, mismo escenario exacto que reprodujo el bug: la request ahora sale a /v1/chat/completions con Authorization: Bearer real, el servidor real responde 200, y el resumen queda persistido real (antes null). PENDING.md: entrada de este hallazgo marcada RESUELTO. | `src/main/compaction-engine.ts`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con el mismo escenario exacto del bug, servidor HTTP local real sin credenciales externas; sin commit, pendiente de que el usuario lo pida |
| Fix real -- compactacion de respaldo extendida a claude-cli/antigravity-cli (Hallazgo 3) | 2026-09-04 | Basado en el Hallazgo 3 confirmado en verify_external_review_findings.md y el diseno revisado en verify_claude_cli_compaction_design.md: maybeCompactChatInBackground() solo se disparaba en el branch de runtimes API -- claude-cli/antigravity-cli nunca la llamaban, ni siquiera antes del retiro/reintegracion de claude-cli (confirmado con evidencia historica real, snapshot pre-dec378c). Friccion real confirmada antes de implementar: agregar la llamada sin mas no alcanzaba -- el fallback de siempre (sin modelo dedicado configurado) usa la MISMA conexion que origino el turno, que para claude-cli/antigravity-cli (subscription, sin apiKey real) produce un throw real dentro de callCompactionModel(), atrapado en silencio por el try/catch de fire-and-forget -- compactacion que nunca compacta nada, sin ningun error visible. Fix, 3 piezas: findAnyApiCapableConnection() (nueva, compaction-engine.ts) recorre settings.providers buscando cualquier conexion real enabled+apiKey real+modelo enabled API-capable; resolveCompactionTarget() gana un fallback de 2do nivel (configurado -> fallback de 1er nivel SI tiene apiKey real -> findAnyApiCapableConnection()) y ahora puede devolver undefined -- branch API existente SIN cambio de comportamiento (su fallback siempre tiene apiKey real por construccion); maybeCompactChatInBackground() corta temprano sin intentar la llamada si el resultado es undefined, nunca finge haber compactado. ipc-agent.ts: mismo patron fire-and-forget del branch API agregado al branch CLI. Alcance explicito: callCompactionModel() sin rama propia para openai-chat (hallazgo lateral de la investigacion previa) queda fuera de este fix, anotado en PENDING.md como pregunta separada. Verificado real (storage aislado via AMATISTA_STORAGE_ROOT, nunca toca datos del usuario; "otra conexion API-capable" = servidor HTTP real local controlado por el propio script, nunca una credencial real externa): con otra conexion disponible y sin modelo dedicado configurado, la request real llego al servidor local (la conexion claude-cli sin apiKey no podria haberla generado), el resumen quedo persistido real en chat_sessions, e inyectado de vuelta en el proximo turno via buildRuntimeContext() sin ningun cambio de codigo ahi; sin ninguna otra conexion disponible, no lanza y no persiste nada. | `src/main/compaction-engine.ts`, `src/main/ipc-agent.ts`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con los 2 casos exactos pedidos, storage aislado, servidor HTTP local real sin credenciales externas; sin commit, pendiente de que el usuario lo pida |
| Fix real -- TOCTOU real de write_file/apply_patch cerrado (Hallazgo 1) | 2026-09-04 | Basado en el Hallazgo 1 confirmado en verify_external_review_findings.md y el diseno revisado en verify_toctou_fix_design.md: el chequeo de staleness de d3a8fe4 solo auditaba "entrar a write_file/apply_patch -> fin de aprobacion" -- si el modelo habia leido el archivo turnos antes (read_file) y otra sesion lo modifico en el medio, el hash tomado AL ENTRAR ya reflejaba el cambio ajeno y el chequeo no detectaba nada, pisando el cambio en silencio con ok:true. Fix: ToolRegistry (singleton) gana sessionFileHashes (Map de instancia, clave sessionId::path -> hash SHA-256 del contenido completo pre-clip()); sessionId = panelId, nuevo campo opcional en ExecuteContext, poblado en ipc-agent.ts (mismo punto donde ya se arma confirm/resolveExploreModel/lspManager). read_file registra el hash al leer; write_file/apply_patch capturan ese hash ANTES de resolveApproval() (mismo criterio que existingHash de d3a8fe4, que queda intacto sin ningun cambio) y agregan un chequeo ADICIONAL despues, comparando el contenido fresco contra el hash registrado -- sin registro (archivo nunca leido, tipicamente uno nuevo) cae al comportamiento de siempre, sin bloquear. Tras escribir con exito, el registro se actualiza (la propia sesion no se autobloquea en su proxima escritura). Limpieza: disconnectSession() llama toolRegistry.clearSessionFileHashes(panelId), mismo punto que lspManager?.stopAll()/mcpManager?.stopAll(). Alcance confirmado: cli-agent-runtime.ts nunca importa tool-registry.ts (claude-cli/antigravity-cli tienen sus propias tools nativas, nunca pasan por aca) -- fix exclusivo de los 4 runtimes API. run_command/revert_file fuera a proposito, anotado en PENDING.md como pregunta abierta. Verificado real (ToolRegistry real standalone, sin mocks) con los 3 casos pedidos: mismo escenario exacto que expuso el gap -- ahora rechazado real, linea ajena intacta (antes: ok:true, dato perdido); archivo nunca leido en la sesion -- sin bloqueo, y segunda escritura de la misma sesion sin releer tampoco se autobloquea; caso feliz (leer+escribir sin interferencia) sin regresion. | `src/main/tool-registry.ts`, `src/main/ipc-agent.ts`, `src/main/runtime-state.ts`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con los 3 casos exactos pedidos, codigo real de ToolRegistry; sin commit, pendiente de que el usuario lo pida |
| Fix real -- runtimeFor() unificado en shared/, elimina el gap de 'openrouter' | 2026-09-04 | Basado en el Hallazgo 2 de la revision externa (verify_external_review_findings.md): settings-store.ts (main) y App.tsx (renderer) tenian 2 copias casi identicas de runtimeFor() -- la del renderer tenia el caso explicito de 'openrouter' (Fase 15), la de main NO, asi que una conexion OpenRouter nacia con runtime:'openai-chat' pero se corrompia sola a runtime:'gemini-api' en el primer reinicio real de la app (loadSettings()/migrateProvider(), corre en cada arranque, reescribe el archivo). Investigacion previa confirmo que las 2 copias eran funciones puras (type, authMode) -> RuntimeKind sin ningun motivo real para diferir -- la divergencia fue un descuido de mantenimiento, no una necesidad de diseno. Enfoque: unificacion, no parche -- mismo patron ya establecido por shared/model-capabilities.ts (isApiCapableModel(), ya importada por main y renderer). Nuevo shared/runtime-for.ts con una sola declaracion (los 5 casos que ya compartian + openrouter); las 2 copias locales borradas enteras. settings-store.ts pasa a llamar runtimeFor(base.type, base.authMode); App.tsx no toco ningun call site (sus 4 usos ya llamaban runtimeFor(type, authMode) con el mismo shape). Ningun otro caso/RuntimeKind tocado. Verificacion real: mismo escenario exacto que reprodujo el bug -- conexion OpenRouter real creada, reinicio real de la app, polling real del settings.json cada 500ms durante 10s -- runtime se mantuvo 'openai-chat' estable, sin ninguna transicion a 'gemini-api'. Conexion de prueba borrada real via la UI, settings.json real del usuario confirmado intacto. | `src/shared/runtime-for.ts` (nuevo), `src/main/settings-store.ts`, `src/renderer/src/App.tsx` | Completa -- npm run typecheck y npm run build en verde; verificado real con la app corriendo, reinicio real, polling real; sin commit, pendiente de que el usuario lo pida |
| Fix real -- HOME de Antigravity por conexion, ya no compartido entre toda la app | 2026-09-02 | Basado en la carrera real confirmada en verify_antigravity_authmode_race.md (mecanismo real reproducido -- 2 conexiones distintas SI podian pisarse el mismo settings.json compartido sin ningun error/aviso; sintoma final no confirmado ahi por un bloqueo de cuota real no relacionado). Fix: getAntigravityHomeDir() (app-paths.ts) paso de una carpeta fija compartida a getAntigravityHomeDir(connectionId) -- subcarpeta distinta por conexion bajo la misma carpeta aislada de siempre; connectionId es provider.id (ProviderProfile.id), ya existia como identificador real, no inventado para esta fase. Las 3 funciones de antigravity-home.ts (antigravityIsolatedEnv/writeAntigravitySettingsForAuthMode/writeAntigravityMcpConfig) ganan el parametro y lo propagan; el unico caller real (cli-agent-runtime.ts, rama antigravity de buildEnv()) pasa provider.id, ya disponible ahi. clearAntigravityHomeDir() (garantizada al arrancar, best-effort al cerrar) SIN cambio de logica -- sigue barriendo la carpeta padre antigravity-home, que ahora contiene una subcarpeta por conexion en vez de archivos directos de una sola, mismo barrido de siempre ya limpia todas. Verificacion real: codigo real compilado standalone (esbuild + stub de electron) contra D:\AMATISTA\data real confirmo rutas distintas para los 2 provider.id reales de las 2 conexiones Antigravity ya existentes del usuario; turno real completo con --model claude-sonnet-4-6 (cuota de Gemini evitada a proposito) completo con exito en ambas conexiones, cada una escribiendo en su propio settings.json real (dato nuevo real: agy soporta modelos Claude por debajo de una conexion subscription); repeticion del mecanismo real de la carrera original (ensanchamiento Atomics.wait + 2 procesos de Node reales y separados corriendo concurrentes de verdad, mas fiel al escenario de 2 paneles que 2 promesas del mismo hilo) confirmo que la colision ya NO es reproducible -- settings.json de A quedo exactamente {}, el de B exactamente {"modelProvider":"gemini"}, ninguno se piso porque son archivos reales distintos. Instrumentacion temporal revertida al 100% (confirmado grep/git diff --stat), clearAntigravityHomeDir() real ejecutado al terminar (antigravity-home real confirmada vacia). PENDING.md: caveat de concurrencia real de la integracion de Antigravity marcado RESUELTO. | `src/main/app-paths.ts`, `src/main/antigravity-home.ts`, `src/main/cli-agent-runtime.ts`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con la app/codigo real, 2 conexiones reales del usuario, turnos reales completos, carrera original re-forzada y confirmada ya no reproducible; sin commit, pendiente de que el usuario lo pida |
| Retiro completo de gemini-cli -- renombre del literal, subproceso CLI retirado | 2026-09-02 | Basado en verify_gemini_cli_removal_scope.md (investigacion previa: gemini-cli standalone discontinuado para cuentas individuales, IneligibleTierError real confirmado contra la cuenta real, Google redirige a Antigravity). Distincion central confirmada por codigo: RuntimeKind:'gemini-cli' nunca fue sinonimo de "spawnea el binario" -- el disambiguador real siempre fue isApiCapableModel(provider, model), que ya distinguia authMode:'subscription' (CLI) de authMode:'api-key' (HTTP, ApiAgentRuntime kind 'gemini-api') para el MISMO literal. Renombre: 'gemini-cli' -> 'gemini-api' (coincide a proposito con ApiAgentKind 'gemini-api', mismo criterio que 'foundry'/'anthropic-api'/'openai-chat' ya seguian) -- tocado en shared/types.ts, settings-store.ts:runtimeFor() (la fuente mas autoritativa, corre en cada loadSettings()), su gemelo en App.tsx, shared/model-capabilities.ts:isApiCapableModel(), settings-provisioning.ts (builtins api-key), explore-tool.ts, compaction-engine.ts. Retiro del subproceso CLI (cli-agent-runtime.ts): sendGemini()/geminiCommand()/rama gemini de buildEnv() borradas, CliAgentKind reducido a 'claude'|'antigravity', los 2 fallbacks implicitos que caian en Gemini vueltos fallthrough honesto de antigravity. ipc-agent.ts: branch CLI de connectSessionForWindow() sin el dispatch a Gemini -- una conexion vieja authMode:'subscription' recibe error explicito en vez de spawnear algo inexistente; mismo bloqueo incondicional en readiness() (App.tsx). Plumbing retirado sin caller: cli:installGemini (ipc-cli.ts), detectGemini() (cli-status.ts), openGeminiLogin() (auth-manager.ts), installGeminiCli()/campo gemini de getCliStatus() (preload). UI: boton de suscripcion Gemini retirado de "Agregar conexion" (el de API key se queda); segmento/botones/hint de Gemini retirados quirurgicamente de la seccion CLI compartida (Claude/Antigravity intactos). Builtin "Gemini Advanced (suscripcion Google)" (settings-provisioning.ts) retirado entero. PENDING.md: nota del bug de parseo -p/prompt-posicional cerrada como obsoleta. Verificacion real con la app corriendo (CDP contra el proceso real, --inspect+--remote-debugging-port, sin LLM/API key): "Agregar conexion"/seccion CLI reales confirmadas sin Gemini-CLI; conexion real Gemini API key creada por click real escribio a disco real runtime:'gemini-api' (confirmado el renombre de punta a punta); selectores reales de compactacion/generacion de imagenes siguen listando Gemini API como candidato, sin regresion; conexion de prueba borrada real al terminar, settings.json real del usuario confirmado intacto. Hallazgo honesto fuera de alcance, no corregido: providerName() (App.tsx) nombra conexiones nuevas solo por type sin ramificar por authMode (mismo limite pre-existente para Claude API key) -- anotado, no arreglado en esta fase. | `src/shared/types.ts`, `src/shared/model-capabilities.ts`, `src/main/settings-store.ts`, `src/main/settings-provisioning.ts`, `src/main/cli-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/main/ipc-cli.ts`, `src/main/cli-status.ts`, `src/main/auth-manager.ts`, `src/main/explore-tool.ts`, `src/main/compaction-engine.ts`, `src/main/mcp-client.ts` (solo comentario), `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `docs/_arch/PENDING.md` | Completa -- npm run typecheck y npm run build en verde; verificado real con la app corriendo via CDP, sin LLM/API key; sin commit, pendiente de que el usuario lo pida |
| Fix real -- deteccion de staleness en write_file/apply_patch (huella de contenido) | 2026-09-02 | Basado en verify_concurrent_write_staleness.md (investigacion previa: reproducido real, 2 escenarios -- carrera pura write_file vs write_file con Promise.all, y staleness forzada real de apply_patch inyectada via el propio ctx.confirm de la sesion A para correr la escritura completa de B durante la espera de aprobacion de A; ambas tools escribian ciego, ok:true, sin ningun chequeo -- ni hash, ni mtime, ni version -- perdiendo el cambio ajeno en silencio con una confirmacion de exito enganosa). Preguntas de diseno confirmadas con codigo real antes de implementar: no existia funcion de hash de CONTENIDO reusable (createHash de node:crypto ya estaba importado en local-vcs.ts pero solo para hashear la ruta del workspace); el punto de inyeccion exacto es identico en las 2 tools (justo despues de que cierra el `if (!approved)`, antes de snapshotFile()/writeFileSync()); ToolExecutionResult no tiene ningun canal estructurado hacia el modelo mas alla de `output` (string), confirmado por los propios comentarios del codigo ("nunca llega al modelo"). Fix: hashFileContent() nueva (sha256, mismo import que local-vcs.ts, variable separada, sin tocar el uso existente ahi) -- se hashea el contenido en el mismo instante que ya se leia (existingContent, antes de resolveApproval()), y justo antes de snapshotFile()/writeFileSync() se relee el archivo real y se compara el hash fresco contra el guardado; si no coinciden, return {ok:false, output: mensaje real orientando al modelo a releer con read_file y reintentar, mismo estilo que el mensaje ya existente de old_str no encontrado} -- nunca llega a tocar el VCS oculto ni el archivo real. resolveApproval()/ctx.confirm() sin tocar, por restriccion explicita. Verificacion real: mismo harness bundleado standalone con esbuild (mismo patron que mcp:lsp:bundle) contra un workspace aislado real, mismo escenario EXACTO de la investigacion (mismo mecanismo de inyeccion via ctx.confirm de A) -- confirmado que A ahora se rechaza con el mensaje real de staleness, sin llegar a escribir, y el cambio de B (que antes se perdia) sobrevive intacto en disco. Caso feliz (escritura normal, sin carrera) confirmado sin cambios, sin falsos positivos. Hallazgo honesto adicional, no escondido: en el escenario de carrera PURA sin ningun gap forzado (ambas danger-full-access, Promise.all, sin pausa real entre lectura y escritura de ninguna de las 2), el chequeo optimista puede no alcanzar a detectar la carrera si ambas relecturas ocurren antes de que cualquiera de las 2 escrituras reales suceda -- residual esperable de un chequeo optimista sin lock exclusivo real, no una regresion ni un descuido; el escenario real que motivo el fix (una pausa real de cualquier duracion -- espera de aprobacion humana, tiempo de decision del LLM, o el ~17s del auto-open del orquestador) queda cubierto al 100%, confirmado 5 corridas adicionales. | `src/main/tool-registry.ts` | Completa -- npm run typecheck y npm run build en verde; verificado real con el mismo escenario exacto de la investigacion, antes/despues, mas el caso feliz sin regresion; hallazgo honesto sobre el limite teorico de un chequeo optimista sin lock, documentado no escondido; sin commit, pendiente de que el usuario lo pida |
| Fix real -- closePanel() no sincronizaba openPanelsRef.current, resurreccion de identidad de panel | 2026-09-02 | Basado en verify_closepanel_race.md (investigacion previa: reproducido real, no teorico -- ventana natural medida 4.1ms entre closePanel()/setOpenPanels() y que el useEffect sincroniza openPanelsRef.current; el auto-open real de send_to_window tarda ~17s en llegar al renderer desde la aprobacion, ventana de oportunidad real durante ese lapso; sintoma real confirmado 2 veces: un panel cerrado por el usuario "resucitaba" con el MISMO panelId, arrastrando estado viejo -- mas serio que el falso limite de MAX_PANELS que PENDING.md especulaba originalmente). Fix: closePanel() muta openPanelsRef.current manualmente y SINCRONICO, mismo patron de ref-espejo que openChatInPanel() ya usaba para el caso inverso (abrir) -- mismo guard que el updater real (nunca toca el ref si el panel no se va a cerrar de verdad, o sea si es el ultimo panel abierto), sin depender del useEffect (que corre despues del commit+paint) para sincronizar despues. openChatInPanel()/el mecanismo de auto-open en si quedaron sin tocar, por restriccion explicita -- el fix es exclusivamente que closePanel() deje de ser la fuente de la desincronizacion. Verificacion real: mismo escenario EXACTO de la investigacion original (panel real como origen, `WORKSPACE_AMATISTA — Panel 5` real como destino, sendToWindowByTitle() real invocada via el mismo mecanismo CDP/V8 inspector sin LLM ni API key -- ninguna disponible en este entorno), esta vez con timing 100% NATURAL, sin ningun widening artificial (se elimino por completo, de 30s a 0s, y el fix se sostuvo igual) -- snapshot leido por openChatInPanel() al llegar el auto-open (12.76s despues del cierre, mismo orden de magnitud que los ~17s medidos antes) YA NO incluye el panel recien cerrado; confirmado adicional cerrando el panel auto-abierto y viendo un panelId DISTINTO en el log (sin reciclaje). | `src/renderer/src/App.tsx` | Completa -- npm run typecheck y npm run build en verde; verificado real con el mismo escenario exacto que expuso el bug, contrastado antes/despues, timing natural sin widening; instrumentacion temporal de diagnostico revertida al 100% (git diff final solo con el fix real); sin commit, pendiente de que el usuario lo pida |
| 3 piezas de UI: menu "..." en header de panel, sidebar contraible, texto deslizante (marquee) en hover | 2026-09-02 | Basado en verify_ui_sidebar_header.md (Tareas 1/2/3 para Pieza 1, Tarea 5 para Pieza 2) + investigacion propia para Pieza 3 (sin doc previo). PIEZA 1: ContextMenuState gana una 4ta variante `panelHeader` (mismo patron discriminado ya usado para chat/message/composer) -- .mcp.json y Eventos (N) (los 2 controles de menor frecuencia real de uso, ya identificados) se mueven detras de un boton "···" nuevo que abre el mismo `.context-menu` compartido; Modelo/Panel/× quedan visibles sin cambio. `.context-menu button:disabled` nueva (no existia, .mcp.json puede llegar deshabilitado sin workspace activo). PIEZA 2: 1 estado nuevo (`sidebarCollapsed`, no persistido a proposito, mismo criterio que modelMenuOpen/settingsOpen) + `.sidebar-toggle` nuevo, SIEMPRE renderizado con `position:fixed` (no adentro de `.sidebar` -- con el sidebar en 0px no habria adentro donde ubicarlo que siguiera clickeable) + `.app.sidebar-collapsed{grid-template-columns:0px minmax(0,1fr) !important}` (1 sola propiedad controla el ancho de esa columna, confirmado en la investigacion previa, sin reestructurar layout; `!important` necesario para ganarle a 2 reglas `.app{...!important}` preexistentes en el archivo). PIEZA 3: `MarqueeSpan` nueva (componente reusable, 4 usos: chat-title-main, chat-title-sub, project, root-title-open) -- la animacion es 100% CSS (`@keyframes` + `:hover`, `container-type:inline-size` + `100cqw` para que el keyframe calcule el desplazamiento EXACTO sin medir nada por JS), el UNICO JS es el gate `is-truncated` (medido con `scrollWidth > clientWidth`, remedido en cada cambio de texto y en cada resize de ventana) -- necesario porque CSS puro con container query units no puede distinguir "esta cortado" de "no esta cortado" sin producir un salto espurio chico en items que ya entran completos. `.project`/`.root-title-open` (botones que antes truncaban directo) pasan a `display:flex` con el overflow movido al `<MarqueeSpan>` hijo -- `.root-title-open` ademas separa el "⌄" en un `.root-title-chevron` (`flex:none`) para que no se deslice junto con el nombre. Bug real encontrado en la propia verificacion en vivo (no en la investigacion): con el sidebar en 0px, `.topbar` (que arranca en x=0) se solapaba con `.sidebar-toggle` (fixed, left:10px) -- fix real: `.app.sidebar-collapsed .topbar{padding-left:52px !important}` (mismo motivo de `!important`, gana a otro `.topbar{padding:0 18px !important}` preexistente). | `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa -- npm run typecheck y npm run build en verde (incluye el fix del solape encontrado en vivo); verificado real con la app en modo dev: Pieza 1 (menu "···" abre con .mcp.json/Eventos, ambos clickeados y confirmados funcionando igual que antes -- Eventos abrio el panel de debug real, .mcp.json disparo `openMcpConfig()` sin error); Pieza 2 (colapsa/expande confirmado con 1 y 2 paneles reales abiertos, sin romper el grid, boton siempre accesible en ambos estados, tooltip correcto en cada uno); Pieza 3 (hover real sobre un chat real "WORKSPACE_AMATISTA — Panel 3" -- capturado en 3 momentos: inicio cortado, ~2.3s deslizado revelando el "3" antes oculto por el ellipsis, y vuelta suave al soltar el hover; confirmado con DevTools que SOLO los 2 chats reales con `scrollWidth > clientWidth` reciben `is-truncated`, los demas -- incluida `WORKSPACE_AMATISTA — Panel 5`, que entra justo -- quedan sin la clase, sin animacion). Fix de scroll horizontal del header (Problemas 1-4, sin commitear todavia) no tocado, confirmado coexistiendo (el cluster de acciones ahora es MAS chico con "···" reemplazando a 2 botones, menos necesidad de scroll que antes). 1 chat de prueba creado durante la verificacion borrado al terminar via `chats:deleteSession` real; confirmado con reload completo que los 6 chats reales del usuario quedaron intactos; sin commit, pendiente de que el usuario lo pida |
| Orquestador panel 1 (gating send_to_window/list_windows) + numeracion visual 1/2/3/4 | 2026-09-02 | Basado en verify_panel_orchestrator.md (investigacion previa: send_to_window ya hace await-and-return real sobre runTurnForWindow, ya opera 100% sobre panelId post-Paneles-1, sin gating de tools por panel hoy). PIEZA 1 (gating por chat "principal"): isPrincipalChat(chatId) nueva en chat-store.ts -- MISMO criterio exacto que ya usa findChatSessionByPanelAlias() para el alias "1"/"principal" (titulo SIN el sufijo " — Panel N"), consultado por chatId real en vez de por alias tipeado; false si el chat no existe (sin evidencia, sin beneficio de la duda). Calculado UNA vez por conexion en connectSessionForWindow() (ipc-agent.ts, no en cada turno) y threadeado como isPrincipalChat?:boolean en ConfigureOptions (api-agent-runtime.ts) -- ApiAgentRuntime.toolCatalog() gana un 3er filtro por nombre (mismo patron ya usado ahi para AMATISTA_EXCLUDED_TOOLS, y en explore-tool.ts para EXPLORE_TOOL_NAMES): send_to_window/list_windows se excluyen del catalogo entero si !isPrincipalChat, resto de tools identico. Log real bajo AMATISTA_DEBUG_TOOLS existente (mismo flag que ya usaba logToolCall). Confirmado real (no solo asumido) que codex-subscription/codex-api y los 3 runtimes CLI (claude-cli/antigravity-cli/gemini-cli) NUNCA tocan TOOL_DEFINITIONS/ToolExecutor/toolRegistry (grep sin resultados en codex-client.ts/cli-agent-runtime.ts) -- ApiAgentRuntime.toolCatalog() es el UNICO lugar del codebase donde send_to_window/list_windows llegan a un modelo, nada mas que gatear. PIEZA 2 (numeracion visual, eje deliberadamente distinto de Pieza 1): ChatPanelProps gana panelIndex:number: prop nueva, poblada en el call site (App.tsx) con openPanels.map((entry, index) => ... panelIndex={index+1}) -- el indice del array YA era el orden visual del grid, no existia ningun estado de "posicion" antes, cero campo nuevo en PanelEntry/openPanels. panel-header-title pasa de {activeWorkspaceName ?? activeChat.title} a {panelIndex} (el nombre del workspace ya se ve en el titulo general, era redundante). Bug real encontrado en la verificacion en vivo (no en la investigacion previa): con contenido de 1 solo caracter, .panel-header-identity (badge+numero) colapsaba a ~17px de ancho -- MENOS que el propio ProviderBadge de 26px, numero invisible aunque presente en el DOM (textContent/opacity/visibility todos correctos) -- causa real: competia por espacio con .panel-header-actions bajo flex-shrink proporcional a su propio flex-basis (auto); con el nombre del workspace (texto largo, flex-basis grande) la perdida proporcional quedaba chica en TERMINOS RELATIVOS (se notaba como ellipsis, nunca colapso total); con 1 digito (flex-basis ~44px) el mismo reparto consumia casi toda su base. Fix real: flex-shrink:0 en .panel-header-identity -- la queda fuera de la competencia, toda la presion de espacio insuficiente la absorbe .panel-header-actions via su propio overflow-x:auto (Problema 4, ya disenado para eso). Verificacion real: harness real (esbuild --packages=external, patron ya establecido por mcp:lsp:bundle) importando chat-store.ts/api-agent-runtime.ts REALES fuera de Electron, isPrincipalChat() confirmado real contra 5 chats REALES de la DB del usuario (2 principales, 3 con sufijo, 1 chatId inexistente -- los 6 resultados exactos a lo esperado) + ApiAgentRuntime.toolCatalog() real via configure() real: isPrincipalChat=true -> 19 tools (incluye ambas), false -> 17 (ninguna), undefined (default, fail-closed) -> 17, resto del catalogo identico en los 3 casos. App real en modo dev, 4 paneles reales (GPT-5.5 via Codex ChatGPT suscripcion), DevTools real: bug de ancho encontrado con querySelectorAll+getBoundingClientRect (no con la vista, que lo ocultaba), fix aplicado con HMR, re-medido -- identityW/titleW pasaron de 0/16.7px a valores reales visibles, "1"/"2"/"3"/"4" confirmados tanto por DOM como visualmente en captura. 3 chats de prueba creados durante la verificacion (Panel 4/6/7) borrados al terminar via chats:deleteSession real (IPC real, no edicion directa de la DB), confirmado que los 6 chats reales pre-existentes del usuario quedaron intactos tras un reload completo. | `src/main/chat-store.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa -- npm run typecheck y npm run build en verde; verificado real (harness real contra DB/clase reales para Pieza 1, app real con 4 paneles + DevTools para Pieza 2, incluyendo 1 bug real de CSS encontrado y corregido durante la propia verificacion); sin commit, pendiente de que el usuario lo pida |
| 4 fixes reales de UI de paneles (0.9.0) | 2026-09-01 | Basado en verify_ui_paneles_0_9_0.md (Problema 5 confirmado que no necesita cambio). Problema 1: chatBorderAccent() (ya existia, Fase arbol de sub-chats -- antes SOLO aplicaba a filas del sidebar) reusada tal cual como borde izquierdo del <ChatPanel> completo, PROVIDER_BRAND.antigravity sigue el mismo camino sin caso especial. Problema 2: sacado el boton "Modelos y cuentas" del topbar (era setSettingsOpen(true) identico al icono de Configuracion del sidebar) -- un solo acceso. Problema 3: deleteChat() llama a closePanel() (ya existente) para el panel afectado en vez del redirect silencioso viejo a un chat cualquiera; caso limite de unico panel abierto (closePanel() se niega a cerrar el ultimo, guard ya existente) resuelto redirigiendo a un chat en blanco NUEVO via createBlankChat(), no a uno borrado. Problema 4: .panel-header-actions tenia flex:none (el cluster modelo+.mcp.json+Eventos+Panel+X nunca se achicaba) -- con exactamente 3 paneles (repeat(3,1fr), mas angosto que el grid 2x2 de 4) se desbordaba real contra el panel vecino, reproducido en vivo antes del fix; cambiado a flex:0 1 auto + min-width:0 + overflow-x:auto -- el cluster se achica y scrollea DENTRO de su propio panel en vez de invadir al vecino. Verificado real con la app en modo dev y 3 paneles reales: boton confirmado ausente (Problema 2); borde real confirmado via DOM (getAttribute('style') en DevTools) -- neutral rgb(156,163,175) antes del primer turno (mismo comportamiento ya establecido para el sidebar, no es bug nuevo), verde real de OpenAI rgb(16,163,127) despues de un turno real con Codex (Problema 1); scrollbar horizontal real confirmada dentro del header en vez de desborde sobre el vecino, con 3 paneles reales conectados (Problema 4); borrar un chat con panel abierto (entre 3 paneles) confirmado que baja el conteo de 3 a 2 -- panel cerrado de verdad, no redirigido (Problema 3), y el caso limite de 1 solo panel confirmado que redirige a un chat en blanco sin romper la UI ni llegar a 0 paneles. | `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa -- npm run typecheck y npm run build en verde; verificado real en vivo los 4 fixes (Problema 5 confirmado sin cambio); sin commit, pendiente de que el usuario lo pida |
| Fix del gate de permisos -- --allowedTools para las 4 tools MCP de LSP | 2026-09-01 | Basado en verify_claude_permission_allowlist.md. permissionArgs() (cli-agent-runtime.ts, rama claude): fuera de danger-full-access, agrega --allowedTools con los 4 nombres EXACTOS (mcp__amatista-lsp__find_definition/find_references/list_symbols/get_diagnostics) -- nunca wildcard (mcp__amatista-lsp__*), confirmado real con multiples issues abiertos en anthropics/claude-code que el wildcard falla en silencio. danger-full-access sin tocar (--dangerously-skip-permissions ya salta todo, seria redundante). Hallazgo real no anticipado: el fix NO funciona en sandbox "read-only" (--permission-mode plan) -- confirmado real que plan mode deniega la tool sin importar --allowedTools (wire log real "tool permission denied", el propio modelo explica que plan mode bloquea toda ejecucion de tools por diseno, sin forma programatica de salir). El codigo quedo implementado en ambas ramas no-danger-full-access tal como se pidio -- en read-only es un no-op inofensivo, sin regresion pero tampoco el fix buscado ahi. Verificado real en los 3 sandbox modes: read-only sigue denegando (sin cambio respecto a antes); default/workspace-write (acceptEdits) confirmado que ahora SI funciona -- permission_denials vacio, wire log real con permissionDecisionMs=0, resultado correcto corroborado por 2 tools distintas (find_definition + list_symbols); danger-full-access confirmado sin cambios. | `src/main/cli-agent-runtime.ts` | Completa -- npm run typecheck y npm run build (con mcp:lsp:bundle) en verde; verificado real en los 3 sandbox modes; 1 hallazgo real documentado (read-only no se beneficia del fix, limitacion estructural de plan mode, no bug de esta implementacion); sin commit, pendiente de que el usuario lo pida |
| Servidor MCP de LSP para los 3 CLIs (alcance reducido, sin aprobacion) | 2026-09-01 | Basado en verify_mcp_server.md + verify_mcp_approval.md. Implementacion real de un servidor MCP propio de Amatista -- alcance chico a proposito: SOLO find_definition/find_references/list_symbols/get_diagnostics, sin aprobacion (las 4 son de solo lectura). Codex queda fuera de alcance (codex app-server --stdio es un proceso persistente via JSON-RPC, arquitectura distinta al spawn per-turno de Claude/Antigravity). @modelcontextprotocol/sdk + zod agregadas -- confirmado real que NO necesitan asarUnpack por si mismas (JS puro), pero el .cjs de SALIDA si lo necesita, mismo motivo real que typescript-language-server/pyright: un script que un CLI externo spawnea como proceso no puede vivir dentro de app.asar. mcp-lsp-server.ts nuevo: script standalone bundleado aparte con esbuild (mcp:lsp:bundle, mismo patron que bench:bundle) a out/main/mcp-lsp-server.cjs, NO por el bundle principal de electron-vite. Lee AMATISTA_MCP_WORKSPACE por ENV, instancia un LspManager real (misma clase de los 4 runtimes API, sin reescribirla), registra las 4 tools via server.tool() del SDK con las MISMAS descriptions ya mejoradas de tool-registry.ts (reusadas literal) + una frase extra por tool apostando (no garantizando, el benchmark ya confirmo que ninguna tactica sola garantiza uso) a que un CLI headless las prefiera. Bug real de path normalization encontrado y arreglado (separador mixto / vs \ en el ENV producia falsos "fuera del workspace"). resolveBundledServerEntry() (lsp-client.ts) rompia fuera de Electron (app.getAppPath() vive solo dentro del runtime real) -- fix real: resolveElectronAppPath() con require('electron') dinamico en try/catch (mismo patron ya usado para officeparser/@napi-rs/canvas) + AMATISTA_APP_PATH (ENV) como fallback para el proceso standalone. Wiring: sendClaude()/sendClaudeWithImages() (cli-agent-runtime.ts) agregan --mcp-config con JSON INLINE (confirmado real que el flag lo acepta, no solo archivos), SIN --strict-mcp-config a proposito (se suma a la config MCP que el usuario ya tenga, nunca la reemplaza); buildEnv() rama antigravity agrega writeAntigravityMcpConfig() nueva (antigravity-home.ts, mismo PATRON que writeAntigravitySettingsForAuthMode(), no literal -- archivo y shape distintos, <HOME>/.gemini/config/mcp_config.json) porque agy es el unico de los 3 sin flag efimero real, misma limitacion heredada de carpeta compartida que settings.json. Bug real de build encontrado en la propia verificacion: npm run dev vacia out/main/ al arrancar, el bundle standalone desaparece -- npm run build/dist quedan en el orden correcto (electron-vite build && mcp:lsp:bundle), pero en dev hace falta correr mcp:lsp:bundle a mano de nuevo tras cada reinicio, documentado, no arreglado (ergonomia de dev, fuera de alcance). Hallazgo real no anticipado: con sandbox "Workspace" (--permission-mode acceptEdits), Claude deniega SOLO las tools MCP sin preguntar a nadie -- acceptEdits no cubre MCP de terceros, solo sus tools nativas de edicion; con "Acceso completo" si funciona. Anotado en PENDING.md para decidir si las 4 tools de LSP deberian pre-aprobarse tambien en "Workspace" (son de solo lectura, mismo criterio que read_file/list_dir/git_status), sin implementarlo en esta fase. Verificado real en 4 niveles: smoke test directo sobre el .cjs bundleado (find_definition real, tras el fix de path); Claude Code CLI real (2.1.241) con --debug mcp, wire log real confirma "Calling MCP tool: find_definition"; Antigravity CLI real (agy 1.1.23), confirmado NO simulado por evidencia real en disco (4 schemas cacheados reales bajo <HOME>/.gemini/antigravity-cli/mcp/amatista-lsp/, transcript real con 8 menciones y el valor exacto 1:17) -- mismo servidor SDK-based que el hand-rolled de la investigacion anterior NO lograba conectar con agy, este si; wiring real de produccion (no un config armado a mano) en la app real corriendo, panel real conectado a Claude Sonnet, workspace real de un proyecto del usuario -- find_definition sobre un identificador real resolvio cross-file correcto a otro archivo real de una dependencia npm real (server/node_modules/pg-protocol), resolucion solo posible con un language server real corriendo. | `src/main/mcp-lsp-server.ts` (nuevo), `src/main/lsp-client.ts`, `src/main/antigravity-home.ts`, `src/main/cli-agent-runtime.ts`, `package.json` | Completa -- npm run typecheck y npm run build (con mcp:lsp:bundle incluido) en verde tras matar todos los procesos de prueba; verificado real en 4 niveles (smoke test directo, Claude CLI con wire log, Antigravity CLI con evidencia en disco, wiring real de produccion en la app corriendo); 1 bug de path normalization + 1 de build-order + 1 hallazgo real de permisos, todos documentados; sin commit, pendiente de que el usuario lo pida |
| Verificacion del named pipe de aprobacion (pieza aislada, servidor MCP propio) | 2026-09-01 | Basado en verify_mcp_server.md + verify_mcp_approval.md -- pieza aislada, sin implementar las 20+ tools reales de Fase 1 todavia. mcp-approval-pipe.ts nuevo: startMcpApprovalPipeServer() abre UN net.createServer() (primer uso real de node:net en el codebase) escuchando en un named pipe fijo de Windows (\\.\pipe\amatista-mcp-approval), un solo listener para toda la app -- protocolo propio de una linea JSON en cada direccion ({panelId,title,detail} / {approved}), el handler llama directo a requestSessionToolApproval() ya existente (el mismo mecanismo real que ya usa McpManager/mcpConfirm hoy), cero codigo nuevo de UI. Arrancado en index.ts junto a la limpieza garantizada de Antigravity. Bug real encontrado en la propia verificacion: Git Bash (MSYS2) mangla el path del pipe al pasarlo como variable de entorno export'ada a un binario nativo de Windows -- aislado con un smoke test directo desde Node (sin pasar por bash) que si conecto sin problema. Fix real: pasar el path/panelId por el campo "env" del --mcp-config JSON en vez de por env exportada -- no aplica al camino real de produccion, donde Amatista spawnea con child_process.spawn() directo, sin shell intermedio. Verificado real de punta a punta con Amatista real en modo dev, panelId real leido via DevTools, y Claude Code CLI real (2.1.241) conectado a un servidor MCP de prueba (approval-test-server.js, NDJSON a mano, vive en el scratchpad de la sesion, no en el repo) con una sola tool echo_with_approval: caso APROBADO (dialogo real aparecio en la ventana real de Amatista con el titulo/detalle exactos, click real en Aprobar, resultado final real del lado de Claude `ECHO: PRUEBA REAL DEL PIPE - CASO APROBAR`) y caso RECHAZADO (mismo flujo, click real en Rechazar, resultado final real `RECHAZADO por el usuario`) -- el bloqueo real del proceso hijo sin polling confirmado en ambos sentidos. 2 console.log temporales de diagnostico (ipc-agent.ts, App.tsx) usados para leer el panelId real removidos al terminar. | `src/main/mcp-approval-pipe.ts` (nuevo), `src/main/index.ts` | Completa -- npm run typecheck y npm run build en verde tras la limpieza de los logs temporales; verificado real de punta a punta (caso aprobado y caso rechazado, dialogo real, bloqueo real, resultado real de vuelta al modelo); sin hallazgos que obliguen a un diseno distinto para Fase 1; sin commit, pendiente de que el usuario lo pida |
| Integracion completa de Antigravity CLI | 2026-09-01 | Basado en verify_antigravity_integration.md (6 tareas) sobre la infraestructura de HOME aislado ya implementada. CliAgentKind/RuntimeKind/ProviderType ganan 'antigravity'/'antigravity-cli'/'antigravity' -- ProviderType nuevo, deliberadamente NO reusa 'google' (mismo tipo de ambiguedad que ya causo un fix real para DeepSeek reusando 'anthropic'). sendAntigravity() nueva (cli-agent-runtime.ts, mismo esqueleto que sendClaude(), spawn+JSON.parse unico al exit, sin streaming) + antigravityCommand() (mismo patron que claudeCommand()/geminiCommand()). buildEnv() rama antigravity: parte de antigravityIsolatedEnv() (ya existente) para CUALQUIER conexion; subscription confirma real que el keyring del SO resuelve la cuenta con el HOME redirigido; api-key escribe settings.json real (GEMINI_API_KEY sola no alcanza, confirmado con key real invalida). permissionArgs() rama antigravity: read-only/workspace-write/danger-full-access mapeados a --mode plan/accept-edits/--dangerously-skip-permissions + --add-dir <workspace> en los 3, deliberadamente SIN --sandbox (193s reales vs 2-8s normal). 4 bugs reales encontrados en la propia verificacion en vivo, ninguno anticipado: (1) agy sale con codigo 1 pese a producir un envelope JSON valido con status:'ERROR' -- sendAntigravity() ahora intenta parsear stdout como JSON PRIMERO, sin importar el exit code; (2) settings.json de una conexion api-key anterior rompe una conexion subscription posterior en la MISMA carpeta compartida (getAntigravityHomeDir() es una sola carpeta por app, no por conexion) -- writeAntigravitySettingsForAuthMode(authMode) nueva, llamada desde AMBAS ramas de buildEnv(), escribe el settings.json correcto para el authMode real de cada turno; limitacion real conocida sin resolver (concurrencia real entre 2 paneles con distinto authMode) anotada en PENDING.md; (3) hallazgo critico que contradice el diseno original: --add-dir NO confina el acceso (es una lista de PERMITIDOS que se SUMA, no un limite duro, confirmado real leyendo un archivo fuera del workspace con exito bajo danger-full-access) -- el confinamiento real SI existe pero via --mode plan/accept-edits (auto-denegado real en headless, confirmado con el mismo pedido); danger-full-access queda genuinamente sin confinacion real posible, mismo perfil de riesgo que el --dangerously-skip-permissions de Claude o el yolo de Gemini; (4) detectAntigravity() reportaba "no instalado" pese a estar instalado real (npmGlobalShimPath() generico asume instalacion via npm, agy no se instala asi) -- antigravityShimPath() nueva en cli-status.ts con la ruta real (%LOCALAPPDATA%\agy\bin\agy.exe). Exposicion externa completa (shared/types.ts, runtime-state.ts, settings-provisioning.ts con antigravitySubscriptionProvider() sembrada automatica via reseedBuiltinSubscription() factorizada, ipc-agent.ts, App.tsx con color de marca propio #7c3aed y rama hermana en el formulario de edicion reusando la estructura de Claude). Verificado real en 2 niveles: runtime real fuera de la app empaquetada (esbuild bundlando el codigo real de produccion, electron externalizado+stub) confirmo los 4 fixes uno por uno (incluida la secuencia api-key->subscription en la misma carpeta, y el contraste danger-full-access sin confinar vs accept-edits auto-denegando); UI real en la app empaquetada e instalada (2 builds+instalaciones reales, 2 UAC aprobados) confirmo la siembra automatica sobre datos de produccion existentes y un turno de suscripcion real end-to-end ("UI-SUBSCRIPTION-OK"). Carpeta real del usuario confirmada sin cambio neto (53 archivos antes y despues de toda la sesion) pese a un incidente real propio (una prueba puntual corrida sin USERPROFILE/HOME redirigidos dejo 1 turno real ahi, detectado por el conteo, limpiado a mano). settings.json real de produccion confirmado limpio al cerrar (21 proveedores, conexion de prueba api-key eliminada por id real, chat de prueba borrado). | `src/main/cli-agent-runtime.ts`, `src/main/antigravity-home.ts`, `src/main/cli-status.ts`, `src/main/ipc-cli.ts`, `src/main/auth-manager.ts`, `src/main/ipc-agent.ts`, `src/main/settings-provisioning.ts`, `src/main/settings-store.ts`, `src/shared/types.ts`, `src/renderer/src/App.tsx`, `src/preload/index.ts`, `src/preload/index.d.ts` | Completa -- npm run typecheck y npm run build en verde en las 2 rondas (implementacion inicial, fix de detectAntigravity()); verificado real en 2 niveles (runtime directo fuera de la app + UI real en la app empaquetada, 2 builds/instalaciones reales, 2 UAC aprobados); 4 bugs reales encontrados y corregidos en la propia verificacion; carpeta real del usuario sin cambio neto pese a un incidente real propio detectado y corregido; sin commit, pendiente de que el usuario lo pida |
| Infraestructura de HOME aislado para Antigravity CLI (pieza base, sin integrar agy al runtime) | 2026-09-01 | Basado en verify_antigravity_cli.md (4 tareas + tarea puntual de redireccion de HOME) -- investigacion completa confirmo real que agy (Antigravity CLI, binario Go 1.1.23) no tiene NINGUN flag/env var/setting equivalente a ephemeral:true (Codex)/--no-session-persistence (Claude) -- un turno headless deja 10 archivos reales por turno (SQLite completa de la conversacion incluida), mas agresivo que los otros 2 CLIs. Unico mecanismo real encontrado, no documentado ni soportado oficialmente por Google: agy resuelve su directorio de estado (~/.gemini/antigravity-cli/) via USERPROFILE/HOME del proceso -- redirigir esas 2 variables al spawnear aisla todo el arbol. getAntigravityHomeDir()/clearAntigravityHomeDir() nuevas en app-paths.ts (mismo patron que STORAGE_ROOT/ensureStorageRootOrExit()) -- getAntigravityHomeDir() usa getAppDataSubdir('antigravity-home') (bajo el storage root real de Amatista), clearAntigravityHomeDir() vacia el CONTENIDO (nunca la carpeta en si, evita locks de creacion). 2 puntos de limpieza en index.ts: al ARRANCAR (justo despues de ensureStorageRootOrExit()/app.setPath(), antes de registrar IPC -- GARANTIZADO, no depende de cierre prolijo) y al CERRAR (app.on('before-quit', ...) nuevo -- no existia ningun hook de cierre antes de esta fase, elegido sobre window-all-closed por ser el hook real que corre una sola vez en cualquier plataforma -- best-effort, try/catch, nunca bloquea el cierre). antigravityIsolatedEnv() nueva en antigravity-home.ts (archivo dedicado nuevo, deliberadamente separado de cli-agent-runtime.ts para no mezclar la pieza de aislamiento con una integracion de runtime que todavia no existe) -- arma {...process.env, USERPROFILE:home, HOME:home}, documentado explicito que aisla la ESCRITURA A DISCO pero no la sesion de cuenta (agy se autentica via el keyring del SO, no via archivo relativo al HOME). Verificado real con el binario agy 1.1.23 ya instalado, ejecutando las 3 funciones reales compiladas (no reimplementadas) fuera de Electron via un loader hook de Node que solo stubea el import de electron, nunca la logica de negocio real: turno real con USERPROFILE/HOME apuntados a D:\AMATISTA\data\antigravity-home\ escribio 42 archivos reales ahi, carpeta real del usuario confirmada sin tocar (mismo conteo antes/despues + cero archivos con LastWriteTime en la ventana real de la corrida, doble verificacion); limpieza al arrancar simulada real (basura real de la corrida anterior, incluido un arbol AppData/ inesperado que agy tambien escribio ahi -- hallazgo no anticipado) -- clearAntigravityHomeDir() real la vacio a 0 archivos, carpeta en si intacta. agy sigue SIN integrarse a CliAgentKind/RuntimeKind/ningun runtime -- eso queda anotado en PENDING.md como proximo paso separado. | `src/main/app-paths.ts`, `src/main/antigravity-home.ts` (nuevo), `src/main/index.ts` | Completa -- npm run typecheck y npm run build en verde; verificado real con el binario agy real (turno real + limpieza real simulada con basura real), sin necesitar empaquetar la app completa (logica pura de main/, sin superficie de UI); sin commit, pendiente de que el usuario lo pida |
| Reintegracion completa de claude-cli | 2026-09-01 | Basado en verify_claude_cli_reintegration.md + backend de dec378c^ ya revisado en chat + panorama real de App.tsx ya diagnosticado + decision confirmada de mantener la siembra automatica incondicional. Confirmacion previa: newProvider('anthropic','subscription') no seteaba allowSubscription:false, sin fix necesario ahi. Backend restaurado casi literal (cli-agent-runtime.ts: CliAgentKind vuelve a 'claude'\|'gemini', sendClaude()/sendClaudeWithImages()/claudeCommand()/claudeImageBlocks() restaurados, mergeados con el fix real de Gemini geminiCommand() que se agrego DESPUES de dec378c -- confirmado con git diff que sendGemini()/geminiCommand() quedaron sin tocar; shared/types.ts recupera 'claude-cli' en RuntimeKind; runtime-state.ts recupera 'claude' en activeRuntime; ipc-agent.ts recupera el branch detectClaude()/detectGemini() -- typecheck sobre el estado intermedio confirmo cero errores, sin switch exhaustivo en el codebase, reintegrado por lectura directa del diff real; cli-status.ts recupera detectClaude() via el mismo versionOf() generico ya existente; auth-manager.ts/ipc-cli.ts recuperan openClaudeLogin()/cli:installClaude; scripts/install-claude-cli.ps1 restaurado tal cual). settings-provisioning.ts: claudeSubscriptionProvider() restaurada, re-sembrada en sanitizeSettings()/buildProvidersFromQConfig()/runtimeFor() (2 lugares: App.tsx renderer y settings-store.ts main) -- decision ya confirmada de mantener la re-siembra incondicional. Bug real encontrado en la propia verificacion en vivo: una instalacion real que ya paso por el retiro (dec378c) y ~2 meses de builds posteriores tenia el builtin qcfg-claude-subscription deshabilitado y con el marcador viejo en el nombre -- sanitizeSettings() restaurado lo reordena al frente pero no le toca name/enabled si ya existe (correcto para no pisar decisiones reales del usuario, pero el estado en disco no era una decision del usuario, era el propio mecanismo de retiro). Fix: unmigrateClaudeSubscriptionBuiltin() nueva en settings-store.ts, acotada SOLO al id builtin fijo (las 8 conexiones fantasma con id aleatorio que el usuario creo a mano antes del retiro quedan deshabilitadas+marcadas tal cual, confirmado que no se resucitan). App.tsx: CLAUDE_EFFORT_LEVELS/runtimeFor()/readiness() restaurados; cliStatus gana claude?: CliStatus; formulario de edicion inline (fix anterior) gana una rama hermana subscription+anthropic con solo status (Claude Code CLI instalado/no instalado), sin duplicar botones de accion -- decision de diseno: la gestion real vive en la seccion global "CLI", mismo patron ya establecido para la cuenta de Codex (su propia seccion, no embebida por conexion); openGeminiCliLogin()/geminiCliInstallHint() generalizadas de vuelta a openCliLogin(providerType)/cliInstallHint(providerType); seccion "CLI" extendida con Claude Code junto a Codex/Gemini. Segundo bug real encontrado en la verificacion en vivo, no anticipado en ninguna investigacion previa: --resume <sessionId> rompia SIEMPRE con --no-session-persistence activo -- Claude Code CLI nunca escribe la sesion a disco, asi que --resume no encuentra nada real ("No conversation found with session ID"), reproducido real en un turno de 2 pasos (texto exitoso, imagen fallida). Causa: a diferencia de Codex (proceso app-server vivo durante toda la conexion, estado en memoria pese a ephemeral:true), claude-cli spawnea un proceso NUEVO por turno -- sin persistencia a disco no hay continuidad server-side real. Fix de 2 lados: cli-agent-runtime.ts deja de pasar --resume para el kind 'claude'; ipc-agent.ts manda el contexto COMPLETO en cada turno para model.runtime==='claude-cli' (no solo el primero). Verificado real tras el fix: conversacion real de 2 turnos en la MISMA conexion, turno 1 texto + turno 2 con imagen real, ambos exitosos. | `src/main/cli-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/main/cli-status.ts`, `src/main/auth-manager.ts`, `src/main/ipc-cli.ts`, `src/main/index.ts`, `src/main/mcp-client.ts`, `src/main/settings-provisioning.ts`, `src/main/settings-store.ts`, `src/shared/types.ts`, `src/shared/model-capabilities.ts`, `src/renderer/src/App.tsx`, `src/preload/index.ts`, `src/preload/index.d.ts`, `scripts/install-claude-cli.ps1` (restaurado) | Completa -- npm run typecheck y npm run build en verde en las 3 rondas (backend inicial, backfill del builtin, fix de --resume); 3 builds/instalaciones reales completas contra la app empaquetada (3 UAC reales aprobados por el usuario); verificado real: turno de texto real, turno con imagen real en conexion nueva, turno con imagen real en 2do turno de la misma conexion (el caso que expuso el bug de --resume), 90 archivos en ~/.claude/projects/ antes y despues de 4 turnos reales (sin cambio), siembra automatica confirmada real sobre datos de produccion existentes, Gemini CLI confirmado SIN regresion (mismo bug preexistente ya documentado en PENDING.md, codigo byte a byte sin cambio via git diff), settings.json con el mismo total de 20 proveedores al cerrar, conexion de prueba Google reactivada temporalmente revertida a enabled:false, chat de prueba borrado; sin commit, pendiente de que el usuario lo pida |
| Fix real — edicion de conexiones (Settings) + aviso de proveedor huerfano | 2026-09-01 | Basado en verify_connection_editing_bug.md + 2 confirmaciones puntuales previas. Bug real preexistente (no relacionado a claude-cli): f9a5d2d (Paneles-2b) borro por completo la edicion de Autenticacion/Endpoint/API key de conexiones ya creadas como efecto colateral no documentado (confirmado con git show, la seccion real de esa fase en CONTRACT.md no menciona esta perdida). Parte 1: boton "Editar" nuevo en cada connection-row, estado dedicado editingProviderId/editForm en App() (confirmado que focusedProvider/focusedPanelId, el mecanismo del catalogo existente, solo se activa clickeando DENTRO de un panel de chat, nada reusable para este trigger). Formulario inline con el mismo patron visual .field ya existente en el CSS (nunca se agrego diseno nuevo). Guardar actualiza el provider por su id real via updateProvider() ya existente -- nunca crea uno nuevo, los chats que lo referencian no quedan huerfanos por editar. Bloque de catalogo (openrouter/openai-compatible) sin tocar, seccion aparte. Gestion de modelos individuales para el resto de proveedores (Foundry/OpenAI/Anthropic/Google/DeepSeek): confirmado con grep que no existe ningun mecanismo real en ningun otro lado -- anotado en PENDING.md como limitacion preexistente mas amplia, fuera de alcance de este fix. Parte 2: conteo real chatSessions.filter(...).length agregado al mensaje de deleteProvider() antes del confirm(); useEffect en ChatPanel que compara activeProvider?.id !== activeChat.providerId y llama setAgentError() (reusa el mecanismo ya cableado y renderizado, sin estado de UI nuevo). Bug real encontrado en la PROPIA verificacion en vivo: el efecto del aviso, declarado junto a activeModel, corria ANTES que un efecto ya existente que escucha catalogChangeNonce y hace setAgentError('') como parte del reset de desconexion general -- deleteProvider() dispara ambos en el mismo commit, y el segundo pisaba el aviso del primero en silencio. Fix real: el efecto se movio a DESPUES del efecto de catalogChangeNonce (mismo archivo, orden de declaracion determina orden de ejecucion dentro de un commit de React) y sumo catalogChangeNonce a sus propias dependencias. Verificado real contra la app empaquetada (2 builds+instalaciones reales via npm run dist, no un servidor de desarrollo): edicion de una conexion real de produccion ("Claude API via Azure", endpoint/API key reales) confirmada persistida con el mismo id, revertida sin dejar rastro; borrado de una conexion de prueba real con un chat real asociado (confirmado con lectura directa de amatista.db/SQLite) mostro el conteo exacto en el confirm() real; primera ronda (sin el fix de orden de efectos) confirmo el fallback real pero SIN el banner -- hallazgo real que motivo el fix; segunda ronda (con el fix, nuevo build+instalacion real) confirmo el banner real, texto exacto: "El proveedor configurado para este chat ya no existe — usando Microsoft Foundry temporalmente" (confirmado con zoom sobre la captura real). | `src/renderer/src/App.tsx` | Completa -- npm run typecheck y npm run build en verde en ambas rondas; verificado real contra la app empaquetada con datos de produccion reales, incluido un bug real de carrera de efectos encontrado y corregido durante la propia verificacion; sin commit, pendiente de que el usuario lo pida |
| Fix real — Codex dejaba rastro en ~/.codex/sessions/ por cada turno automatizado | 2026-08-31 | Basado en Tarea 1 de verify_claude_cli_reintegration.md: CodexClient.start() armaba el payload de thread/start (protocolo JSON-RPC de codex app-server --stdio) sin ningun campo de persistencia -- confirmado real que esto escribia un archivo nuevo en ~/.codex/sessions/ por cada turno, visible via codex resume, mismo tipo de ruido que motivo el retiro completo de claude-cli. Fix de una sola linea: ephemeral:true agregado al objeto de parametros de thread/start (junto a model/cwd/sandbox/approvalPolicy ya existentes), sin tocar initialize/initialized/turn/start ni ningun otro punto del ciclo de vida -- campo confirmado real contra el schema oficial (codex app-server generate-json-schema) en la investigacion previa. Verificado real con la clase CodexClient REAL compilada (mismo codigo de produccion, no una reimplementacion a mano) contra Codex real (codex-subscription, gpt-5.5): 101 archivos antes, thread real iniciado con ephemeral:true confirmado en la respuesta, turno completo real (respuesta real del modelo "VERIFICACION REAL OK", turn/completed real con duracion real 4854ms), 101 archivos despues -- sin cambio, turno funciona identico. codex-account-bridge.ts (el otro cliente que spawnea codex app-server --stdio) no llama thread/start en ningun punto (confirmado con grep) -- fuera de alcance porque el metodo no existe ahi, no por recorte. | `src/main/codex-client.ts` | Completa -- npm run typecheck y npm run build en verde; verificado real con la clase de produccion contra Codex real, conteo de archivos antes/despues sin cambio; sin commit, pendiente de que el usuario lo pida |
| Feature "read_document" — PDF/DOCX/XLSX/HTML paginado real, fallback de vision para paginas escaneadas | 2026-08-31 | Basado en 2 rondas de Tarea 0 (verify_large_documents.md + verify_read_document_tool.md). Nuevo modulo document-reader.ts + tool read_document. Libreria por formato: pdfjs-dist@latest (6.3.289) STANDALONE para PDF (nunca la copia interna de officeParser, 6.1.200, que cae en el rango con CVE alta GHSA-hq66-cqwq-w95j), officeParser@7.8.0 para DOCX/XLSX/HTML (confirmado real que no arrastra el paquete xlsx/SheetJS vulnerable). Paginado real, nunca el documento completo de una: read_document(path, page?) devuelve totalUnits/unitIndex siempre -- page es la pagina real en PDF (pdfjs-dist, getTextContent() por pagina), el indice de un chunk de tamano fijo (1500/150) en DOCX/XLSX/HTML (officeParser convert 'chunks', chunkSize chico a proposito -- confirmado real en la investigacion que un chunkSize grande cruza paginas y la metadata.pageNumber queda mal atribuida). Cache en memoria de los chunks de officeParser por documento (clave ruta+mtime) -- sin esto cada pagina pedida reparsearia el documento entero; es cache de performance, NO el mecanismo de resumen jerarquico (fuera de alcance explicito de esta fase). Fallback de vision real para paginas de PDF sin texto (getTextContent().items.length===0): render real a PNG via pdfjs-dist page.render() + @napi-rs/canvas, entregado en el mismo formato data:image/png;base64 que attachments.ts. Hallazgo real de arquitectura: el mecanismo de vision existente (Fase 17) solo inyecta imagenes al turno actual del usuario, nunca dentro de un resultado de tool -- se agrego ToolExecutionResult.resultImageDataUrl (nuevo, a diferencia de generatedAttachment SI llega al modelo) conectado en sendAnthropicApi(). Confirmado real (busqueda, no supuesto) que OpenAI Chat Completions/Foundry/Gemini no aceptan imagenes en un mensaje de rol tool -- el fallback de imagen real funciona SOLO en anthropic-api (Claude directo + DeepSeek), los otros 3 runtimes reciben una nota de texto explicita en vez de perder la senal en silencio. Empaquetado: los 3 paquetes (+ arbol transitivo de officeParser, incluyendo tesseract.js de 44MB en WASM como dependencia dura aunque ocr:false es el default explicito, nunca activado) marcados external en electron.vite.config.ts (confirmado real via grep sobre out/main/index.js: requires/import() reales, no bundleados) y agregados a package.json build.files; asarUnpack solo para @napi-rs/** (el unico binario nativo real de los 3). Bug real de Windows encontrado y corregido: pdfjs-dist interpreta standardFontDataUrl/cMapUrl como URL real, no ruta de SO -- una ruta con backslashes revienta, corregido con pathToFileURL(). Bug real MAS SERIO encontrado en el primer npm run dist real (no detectable con npm run build, que no empaqueta): electron-builder incluyo en el instalador SOLO la copia vulnerable de pdfjs-dist (la de officeParser) y omitio la nuestra por completo -- confirmado listando el app.asar real generado. Fix real: package.json overrides fuerza pdfjs-dist a ^6.3.289 en TODO el arbol, colapsando a una unica copia (confirmado con npm install real + npm audit real: 1 hallazgo alto -> 0 relacionados a pdfjs-dist) -- mejor que el diseno original, la copia vulnerable ya no existe en ningun lado en vez de solo evitarse. Verificado con un segundo npm run dist real: app.asar confirma una sola ruta pdfjs-dist (con package.json/legacy/cmaps/standard_fonts reales adentro), officeparser presente (54 entradas), el unico .node real del arbol correctamente en app.asar.unpacked (no dentro del archivo). Verificacion funcional real con 4 documentos generados en scratchpad (pdf-lib/docx/exceljs, nunca dependencias de Amatista): PDF de 3 paginas (1 y 3 con texto real, 2 una imagen rasterizada real sin ninguna capa de texto -- pagina "escaneada" genuina) extrajo texto real y distinto en 1/3, detecto y renderizo la pagina 2 a PNG real (verificado visualmente legible), DOCX/XLSX/HTML extrajeron su contenido real correctamente (XLSX con sheetName real), pagina fuera de rango clampeo sin excepcion, formato no soportado (.txt) rechazado con error claro. | `src/main/document-reader.ts` (nuevo), `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `electron.vite.config.ts`, `package.json` | Completa -- npm run typecheck y npm run build en verde; npm run dist real corrido 2 veces (el bug de empaquetado se encontro y corrigio en la primera pasada); verificacion funcional real con 4 formatos + 2 casos limite; sin commit, pendiente de que el usuario lo pida |
| LSP forzado — comparacion limpia REAL final, DeepSeek directo, mismo modelo en las 10 corridas | 2026-08-31 | Tercer intento de comparacion limpia tras 2 fallos reales de infraestructura: (1) GPT-5.2/OpenAI se quedo sin credito real a mitad de tanda; (2) un recurso Foundry dedicado nuevo (amatistabenchmark-resource, confirmado sin compartir cuota con el proyecto Q, verificado viable con HTTP 200 real y tool-calling real) fallo 10/10 por un rate limit real y muy bajo del deployment DeepSeek-V4-Flash ahi (cortadas en 10-32s, 2-4 tool calls, sin intento real completo -- no midio capacidad del modelo, midio el techo del deployment). Investigado y confirmado viable DeepSeek DIRECTO (api.deepseek.com, sin Azure): mismo mecanismo ya usado para Claude-via-Azure (kind:'anthropic-api', endpoint custom), tool-calling real confirmado con las 18 tools de Amatista, y la documentacion oficial publica limites de CONCURRENCIA (2500 simultaneas para deepseek-v4-flash), no RPM/TPM -- irrelevante para un harness secuencial de 1 a la vez. run-batch.sh generalizado (respeta BENCH_PROVIDER_KIND/BENCH_MODEL/BENCH_ENDPOINT/BENCH_AZURE_KEY si vienen exportados desde afuera, default original sin cambios). Resultado real: las 10 corridas completaron SIN NINGUN error de infraestructura -- cero rate limits, cero problemas de credito, cero fallos duros, la comparacion finalmente limpia. Resolved: Natural 4/5 (80%) vs Forzado 4/5 (80%) -- sin diferencia real en esta comparacion limpia. qdrant-c_51b3a62 fallo en ambas condiciones por el MISMO motivo real, verificado en el log real (no supuesto): error de compilacion Rust genuino (mod declarado sin archivo creado), nunca llego a tests -- descartada explicitamente la hipotesis de falso negativo por puertos/red (qdrant abre puertos reales en tests) revisando el log real. Hallazgo real: por primera vez en todo el experimento se usaron find_definition/find_references (nunca con GPT-5.2) -- find_references en 3/5 celdas forzadas, find_definition una vez. Hallazgo real sobre el "cache hit rate": la formula cached/total de corridas anteriores da valores sin sentido aca (2478%-9906%) -- no es un bug de calculo, cached se acumula por cada vuelta real del loop (refleja el contexto completo cacheado hasta ese punto, que crece), total acumula solo los deltas incrementales -- con turnos de 20 a 115 tool calls reales (mucho mas que GPT-5.2) el cache acumulado supera ampliamente el total, la metrica de % deja de ser interpretable con muchas vueltas -- se reportaron valores crudos en vez de forzar un porcentaje enganoso. Uso masivo real de run_command (4 a 115 veces por corrida, mucho mas que GPT-5.2), no investigado en detalle su contenido en esta ronda. Praxis Liber confirmado sano: disco raiz 35G->47G (crecimiento esperado por multiples git clone reales, nada de Docker), daemon del benchmark en 0 activo, 20 contenedores preexistentes sin interrupcion en las 10 corridas. | `benchmark/run-batch.sh` | Completa -- 10/10 celdas con datos 100% reales, sin ninguna interrupcion de infraestructura; comparacion limpia final documentada en docs/_arch/CONTRACT.md; sin commit, pendiente de que el usuario lo pida |
| LSP forzado — comparacion controlada FINAL, 5 tareas x 2 condiciones (natural vs. forzado) | 2026-08-31 | Cierra el experimento de LSP forzado: mismas 4 instancias del hallazgo anterior corridas ahora en condicion NATURAL (catalogo completo, sin AMATISTA_EXCLUDED_TOOLS, sin la instruccion explicita) -- confirmado real antes de correr (no asumido) que la variable no estaba seteada en una sesion SSH nueva y que run-batch.sh nunca la toca. Hallazgo real durante la corrida: la cuenta real de OpenAI se quedo sin credito ("You have no credits remaining", distinto del rate limit TPM de siempre) -- solo 2 de las 4 tuvieron intento real antes del corte (icloud-photos-downloader parcial, ant-design-52470 completa), qdrant-c_51b3a62/qdrant-c_83bb3a3 fallaron en <1.5s sin ningun intento real. Pausa real solicitada al usuario via AskUserQuestion en vez de reintentar a ciegas o inventar resultado -- el usuario reporto un problema real de pago (tarjeta rechazada) y pidio usar Foundry para esas 2. Confirmado real antes de asumir: gpt-5.2 no existe como deployment en el recurso de Foundry (404 DeploymentNotFound, probado directo) -- esas 2 corrieron con gpt-5.5 (ya confirmado en Fase 1/2), unica desviacion real del diseno "mismo modelo" para 2 de las 10 celdas finales, documentada explicita. Tabla final de 10 filas (5 tareas x 2 condiciones) completada con datos 100% reales. Resolved agregado: Natural 2/5 (40%) vs Forzado 4/5 (80%) -- diferencia real y marcada, pero con caveats reales que impiden conclusion estadistica fuerte con n=5 (2 de las 5 celdas naturales con intento incompleto o modelo distinto) -- documentado como evidencia direccional, no medicion controlada limpia, sin sobre-interpretar. Praxis Liber confirmado sano al cierre: disco raiz sin cambio, daemon del benchmark limpio, 20 contenedores preexistentes sin interrupcion en ninguna de las corridas de todo el experimento. | (ninguno -- corrida de datos, cierre del experimento) | Completa -- 10/10 celdas con datos reales (2 con nota real de modelo distinto por fuerza mayor de pago); tabla final documentada en docs/_arch/CONTRACT.md; sin commit, pendiente de que el usuario lo pida |
| LSP forzado — 4 instancias adicionales, resumen agregado de 5 (optuna + 4) | 2026-08-31 | Corridas las 4 instancias de la tanda de 14 que nunca habian tenido intento real (rechazadas por rate limit antes de cualquier procesamiento: icloud-photos-downloader python, ant-design-52470 typescript, qdrant-c_51b3a62 rust, qdrant-c_83bb3a3 rust), bajo las mismas 2 restricciones ya verificadas en optuna (AMATISTA_EXCLUDED_TOOLS=search_files + instruccion explicita anti-run_command), mismo gpt-5.2, mismo AMATISTA_MAX_TOOL_LOOP=300, secuencial via run-batch.sh reusado tal cual. Resultado real: 3/4 resolved:true (icloud-photos-downloader, ant-design-52470, qdrant-83bb3a3; fallo real qdrant-51b3a62), las 4 con callSucceeded:true (sin rate limit esta vez), cero run_command en las 4 (instruccion respetada al 100%). LSP usado en 3/4 (list_symbols, 1-2 veces cada una) -- ant-design-52470 resolvio con read_file puro sin necesitar LSP ni ningun sustituto, evidencia de que a veces la tarea real simplemente no lo requiere. Resumen agregado de las 5 instancias totales del experimento (optuna corrida 3 + estas 4): resolved 4/5 (80%), LSP genuinamente usado en 4/5 (80%), evasion 0/5 (instruccion respetada en el 100% de los casos reales en toda la sesion de LSP forzado), find_definition/find_references nunca usadas (solo list_symbols), tokens en rango estrecho 767K-815K sin correlacion obvia con resolved, cache hit rate 92.3%-95.9% consistente con las 3 corridas de optuna -- sin degradacion real por el catalogo restringido. Hallazgo operativo no critico: /mnt/benchmark-storage mostro 12GB reales en uso pese a que docker system df del daemon aislado reporta 0 activo -- contenido residual de containerd (garbage collection propio, no disparado por docker rmi), no confirmable en detalle sin acceso root, insignificante frente a los 2.6TB libres. Praxis Liber confirmado sano: disco raiz sin cambio, daemon del benchmark en 0 activo, 20 contenedores preexistentes sin interrupcion. | (ninguno -- corrida de datos sobre el mecanismo ya implementado) | Completa -- 4/4 corridas con turno real completo, 3/4 resolved:true; resumen agregado de 5 instancias documentado en docs/_arch/CONTRACT.md; sin commit, pendiente de que el usuario lo pida |
| LSP forzado — 3ra corrida optuna__optuna-6197, search_files excluida + instruccion explicita anti-run_command | 2026-08-31 | Tercera corrida real de la misma tarea exacta (optuna__optuna-6197, mismo gpt-5.2, mismo AMATISTA_MAX_TOOL_LOOP=300, mismo AMATISTA_EXCLUDED_TOOLS=search_files), agregando al problem_statement de ESTA corrida puntual (no un cambio permanente de Amatista) una instruccion explicita al frente prohibiendo usar run_command como sustituto de grep tras el hallazgo de la 2da corrida (el modelo habia compensado la ausencia de search_files con scripts Python inline via run_command en vez de usar las tools LSP de forma consistente). Resultado real: resolved:true (3/3 en las 3 corridas), callSucceeded:true (a diferencia de la 2da, que se corto por rate limit), 772062 tokens reales (94.6% cache hit), 105.8s. Confirmado real que la instruccion SI se respeto: cero llamadas a run_command en el toolCallLog completo, sin ningun otro mecanismo de evasion detectado -- el propio texto final del modelo lo reconoce explicito ("para cumplir 100% con tu instruccion"). El gap de usage:undefined documentado en la 2da corrida NO reaparecio (turno completo, sin excepcion, usage capturado normal) -- confirma que el gap es especifico del path de excepcion de send(), no un problema general. list_symbols se mantuvo en exactamente 1 uso en las 2 corridas restringidas (2da y 3ra) -- la instruccion cambio COMO evito grep (de run_command-como-sustituto a ningun sustituto), no aumento el uso real de las tools LSP mas alla de esa unica llamada. Praxis Liber confirmado sano tras esta 3ra corrida tambien: disco raiz sin cambio, daemon del benchmark limpio, 20 contenedores preexistentes sin interrupcion. | (ninguno -- corrida de datos sobre el mecanismo ya implementado en la fila anterior) | Completa -- 3/3 corridas resolved:true; comparacion completa de las 3 documentada en docs/_arch/CONTRACT.md; sin commit, pendiente de que el usuario lo pida |
| LSP forzado — AMATISTA_EXCLUDED_TOOLS, comparacion real optuna__optuna-6197 con/sin search_files | 2026-08-31 | Investigacion previa (verify_lsp_forced_batch.md) confirmo que toolCatalog() (api-agent-runtime.ts) es el unico choke point real usado por los 4 proveedores API, y que nada mas depende de que el catalogo sea siempre completo. Implementado AMATISTA_EXCLUDED_TOOLS (lista separada por comas), mismo patron que AMATISTA_STORAGE_ROOT/AMATISTA_MAX_TOOL_LOOP -- filtra solo TOOL_DEFINITIONS nativas (MCP intacto por decision explicita, no aplica al benchmark), sin la variable cero cambio de comportamiento. Verificado real (no solo por lectura de codigo): interceptado el fetch() real con un proxy transparente en un turno real contra la API real de OpenAI con AMATISTA_EXCLUDED_TOOLS=search_files -- el request que efectivamente viajo tenia 17 tools (18 menos search_files), confirmado search_files:false y read_file:true en el payload real. Comparacion real, misma tarea exacta (optuna__optuna-6197, mismo gpt-5.2, mismo AMATISTA_MAX_TOOL_LOOP=300): corrida natural (Fase 2, tanda de 14) resolved:true, callSucceeded:true, 1208680 tokens, 145s, 0 tools LSP usadas; corrida con search_files excluida resolved:true tambien, callSucceeded:false (cortado por el mismo rate limit real de organizacion de siempre), 114s, y esta vez SI uso list_symbols (1 vez) -- confirma que el catalogo restringido cambia el comportamiento real del modelo. Efecto colateral real no anticipado: sin search_files el modelo recurrio repetidamente a run_command con scripts Python inline como sustituto de grep, un patron mas indirecto que list_symbols puro -- list_symbols se uso una sola vez pese a la exploracion extensa, run_command-como-grep fue el reemplazo dominante. Hallazgo real de gap del harness, no oculto: usage quedo undefined en la corrida cortada porque el harness de benchmark solo captura usage en el path exitoso de send(), no en el path de excepcion -- api-agent-runtime.ts si acumula usage internamente en ese caso (evento 'usage'), el harness simplemente no lo escucha; pendiente de mejora, no corregido en esta ronda. Ajuste de plan real durante la ejecucion: se lanzo primero la tanda completa de 14 con el catalogo restringido, pero el usuario pidio acotar a 1 sola instancia para comparacion directa -- la tanda de 14 se detuvo a mitad de camino (TaskStop local no mato el proceso remoto por SSH, requirio kill -9 directo por PID en Praxis Liber), confirmado sin residuos (0 contenedores/imagenes huerfanas, disco del benchmark limpio) antes de arrancar la corrida puntual. Praxis Liber confirmado sano al final: disco raiz sin cambio, daemon del benchmark limpio, 20 contenedores preexistentes sin interrupcion. | `src/main/api-agent-runtime.ts` | Completa -- npm run typecheck y npm run build en verde; mecanismo verificado real contra la API real de OpenAI (payload real interceptado) y contra una evaluacion Docker real completa; sin commit, pendiente de que el usuario lo pida |
| Fix real de descriptions — find_definition/find_references/list_symbols no competian contra search_files | 2026-08-31 | Hallazgo real revisando el toolCallLog del piloto exitoso de Fase 2 (albumentations, callSucceeded:true): el agente resolvio toda la tarea con list_dir/read_file/search_files/apply_patch/get_diagnostics, cero llamadas a find_definition/find_references/list_symbols, pese a ser el tipo de tarea (agregar una funcion e integrarla en varios archivos de test) donde navegacion por simbolos real aporta sobre grep. Causa confirmada pegando las 3 description reales de TOOL_DEFINITIONS y comparando contra search_files: las 3 explicaban bien el mecanismo LSP pero ninguna competia activamente por la decision del modelo frente a grep -- a diferencia de search_files, que vende agresivamente su caso de uso. El unico contraste real (find_references) estaba colgado al final sin peso; find_definition no mencionaba grep en absoluto; la limitacion real de list_symbols con query (depende de que el LSP del lenguaje ya arranco en la sesion) estaba enterrada en la explicacion del parametro. Fix: reescritas las 3 description (solo texto, cero cambio de comportamiento real) siguiendo el patron de search_files -- find_definition ahora abre con el riesgo real de grep (declaracion con el mismo nombre en otro scope, o nada si el identificador llego via import renombrado) antes de explicar el mecanismo; find_references mueve el caso de uso (antes de renombrar/eliminar) al frente con 2 ejemplos concretos; list_symbols destaca la limitacion real de query como motivo explicito para preferir path en la primera exploracion de un archivo nuevo. | `src/main/tool-registry.ts` | Completa -- npm run typecheck y npm run build en verde; no hizo falta correr el benchmark de nuevo (verificacion pedida por el usuario: confirmar que las descriptions nuevas compilan bien formadas); sin commit, pendiente de que el usuario lo pida |
| Fase 3 del benchmark — evaluacion real con Docker, disco dedicado, piloto completo | 2026-08-31 | Cierra las 3 fases del harness: evaluacion OFICIAL real (Docker, test_patch aplicado, tests reales), no autoevaluacion del agente. Investigacion (verify_benchmark_fase3.md) confirmo que el propio repo del dataset de HuggingFace (no solo sus filas) expone eval.json + test_run.py, un harness de evaluacion oficial listo para usar -- resuelve un gap real de SWE-Factory (pedia un campo eval_script que el parquet publico no expone). Hallazgo critico de disco, confirmado empirico: 102 imagenes suman 208GB comprimidas / ~875GB reales sin comprimir (cero reuso de capas entre instancias, SHARED SIZE 0B) -- casi todo el espacio libre de Praxis Liber. Resuelto con un disco fisico de 2.7TB ex-Windows sin usar, formateado ext4 (confirmacion explicita del usuario, irreversible) y montado en /mnt/benchmark-storage (2.6TB reales). Docker: daemon dedicado (docker-benchmark.service + containerd-benchmark.service, disco/containerd propios) en vez de mover el data-root principal -- hallazgo real durante la implementacion: el primer intento sin --containerd explicito compartia el content-store del sistema (todas las imagenes de Coolify/yayoschat visibles en el daemon nuevo), corregido con un containerd dedicado; verificado real que el disco nuevo crece con cada pull mientras el raiz no se mueve, y que los 20 contenedores preexistentes siguieron sin interrupcion durante todo el proceso. Harness nuevo tracked en benchmark/fase3/ (fetch-promax-assets.sh + build-preds.js + merge-results.js + run-pilot.sh) -- transforma result.json de Fase 2 a preds.json, invoca test_run.py real (--workers 1 --cleanup, secuencial nativo, mantenido pese al espacio nuevo) via DOCKER_HOST apuntado al daemon aislado, mergea el resultado real en final_result.json. Piloto real con los 2 patches ya obtenidos en Fase 2: resolved=false en ambos, pero los 2 golden patches pasaron 2/2 validando el harness. krep fallo en compilacion (gcc exit 2, patch incompleto por el rate limit de Fase 2). albumentations fallo en 42 tests reales de pytest, pero es UN SOLO bug real (mismo traceback raiz en las 42 fallas): pseudo-inversa de stain_matrix mal orientada (2,3) en vez de (3,2) en apply_he_stain_augmentation(), diagnosticado exacto. | `.gitignore`, `benchmark/fase3/fetch-promax-assets.sh` (nuevo), `benchmark/fase3/build-preds.js` (nuevo), `benchmark/fase3/merge-results.js` (nuevo), `benchmark/fase3/run-pilot.sh` (nuevo) | Completa, con piloto real corrido de punta a punta -- sin .ts/.tsx tocados (no aplica typecheck/build); disco nuevo montado y persistente, Docker aislado verificado en ambas direcciones, servicios preexistentes de Praxis Liber sin interrupcion confirmada; sin commit, pendiente de que el usuario lo pida |
| Fase 2 del benchmark, retomada — OpenAI directo (gpt-5.2), fix real de max_completion_tokens, primer patch no vacio | 2026-08-31 | Retomada por pedido del usuario: OpenAI directo (api.openai.com, sin Azure), kind:'openai-chat' (Fase 15, hasta ahora solo probado contra OpenRouter), AMATISTA_MAX_TOOL_LOOP=300 (metodologia real de ProMax, paper seccion 4.1). Confirmacion de modelo pedida antes de correr nada: el usuario creia que GPT-5.2 (modelo exacto de ProMax) ya no estaba disponible -- verificado real que SI (GET /v1/models real lista gpt-5.2, llamada de chat completions real respondio 200) -- se uso el modelo EXACTO, no una version mas nueva, por instruccion del usuario tras confirmar disponibilidad real. providerKind extendido a 'openai-chat' en run-instance.ts, BENCH_MODEL obligatoria (sin default) para este proveedor -- el modelo real se confirma siempre antes de correr, nunca se asume. Bug real de produccion encontrado en la primera corrida real (no en investigacion previa): la API real de OpenAI rechaza max_tokens con 400 explicito para gpt-5.2, pide max_completion_tokens -- confirmado real que mandar los dos a la vez NO alcanza (rechaza igual con max_tokens presente, no ignora el parametro no reconocido), descartando la primera opcion elegida por el usuario antes de implementar nada. kind:'openai-chat' sirve tambien OpenRouter en produccion (Fase 15) -- muchos de sus modelos siguen esperando max_tokens, asi que cambiar el nombre incondicional arriesgaba regresion real. Fix: openAiMaxTokensField(model), deteccion por prefijo real (/^(o[1-9]|gpt-5)/i, familia real "reasoning" de OpenAI que documenta el cambio) -- todo lo demas (gpt-4o, gpt-3.5, modelos de OpenRouter) sigue mandando max_tokens exacto, verificado con 7 casos reales. Incidente operativo real durante la transferencia de la key: el primer intento del usuario resulto en un archivo de 476 bytes en vez de 164 -- la interfaz de chat enmascaro visualmente la key al copiarla de un mensaje previo (cada caracter tras "sk-proj-" sustituido por el caracter "bullet"), diagnosticado con od -c sin exponer la key completa, resuelto pidiendo pegarla desde la fuente original. Verificacion real: 4 corridas reales contra Praxis Liber con la instancia davidesantangelo__krep-18 (C). Corrida 1 (antes del fix): 400 inmediato, patchIsEmpty:true, confirmo el bug. Corridas 2-4 (con el fix): las 3 produjeron patch real NO VACIO y correctamente dirigido -- 11, 13 y 11 apply_patch reales respectivamente, las 3 cortadas por el MISMO limite real de organizacion (TPM 500000 tokens/min, Used ~475000-500000, confirmado contencion real y sostenida 3/3 veces con cooldowns de 15-20s entre intentos) -- mismo patron de contencion de cuota compartida ya visto con Azure/Anthropic, ahora confirmado en un tercer proveedor. El mejor patch (corrida 3, 3 archivos, 13 apply_patch) implementa correctamente y verificable linea por linea contra el problem_statement real: switch de macros por archivo en test_compat.h (incluyendo un fix de un case_sensitive por defecto incorrecto y deduplicacion en un helper nuevo), los 8 renombres _new pedidos en test_krep.c + el fix del assert SIMD real ("dolor" 1->2 con el comentario correcto), y el define correcto en test_multiple_patterns.c. Parcial (rate limit corto antes de main()/aho_corasick.h) pero real y correcto en lo que alcanzo -- muy por encima del criterio minimo pedido. Mecanismo confirmado end-to-end con un TERCER proveedor real (Foundry, Anthropic-via-Azure, y ahora OpenAI directo): clone/checkout real, loop de tool-calling real (39 tool calls reales acumulados en las 3 corridas con patch), git diff real, result.json real, manejo de errores 100% correcto en las 4 corridas. | `src/main/api-agent-runtime.ts`, `benchmark/run-instance.ts` | Completa, con patch real no vacio obtenido (objetivo minimo superado) -- `npm run typecheck` y `npm run build` en verde; mecanismo + fix real de produccion verificados contra un tercer proveedor real; sin commit, pendiente de que el usuario lo pida |
| Fase 2 del benchmark SWE-Bench ProMax — harness real (benchmark/), MAX_TOOL_LOOP configurable, pilot end-to-end en Praxis Liber | 2026-08-31 | Basado en Tarea 0 confirmada (docs/_arch/verify_benchmark_harness.md): write_file/apply_patch/run_command no asumen workspace conocido (resolveWithinWorkspace es traversal-check puro, VCS oculto se auto-inicializa on-demand), MAX_TOOL_LOOP=60 confirmado const fijo no configurable, git diff externo confirmado suficiente para capturar el patch, send() confirmado que resuelve solo (sin tool calls en la respuesta = turno cerrado). MAX_TOOL_LOOP paso a Number(process.env.AMATISTA_MAX_TOOL_LOOP) || 60, mismo patron que AMATISTA_STORAGE_ROOT (Fase 1) -- sin la variable, comportamiento identico. Harness nuevo tracked en benchmark/ (run-instance.ts + run-instance-wrapper.cjs + electron-stub.cjs + npm run bench:bundle): clona el repo real al base_commit real, corre ApiAgentRuntime/ToolRegistry reales (sandbox danger-full-access, providerKind configurable foundry/anthropic-api), Date.now() antes/despues, problem_statement real sin modificar, usage+toolCallLog reales, git diff externo, result.json estructurado, try/catch explicito (MAX_TOOL_LOOP agotado = callSucceeded:false, nunca exito silencioso). benchmark/runs/ y benchmark/tasks/ gitignored (clones de terceros + texto del dataset, no van al repo). Verificacion real: 12 corridas end-to-end reales contra Praxis Liber (conectividad SSH ya configurada de sesion anterior, confirmada real; Docker activo con 19 contenedores de otros proyectos, ninguno tocado) usando 2 instancias reales del dataset de HuggingFace (albumentations-team__albumentations-2337 y davidesantangelo__krep-18) y 2 proveedores reales via Azure (foundry, anthropic-api). Mecanismo confirmado end-to-end en las 12: clone/checkout real, loop de tool-calling real (21 tool calls reales en la corrida mas profunda -- exploracion genuina de transforms.py/functional.py de 6485/3138 lineas, localizo PlanckianJitter como patron de referencia, reviso tests reales), git diff real (patch vacio correctamente detectado en fallos), result.json real, manejo de errores 100% correcto (nunca exito silencioso). No se obtuvo un patch no-vacio pese a 12 intentos -- 2 hallazgos reales de infraestructura, ninguno bug del harness ni de portabilidad Linux: (1) Foundry 500 real 4/4 intentos, reproducido exacto con curl crudo (200, ~2s) vs fetch() de Node puro mismo body (500, ~42.5s) en Windows Y Linux por igual -- descarta payload/modelo/portabilidad, caracteristica real de Node fetch/undici contra este deployment de Azure, flageado para investigacion aparte; (2) Anthropic via Azure rate limit real 40000 tokens/60s, confirmado NO por acumulacion de reintentos propios (persiste con 150s de cooldown limpio) sino por contencion real de cuota compartida con el proyecto Q real del usuario en el mismo recurso Azure. Seguridad: clasificador de Claude Code bloqueo consistente (2 intentos) mover el archivo de la API key a Praxis Liber via scp/ssh -- resuelto correctamente pidiendole al usuario que la colocara el mismo desde su propia sesion SSH, nunca movida por el agente. | `src/main/api-agent-runtime.ts`, `benchmark/run-instance.ts` (nuevo), `benchmark/run-instance-wrapper.cjs` (nuevo), `benchmark/electron-stub.cjs` (nuevo), `package.json`, `.gitignore` | Completa salvo patch exitoso (bloqueado por 2 hallazgos reales de infraestructura ajenos al harness, ambos documentados) — `npm run typecheck` y `npm run build` en verde; mecanismo verificado real de punta a punta 12 veces contra 2 proveedores reales; sin commit, pendiente de que el usuario lo pida |
| Fase 1 del benchmark SWE-Bench ProMax — aislamiento de datos, portabilidad Linux, instrumentacion real de tokens | 2026-08-31 | Prerequisito de infraestructura para correr SWE-Bench ProMax (170 instancias reales confirmadas via API oficial de HuggingFace, 102 en Python/TypeScript/Go/Rust) en Praxis Liber (servidor Ubuntu de pruebas del usuario) contra un modelo real de Microsoft/Azure via Foundry. AISLAMIENTO: STORAGE_ROOT (app-paths.ts) pasa de constante fija a leer AMATISTA_STORAGE_ROOT si esta seteada, cae al valor real de siempre (D:\AMATISTA\data) si no -- confirmado sin regresion real, y confirmado que app-paths.ts es la UNICA fuente real de esa ruta en todo el codebase (8 archivos consumidores, los 8 via getAppDataSubdir/getAppDataRoot, cero bypass). PORTABILIDAD: categorizacion real de los 15 archivos con referencias a Windows -- 5 eran falso positivo puro (colision del patron .exe con .execute()), de los 10 reales 14 de 15 casos ya estaban protegidos (guard de plataforma, u opciones que Node ya ignora fuera de Windows). 1 caso real sin proteger y grave: la description de run_command le mentia al modelo diciendo "shell real es Windows, usa dir/type/del/%VAR%" sin chequeo de plataforma -- en Linux hubiera invalidado cualquier resultado del benchmark que dependiera de esa tool. Fix: RUN_COMMAND_SHELL_HINT condicionado a process.platform==='win32', verificado real con process.platform parcheado (Docker/WSL considerados y descartados por desproporcionados para un ternario de una linea). INSTRUMENTACION: extractUsageTokens() pasa de number a UsageBreakdown {total, input?, output?, cached?} -- 3 ramas reales (Gemini/OpenAI-chat/Foundry+Anthropic compartida), cada campo de cache leido con el nombre real confirmado por proveedor contra el SDK/documentacion oficial de cada uno (ninguno se leia antes). Fix de paso: el fallback de OpenAI Chat usaba nombres que no existen en esa API real (input_tokens/output_tokens en vez de prompt_tokens/completion_tokens), nunca se manifestaba como bug porque total_tokens ganaba primero. Para Anthropic, distincion real entre cache_read_input_tokens (tokens servidos desde cache, lo que importa medir) y cache_creation_input_tokens (tokens escritos al cache, costo, no ahorro) -- se lee el primero. reportUsage() acumula el desglose nuevo de forma aditiva (turnInputTokens/turnOutputTokens/turnCachedTokens), turnTokens y el evento 'usage' emitido quedan exactamente iguales, sin regresion. ApiAgentResult.usage nuevo en los 4 puntos de retorno exitoso. Latencia de turno deliberadamente NO tocada en el motor -- el harness la mide el mismo con Date.now(). Verificado real contra 2 proveedores reales (keys reales provistas por el usuario para esta prueba puntual, manejadas solo en memoria/scratchpad, confirmado borradas al terminar): Foundry con un cache hit real de 75% del input (no forzado), Anthropic via Azure correctamente SIN cached (Amatista nunca manda cache_control a ese proveedor, no se forzo el caso). | `src/main/api-agent-runtime.ts`, `src/main/app-paths.ts`, `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real en los 3 frentes (aislamiento, portabilidad, tokens) contra codigo/APIs/proveedores reales; sin commit, pendiente de que el usuario lo pida |
| Demo grabada de los 4 lenguajes — trazabilidad JSON-RPC, fix real de error de protocolo, verificación end-to-end | 2026-08-31 | Basado en Tarea 0 confirmada (docs/_arch/verify_lsp_demo_scope.md). LspClient.pending extendido con method/sentAt (mismo cambio sirve trazabilidad Y latencia). Trazabilidad JSON-RPC gateada por AMATISTA_DEBUG_TOOLS (mismo patron ya usado en tool-registry.ts/api-agent-runtime.ts) en request() (lado enviado) y handleChunk() (lado recibido, con latencyMs real). Bug real encontrado y corregido DURANTE la construccion de la demo (no en la investigacion previa): reject(msg.error) pasaba el objeto crudo del protocolo JSON-RPC directo, nunca una instancia real de Error -- el catch generico de ToolRegistry.execute() caia a String(error), produciendo el literal "[object Object]" en vez del mensaje real. Confirmado real contra rust-analyzer respondiendo {code:-32801, message:"content modified"} (error de protocolo REAL y estandar, servidor todavia indexando un crate nuevo) -- bug latente desde Fase 20 (afectaba tambien initialize/shutdown en teoria) nunca antes manifestado porque esos 2 casi nunca reciben un error real de protocolo. Fix: handleChunk() normaliza msg.error a una instancia real de Error con el mensaje real extraido antes de rechazar. Workspace de demo nuevo (scratchpad, no versionado) con relaciones especificas por lenguaje: TypeScript (interface Producto usada en 2 archivos), Python (calcular_impuesto cruzando 2 archivos), Rust (trait Forma + struct Circulo cruzando archivos), Go (modulo real multi-paquete, funcion Sumar llamada desde OTRO paquete). Hallazgo real no anticipado, corregido en el guion (no en produccion): typescript-language-server sin tsconfig.json solo conoce archivos ya abiertos (didOpen) -- no escanea el directorio buscando reverse-dependents; se abren los archivos relevantes primero. Verificado real: trazabilidad JSON-RPC completa (23 pares send/recv reales con latencyMs real, desde 0ms hasta ~2000ms en frio); aislamiento de diagnosticos reproducido en camara (escenario exacto del bug original, ahora corregido); los 4 lenguajes con su prueba especifica (TS: 5 ubicaciones reales en 3 archivos; Python: capability de pyright confirmada EN VIVO como objeto, no booleano; Rust: find_definition cruzando a formas.rs tras superar el error real -32801; Go: find_references cruzando a OTRO paquete real); ahorro de tokens con calentamiento explicito documentado (29 caracteres vs 45517 reales, 1569.6x). Estandares adicionales aplicados: checksum SHA-256 real del log paralelo + verificacion automatizada de monotonicidad de sus 103 timestamps (ambos limpios). Bloqueo real de infraestructura, no resuelto: la grabacion de video via ffmpeg+gdigrab no se pudo completar -- las ventanas lanzadas por script nunca se volvieron visibles en las capturas de la herramienta de control de escritorio (diagnosticado real: la ventana de la propia app Claude ocupa el foreground de esa pantalla, bloqueando SetForegroundWindow() de otras ventanas por la prevencion de robo de foco de Windows) -- documentado en detalle con la evidencia real de cada intento, sustituido por el log paralelo completo + una captura real de Amatista 0.8.2 corriendo en modo dev. | `src/main/lsp-client.ts` | Completa salvo el video -- `npm run typecheck` y `npm run build` en verde; verificado real (trazabilidad, latencia, aislamiento, los 4 lenguajes, ahorro de tokens); grabacion de video bloqueada por una limitacion real de la sesion de escritorio de este entorno, documentada explicita; sin commit, pendiente de que el usuario lo pida |
| Fix: get_diagnostics() sin path contaminado por archivos solo navegados (ensureOpen()) | 2026-08-30 | Bug real encontrado durante la investigacion de la demo grabada (docs/_arch/verify_lsp_demo_scope.md, Tarea 3), sobre la feature "Indexacion por simbolos via LSP" ya commiteada (a4cf352): get_diagnostics() sin path recorria trackedPaths() -- TODOS los archivos con al menos un didOpen, sin distinguir si se abrieron para EDITAR (write_file/apply_patch) o solo para NAVEGAR (ensureOpen(), usado por find_definition/find_references/list_symbols). Confirmado real con un harness en vivo: navegar a un archivo con un error preexistente via list_symbols (nunca tocado con write_file en la sesion) hacia que su error apareciera en get_diagnostics() sin path, contradiciendo la propia descripcion de la tool. Fix: LspClient gana editedPaths (Set, subconjunto de openVersions) -- notifyFileChanged() gana un 3er parametro reason:'edit'|'navigate' (default 'edit', cero cambio para notifyFileWritten()), solo agrega a editedPaths cuando reason==='edit'; ensureOpen() (lsp-manager.ts) pasa 'navigate' explicito. Una edicion real nunca se degrada (isTracked() ya es true, ensureOpen() ni siquiera vuelve a llamar notifyFileChanged() para un archivo ya editado). editedTrackedPaths() (nuevo, subconjunto de trackedPaths()) reemplaza a trackedPaths() SOLO en la rama SIN path de LspManager.getDiagnostics() -- la rama CON path sigue igual, sin cambios (el modelo pidio ese archivo a proposito, sin contaminacion posible ahi). Firma publica de get_diagnostics de cara al modelo sin cambios. Verificado real, los 3 casos: (1) navegado sin write_file, get_diagnostics() sin path -- su error NO aparece; (2) un archivo SI editado con write_file con un error real nuevo, get_diagnostics() sin path -- SI aparece, el navegado sigue sin aparecer; (3) get_diagnostics(path) explicito sobre el archivo solo navegado -- SI aparece, nunca estuvo afectado. Hallazgo colateral en el propio fixture de prueba (no en produccion): reusar el mismo nombre de variable entre 2 archivos .ts sueltos sin tsconfig.json produjo un TS2451 real de redeclaracion cruzada -- corregido renombrando la variable del fixture. | `src/main/lsp-client.ts`, `src/main/lsp-manager.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real con los 3 casos exactos pedidos, mismo workspace mixto de siempre; sin commit, pendiente de que el usuario lo pida |
| Feature "Indexación por símbolos vía LSP" — find_definition/find_references/list_symbols, apertura bajo demanda | 2026-08-30 | Basado en Tarea 0 confirmada (docs/_arch/verify_lsp_symbols.md + adenda): LspClient.request() ya era generico (no hizo falta tocar el framer), los 4 servidores ya integrados confirman soporte real de definitionProvider/referencesProvider/documentSymbolProvider/workspaceSymbolProvider (evidencia real, initialize contra los 4 binarios), y la restriccion de get_diagnostics a "solo archivos tocados" fue un recorte de ALCANCE de Fase 20 (no un limite de recursos deliberado, confirmado con el commit original 3ec260f) -- asi que ensureOpen() sin limite es seguro. toLspPosition()/fromLspPosition() (lsp-client.ts): unico punto de conversion 1-indexado<->0-indexado en AMBAS direcciones -- onPublishDiagnostics() se refactorizo para reusar la misma funcion (cero cambio de comportamiento), la direccion inversa (mandar una posicion AL servidor) es nueva. toLspLocations()/flattenDocumentSymbols(): normalizan las 3 formas reales de definition (Location/Location[]/LocationLink[]) y las 2 de documentSymbol (jerarquico con children, o plano tipo SymbolInformation) a un shape comun. serverCapabilities/supportsCapability() (nuevo): captura el capabilities REAL de initialize (antes se descartaba), chequeo por TRUTHINESS -- confirmado real que pyright anuncia estas capabilities como objeto, no booleano, un === true estricto daria falso negativo. 4 metodos publicos nuevos en LspClient (definition/references/documentSymbol/workspaceSymbol), todos sobre el request()/pending ya genericos. LspManager.ensureOpen() (nuevo): abre un archivo bajo demanda leyendo su contenido REAL de disco + notifyFileChanged() -- mismo mecanismo que notifyFileWritten() ya usa sin limite desde Fase 20, awaitable (a diferencia de ese, que es fire-and-forget). findDefinition()/findReferences()/listSymbolsInFile() rutean por extension + ensureOpen() + chequeo de capability; searchSymbols() DELIBERADAMENTE no llama ensureOpen(), solo consulta clientes YA corriendo (mismo patron que getDiagnostics() sin path), con queriedLanguages para transparencia real del alcance. 3 tools nuevas (tool-registry.ts): find_definition(path,line,column), find_references(path,line,column,include_declaration?), list_symbols(path?,query? -- mutuamente excluyentes) -- sin aprobacion, precedente real citado (search_files/get_diagnostics nunca pasan por resolveApproval()). Verificado real con 2 de los 4 lenguajes (TypeScript kind:'node' + Go kind:'native', cubriendo los 2 caminos de spawn estructuralmente distintos -- Python/Rust comparten el mismo camino que uno de los 2 y ya confirmaron las capabilities en Tarea 0, no hizo falta repetir): archivos nav_utils.ts/nav_main.ts/navutils.go/navmain.go creados en disco DIRECTO, NUNCA tocados con write_file/apply_patch en el harness -- find_definition/find_references/list_symbols devolvieron resultados reales de todos modos, prueba directa de que ensureOpen() los abrio bajo demanda. find_definition sobre una llamada real resolvio la ubicacion EXACTA de la definicion en los 2 lenguajes; find_references encontro TODAS las ocurrencias reales (declaracion + import + 2 llamadas en TS; declaracion + 2 llamadas en Go); list_symbols(query) confirmo agregacion real entre 2 clientes de lenguajes distintos con matching fuzzy real. Hallazgo real documentado (matiz de protocolo, no bug): documentSymbol da la posicion de inicio de la declaracion (no del identificador) para funciones de nivel superior en TypeScript Y Go por igual, pero posicion exacta para Property/Struct -- find_definition/find_references si dan siempre la posicion exacta del identificador. Validaciones (path+query juntos, ninguno, archivo inexistente) confirmadas rechazadas explicito. stopAll() limpio, sin gopls.exe/node.exe colgados. | `src/main/lsp-client.ts`, `src/main/lsp-manager.ts`, `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real con 2 lenguajes (los 2 caminos de spawn distintos), apertura bajo demanda confirmada sobre archivos nunca tocados en la sesion; sin commit, pendiente de que el usuario lo pida |
| Feature "LSP para Go" — gopls real, 4 lenguajes coexistiendo, distinción "no instalado" vs "no pudo analizar" | 2026-08-30 | Basado en Tarea 0 confirmada (docs/_arch/verify_go_lsp.md): gopls es analogo a rust-analyzer (binario externo, no bundleable, paquete npm real es un holding vacio) -- encaja en `kind:'native'`/`args:[]` sin campos nuevos en la tabla, con 2 diferencias reales de VALOR: el argumento de deteccion y una dependencia transitiva real (`go`). `respondsToVersion()` generalizado de `['--version']` hardcodeado a un parametro `versionArgs: string[]` -- Rust sigue pasando `['--version']` (sin cambio), Go pasa `['version']` (confirmado real que `gopls --version` falla con `flag provided but not defined: -version`, exit code 2, pese a estar genuinamente instalado; el comando correcto es el subcomando sin guiones). `LanguageServerConfig` gana `extraPathDirs?: () => Promise<string[]>` -- hallazgo real: gopls hace el handshake `initialize` perfecto sin `go` en el PATH del hijo, pero nunca publica diagnosticos reales (shellea a `go` internamente para cargar paquetes). `resolveGoBinDirectory()` (mismo patron de 2 niveles que rust-analyzer: PATH heredado primero, `C:\Program Files\Go\bin` como fallback de convencion conocida) resuelve el directorio a inyectar; `LspClient.start()` arma el `env` del `spawn()` agregandolo al PATH heredado solo si `extraPathDirs` esta declarado -- TypeScript/Python/Rust no lo declaran, cero cambio de comportamiento para esos 3. Hallazgo real #2, encontrado DURANTE la propia verificacion (no anticipado): `onPublishDiagnostics()` limpiaba el error operacional (`lastErrorMessage`, poblado desde `window/showMessage` tipo Error real, capacidad nueva y generica en `LspClient`) con CUALQUIER `publishDiagnostics`, incluso uno que era SINTOMA del mismo problema -- confirmado real que con `go` no resoluble gopls SI publica un diagnostico (`source:"go list"`, "No active builds contain..."), que no es analisis real sino el mismo sintoma por otra via; la limpieza incondicional borraba el error real antes de que `get_diagnostics` pudiera mostrarlo. Fix real: solo limpiar `lastErrorMessage` si el lote de diagnosticos entrante NO contiene una entrada `source==='go list'`; `tool-registry.ts` filtra ese mismo sintoma de la vista antes de decidir si mostrar el error operacional o los diagnosticos reales. `LspManager.operationalErrorFor(path)` (nuevo, distinto de `startupFailureFor()`): "arranco bien pero no puede analizar" vs "nunca pudo arrancar", con mensajes reales y DISTINGUIBLES confirmados con evidencia. Verificado con Go real instalado en la maquina (`winget install GoLang.Go` -> go1.27.0, `go install golang.org/x/tools/gopls@latest` -> v0.23.0), en el MISMO workspace que ya tenia `.py`+`.ts`+`.rs`: `write_file` de un `.go` con error real de tipos arranco gopls real sin matar los otros 3 clientes ya vivos; `get_diagnostics` devolvio (con reintentos reales mientras gopls cargaba paquetes) el diagnostico REAL del compilador Go (`cannot use "dos" (untyped string constant) as int value`); `get_diagnostics()` sin path junto los 4; coexistencia real de 5 procesos confirmada a nivel de sistema operativo (`Get-CimInstance Win32_Process`, PowerShell, en paralelo) -- 2 `node.exe` (pyright + typescript-language-server) + 1 `rust-analyzer.exe` + 2 `gopls.exe` (gopls real spawnea un proceso propio adicional, comportamiento normal del binario); `stopAll()` los termino a los 4 sin residuos. El propio arranque exitoso de gopls en produccion confirma implicitamente que el fix del argumento de deteccion (`'version'` sin guiones) es correcto, no solo en una prueba aislada. Caso "gopls arranco pero go no resoluble" verificado real: no se pudo renombrar el `go.exe` real (Permission denied, requiere admin sobre Program Files) -- se simulo apuntando temporalmente la constante `GO_INSTALL_DIR` a una ruta inexistente (scaffold revertido despues), `get_diagnostics` devolvio "no se pudo analizar -- Error loading workspace folders...go command required, not found", mensaje real y DISTINTO del de "no instalado"; `startupFailureFor()` confirmado `undefined` en ese caso (prueba de que no es el caso "nunca arranco"); se revirtio la constante y se repitio el flujo completo de los 4 lenguajes con exito identico. Hallazgo colateral flageado para tarea aparte (`spawn_task`) y CERRADO dentro de esta misma fase, en un fix de seguimiento: `get_diagnostics` mostraba el `code` de cualquier diagnostico con un prefijo "TS" hardcodeado, leftover de cuando la tool solo servia TypeScript -- confirmado real en las 3 fases (`TSreportArgumentType`, `TSE0308`, `TSIncompatibleAssign`). Extraccion mecanica previa (`docs/_arch/verify_ts_prefix_bug.md`) confirmo que `result.path` (path absoluto del archivo puntual del loop, ya usado en `path.relative()`/`operationalErrorFor()`) esta en scope; fix con `languageServerConfigFor(result.path)?.languageId` (mismo mecanismo de ruteo por extension que ya usa `LspManager`, importado ahora tambien en `tool-registry.ts`) para condicionar el prefijo `"TS"` solo a TypeScript real. Verificado real con el mismo harness de 4 lenguajes: `bad.ts` sigue con `TS2345` (sin regresion), `bad.py`/`bad.rs`/`bad.go` ahora muestran `reportArgumentType`/`E0308`/`IncompatibleAssign` sin el prefijo incorrecto. | `src/main/lsp-client.ts`, `src/main/lsp-manager.ts`, `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real con Go instalado de verdad en la maquina, 4 language servers reales coexistiendo confirmado a nivel de SO, y los 2 casos de fallo distintos ("no instalado" vs "no pudo analizar") reproducidos reales; sin commit, pendiente de que el usuario lo pida |
| Feature "LSP para Rust" — rust-analyzer real, binario externo, 3 lenguajes coexistiendo | 2026-08-30 | Basado en Tarea 0 confirmada (docs/_arch/verify_rust_lsp.md): a diferencia de pyright/typescript-language-server, rust-analyzer NO es bundleable (paquete npm real es un holding vacio; el unico paquete npm relacionado real descarga el binario nativo en runtime) -- se detecta, no se bundlea, mismo modelo que Codex/Gemini CLI. `LanguageServerConfig` gana `kind:'node'\|'native'` y `args: string[]` (`lsp-client.ts`) -- TypeScript/Python quedan `kind:'node'`, `args:['--stdio']`, sin cambio de comportamiento (path resuelto identico al de antes). `resolveRustAnalyzerEntry()`: 2 niveles (PATH real via `execFile --version` sin shell:true -- rust-analyzer es un .exe real, no un shim .cmd como Gemini; fallback a `%USERPROFILE%\.cargo\bin\rust-analyzer.exe`), mismo patron que `versionOf()` de cli-status.ts pero sin reusar `npmGlobalShimPath()` (especifico de npm install -g). Hallazgo real #1 (anticipado en Tarea 3): `kind:'native'` cambia COMO se spawnea -- `LspClient.start()` gana una rama que spawnea el binario DIRECTO (sin process.execPath/ELECTRON_RUN_AS_NODE) para 'native'. Hallazgo real #2, encontrado DURANTE la propia verificacion (no anticipado): rust-analyzer usa stdio POR DEFECTO y RECHAZA `--stdio` como flag desconocido (confirmado ejecutando el binario real y aislado: `unexpected flag: --stdio`, exit code 2 en ~70ms) -- el primer intento pasaba `--stdio` incondicional (mismo que TS/Python), el proceso moria casi al instante y el cliente esperaba una respuesta de `initialize` que nunca iba a llegar (timeout largo y enganoso, probado hasta 90s sin exito, parecia lentitud real cuando la causa era el flag matando el proceso de entrada). Fix real: `args` por lenguaje en la tabla (`[]` para Rust) en vez de asumir el mismo flag para todos -- con el fix, `initialize` de rust-analyzer respondio en ~62ms, mas rapido que pyright/TS. `LspManager` gana `failures: Map<languageId,string>` + `startupFailureFor(path)` -- reemplaza el catch+log silencioso (suficiente mientras los 2 lenguajes bundleados nunca fallaban) por un mensaje real y accionable cuando rust-analyzer no esta instalado (mismo tono que geminiCliInstallHint(), sin boton de auto-instalacion). Verificado con Rust real instalado en la maquina (`winget install Rustlang.Rustup` + `rustup component add rust-analyzer`, real, `rust-analyzer --version` -> 1.98.0): workspace real con `.ts`+`.py`+`.rs` mezclados, `write_file` de un `.rs` con error real de tipos arranco rust-analyzer real sin matar los clientes de Python/TypeScript ya vivos, `get_diagnostics` devolvio (con reintentos reales mientras indexaba el crate) el diagnostico REAL de rustc (`E0308: mismatched types`), `get_diagnostics()` sin path junto los 3; coexistencia real de LOS 3 procesos confirmada a nivel de sistema operativo durante 30s completos (`Get-CimInstance Win32_Process`, PowerShell, en paralelo) -- 2 `node.exe` + 1 `rust-analyzer.exe` reales, nunca uno reemplazando a otro; `stopAll()` los termino a los 3 sin residuos. Caso "no instalado" verificado real: se renombro el binario real a mano, `get_diagnostics` devolvio el mensaje de instalacion real (no el generico "nunca tocado" ni un fallo silencioso), binario restaurado despues. | `src/main/lsp-client.ts`, `src/main/lsp-manager.ts`, `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real con Rust instalado de verdad en la maquina, 3 language servers reales coexistiendo confirmado a nivel de SO, y el caso "no instalado" reproducido real; sin commit, pendiente de que el usuario lo pida |
| Feature "LSP para Python" — pyright real, coexistencia con TypeScript | 2026-08-30 | Basado en Tarea 0 confirmada (docs/_arch/verify_python_lsp.md): `pyright` real via npm, mismo protocolo/mecanismo de spawn que ya usaba `typescript-language-server`. `pyright` instalado como dependency real (`^1.1.413`), agregado a `build.files`/`build.asarUnpack` (mismo patron que TS). `lsp-client.ts`: tabla `LANGUAGE_SERVERS` nueva (`{languageId, extensions, resolveEntry}`, misma forma para TypeScript y Python, sin trato especial para ninguna) reemplaza los 3 puntos hardcodeados a TS (`LSP_SUPPORTED_EXTENSIONS`, `languageIdFor()`, `resolveLanguageServerEntry()`). `resolveBundledServerEntry(packageName, binName)` nueva -- lee `bin` del `package.json` REAL del paquete instalado en vez de hardcodear la ruta interna, mismo criterio robusto que `geminiCommand()` (confirmado con evidencia real que ambos paquetes exponen `bin` de forma analoga; para TypeScript el path resuelto coincide EXACTO con el que estaba hardcodeado antes -- cero cambio de comportamiento). Unico matiz preservado sin generalizar (intrinseco a TypeScript, no a el mecanismo): `.tsx` sigue declarando `languageId:'typescriptreact'` pese a compartir servidor con `.ts`. `lsp-manager.ts`: `LspManager.client` (campo unico) pasa a `Map<languageId, LspClient>` -- arranque perezoso POR LENGUAJE, tocar un `.py` nunca afecta al cliente de `.ts` si ya estaba corriendo y viceversa. `notifyFileWritten()` rutea por extension real del archivo; `getDiagnostics(path?)` rutea al cliente correcto con `path`, junta diagnosticos de TODOS los clientes vivos sin `path` (helper `diagnosticsFromClient()` extraido, reusado en los 2 casos); `stopAll()` para todos los clientes del mapa. `get_diagnostics` (tool): sin cambios de firma/logica (ya recibia `path`), solo texto de descripcion actualizado para mencionar Python. Verificado con harness standalone real (esbuild + stub de electron, MISMO patron que la verificacion original de Fase 20 -- `ToolRegistry.execute()` invocado directo contra las clases reales), workspace con `.ts` y `.py` mezclados: `write_file` de un `.py` con error real de tipos arranco pyright real y `get_diagnostics` devolvio el diagnostico REAL de pyright; `write_file` de un `.ts` en el MISMO workspace arranco typescript-language-server real SIN matar el cliente de Python (`isRunning('python')` siguio `true`), `get_diagnostics` de ese archivo devolvio el diagnostico REAL de TypeScript; `get_diagnostics()` sin path junto los 2; coexistencia real confirmada a nivel de sistema operativo (`Get-CimInstance Win32_Process`, PowerShell, corrido en paralelo mientras el harness ejecutaba) -- 2 procesos `node.exe` reales, mismo proceso padre, uno con `pyright` y otro con `typescript-language-server` en su `CommandLine`, coexistiendo en el mismo instante real, no uno reemplazando al otro; `stopAll()` los termino a los 2, `tasklist` confirmo sin residuos de la sesion de verificacion. | `src/main/lsp-client.ts`, `src/main/lsp-manager.ts`, `src/main/tool-registry.ts`, `package.json` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real (2 language servers reales coexistiendo, confirmado a nivel de SO); sin commit, pendiente de que el usuario lo pida |
| Feature "generación de imágenes" — tool generate_image real vía Foundry | 2026-08-30 | Basado en Tarea 0 confirmada (docs/_arch/verify_image_generation.md): endpoint real de Foundry `/images/generations` (separado de `/responses`, `gpt-image-2` contra `/responses` da 400 "unsupported"), respuesta `data[0].b64_json` (base64 inline, sin URL). Configurabilidad: `AppSettings.imageGenerationProviderId`/`imageGenerationModelId`, mismo patrón que compactación pero PARALELO (sin compartir código/campos) -- 3 archivos: `shared/types.ts`, `settings-store.ts` (mismo cuidado que el bug historico de Fase 14), `ipc-settings.ts` (agregados a la whitelist de `settings:save` -- confirmado critico, verificado que sin esto el cambio real se ignoraba). `resolveConfiguredImageGenerationModel()` (nuevo `src/main/image-generation.ts`) sin elección explícita sugiere el primer modelo habilitado que matchee `isLikelyImageModel()` (heurística nueva en `shared/model-capabilities.ts`, sobre el nombre real del deployment) -- elección explícita siempre gana. `generateImage()` hace la llamada real a Foundry y arma el `ChatAttachment` con `buildAttachmentFromDataUrl()` (sin cambios, ya reusable). Settings UI: sección nueva "Generación de imágenes", mismo componente que el selector de compactación, muestra la sugerencia implícita real en el hint. Tool `generate_image` (`{prompt}`, sin tamaño/calidad todavía) con aprobación SIEMPRE incondicional, mismo criterio que `send_to_window`. `ToolExecutionResult.generatedAttachment` (patrón lateral igual a `lineDiff`, nunca llega al modelo como texto) acumulado durante todo `MAX_TOOL_LOOP` (`ApiAgentRuntime.generatedAttachments`, poblado en `runTool()/finish()`, volcado en un unico punto de union dentro de `send()`) y propagado hasta `App.tsx` (`appendAssistantMessage()` nuevo parametro `attachments`) vía el evento `item/agentMessage/delta`. Distinción visual: `ChatAttachment.origin?:'generated'`, badge "✦ IA" + "· GENERADA" en `AttachmentCard`. Hallazgo real cerrado en la misma ronda: la primera pasada agregó `origin` al tipo/UI pero no al esquema SQLite/INSERT/SELECT de `chat-store.ts` -- la primera imagen generada perdía el `origin` en silencio (confirmado real al ejecutar el propio paso de verificación "sobrevive un reinicio"), corregido con la migración `ALTER TABLE chat_attachments ADD COLUMN origin` + el INSERT/SELECT reales antes de cerrar. Verificado con UI real (sin bypass): conectar panel real, escribir y enviar un prompt real pidiendo generate_image, aprobación real vía click en el diálogo real, imagen real (PNG 1024x1024) renderizada con badge; cambio real de modelo en Settings a uno NO apto (gpt-5.4) produjo un error real y distinto de Foundry nombrando ese modelo, confirmando que el cambio se usó; vuelta al modelo correcto generó con éxito de nuevo; reinicio COMPLETO de electron.exe (no solo recarga) confirmó que ambas imágenes sobreviven, la primera (pre-fix) correctamente sin badge y la segunda (post-fix) con el badge intacto -- persistencia real en SQLite, no solo en memoria. | `src/main/image-generation.ts` (nuevo), `src/main/chat-store.ts`, `src/main/ipc-agent.ts`, `src/main/ipc-settings.ts`, `src/main/settings-store.ts`, `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css`, `src/shared/model-capabilities.ts`, `src/shared/types.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real contra Foundry real (API key real), con aprobación real, persistencia real confirmada en SQLite y tras un reinicio completo de la app; sin commit, pendiente de que el usuario lo pida |
| Fix `resolveOrCreateChatForPath()` — conflación title/workspaceName + reparación de dato real contaminado | 2026-08-30 | Bug real encontrado durante la verificación de Feature "árbol de sub-chats": `resolveOrCreateChatForPath()` usaba el mismo parámetro `name` para `title` Y `workspaceName` -- `addPanelForChat()` le pasaba el título COMPUESTO (`"X — Panel N"`), contaminando `workspaceName` del hijo. Encadenado, el siguiente "Agregar panel" sobre ese hijo generaba títulos tipo `"X — Panel N — Panel M"`. Confirmado con datos reales (no hipotético): 1 de los 2 chats reales de producción ya estaba contaminado (`workspace_name: "YAYOSCHAT — Panel 2"` en vez de `"YAYOSCHAT"`, confirmado por comparación contra su hermano limpio). Fix en 3 partes: (1) `resolveOrCreateChatForPath()` gana 5º parámetro opcional `workspaceName` -- si se pasa se usa tal cual, si no cae a `name` (sin cambio para los 2 call sites de proyecto). (2) `addPanelForChat()` resuelve la raíz real del grupo antes de generar el nombre nuevo vía `resolveGroupRoot()` nuevo -- si `origin.workspaceName` matchea `PANEL_SUFFIX_RE` (constante nueva en App.tsx, mismo patrón que en chat-store.ts, no importable entre procesos), sube hasta un hermano del mismo `workspacePath` con nombre limpio (o sin `parentChatId` si ninguno sobrevive limpio); el nuevo chat pasa a ser hermano de la raíz real (`parentChatId = root.id`), no hijo del padre inmediato contaminado. (3) Migración real `migrateContaminatedWorkspaceNames()` (chat-store.ts), corre en cada arranque dentro de `db()`, idempotente -- repara `workspace_name` de filas contaminadas usando el `workspace_name` limpio de un hermano del mismo `workspace_path`, o `PANEL_SUFFIX_RE.replace()` como mejor esfuerzo si ninguno sobrevive limpio; solo toca esa columna. Verificado real, 2 casos: backup previo de la DB real (`amatista.db.bak_pre_workspace_name_fix`) + migración corrida contra producción real, confirmada por 2 vías independientes (`loadChats()` real y lectura directa en disco vía `node:sqlite readOnly`) -- `workspace_name` reparado a `"YAYOSCHAT"`, `title`/`created_at`/`updated_at`/`provider_id`/`model_id`/`parent_chat_id` intactos; reproducción exacta del escenario "Agregar panel desde un hijo contaminado" con datos sintéticos aislados (`workspace_path` dedicado) -- el chat nuevo salió con `workspaceName` limpio, `parentChatId` apuntando a la raíz real (no al padre inmediato), título sin encadenar. Datos de prueba borrados vía `deleteChatSession()` real al terminar. | `src/main/chat-store.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real contra la DB de producción (con backup previo) y con datos sintéticos aislados; sin commit, pendiente de que el usuario lo pida |
| Feature "árbol de sub-chats" — parentChatId + sidebar anidado | 2026-08-30 | Pedido real del usuario: un chat nacido de "Agregar panel" es un sub-chat del origen, el sidebar debia mostrarlo anidado tipo arbol, no como chat independiente. Columna nueva `parent_chat_id` (mismo patron ALTER+try/catch ya usado 4 veces, sin FOREIGN KEY a proposito -- borrar el padre no debe arrastrar hijos, un huerfano se trata como raiz). `resolveOrCreateChatForPath()` gana 4to parametro opcional `parentChatId` (los 2 call sites de proyecto sin cambio de comportamiento); `addPanelForChat()` pasa `origin.id`. Sidebar: `buildChatRows()` nueva, aplana `chatSessions` en orden de render -- raices mantienen el orden real (`updated_at DESC`), hijos van justo debajo ordenados por el numero real de "Panel N" en su titulo (`generateUniquePanelTitle()` sin tocar), indentados via `marginLeft` inline segun profundidad. Cada fila (raiz o anidada) gana un borde de color segun `providerIdentity(providers.find(p => p.id === chat.providerId))?.accent` -- mismo mecanismo de Fase 21, reusado tal cual, provider PERSISTIDO (last-used) no conexion en vivo; sin provider reconocible cae a gris neutral. Verificado con CDP real, 2 casos contra la DB de produccion: arbol con 3 hijos reales (creados via clic real en "Agregar panel" x3, `parentChatId` confirmado via `loadChats()` real, providerId distinto por hijo via `ensureChatSession()` real -- Foundry/OpenRouter/DeepSeek) renderizo nested con 3 colores de borde reales y distintos medidos con `getComputedStyle()`; un chat normal sin hijos crea la fila identica a como era antes de la feature (sin indentacion, sin marca de arbol). Los 4 chats de prueba se borraron via `deleteChatSession()` real al terminar. | `src/main/chat-store.ts`, `src/main/ipc-chats.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/shared/types.ts`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con CDP real contra la DB de produccion, datos de prueba limpiados via IPC real; sin commit, pendiente de que el usuario lo pida |
| Fix `openChatInPanel()` — falso error "4 paneles" en auto-open via IPC | 2026-08-30 | Bug real confirmado con logging temporal (retirado): `openChatInPanel()` asumia que el updater de `setOpenPanels()` corre sincronico antes de su propio `return` -- cierto para clicks de React, FALSO cuando el caller es el listener de IPC nativo `panel:openAndConnectRequest` (auto-open de send_to_window). Devolvia siempre `null`, reportando el error hardcodeado de MAX_PANELS aunque el panel se abriera bien milisegundos despues. Fix: mismo patron de ref-espejo ya usado en la app (activeChatIdRef/turnStepsRef) -- openPanelsRef sincronizado por useEffect + actualizado manualmente dentro de la funcion, el panelId que se devuelve se calcula leyendo el ref (sincronico de verdad) antes de llamar a setOpenPanels(), que sigue siendo la unica fuente real del estado sin cambios en su logica interna. Verificado con CDP real reproduciendo el mismo escenario exacto: antes del fix, error falso pese a panel abierto; despues, exito real en el primer intento, mensaje entregado y confirmado en los mensajes reales de ambos chats. | `src/renderer/src/App.tsx` | Completa — `npm run build` (typecheck incluido) en verde; verificado con CDP real, mismo escenario que encontro el bug; sin commit, se junta con Feature "Panel N", pendiente de que el usuario lo pida |
| Feature "Panel N" — alias corto en send_to_window | 2026-08-30 | Pedido real del usuario: el modelo tenia que repetir el titulo completo del panel destino en send_to_window. Rediseño del usuario descarto el round-trip main-renderer originalmente propuesto: "Panel N" no es posicion visual, es el sufijo ESTABLE que generateUniquePanelTitle() (sin tocar) ya graba en el titulo real (" — Panel N"); "1"/"principal" es el chat del mismo workspace sin ese sufijo. findChatSessionByPanelAlias() (chat-store.ts) resuelve el alias contra chat_sessions filtrado por workspace_path de la sesion de origen (session.activeWorkspace, ya disponible, sin pedirle nada al renderer) -- si el alias no matchea el patron, cae al camino de titulo exacto existente sin cambios. list_windows ahora muestra el alias corto junto al titulo completo para los chats que ya lo tienen. Verificado con CDP real, 3 casos con chats reales del mismo workspace: "Panel 2" y "2" (solo el numero) resolvieron ambos al mismo chat correcto; "principal" enviado desde Panel 2 llego al chat sin sufijo. Hallazgo colateral documentado (no de esta feature): el handshake de auto-open existente reporto un error falso pese a abrir el panel correctamente -- comportamiento pre-existente, fuera de alcance. | `src/main/chat-store.ts`, `src/main/cross-window-messaging.ts`, `src/main/tool-registry.ts`, `src/main/ipc-agent.ts` | Completa — `npm run build` (typecheck incluido) en verde; verificado con CDP real, 3 casos de alias contra chats reales; sin commit, pendiente de que el usuario lo pida |
| Fix contraste — Tanda 1+3 (auditoria mecanica) | 2026-08-30 | 5 hallazgos de la auditoria mecanica completa (322 elementos escaneados con getComputedStyle() real, docs/_arch/verify_contrast_audit.md) corregidos: (1) `.add-connection-grid` (JSX) vs `.add-grid` (CSS) -- clase renombrada sin actualizar el selector, mismo patron que `.connection-toggle`/`.danger-link` de una fase anterior, ratio 1.08 -> 9.54; (2) `.settings-actions-row` y (3) `.model-catalog-row` nunca tuvieron CSS propio, ratio 1.08 -> 9.18 y 4.91, reusando el tono ya establecido de `.add-connection-grid button` y `.connection-action` respectivamente, sin inventar estilo nuevo; (8) `.connection-action-danger` (mi propio fix anterior) medía 3.91:1 real, apenas debajo del piso -- ajustado a `#a47979`, 4.34:1 con margen; (10) `.approval-dialog small` medía 3.98:1 -- ajustado a `#888`, 4.38:1. Verificado con CDP real sobre los 5 casos, incluyendo un dialogo de aprobacion real de `toolApproval` disparado con un `write_file` real de Foundry (rechazado 2 veces, archivo nunca escrito). | `src/renderer/src/assets/main.css` | Completa — `npm run build` (typecheck incluido) en verde; verificado con `getComputedStyle()` real via CDP, los 5 casos con ratio antes/despues documentado; sin commit, pendiente de que el usuario lo pida |
| Fix nombrado de paneles nuevos (titulos duplicados) | 2026-08-30 | Bug real: `addPanelForChat()` creaba un `ChatSession` DISTINTO en cada "Agregar panel" (fix anterior, ids reales distintos) pero le pasaba el mismo nombre sin desambiguar -- 2 clicks seguidos daban 2 chats con titulo IDENTICO. `findChatSessionByTitle()` (usado por `send_to_window`) resuelve por titulo con `ORDER BY updated_at DESC LIMIT 1` -- con duplicados, un mensaje dirigido "al panel de antes" podia terminar en el mas reciente, no en el querido. Fix: `generateUniquePanelTitle(baseName)` prueba `"{base} — Panel 2"`, `"Panel 3"`... contra `chatSessions` en memoria (case-insensitive, mismo criterio COLLATE NOCASE que el lado SQLite) hasta encontrar uno libre -- no asume que el proximo numero es 2. Vive solo en `addPanelForChat()`, sin tocar `resolveOrCreateChatForPath()` (los otros 2 call sites ya usan nombres distintos). Verificado con CDP real + scaffold temporal (`debug:sendToWindowByTitle`, retirado y confirmado con grep): 2 titulos unicos confirmados (`Panel 2`/`Panel 3`); reproducido el escenario exacto del bug (origen con actividad real reciente, updated_at mas nuevo) y send_to_window por titulo llego al panel correcto igual, con entrega cross-window correctamente etiquetada en el origen. Hallazgo colateral documentado por transparencia: el chat real YAYOSCHAT (157 mensajes) ya no estaba en la base al relanzar -- confirmado que ningun paso de esta sesion lo borro, consistente con uso real del usuario en el tiempo transcurrido; se siguio la verificacion con el chat actual por decision del usuario. | `src/renderer/src/App.tsx` | Completa — `npm run build` (typecheck incluido) en verde; verificado con CDP real y turno real de Foundry; sin commit, se junta con FIX 1/FIX 2/FIX menu contextual, pendiente de que el usuario lo pida |
| Menú contextual del composer — falso positivo + fix real | 2026-08-30 | Diagnostico reportado (rama 'composer' faltante en el render de contextMenu) descartado con evidencia: esa rama YA existe -- es el `else` final del ternario `chat`/`message`/`else`, cubre `'composer'` sin necesitar comparacion explicita (por eso el grep de esa comparacion literal no encontraba nada). Confirmado con CDP real: menu real con las 4 opciones al hacer click derecho en el composer. Bug real distinto encontrado en la misma verificacion: "Seleccionar todo" no dejaba seleccion visible -- `setSelectionRange()` sincrono quedaba pisado por el re-render que dispara `setContextMenu(null)` (mismo handler, corre justo despues). `cutComposerText()`/`pasteComposerText()` (mismo archivo) ya evitaban este problema con `requestAnimationFrame()`; `selectAllComposerText()` era la unica de las 3 sin ese patron. Fix: mismo `requestAnimationFrame()`, mas leer `ta.value.length` en vez del `prompt` cerrado (evita un closure potencialmente desactualizado). Verificado con CDP real, flujo completo sin contaminacion: Seleccionar todo (0 a longitud completa) -> Copiar (clipboard exacto) -> Pegar (insercion correcta) -> Seleccionar todo + Cortar (composer vacio, confirma seleccion total real). Nota metodologica: clicks reales de CDP por coordenadas resultaron poco confiables contra este overlay puntual -- se uso click sintetico (mismo pipeline de React) para la verificacion final, mismo criterio que el falso positivo del FOREIGN KEY de Paneles-3. | `src/renderer/src/App.tsx` | Completa — `npm run build` (typecheck incluido) en verde; verificado con CDP real, flujo de 4 acciones en cadena sobre texto real; sin commit, se junta con FIX 1/FIX 2, pendiente de que el usuario lo pida |
| Fix `addPanelForChat()` — reabría el chat de origen | 2026-08-30 | Bug real: "Agregar panel" llamaba `openChatInPanel(chatId)` con el chatId del chat de ORIGEN -- la regla de no-duplicados (correcta, sin tocar) solo enfocaba el panel ya abierto en vez de crear uno nuevo. `createProjectChat()` (la función que debía imitar) ya no existe; su reemplazo real, `resolveOrCreateProjectChat()`, exige un `ProjectEntry` completo (`id`/`rootId` obligatorios, `src/shared/types.ts`) que un chat de origen no puede proveer sin fabricar dato falso. Fix: extraída `resolveOrCreateChatForPath(path, name, forceNew)` -- logica real sin depender de `ProjectEntry`, reusada por los 3 call sites. `addPanelForChat()` reescrita: sin `workspacePath` en el origen avisa con `setNotice()` y no hace nada; con workspace, crea SIEMPRE un chat nuevo (`forceNew=true`) y lo abre en un panel nuevo, nunca reabre el origen. Verificado con CDP real, 3 casos: (A+C) 2 "Agregar panel" seguidos desde el mismo `YAYOSCHAT` crearon 2 chats DISTINTOS y 2 paneles nuevos, origen intacto; (B) hallazgo real -- ningún chat visible puede tener `workspacePath` falso hoy (`workspace:default` nunca es null + migración de `bootstrap()` parchea cualquier legado), asi que el guard se probó inyectando un `ChatSession` sin workspace directo en el hook `useState` real de React (sin tocar codigo fuente, sin persistir nada) -- notice real mostrado exacto, ningún panel/chat nuevo creado. | `src/renderer/src/App.tsx` | Completa — `npm run build` (typecheck incluido) en verde; verificado con CDP real, incluyendo un caso via manipulacion directa del hook de React para forzar una precondición que ya no ocurre por flujo normal; sin commit, se junta con el fix del selector de modelo, pendiente de que el usuario lo pida |
| Fix selector de modelo invisible en `<ChatPanel>` | 2026-08-29 | Bug real: `.model-menu` seguía con `bottom: 42px` (base + override `!important` "V0.3.8"), heredado de cuando `.model-btn` vivía al final del composer fijo (Fase 21). Paneles-2b movió el botón a `.panel-header` (arriba del panel) sin ajustar el sentido del despliegue -- el menú intentaba abrirse hacia arriba desde un botón ya en el borde superior, quedando fuera del área visible de `.chat-panel` (`overflow: hidden`). Fix: ambas reglas cambian a `top: calc(100% + 6px)`, sin tocar `right`/`width`/`max-height`/`z-index`. Verificado con CDP real (`getBoundingClientRect()`+`getComputedStyle()`, no solo compilación): con 1 panel el menú queda 100% dentro del área visible; con 4 paneles reales (grid 2x2, panel más angosto probado) nace visible justo debajo del botón, con la cola recortada por el borde inferior del panel (esperado, `overflow-y: auto` ya cubre ese caso) -- muy distinto del bug original (invisible por completo). | `src/renderer/src/assets/main.css` | Completa — `npm run build` (typecheck incluido) en verde; verificado con CDP real contra la app en producción, 4 paneles reales abiertos y luego limpiados; sin commit, pendiente de que el usuario lo pida |
| Fix contraste — Conexiones (Settings) | 2026-08-29 | Bug real: los 2 botones dentro de `.connection-actions` ("Desactivar"/"Activar"/"Eliminar") nunca tuvieron `className`, caían al estilo default del navegador (gris sobre gris) sobre el fondo `#202020` del panel. Causa confirmada por extracción mecánica previa (`verify_contrast_bug.md`): `.connection-toggle`/`.danger-link` en `main.css` (2 bloques, uno con overrides `!important`) eran huérfanas — ningún JSX vivo las referencia, confirmado con `grep -rn` sobre todo `src/` antes de borrarlas. Fix: clases nuevas `.connection-action`/`.connection-action-danger`, reusando el tono ya establecido por `.root-remove`/`.reset-local` (no un rojo genérico). Verificado con CDP `getComputedStyle()` real, no a ojo: `Desactivar` `rgb(141,141,141)` y `Eliminar` `rgb(155,114,114)` sobre fondo real `rgb(32,32,32)`, contraste ≈5:1 y ≈4:1 respectivamente. | `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con `getComputedStyle()` real vía CDP contra la app real; sin commit, pendiente de que el usuario lo pida |
| Fase Paneles-3 — auto-open real: main pide, renderer abre y conecta | 2026-08-29 | Cierra el árbol Paneles-2/3/4 original. Investigación previa (4 tareas, `verify_panels_scope.md`) confirmó: ningún `ChatPanel` se autoconecta al montarse (abrir ≠ conectar); `requestSessionToolApproval()` no es reusable tal cual (su mapa vive dentro de una sesión que todavía no existe para este caso); `window.alert()` al tope de `MAX_PANELS` congela TODA la app, no solo el intento nuevo; la protección de auto-envío se hereda gratis por orden, la de no-duplicados no. Mecanismo nuevo: `pendingPanelOpenRequests` (mapa GLOBAL, no por-sesión, en `cross-window-messaging.ts`) + `sendToShell()` (`runtime-state.ts`, variante de `sendToWindow()` sin panelId embebido, dirigida a `App()`) + respuesta con objeto (`{success, panelId?, error?}`, no booleano) vía `panel:openAndConnectResponse`. Timeout real de 8s, justificado con el mismo número base que ya justificó el timeout de LSP (Fase 20, ~2.7-3.7s cold-start) más margen para el roundtrip de React que ese caso no tenía. Guard de "nunca usado" restaurado explícito antes del auto-open (falla en 2ms sin intentar nada si el chat no tiene provider/modelo). Handshake de 2 pasos en `App.tsx`: `openChatInPanel()` real (nunca `setOpenPanels()` directo, preserva no-duplicados) devuelve ahora el `panelId` usado; ese panel recibe un prop nuevo (`autoConnectRequestId`) que dispara `connectAgent()` solo, mismo mecanismo interno que ya usaban `runTurn()`/`sendPrompt()`/el botón manual. `window.alert()` reemplazado por `setNotice()` (no bloqueante) tanto para el flujo humano como el automático. Verificación real con CDP (Foundry real): CASO A (destino real sin panel abierto) — panel 1→2 confirmado por `getComputedStyle()`, auto-conectó solo, turno real corrió (`{ok:true,text:"LISTO"}`), entrega real al origen confirmada; encontró y diagnosticó a fondo un `FOREIGN KEY constraint failed` que resultó ser un artefacto de la propia prueba (`ensureChatSession()` vía IPC directo no actualiza `chatSessions` de React, produciendo un chat "imposible en producción"), no un bug real — confirmado corrigiendo el setup de la prueba, no el código. CASO B (chat nunca usado) — falla en 2ms, sin auto-open. CASO C (tope de 4 paneles) — intento de un 5º resolvió en 674ms sin colgar la app, paneles finales 4 no 5, sidebar seguía respondiendo. Scaffold de verificación y diagnóstico temporal retirados, confirmado con grep. Base real limpiada (solo el chat real del usuario sobrevive, `activeProviderId`/`providers` sin cambios). | `src/main/cross-window-messaging.ts`, `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` limpios; verificado real end-to-end contra la app real + Foundry real, los 3 casos confirmados, scaffold retirado, base real limpiada. Sin commit — pendiente de que el usuario lo pida explícitamente |
| Fix carrera de `settings:save`: merge por campo en vez de reemplazo total | 2026-08-29 | Cierra el hallazgo documentado desde Paneles-2a, agravado por Paneles-2b (paneles reales en paralelo). Investigación previa (5 tareas, `verify_settings_race.md`) confirmó: el renderer llama `settings:get` una sola vez al arrancar, nunca se refresca — cualquiera de 12 acciones reales que disparan `settings:save` manda una copia potencialmente vieja para los campos que main actualiza de forma autónoma (`activeProviderId`/`activeModelId`/`activeProjectPath`, vía `withSettingsLock()`, Paneles-2a) — no una carrera de milisegundos, una pérdida garantizada bajo el orden de eventos más común. `projectRoots` resultó un caso mixto: 2 canales incrementales reales (`projects:addRoot`/`removeRoot` en main, y el propio `addProjectRoot()` del renderer) pero el segundo resultó enteramente redundante — Canal 1 ya persiste el root antes de que el segundo corra. Fix en 2 partes: `settings:save` (`ipc-settings.ts`) deja de reemplazar `AppSettings` entero — fusiona campo por campo, tomando del payload (via `sanitizeSettings()`, sin cambios) solo `providers`/`compactionProviderId`/`compactionModelId`/`turnWatchdogSeconds` (genuinamente renderer-owned, confirmado que main nunca los toca en vivo); `activeProviderId`/`activeModelId`/`activeProjectPath`/`projectRoots` se ignoran por completo del payload, main siempre gana con su propio valor vivo. `addProjectRoot()` (`App.tsx`) pierde el `mutateSettings(...,true)` redundante — pasa a una actualización local sin guardado, defensa en profundidad sobre el fix del handler. Restricción respetada: `withSettingsLock()`/`connectSessionForWindow()`/`workspace:open()` sin tocar. Verificación real repitiendo EXACTO el escenario de la Tarea 3 de la investigación: antes del fix, conectar un panel a Gemini y luego disparar cualquier `settings:save` revertía el cambio real a Foundry (confirmado); después del fix, mismo script, mismo orden de eventos — `activeProviderId` sobrevivió, `PERDIDA DE DATOS CONFIRMADA: false`. `projectRoots`: 2 remociones reales concurrentes (`Promise.all`, sin `await` intermedio) sobre 2 roots sembrados — ninguna se pisó, el root preexistente quedó intacto (verificado con `removeRoot` en vez de `addRoot`, ya que el diálogo nativo de carpeta no se puede automatizar 2 veces a la vez vía CDP — misma garantía de atomicidad, mismo patrón de handler). Base real limpiada, confirmado que `settings:save` ya no puede usarse para pisar `activeProviderId`/`activeModelId`/`activeProjectPath`/`projectRoots` ni por accidente ni para limpieza de datos de prueba (tuvieron que restaurarse editando el archivo directo con la app cerrada) — exactamente el comportamiento buscado. | `src/main/ipc-settings.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` limpios; verificado real end-to-end contra la app real, mismo escenario de pérdida ya reproducido en la investigación, ahora confirmado resuelto. Sin commit — pendiente de que el usuario lo pida explícitamente |
| Fix Gemini CLI: `geminiCommand()`, bug real de arg-splitting con `shell:true` | 2026-08-29 | Bug pre-existente (encontrado durante la limpieza de claude-cli, no introducido por ella): `sendGemini()` fallaba contra el binario real con `"Cannot use both a positional prompt and the --prompt (-p) flag together"`. Investigación previa (Tarea 0, reproducción real antes de tocar código) confirmó el mecanismo exacto: `gemini` en Windows resuelve al shim `.cmd` de `npm install -g` — `spawn()` sin `shell:true` no puede invocarlo (`ENOENT`, confirmado) — pero `shell:true` CONCATENA el array de args en vez de citarlos (warning de deprecación del propio Node), así que un prompt multilínea real se parte en decenas de argv sueltos al pasar por `cmd.exe`: Gemini CLI ve `-p` con solo el primer fragmento y el resto reinterpretado como posicionales. Reproducido en vivo con un prompt de prueba multilínea: mismo error exacto que producción. Fix: `geminiCommand()` (nueva, `cli-agent-runtime.ts`) — mismo patrón que `claudeCommand()` (retirado en la limpieza de claude-cli) resolvía para Claude, resolver la ruta real evita el problema de raíz en vez de escapar mejor el argumento. Diferencia real: el entrypoint de gemini-cli es un `.js` (`bundle/gemini.js`), no un `.exe` — resuelto leyendo `package.json.bin.gemini` del paquete real (decisión deliberada sobre hardcodear la ruta relativa: más robusto a que una versión futura reestructure el bundle interno, cuyos nombres `chunk-XXXX.js` NO son deterministas — confirmado real; `bin` es el contrato público que el propio npm usa para su shim). `existsSync()` antes de usar la ruta — si no resuelve (paquete no instalado ahí, `APPDATA` ausente, o no-Windows), cae al mecanismo viejo exacto, sin regresión. Spawneado con `process.execPath` + `ELECTRON_RUN_AS_NODE:'1'`, `shell:false` — mismo patrón ya probado en producción que `lsp-client.ts` (Fase 20) usa para `typescript-language-server`. Verificado real vía la CLASE REAL `CliAgentRuntime` (bundleada standalone con esbuild, mismo mecanismo ya usado en Fase 21.5 para `chat-store.ts` — no el script de la investigación) con el mismo prompt largo/multilínea: el error de parseo NO aparece más, el proceso avanza a lógica interna real del CLI. Falla después por `IneligibleTierError` (cuenta Gemini de esta máquina en un tier no soportado para CLI headless) — documentado como limitación de cuenta, fuera de alcance, sin tocar: la métrica de éxito de este fix es que el prompt llegue intacto, no que el turno complete con una cuenta que tiene otro problema aparte. | `src/main/cli-agent-runtime.ts` | Completa — `npm run typecheck` y `npm run build` limpios; verificado real con la clase de producción real (bundle standalone), no un mock. Sin commit — pendiente de que el usuario lo pida explícitamente |
| Fase Paneles-2b — `<ChatPanel>` real, layout dinámico 1-4 | 2026-08-29 | Cierra la Tarea 0 de investigación (`verify_panels_scope.md`, Paneles-2b): parte `App.tsx` en un contenedor (shell: sidebar/topbar/Settings/overlays compartidos) + `<ChatPanel panelId chatId>` real, N instancias dinámicas (1-4, `MAX_PANELS`). 3 decisiones confirmadas: `openPanels: Array<{panelId,chatId}>` en memoria (NO persistido, siempre 1 panel al arranque), mismo chat PROHIBIDO en 2 paneles (`openChatInPanel()`, único punto de aplicación real — si el chat ya está abierto, enfoca en vez de crear), `focusedPanelId` nuevo alimentando lo que sidebar/topbar muestran. Hallazgo de la Tarea 0 confirmado con evidencia de descarte: ni `chat_sessions.provider_id/model_id` (mide "usado alguna vez") ni `sessionRegistry` de main (mide "conectado en vivo", perezoso) alcanzan para "qué paneles están abiertos ahora" — concepto genuinamente nuevo. Simplificación real no anticipada: `activeProject` deja de ser estado — con `chatId` como prop CONTROLADA por el contenedor (el panel nunca decide qué chat mostrar) ya no existe la ventana transitoria que forzaba `activeProject` a ser un `useState` propio, pasa a derivarse de `activeChat.workspacePath/workspaceName` directo; un `useEffect` sobre `chatId` reemplaza a `switchToProject()` (conecta workspace + desconecta agente al cambiar). `chats` (caché de mensajes) se deja COMPARTIDO (deviación documentada de la clasificación previa de Paneles-2, que lo marcaba por-panel) — `bootstrap()` carga todo de una vez y la regla de no-duplicados hace que duplicarlo por panel no aporte nada. 2 casos nuevos del patrón "overlay único, disparado desde un panel" no cubiertos por `imagePreview`/`contextMenu` (ya resueltos en Paneles-2): `approval`/`toolApproval` reportan un handle con la closure de respuesta YA resuelta (`ApprovalHandle`/`ToolApprovalHandle`) en vez de requerir un registro de referencias por panelId; `contextMenu` tipo `'message'`/`'composer'` pasa a llevar las closures ya armadas (`onCopy`/`onEdit`/`onRegenerate`, `onCut`/`onCopy`/`onPaste`/`onSelectAll`) en vez de datos crudos que el contenedor ya no puede resolver por sí solo. Rediseño visual (parte de la fase, no cosmético aparte): selector de modelo (Fase 21, lógica sin cambios) se muda del final del composer a una cabecera propia de cada panel (`.panel-header`, identidad + acordeón + `.mcp.json`/Eventos/Agregar panel/Cerrar) — topbar global queda solo con "Pantalla completa"/"Modelos y cuentas". Layout: CSS Grid (no flex, documentado por qué — 1 propiedad por caso de conteo cubre 1/2/3/4 sin condicionales), `gridTemplateColumns/Rows` calculados en JS según `openPanels.length`. Sidebar/topbar generalizados a leer `panelStatuses[focusedPanelId]` (cada panel reporta un `PanelStatus` vía `onStatusChange`) en vez de un único `activeChat`/`agentState` global — nueva clase visual `.chat-row.open-elsewhere` para chats abiertos en un panel no enfocado. Acciones nuevas (reemplazo conceptual de "Abrir en ventana nueva", retirado en Paneles-1): "Agregar panel" (menú contextual de chat, mismo lugar) y cerrar panel (botón ×, nunca el último, desconecta esa sesión exactamente como ya se desconecta cualquiera hoy). Settings/Configuración generalizado al panel ENFOCADO (`syncCodexModels()`/`checkCodexAccount()`/`syncOpenAiChatCatalog()` resuelven contra `panelStatuses[focusedPanelId]`, `addProvider()`/`addDeepSeekProvider()` ganan `setFocusedChatModel()` shell-level) — confirma en código el hallazgo que Paneles-2 Tarea 3 ya había anticipado. `catalogChangeNonce` generaliza a N paneles el `void disconnect()` incondicional que cualquier acción de catálogo ya disparaba con 1 sola sesión implícita. `bootstrap()` simplificado: el bloque de `next.activeProjectPath` al arrancar se elimina (cada panel ya abre el workspace de su propio chat solo); `ensureStoredChat()` (Paneles-2a) queda exclusivo de `ChatPanel`, los call sites shell-level (bootstrap/migración/resetLocalState/proyectos/deleteChat) pasan a `persistChatSessionMeta()` nueva. Verificación real (CDP + Foundry/Codex/DeepSeek reales): 1 panel al arranque confirmado por DOM; agregar 2do panel con chat distinto confirmó `grid-template-columns` real de 2 columnas iguales (`645.5px 645.5px`); reintentar el mismo chat mantuvo el conteo en 2 (no duplicó); cerrar un panel devolvió a 1 sin afectar al restante; 3 paneles reales conectados simultáneo a Foundry/Codex/DeepSeek confirmados con `getComputedStyle()` sobre `.provider-badge` — los 3 colores exactos de `PROVIDER_BRAND` correspondientes, sin mezclarse entre paneles. Base real limpiada (4 chats de prueba borrados, `settings.json` restaurado a `qcfg-foundry`/`qcfg-foundry-chat`, 10 proveedores intactos), sin `electron.exe` colgado. | `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` limpios; verificado real end-to-end contra la app real + Foundry/Codex/DeepSeek reales, hasta 3 paneles simultáneos, datos de prueba limpiados y `settings.json` restaurado a su valor original. Sin commit — pendiente de que el usuario lo pida explícitamente |
| Fase Paneles-2a — modelo activo por chat, no por AppSettings global | 2026-08-29 | Investigación previa (2 rondas, `verify_panels_scope.md`) confirmó que `chat_sessions.provider_id`/`model_id` (ya persistidos desde Mensajería Paso 3) alcanzan como fuente de "modelo activo del panel" — `AppSettings.activeProviderId`/`activeModelId` se repurpose (no se eliminan del schema) a "default sugerido de la app" para un chat sin modelo propio, cero migración de `settings.json`. `pickProvider()`/`pickModel()` (`App.tsx`) ganan un fallback de 3 niveles: (1) provider/model del chat activo de este panel, (2) default de la app, (3) cualquiera habilitado (preexistente) — 2 `find()` encadenados con `??`, sin mecanismo de self-healing nuevo. `ChatSession` deja de descartar `providerId`/`modelId` que `loadChats()` ya traía. `setActiveChatModel()` (nueva) reemplaza las escrituras de `selectProvider()`/`selectModel()`/`addProvider()`/`addDeepSeekProvider()` a `settings.activeProviderId/activeModelId` — ahora escriben el provider/model del CHAT activo del panel, vía el mismo canal que `ensureStoredChat()`. `bootstrap()`/`syncCodexProvider()`/`deleteProvider()`/`deleteModel()` sin tocar (restricción explícita, ya globales correctos). **Bug real encontrado por la propia Tarea 5 (no en la investigación previa)**: `ensureStoredChat(chat)` ya escribía `providerId`/`modelId` de la closure `activeProvider`/`activeModel` desde Paso 3 (inerte hasta ahora) — 6 call sites (`"+ Nuevo chat"`, `createProjectChat()`, fallback de `deleteChat()`, más 3 de bootstrap/reset) la llaman con un chat recién creado en el MISMO tick que `setActiveChatId()`, antes de que React re-renderice — la closure todavía apuntaba al chat VIEJO, así que un chat nuevo quedaba estampado con el modelo del chat anterior en vez de sin ninguno propio. Confirmado real con CDP (clic real en "+ Nuevo chat" con Codex activo → chat nuevo con `providerId` de Codex en SQLite, pese a que el default de la app era Gemini). Fix: `chat.providerId ?? (chat.id === activeChat.id ? activeProvider?.id : undefined)` — prioriza lo que el chat YA tiene, solo usa la sesión conectada real cuando el chat recibido ES el activo y no tiene valor propio (el caso legítimo de `sendMessage()`). Re-verificado tras el fix: el chat nuevo quedó sin `providerId`/`modelId`. Race de escritura de `settings`: mecanismo elegido `withSettingsLock()` (cola async explícita, `runtime-state.ts`) en `connectSessionForWindow()`/`workspace:open()` — análisis real confirmó que estos 2 sitios NO pierden datos entre sí hoy (lectura-viva-y-escritura sin `await` en medio), la cola es una garantía estructural explícita a prueba de refactor futuro, no un fix de una pérdida real. Race más severa y NO cerrada (documentada, no oculta): `settings:save` reemplaza `settings` entero con la copia del renderer, pudiendo pisar un cambio más fresco de otro panel — arreglarla exigiría fusionar en vez de reemplazar, tocando código cerca de las funciones restringidas; anotada en PENDING.md. Verificación real (Tarea 5, CDP + Foundry/Gemini reales, 2 rondas): Caso 1 (2 paneles conectando a proveedores distintos casi simultáneo) — los 10 proveedores de `settings.json` quedaron intactos, ninguno perdido; Caso 2 (chat nuevo) — confirmado post-fix sin provider/model propio; Caso 3 (chat con turno previo en un modelo específico, default de la app apuntando a otro) — el chat reabierto mostró su propio modelo (GPT-5.4/Codex), no el default (Gemini). Base real limpiada (chat de prueba borrado vía `chats:deleteSession`, `settings.json` restaurado a su valor previo), sin `electron.exe` colgado. | `src/renderer/src/App.tsx`, `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/main/ipc-projects-workspace.ts` | Completa — `npm run typecheck` y `npm run build` limpios (2 rondas, antes y después del fix de `ensureStoredChat()`); verificado real end-to-end contra la app real + Foundry/Gemini reales + SQLite real, datos de prueba limpiados y `settings.json` restaurado a su valor original. Pendiente: la race de `settings:save` (wholesale replace) — ver `docs/_arch/PENDING.md`. Sin commit — pendiente de que el usuario lo pida explícitamente |
| Fase Paneles-1 — backend: panelId reemplaza windowId | 2026-08-29 | Tarea 0 de investigación (2 rondas, `docs/_arch/verify_panels_scope.md`) confirmó: cambio de tipo puro (`windowId: number` → `panelId: string`, 13 firmas + 2 `Map<number,...>`, cero lógica interna a tocar), 5 archivos con el patrón `originWindowId`/`callerWorkspace` vía `event.sender` (no 6, corregido), un wrapper `forPanel()` en preload viable para no tocar 58 call sites a mano, y que los 4 canales de broadcast relevantes pasan todos por una única función (`sendToWindow()`). Implementado sobre esa base: `runtime-state.ts` reemplaza `windowRegistry`/`WindowEntry`/`registerWindow`/`setWindowChatId`/`isWindowUsable`/`broadcastToAllWindows`/`sendToRenderer` (los últimos 2 sin ningún caller real, retirados como hallazgo) por `mainWindow: BrowserWindow | null` + `setMainWindow()` — una sola referencia, `sendToWindow(panelId, channel, payload)` embebe `panelId` directo en el payload sin ningún lookup. Los 16 handlers reales (`ipc-agent.ts` ×7, `ipc-window.ts` ×2 retirados, `ipc-agents-md.ts` ×2, `ipc-mcp.ts` ×2, `ipc-projects-workspace.ts` ×4) leen `panelId` del payload — 8 de los 16 no recibían ningún payload antes y ganaron uno nuevo. `forPanel(panelId)` (`preload/index.ts`) inyecta `panelId` en cada invoke de las funciones de sesión y filtra cada evento entrante ANTES de invocar el callback del panel (`data.panelId === panelId`) — resuelve el dispatcher de Paneles-2 sin tabla de ruteo separada en el renderer; extiende el listado original con `onAgentEvent`/`onIncomingMessage` (necesarios por los 4 canales de la Tarea 4, no nombrados explícitamente en la enumeración de la Tarea 3). `App.tsx`: `panelId` generado una vez (`crypto.randomUUID()`) + `api = window.universalAgent.forPanel(panelId)`; de los 58 call sites reales, 15 migrados a `api.*`, 2 retirados (`openInNewWindow`, `setActiveChatId` — con su botón/`useEffect`), 41 sin tocar — conteo reconciliado exacto contra el grep original. CASO ESPECIAL: `resolveOrOpenWindowForChat()` (`cross-window-messaging.ts`, abría una `BrowserWindow` nueva) se reemplaza por `findConnectedPanelForChat()` (escanea `sessionRegistry` directo, sin auto-apertura) — `send_to_window` a un chat sin panel conectado en vivo falla limpio con mensaje claro nuevo en vez de intentar crear un concepto que ya no existe; regresión temporal documentada, no silenciosa, Paneles-3 la resuelve con un handshake asíncrono real main↔renderer. Verificado real (CDP + Foundry real + `amatista.db` real, scaffold de debug retirado): mismo `panelId` simulado en 2 llamadas seguidas (`connectAgent` + `sendMessage`) corrió un turno real sin error de tipo ni runtime; `send_to_window` a un chat sin panel abierto falló con el mensaje nuevo exacto, 0 ventanas nuevas; verificación adicional confirmó que el camino "destino ya conectado en vivo" sigue funcionando sin cambios. | `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/main/ipc-window.ts`, `src/main/ipc-agents-md.ts`, `src/main/ipc-mcp.ts`, `src/main/ipc-projects-workspace.ts`, `src/main/window-manager.ts`, `src/main/index.ts`, `src/main/cross-window-messaging.ts`, `src/main/tool-registry.ts` (comentario), `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` limpios en el primer intento completo (sin ninguna ronda de corrección); verificado real end-to-end contra la app real + Foundry real + SQLite real, scaffold de debug retirado y confirmado con grep, datos de prueba limpiados de la base real. Pendiente para Paneles-2/3/4: ver `docs/_arch/PENDING.md` |
| UI Paso 1 — list_windows() + protección de auto-envío | 2026-08-29 | Que la IA sepa qué chats/ventanas existen antes de usar `send_to_window`, sin que el usuario tenga que deletrear el título exacto. Investigación previa (3 rondas) confirmó: tool bajo demanda (no contexto siempre-on, mismo criterio de costo recurrente ya aplicado a LSP en Fase 20 — un dato usado en pocos turnos no se inyecta en todos), `provider_id`/`model_id` de `chat_sessions` resueltos contra `settings.providers` alcanzan sin schema nuevo, y el alcance correcto es TODOS los chats con provider/model real (no solo ventanas abiertas ahora) porque el guard de `send_to_window` nunca mira si hay una ventana abierta — justificado con el propio guard, no en abstracto (incluso encontró el contraejemplo: una ventana abierta pero nunca usada igual falla el guard). Tool `list_windows()` nueva (`tool-registry.ts`): sin parámetros, solo lectura, sin aprobación, mismo criterio que `search_files`/`get_diagnostics`. `listChatSessionsForWindowDiscovery()` (`chat-store.ts`, nueva): SQL crudo con el mismo patrón de tope que `SEARCH_FILES_MAX_MATCHES` (`LIMIT 20`, `ORDER BY updated_at DESC`), sin resolver nada contra `settings` (no puede vivir ahí). `listWindowsForSession()` (`ipc-agent.ts`, nueva, standalone): excluye el chat de la propia sesión, resuelve `"proveedor eliminado"` para providers borrados o deshabilitados (un solo `find()`+`enabled`, sin distinguir los dos casos), `"no usable todavia (nunca se uso...)"` para `provider_id`/`model_id` `NULL` (misma frase que ya usa el error real de `send_to_window`, reusada como dato informativo), y `"${provider.name} ${modelLabel}"` en el caso feliz. Inyectada en `ExecuteContext.listWindows`, síncrona (a diferencia de `sendToWindowByTitle`, no necesita import dinámico — no importa nada de `cross-window-messaging.ts`). Protección de auto-envío agregada en `sendToWindowByTitle()`: comparación por chatId (no título, por los duplicados ya conocidos) en el punto MÁS TEMPRANO posible — justo tras resolver `match.id`, antes del guard de provider/model, antes de abrir cualquier ventana. Verificado real (CDP + `amatista.db` real + Foundry real, scaffold `debug:callTool` reusando `toolRegistry.execute()` real, retirado al terminar): `list_windows()` contra 3 chats sembrados con estados distintos (real/`NULL`/proveedor borrado) devolvió los 3 textos exactos esperados, más un 4to chat real preexistente del usuario (confirma que trae datos reales de producción), con `YAYOSCHAT` (el chat de origen) confirmado ausente de su propia lista; auto-envío a `YAYOSCHAT` desde la propia Ventana A rechazado limpio, 0 páginas nuevas abiertas; `send_to_window` normal contra otro chat real sigue funcionando exacto igual (turno real, entrega real, marca visual real). Base real limpiada sin residuos al terminar. | `src/main/chat-store.ts`, `src/main/tool-registry.ts`, `src/main/ipc-agent.ts`, `src/main/cross-window-messaging.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real end-to-end contra la app real + SQLite real + Foundry real, scaffold de debug retirado y confirmado con grep, datos de prueba limpiados de la base real |
| Mensajería entre ventanas (Paso 3, completo) | 2026-08-28 | Cierra la mensajería entre ventanas (Pasos 1-3): tool `send_to_window(destino, mensaje)`, auto-open/auto-connect, aprobación siempre-on, distinción visual. Tarea 1: `connectSessionForWindow(windowId, payload)` factorizada de `agent:connect` (`ipc-agent.ts`) — mismo patrón exacto que `runTurnForWindow()` de Paso 2, confirmado por lectura que `event` solo se usaba una vez en las ~180 líneas del handler. Tarea 2: `findChatSessionByTitle()` nueva (`chat-store.ts`, `SELECT ... WHERE title = ? COLLATE NOCASE ORDER BY updated_at DESC LIMIT 1` — `title` no tiene `UNIQUE`, duplicados se resuelven al más reciente sin avisar, comportamiento natural de la query, decisión explícita del usuario) + `resolveWindowForChatTitle()` (`cross-window-messaging.ts`, busca en `windowRegistry` o abre una ventana nueva vía `createAppWindow()`, mismo mecanismo real que "Abrir en ventana nueva"). Tarea 1+2 combinadas en `sendToWindowByTitle()` (`cross-window-messaging.ts`): auto-conecta la ventana destino SOLO si `chat_sessions.provider_id`/`model_id` no son `NULL` (backfillados en cada turno vía `ensureStoredChat()`, `COALESCE` preserva el último valor real) — si el chat destino nunca tuvo un turno, error claro sin adivinar. Tarea 3: tool nueva en `tool-registry.ts` (`TOOL_DEFINITIONS` + caso en el switch) — aprobación SIEMPRE incondicional vía `ctx.confirm()` directo (deliberadamente sin `resolveApproval()`, que auto-aprueba/rechaza según sandbox — el sandbox de la conexión actual no tiene relación con la seguridad de mandar un mensaje a otra sesión). `ExecuteContext` gana `sendToWindowByTitle?` (closure inyectada por `ipc-agent.ts` en `agent:connect`, cerrada sobre el `windowId` de origen) — resuelta vía **import dinámico** (`await import('./cross-window-messaging.js')`), a propósito y no un `import` estático: `cross-window-messaging.ts` ya importa `connectSessionForWindow`/`runTurnForWindow` DESDE `ipc-agent.ts` (Paso 2/3), así que un `import` estático de vuelta crearía un ciclo de módulos real — confirmado con `npm run build` que el import dinámico produce un chunk separado (`out/main/cross-window-messaging-*.js`), sin ningún warning de bundler. Tarea 4: `CrossWindowMeta` nueva (`shared/types.ts`, `direction`/`windowLabel`/`providerType` — `providerType` solo, no el `ProviderProfile` completo, con el caveat documentado de que no distingue el caso especial DeepSeek) en los 3 lugares confirmados por la investigación previa (`StoredChatMessage`, `ChatMessage` del renderer, `toChatMessage()`) más columna nueva `chat_messages.cross_window` (SQLite, mismo patrón `ALTER TABLE` + `try/catch` que `tool_steps`/`summary`). Tarea 5: `crossWindowBrand()` (`App.tsx`) reusa `PROVIDER_BRAND` (Fase 21) real, sin colores nuevos; `onIncomingMessage` consumido por primera vez en `App.tsx` (canal expuesto desde Paso 2, sin listener hasta ahora — hallazgo confirmado, no solo sospechado: `onIncomingMessage` tampoco estaba declarado en `preload/index.d.ts`, agregado). Verificación real (Tarea 6, CDP contra la app real + `D:\AMATISTA\data\config\amatista.db` real, Foundry real): CASO 1 (chat destino "Ventana-Test-B" sembrado sin turno previo) falló con el mensaje claro exacto esperado, sin crashear, sin auto-conectar nada — confirmado con un scaffold de debug temporal (retirado, confirmado con grep sin rastro) que invoca `sendToWindowByTitle()` real, evitando depender de que un LLM real decida frasear la tool call. CASO 2 (mismo chat, backfillado con provider/model reales simulando un turno previo): auto-conectó de verdad, corrió un turno real contra Foundry, entregó el resultado a la ventana de origen con `crossWindow` correcto — confirmado con 3 capas de evidencia real: (a) releído directo de SQLite tras la corrida (`crossWindow` persistido exacto), (b) `document.body.innerText` con el texto de la respuesta visible tras simular el click real en el chat de origen, (c) `getComputedStyle()` real sobre el DOM confirmando `border-left` y color del badge = color de marca real de Foundry (`#0078d4`), no un color inventado. Hallazgo real durante la propia verificación (error de setup, no de la implementación): el chat de prueba recién sembrado quedaba como "el más reciente" y por lo tanto como `activeChatId` por defecto de la ventana de origen al arrancar — corregido en el driver de verificación forzando `setActiveChatId()` real antes de cada corrida, no una falla del código de la fase. Toda la base real (`amatista.db`) limpiada de mensajes/chats de prueba al terminar, confirmado sin residuos con una relectura directa. | `src/main/chat-store.ts`, `src/main/ipc-agent.ts`, `src/main/cross-window-messaging.ts`, `src/main/tool-registry.ts`, `src/shared/types.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real end-to-end contra la app real + SQLite real + Foundry real (no solo typecheck), scaffold de debug temporal retirado y confirmado con grep, datos de prueba limpiados de la base real |
| Limpieza completa de claude-cli | 2026-08-28 | Retiro total de `claude-cli` como runtime (nunca funcional al ritmo del resto) — decisión confirmada con el usuario: conexión Claude Pro por suscripción existente NO se borra, se deshabilita automáticamente (`enabled:false`) + aviso, reactivable a mano. Reemplaza 3 mecanismos de re-siembra viejos que forzaban `runtime:'claude-cli'` (`claudeSubscriptionProvider()`/`sanitizeSettings()`, siembra incondicional de `buildProvidersFromQConfig()`, `runtimeFor()` de `settings-store.ts`) por una migración idempotente basada en marcador (`CLAUDE_CLI_REMOVED_MARKER`, duplicado renderer/main mismo patrón que `DEEPSEEK_ANTHROPIC_ENDPOINT`) que corre en dos puntos (`sanitizeSettings()` y `loadSettings()`) sin nunca repisar una reactivación manual. `runtimeFor()` simplificado: `type:'anthropic'` siempre `'anthropic-api'`, ya no bifurca por `authMode`. Edge case real confirmado contra datos de producción (no hipotético): conexiones Claude API-key/Azure viejas sin `allowSubscription` seteado (mismo patrón que el backfill de DeepSeek, Fase 14) — `backfillAnthropicApiKeyAllowSubscription()` nueva. `cli-agent-runtime.ts` reescrito completo: `CliAgentKind` reducido a `'gemini'` (campo `kind` conservado a propósito, no simplificado a clase sin parámetro — mismo costo de reversión ya documentado en otras fases de esta app), removidos `claudeImageBlocks`/`parseDataUrl`/`claudeCommand`/`sendClaude`/`sendClaudeWithImages`/`currentImageAttachments` (huérfana, sin llamador real sin Claude), `send()` pierde el parámetro `effort`. `sendGemini()` sin ningún cambio de línea. `RuntimeKind`/`CliAgentKind`/`SessionRuntimeState.activeRuntime` pierden sus miembros `claude-cli`/`claude` — usado el error de `tsc` resultante como checklist real de completitud (7 errores, los 7 dentro de `ipc-agent.ts`, cero sorpresas fuera de la investigación previa). Resto del retiro mecánico: `detectClaude()`, `cli:installClaude`/`installClaudeCli`, `openClaudeLogin()`, rama `anthropic` de `auth:openCliLogin`, campo `claude` de `cli:status`, `CLAUDE_EFFORT_LEVELS`, botón "Claude Pro" de "+ Agregar conexión", `openCliLogin()`/`cliInstallHint()` genéricas reemplazadas por versiones Gemini-únicamente, `scripts/install-claude-cli.ps1` borrado (nunca invocado desde código real, y su texto instruía pasos de UI ya inexistentes). Verificado real contra `D:\AMATISTA\data\config\settings.json` de producción: migración de la conexión Claude Pro real confirmada leyendo el archivo de vuelta (`enabled:false`, nombre marcado, modelos `runtime:"anthropic-api"`); Gemini CLI real conecta igual que siempre, `sendMessage()` real reveló una falla de parseo de argumentos del binario `gemini` instalado (`"Cannot use both a positional prompt and the --prompt (-p) flag together"`) — confirmada pre-existente y no causada por esta limpieza (`sendGemini()` sin cambios), anotada en PENDING.md, fuera de alcance por la restricción explícita de no tocar Gemini; Claude API-key/Azure real (`qcfg-azure-claude`) con estructura post-migración correcta, intento de conexión real devolvió "Proveedor no disponible." por estar `enabled:false` de antes (confirmado pre-existente al usuario, no causado por esta fase — la migración nunca toca proveedores `authMode:'api-key'`). Aviso al usuario confirmado real vía CDP: solo visible al abrir Configuración (mismo comportamiento que cualquier otro `notice` de esta app, no un toast global — documentado explícito en CONTRACT.md, no oculto). Grep final: cero referencias funcionales sobrevivientes de `claude-cli`/`'claude'` como `CliAgentKind`. | `src/main/settings-provisioning.ts`, `src/main/settings-store.ts`, `src/main/index.ts`, `src/main/cli-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/main/cli-status.ts`, `src/main/ipc-cli.ts`, `src/main/auth-manager.ts`, `src/main/mcp-client.ts`, `src/shared/model-capabilities.ts`, `src/shared/types.ts`, `src/main/runtime-state.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `scripts/install-claude-cli.ps1` (borrado) | Completa — `npm run typecheck` y `npm run build` en verde; verificado real contra `settings.json` de producción real (no solo typecheck), sin commit (pendiente de que el usuario lo pida explícitamente) |
| Mensajería entre ventanas (Paso 2) | 2026-08-28 | El motor: turno real en OTRA ventana desde main + entrega a la ventana de origen. Sin aprobación ni tool `send_to_window` todavía (Paso 3). Tarea 1: `agent:send` factorizado en `runTurnForWindow(windowId, payload)` exportada (sin `IpcMainInvokeEvent`) — `event` se usaba solo una vez, al principio, confirmado exacto en la investigación previa. `agent:send` real queda de wrapper delgado. Hallazgo real durante la verificación (no anticipado): `CodexClient.sendTurn()` resuelve apenas el app-server acepta el turno (ack de `turn/start`), no cuando termina — confirmado con logging temporal contra la app real (primera corrida: 1 solo evento capturado antes de que `sendTurn()` ya resolviera, sin deltas). Nunca importó antes porque el renderer arma el texto en vivo desde los mismos eventos, independiente del retorno de `agent:send()`. Fix: la rama Codex ahora espera de verdad `turn/completed`/`turn/cancelled` (acumulando deltas via listener temporal, timeout defensivo 120s) — efecto colateral real y documentado: `agent:send` real para Codex ahora también espera el turno completo, no solo el ack (bajo riesgo confirmado, y las 3 ramas quedan consistentes por primera vez). Tarea 2: nuevo módulo `cross-window-messaging.ts`, `deliverResultToOriginWindow()` persiste el resultado como mensaje real (`saveChatMessage`, invocable directo desde main) en el chat de origen y notifica vía canal nuevo `chat:incomingMessage` — deliberadamente separado de `agent:event` (investigado: `handleAgentEvent()` está armado alrededor de "mi turno en curso", forzar un mensaje ya-completo ahí arriesgaría pisar un turno real en vuelo). Tarea 3: `sendMessageToWindow()` conecta las dos piezas, sin aprobación ni tool. Verificado real (Tarea 4): 2 ventanas reales, ventana B conectada a Codex real (suscripción real), motor disparado desde A contra B, texto real del modelo recibido ("MOTOR-CROSS-WINDOW-OK"), evento `chat:incomingMessage` confirmado en la ventana A, persistencia confirmada leyendo `loadChats()` de vuelta (no memoria), mensaje de prueba limpiado del chat real al terminar. Scaffold de verificación retirado, confirmado con grep sin rastro. | `src/main/ipc-agent.ts`, `src/main/cross-window-messaging.ts` (nuevo), `src/main/ipc-window.ts`, `src/preload/index.ts`, `src/preload/index.d.ts` | `npm run typecheck` y `npm run build` en verde. Verificado real con app + 2 ventanas + Codex real, no solo typecheck. Sin tool `send_to_window`, sin aprobación, sin distinción visual todavía — ver `docs/_arch/PENDING.md` (Paso 3) |
| Mensajería entre ventanas (Paso 1) | 2026-08-28 | `WindowEntry.chatId` (Fase 22a) deja de ser código muerto — se seteaba una sola vez al crear la ventana y nunca se actualizaba; `setWindowChatId()` existía desde 22a pero no se llamaba desde ningún lado (confirmado con grep). Precedido por investigación de diseño (Tarea 0, 5 preguntas, resumen completo en `docs/_arch/PENDING.md`) sobre la tool `send_to_window` — este paso corrige solo la pieza de datos "windowId ↔ chatId real en vivo", no toca el motor de turno cross-window ni la tool en sí. Decisión: canal IPC dedicado `window:setActiveChatId` (no reusar `disconnect()`, mezclaría responsabilidades) disparado por un `useEffect(() => {...}, [activeChatId])` nuevo en `App.tsx`, junto al que ya sincroniza `activeChatIdRef` — cubre los 5 sitios `(c)` sin tocarlos uno por uno. Main resuelve la ventana vía `event.sender` (mismo patrón de siempre) y delega a `setWindowChatId()`. Agregado un log siempre-on en `runtime-state.ts` para el cambio de chatId, mismo criterio que el resto de logs de ciclo de vida del registro. Verificado real vía CDP: los primeros 2 cambios salieron del arranque real de la app sin intervención del script (`general-chat` → chat real más reciente), 3 cambios más disparados directo confirmaron actualización consecutiva sin quedar pegado en ningún valor intermedio, corroborado en el log del proceso main. Scaffold temporal de verificación (`debug:getWindowRegistry`) usado y retirado, confirmado con grep sin rastro. | `src/main/ipc-window.ts`, `src/main/runtime-state.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx` | `npm run typecheck` y `npm run build` en verde, antes y después de retirar el scaffold. Verificado real con app + CDP, no solo typecheck. Resto del diseño (motor de turno cross-window, entrega del resultado, campo de origen del mensaje, tool `send_to_window` en sí) sin implementar todavía — ver `docs/_arch/PENDING.md` |
| 22c (parte 1) | 2026-08-28 | Fix real: sesión conectada deja de depender de `settings.providers` global. Bug real (no diseño) encontrado en la investigación previa de esta sub-fase: con una sesión ya conectada, `agent:send` hacía un lookup fresco contra `settings.providers.find(...)` en CADA turno, antes de tocar cualquier runtime — si otra ventana borraba/deshabilitaba ese provider/modelo mientras la sesión seguía conectada, el próximo turno tiraba "Modelo/proveedor no disponible." pese a que el runtime ya conectado (apiRuntime/cliRuntime/codexClient) nunca vuelve a mirar settings por su cuenta (confirmado con grep, cero referencias reales en los 3). Confirmado también que `provider`/`model` se usaban en 3 lugares más además del guard: `buildRuntimeContext` (providerName/modelName/model.runtime del envelope), rama Codex (model.model directo a sendTurn), `maybeCompactChatInBackground` (fallbackProvider/fallbackModel para compactación sin modelo configurado explícito). Fix: `SessionRuntimeState` gana `provider`/`model` (objetos completos, no ids) — `agent:connect` los resuelve/valida contra settings.providers UNA vez al conectar (ahí sí sigue siendo correcto validar) y los guarda en la sesión; `agent:send` pasa a preferir `session.provider ?? settings.providers.find(...)` — con sesión ya conectada el fallback nunca corre. Los 3 usos reales no cambiaron de forma, solo de dónde sale el objeto. `disconnectSession()` resetea provider/model a null junto con el resto. Sin mecanismo de detección/aviso agregado — evaluado y descartado, no hace falta: nada se rompe con este fix. Verificado real, mismo escenario que la reproducción del bug: sesión Codex real conectada (suscripción real), provider borrado de verdad de settings.providers (confirmado ausente), segundo agent:send — antes del fix: `{"error":"...Modelo/proveedor no disponible."}`; después del fix: `{"success":true}`. settings.json real restaurado exacto al terminar, confirmado leyendo providers de vuelta (los 10 originales completos). App cerrada, tasklist confirma cero electron.exe colgado. | `src/main/runtime-state.ts`, `src/main/ipc-agent.ts` | `npm run typecheck` y `npm run build` en verde. Verificado real con app + suscripción real, no solo typecheck. Fuera de alcance de esta parte (resto de Fase 22c): los 13 `disconnect()` de App.tsx sin tocar todavía (clasificación (a)/(b)/(c) ya hecha, fix de cada categoría pendiente); staleness de write_file/apply_patch entre sesiones sin mecanismo |
| 22b | 2026-08-28 | Concurrencia real de conexión, indexada por ventana (alcance acotado: SOLO conexión/turno, los 13 `disconnect()` de Settings en App.tsx quedan para 22c). Tarea 0 (investigación previa): `wireCodex`/`wireCli`/`wireApi` recibían solo la instancia del runtime, cero contexto — agregarles `windowId` fue aditivo puro. Hallazgo que amplió el alcance: `activeWorkspace`/`disconnectAgent()` las usaban 6 archivos más además de `ipc-agent.ts` (`ipc-projects-workspace.ts`, `ipc-agents-md.ts`, `ipc-mcp.ts`, `ipc-cli.ts`, `ipc-settings.ts`, `index.ts`) — confirmado con el usuario (AskUserQuestion) migrar los 6 también, mismo patrón mecánico, no clasificación nueva. Tarea 1: `SessionRuntimeState` (las 9+4 variables identificadas en Fase 22 Tarea 0, más `pendingToolApprovals` agregado y justificado esta fase) vive en `sessionRegistry: Map<number, SessionRuntimeState>` indexado por `BrowserWindow.id` (misma clave que `windowRegistry` de 22a, Map distinto). `getSession(windowId)` nunca null, crea vacío si no existe. `disconnectSession(windowId)` reemplaza a `disconnectAgent()`, acotado a una sola entrada. `activeConnectionWindowId` (22a, singular a propósito) eliminado por completo — ya no hace falta. Tarea 2: `agent:connect`/`agent:send`/`agent:cancel`/`agent:reply`/`agent:toolApproval:respond`/`agent:toolTrust:disable`/`agent:disconnect` resuelven `windowId` primero, operan sobre `getSession(windowId)` — conectar desde una ventana ya NO mata la conexión de otra. Guard de carrera de Fase 12 (`assertWorkspaceStillActive` → `assertSessionWorkspaceStillActive`) comparado por sesión. Callbacks `confirm`/`mcpConfirm` (contratos externos, no tocados) cerrados sobre `windowId` capturado al conectar. Tarea 3: `sendAgentEvent()` (con fallback a broadcast) reemplazado por `sendSessionEvent(windowId, payload)` — `windowId` obligatorio, no opcional, el fallback de 22a ya no aplica. `wireCodex`/`wireCli`/`wireApi` capturan `windowId` en su firma. Migración de los 6 periféricos: `workspace:open`/`refresh`/`readFile`/`saveFile` por sesión; `projects:removeRoot` generaliza el mismo chequeo (`activeWorkspace.startsWith(root.path)`) a todas las sesiones via `sessionRegistry`, no clasificación nueva; `codex:logout`/`settings:resetLocalState`/`window-all-closed` generalizados a `disconnectAllSessions()` (mismo resultado de siempre escalado a N); `window-manager.ts` pasa de disconnect defensivo condicionado a `disconnectSession(window.id)` directo sin condición (correcto ahora que cada ventana tiene su propia sesión real). | `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/main/ipc-projects-workspace.ts`, `src/main/ipc-agents-md.ts`, `src/main/ipc-mcp.ts`, `src/main/ipc-cli.ts`, `src/main/ipc-settings.ts`, `src/main/index.ts`, `src/main/window-manager.ts` | `npm run typecheck` y `npm run build` en verde. Verificación con app real (2 BrowserWindow con conexiones simultáneas independientes) NO realizada todavía en esta ronda — pendiente si se prioriza. Fuera de alcance a propósito (Fase 22c): los 13 `disconnect()` de App.tsx sin tocar; staleness de write_file/apply_patch entre sesiones sin mecanismo; sessionRegistry no se limpia al cerrar ventana (asimetría con windowRegistry, anotada en PENDING.md) |
| 22a | 2026-08-28 | Infraestructura real de multi-ventana (alcance acotado: NO conecta runtimes por ventana, eso es 22b). Tarea 1: reemplazado el `mainWindow: BrowserWindow \| null` singular (pisado por `setMainWindow()`) por `windowRegistry: Map<number, {window, chatId}>` indexado por `BrowserWindow.id` real, limpieza automática vía `window.on('closed', ...)` armada en `registerWindow()`. Efecto colateral necesario: `mainWindow.on('closed')` mataba `disconnectAgent()` incondicionalmente — correcto con 1 ventana, pero con N mataría la conexión compartida al cerrar cualquier ventana secundaria; movido a disparar solo si `windowRegistry.size === 0` (mismo resultado que antes con 1 ventana, sin el efecto colateral nuevo). Tarea 2: canal `window:openInNewWindow` (`createAppWindow()`, nuevo `window-manager.ts`, evita import circular con index.ts) — el chat a mostrar viaja como query string (`?chatId=...`) en la URL/archivo cargado, funciona idéntico en dev (`loadURL` contra electron-vite) y producción (`loadFile` con `{query}`); leído en el renderer via `BOOT_CHAT_ID = new URLSearchParams(window.location.search).get('chatId')`, usado en `bootstrap()` para no caer al chat-más-reciente-global si esta ventana pidió uno puntual. Botón "Abrir en ventana nueva" en el menú contextual de cada fila de chat. Tarea 3: `agent:connect`/`agent:send`/`agent:cancel` usan `event.sender` (`BrowserWindow.fromWebContents`) para identificar la ventana de origen — todavía informativo, no aísla ni rechaza nada; guardado en `activeConnectionWindowId` (nuevo, `runtime-state.ts`), reseteado en `disconnectAgent()`; `agent:send` loguea (DEBUG_TOOLS) si la ventana llamante no coincide con la dueña de la conexión — dato que Fase 22b va a necesitar para empezar a rechazar, no repetir este trabajo. `ipc-window.ts` (`window:getFullscreen`/`setFullscreen`) migrado al mismo mecanismo, mejora real (antes operaba siempre sobre la única ventana que hubiera existido). Tarea 4: `sendToRenderer()`/`sendAgentEvent()` aceptan `windowId` opcional — sin él, caen a `activeConnectionWindowId` si esa ventana sigue abierta, o hacen broadcast a todas (fallback documentado como temporal). Verificado real (Tarea 5): app real levantada (`npx electron --remote-debugging-port`, `AMATISTA_DEBUG_TOOLS=1`, contra `D:\AMATISTA\data` de producción, sin enviar mensajes/tocar datos reales) más un driver Node vía Chrome DevTools Protocol (WebSocket nativo de Node 24, sin dependencias nuevas) — confirmado con datos reales: 2 `BrowserWindow` reales con ids 1/2 vía `openInNewWindow()`; evento dirigido a la ventana 1 llega SOLO a la 1 (la 2 no lo recibe) y viceversa (log real: `"agent:event" -> ventana 1 (dirigido)` / `-> ventana 2 (dirigido)`); cerrar la ventana 2 la saca del registro (`quedan 1`) sin afectar a la 1 (sigue respondiendo). Scaffold de verificación (`debug:testEvent` IPC + `debugTestEvent` en preload) usado y retirado antes de cerrar la fase — no forma parte del entregable. **Ronda de seguimiento**: la primera pasada de Tarea 5 solo había ejercido la rama `loadFile` (producción) sin darse cuenta; verificado por separado el camino `loadURL` (dev) con `npx electron-vite dev --remoteDebuggingPort` — CDP contra la ventana 2 real confirmó `window.location.href` = `http://localhost:5173/?chatId=<id de prueba>`, query string parseado correctamente DESDE ADENTRO de esa ventana, coincide exacto con el id pedido a `openInNewWindow()`. Procesos y puertos (5173/9334) confirmados sin nada colgado tras cerrar (`tasklist`/`netstat`, solo `TIME_WAIT` residual). | `src/main/runtime-state.ts`, `src/main/window-manager.ts` (nuevo), `src/main/index.ts`, `src/main/ipc-window.ts`, `src/main/ipc-agent.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real (2 BrowserWindow reales, CDP + logs del proceso main), no solo "debería funcionar". Fuera de alcance a propósito (Fase 22b): `agent:connect` sigue matando la conexión anterior incondicionalmente; `activeConnectionWindowId` es solo informativo, no aísla ni rechaza nada; las 9 variables singulares de runtime-state.ts (codexClient/cliRuntime/apiRuntime/mcpManager/lspManager/activeRuntime/activeWorkspace/activeThreadId/activeChatId) sin tocar |
| 21.5 | 2026-08-27 | Desacople de identidad de chat/workspace, previo a Fase 22 (sesiones concurrentes). Bug real: openProject() usaba id: project.path como id del chat, con dedup por path -- estructuralmente imposible tener 2 chats en el mismo proyecto. Tarea 0 (investigacion antes de tocar codigo): chat-store.ts/ipc-agent.ts/runtime-state.ts YA estaban completamente desacoplados (chatId tratado como string opaco en todos lados, workspace_path/activeWorkspace son campos separados desde el diseno original, confirmado con grep que no hay validacion de path sobre el id en ningun lado) -- el bug vivia SOLO en openProject(). Un segundo sitio sospechoso (handleAgentEvent(), variable local llamada "workspace") resulto ya-correcto: prioriza event.chatId antes que event.workspace, funciona hoy y sigue funcionando -- riesgo residual anotado en PENDING.md para Fase 22, no corregido ahora. Precedente ya correcto en el mismo archivo: "+ Nuevo chat" ya usaba crypto.randomUUID(). No existia una mecanica separada de "volver al ultimo chat del proyecto" -- el dedup-por-path de openProject() ERA el unico mecanismo, por coincidencia id===path, no por diseno. Implementado con el usuario: openProject() ahora busca el chat MAS RECIENTE real (ChatSession.updatedAt nuevo, mismo dato que ya persiste chat-store.ts, bumpeado en vivo en cada turno via ensureStoredChat()) con ese workspacePath -- crea uno nuevo solo si no hay ninguno. newProjectSession() (accion nueva, boton "+" inline junto a cada fila de PROYECTOS, no un menu contextual nuevo -- no existia uno para filas de PROYECTOS) SIEMPRE crea un chat adicional. void disconnect() agregado a ambas: workspace:open (main) ya dispara su propio disconnectAgent() cuando el PATH cambia, pero no cubre el caso nuevo que este fix hace alcanzable (dos chats hermanos del MISMO workspacePath). Verificado real (no solo lectura): chat-store.ts real bundleado standalone, SQLite real via node:sqlite, app-paths.ts stubbeado a scratchpad (nunca toco D:\AMATISTA\data) -- secuencia real chat A (con mensaje) -> chat B (mismo workspacePath) -> confirmado que el mas reciente real es B -> chat C -> 3 ids distintos, historial de A y B intacto, C vacio. | `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real contra chat-store.ts real (SQLite real, DB de scratchpad aislada) |
| 21 | 2026-08-27 | Rediseno de identidad de proveedores + selector de modelo tipo acordeon, sobre un mockup HTML aprobado por el usuario (pegado directo en el chat -- la ruta sandbox original no existia en esta maquina, confirmado antes de tocar nada). providerDisplayName() (heuristica por substring, nombres DISTINTOS para la misma marca segun authMode: "Claude Pro" vs "Claude API via Azure") eliminada, reemplazada por providerIdentity() -- switch exhaustivo sobre los 7 ProviderType reales (confirmados con grep, incluye openrouter), clave primaria provider.type. Unico caso especial real: DeepSeek, comparacion por IGUALDAD exacta de endpoint (DEEPSEEK_ANTHROPIC_ENDPOINT, nueva unica fuente de verdad, antes duplicado como literal), no substring. Metodo de conexion (Suscripcion/API key) pasa a ser SIEMPRE una pill separada (MethodPill), nunca mas concatenado al nombre -- Claude API key/Azure y Claude Pro ya no tienen nombres distintos, ambos son "Anthropic". providerGroupLabel() eliminada (sin call site tras el rediseno); providerSubtitle() mantenida y reutilizada como fallback de la nueva providerConnectionSubtitle() (conteo real de modelos habilitados) para conexiones de solo-suscripcion sin modelos que contar. ProviderBadge (circulo+inicial+halo) y MethodPill: 2 componentes reusados tal cual en la lista de conexiones Y en el acordeon, no duplicados. Desviacion deliberada del mockup: el color de "modelo seleccionado" dejo de ser un naranja fijo (funcionaba en el demo de una sola marca) y pasa a ser el accent real del grupo/marca abierto. Bug real encontrado y corregido: un override CSS !important preexistente (.connection-main span, selector descendiente sin '>') se filtraba hasta la pill anidada y le pisaba color/tamano -- acotado a hijo directo (.connection-main > span). Acordeon real: expandedProviderId es un unico string nullable (no un Set), expandir un grupo reemplaza el valor -- invariante "un solo grupo abierto" estructural, no logica de "cerrar los demas". selectModel() ahora tambien resetea expandedProviderId ademas de modelMenuOpen -- cierre COMPLETO del menu al elegir, no solo el grupo. Boton AGENTS.md sacado del topbar (localizado por title/onClick, no por posicion) -- openAgentsMd() (funcion wrapper) se dejo sin borrar (sin otro call site, pero no se pidio explicitamente borrarla y no rompe tsc sin noUnusedLocals); agents-md.ts/ipc-agents-md.ts sin tocar. | `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; verificado por lectura (sin forma de correr la UI desde este entorno): 7 ProviderType reales cubiertos, 6 colores de marca exactos del mockup, invariante del acordeon confirmado en el codigo |
| 20 | 2026-08-26 | LSP real para TypeScript (diagnosticos en vivo), sobre la investigacion previa (framing Content-Length, handshake, push-only, URIs de Windows, shutdown+exit, latencia real). Hallazgo que cambio el plan de instalacion: el proyecto YA tiene typescript@^6.0.0 como devDependency y esa version (6.0.3) SI trae tsserver.js (a diferencia de 7.x) -- confirmado en vivo, se reuso la MISMA instalacion para tsc y para el language server en vez de sumar una segunda version en paralelo (typescript@^5 hubiera chocado de rango). typescript paso de devDependencies a dependencies; typescript-language-server sumado como dependency nueva. Empaquetado no trivial: "files" del build es un allowlist explicito que hoy no shipea node_modules -- typescript-language-server/typescript no se pueden bundlear como JS normal (son procesos reales que leen sus propios datos en disco en runtime), se agregaron a files + asarUnpack, spawn real via process.execPath + ELECTRON_RUN_AS_NODE=1 (sin .cmd/shell, a diferencia del prototipo de investigacion). Modulos nuevos: lsp-framer.ts (promovido tal cual del prototipo ya validado), lsp-client.ts (LspClient: handshake, notifyFileChanged con didOpen/didChange incremental, cache de diagnosticos, normalizacion URI-path decodificando siempre, shutdown real con fallback a kill), lsp-manager.ts (LspManager: arranque perezoso real -- nunca en agent:connect, recien en el primer touch de un .ts real; get_diagnostics con timeout documentado de 5000ms, justificado con la latencia real medida ~3.7s en frio). Wiring limitado a los 4 runtimes API (ExecuteContext.lspManager opcional, mismo patron que resolveExploreModel) -- runtimes CLI fuera de alcance por completo, documentado como decision de alcance, no omision (mismo criterio que Fase 17 Parte 1). write_file/apply_patch notifican al LSP fire-and-forget, sin bloquear el resultado de la tool. Tool nueva get_diagnostics(path?), solo lectura, sin aprobacion. Verificado real (Tarea 5) contra las clases reales (ToolRegistry.execute() real, no un mock, mismo codigo que runTool() de cualquier runtime API dispara, aunque sin pasar por un turno de LLM real por falta de key transitoria): arranque perezoso confirmado (isRunning() false hasta el primer write_file, get_diagnostics NO lo dispara), error deliberado real detectado y reportado correcto (linea/columna/codigo), diagnostico limpio tras el fix, shutdown limpio confirmado incluso a nivel de SO (0 node.exe colgados). Impacto real en el instalador medido con un npm run dist real: 117.554.269 -> 120.932.931 bytes, +3.378.662 bytes (~3.22MB) comprimido sobre ~22.4MB sin comprimir. PENDING.md revisado, sin items relacionados. | `src/main/lsp-framer.ts`, `src/main/lsp-client.ts`, `src/main/lsp-manager.ts` (nuevos), `src/main/tool-registry.ts`, `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `package.json` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real contra las clases de produccion (bundle esbuild standalone) + un npm run dist real completo para medir el tamano del instalador; falta la verificacion con un turno de LLM real (sin key en esta ronda) |
| 19 | 2026-08-26 | Log de actividad en vivo del turno pasa a mostrar el archivo/comando especifico + diff de lineas (+X/-Y) en vez de solo "tool completado" generico. Tarea 0 (investigacion real contra codex app-server): los eventos nativos de Codex YA traen detalle rico (item.type:'commandExecution' -> item.command real; item.type:'fileChange' -> item.changes[].path + .diff), mucho mas que el generico name/phase de ApiAgentRuntime -- wireCodex() no necesito ningun cambio (reenvia raw/notification sin sintetizar nada). Hallazgo real colateral: summarizeCodexItem() (App.tsx) ya intentaba leer item.path/item.file para items de archivo, campos que NUNCA existen en el shape real (es item.changes[].path) -- nunca matcheaba antes de este fix, corregido para leer el campo real; el verbo (Creando/Editando/Borrando) solo se etiqueta con certeza para kind.type:'add' (unico confirmado en vivo, el helper de sandbox de Windows no esta instalado en la maquina de investigacion y bloqueo probar 'modify'/'delete'). Camino API: toolStatusArgDetail() (api-agent-runtime.ts, nueva) extrae path/command/pattern segun la tool desde los args que runTool() ya recibe, sin cambiar que se ejecuta. ToolExecutionResult gana lineDiff opcional; formatWriteFileDiff() (tool-registry.ts) paso de devolver solo el string del dialogo de aprobacion a devolver {preview, added, removed} del MISMO DiffLine[] de computeLineDiff() ya calculado ahi (countLineChanges(), un tabulado simple, confirmado con grep que computeLineDiff( tiene un unico call site -- nunca se recalcula). write_file/apply_patch adjuntan lineDiff a su resultado exitoso; revert_file solo ajustado a la nueva firma (.preview), sin ganar lineDiff (fuera del alcance pedido). UI: toolCallTargetLabel()/lineDiffLabel() arman "Ejecutando: write_file (src/App.tsx)" y "write_file completado (src/App.tsx, +100 -20)" -- antes mostraba el workspace conectado (siempre el mismo valor durante todo el turno) en vez del argumento real de cada llamada. | `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; Tarea 0 verificada en vivo contra el transporte real de Codex; conteo +X/-Y confirmado como reuso del mismo computeLineDiff(), no un calculo nuevo |
| Fix | 2026-08-26 | Watchdog stale: TURN_WATCHDOG_MS (Fase 14) se leia congelado en el closure del primer render dentro de startTurnWatch(), porque handleAgentEvent (unica que la llama) se suscribe una sola vez via useEffect(...,[]) (confirmado con grep antes de tocar codigo, linea ~873, deps vacias) -- si el usuario cambiaba el timeout en Settings a mitad de sesion, el cambio nunca se aplicaba hasta recargar la app. Mismo mecanismo exacto que ya resolvia turnStepsRef para el log de pasos del turno: turnWatchdogMsRef = useRef(TURN_WATCHDOG_MS) + useEffect(() => {turnWatchdogMsRef.current = TURN_WATCHDOG_MS}, [TURN_WATCHDOG_MS]) -- el efecto si se re-ejecuta en cada render donde cambia el valor (a diferencia del useEffect de suscripcion, que tiene deps vacias), asi que el ref queda sincronizado con Settings en vivo aunque startTurnWatch() siga en el closure congelado. Las dos referencias directas dentro de la funcion (el setTimeout y el string del mensaje de error) pasan a leer turnWatchdogMsRef.current. Verificado por lectura directa del codigo resultante (mismo criterio ya usado para sendClaude() en Fase 17 Parte 2): grep confirma cero referencias directas a TURN_WATCHDOG_MS dentro de startTurnWatch(). | `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; verificado por lectura, sin test end-to-end (no hacia falta) |
| 18 | 2026-08-26 | Sincronizacion real de catalogo para conexiones openai-chat (OpenRouter y cualquier backend Chat-Completions-compatible), mismo patron que syncCodexProvider()/listCodexModels() de Codex pero SIN reemplazo completo del array de modelos -- con 400+ modelos posibles eso seria inmanejable, en su lugar un panel buscable donde el usuario agrega modelos puntuales. Fetch real GET <endpoint>/models (modulo nuevo openai-chat-catalog.ts), confirmado en vivo contra OpenRouter real sin necesitar key (endpoint publico): 417 modelos reales, shape {data:[{id,name,context_length,supported_parameters,architecture:{input_modalities},top_provider:{max_completion_tokens}}]}. Parseo defensivo de 4 senales: supportsTools ("tools" exacto en supported_parameters, 348/417 lo traen), supportsVision ("image" en architecture.input_modalities), maxOutputTokens (top_provider.max_completion_tokens, confirmado 131072 real para stealth/ox-alpha, coincide con lo documentado a mano en Fase 15), contextLength (solo informativo). IPC nuevo dedicado (ipc-openai-chat-catalog.ts, no sumado a ipc-cli.ts por no tener relacion con CLIs) recibe {endpoint,apiKey} directo del provider activo en memoria del renderer. UI: boton "Sincronizar modelos" solo para type:'openrouter', panel con input de busqueda (filtra por id/displayName, nunca un select gigante ni scroll infinito), checkbox "Mostrar todos" para destapar el filtro-por-default de tools, lista con scroll acotado y boton "Agregar" por fila que suma ESE modelo puntual con los campos precargados desde datos reales (addCatalogModel(), no addManualModel() vacio). Verificado real contra la funcion real (bundle esbuild standalone, --external:electron + stub): 417 modelos parseados sin error, stealth/ox-alpha con los 4 campos correctos, busqueda "ox" lo encuentra. | `src/main/openai-chat-catalog.ts`, `src/main/ipc-openai-chat-catalog.ts` (nuevos), `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; verificado real contra OpenRouter sin key (417 modelos, ox-alpha, busqueda); test con key transitoria real pendiente de que el usuario la provea |
| 17 (Parte 2) | 2026-08-26 | Vision real en los 2 runtimes CLI (claude-cli/codex), cierra Fase 17 completa junto con Parte 1. Investigacion previa (3 hallazgos empiricos, ninguno asumido): Codex localImage NO respeta el cwd del spawn (a diferencia de Read de Claude Code, que si lo rechaza fuera de cwd); Codex {type:'image', url:'data:...'} acepta un data URI embebido sin filesystem (probado con logoamatista.png real -- la 1x1 de ejemplo de Anthropic dio un falso negativo, limitacion conocida de imagenes <200px); claude -p SI tiene mecanismo de imagen embebida pero no es un flag, es --input-format stream-json --output-format stream-json con el mensaje por stdin en shape Messages API real -- corrigio la hipotesis inicial de necesitar un archivo temporal. Riesgo identificado antes de tocar codigo: sendClaude() actual hace un JSON.parse(stdout) unico al final asumiendo un blob, pero stream-json emite varias lineas JSON por turno -- migrar TODO sendClaude() habria expuesto una regresion silenciosa (el try/catch absorbe el parse roto y devuelve lineas crudas como respuesta) al 100% de los turnos, no solo a los que tienen imagen. Decision: bifurcacion condicional, no migracion completa -- turno sin imagenes usa sendClaude() sin cambio de codigo ejecutado, turno con imagenes delega a sendClaudeWithImages() nuevo con parseo linea-por-linea (mismo patron readline que sendGemini()). Codex: un solo cambio en sendTurn(), input suma {type:'image', url:attachment.preview} por cada imagen del turno actual. Claude: parseDataUrl()/claudeImageBlocks() duplicados localmente en cli-agent-runtime.ts a proposito (no reusados de api-agent-runtime.ts, restriccion de la fase de no tocar ese archivo + ninguna de las dos funciones esta exportada). Bug real encontrado en la Tarea 3 (no en la investigacion): --print + --output-format=stream-json sin --verbose es rechazado por el CLI, y el intento de escritura a stdin igual generaba un 'error' sin manejar que tumbaba el proceso Node entero -- fix de dos partes, --verbose agregado a los args + handler defensivo en child.stdin. Verificado real contra CliAgentRuntime/CodexClient reales (bundle esbuild standalone): 3 turnos Claude en la misma sesion (sin imagen establece sessionId -> con imagen +--resume +--effort low, descripcion correcta -> sin imagen de nuevo, recuerda el turno 1, cero regresion); Codex con imagen real via sendTurn()+notificacion turn/completed, descripcion correcta. | `src/main/codex-client.ts`, `src/main/cli-agent-runtime.ts` | Completa (Parte 2, Fase 17 completa) — `npm run typecheck` y `npm run build` en verde; verificado end-to-end contra las clases reales de produccion (bundle esbuild), 3 casos con imagen real (Claude x1 + Codex x1) mas continuidad de sesion confirmada en Claude, no solo scripts de investigacion standalone |
| 17 (Parte 1) | 2026-08-25 | Vision real en los 4 runtimes API (foundry/anthropic-api/gemini-api/openai-chat) -- hasta ahora ninguno mandaba bytes de imagen pese a capabilities.vision:true, runtimeAttachments() descartaba ChatAttachment.preview y solo se inyectaba una referencia de texto. Tarea 0 (investigacion, sin tocar codigo): shape openai-chat confirmado en vivo contra OpenRouter real (stealth/ox-alpha, HTTP 200, descripcion coincide con la imagen real de prueba); Codex (LocalImageUserInput en el schema real, probado en vivo) y claude-cli (Read nativo multimodal, confirmado empiricamente) quedan fuera de esta Parte 1, pendientes de la ronda de attachment storage; Anthropic/Foundry/Gemini verificados solo contra documentacion oficial, sin key en texto plano disponible (keys cifradas con safeStorage, desencriptar headless bloqueado por el clasificador de seguridad). Tarea 1: preview dejaba de descartarse en DOS chokepoints independientes (runtimeAttachments() en App.tsx Y runtimeAttachmentView() en attachments.ts -- este ultimo es el real, buildRuntimeContext() lo llama sobre payload.attachments, no sobre el array del renderer directamente), solo para kind:'image'. Tarea 2: helper parseDataUrl() compartido separa (mimeType, base64Puro) una sola vez; Foundry/openai-chat mandan el data URL completo (preview tal cual), Anthropic/Gemini el base64 sin prefijo; imagen solo en el turno ACTUAL (context.attachments es siempre el turno actual por construccion), historial sigue como referencia de texto, nunca se re-manda una imagen vieja. Tarea 3: guard de tamano antes de armar el payload (assertImageAttachmentsWithinLimit(), chokepoint unico en send()), limites reales verificados contra documentacion oficial: Anthropic 10MB base64-encoded (exacto), Foundry 20MB (exacto), Gemini 20MB del request completo (exacto), openai-chat sin numero exacto documentado por OpenAI -> se uso el mas conservador de los confirmados (10MB), por instruccion explicita del usuario. | `src/renderer/src/App.tsx`, `src/main/attachments.ts`, `src/main/api-agent-runtime.ts` | Completa (Parte 1) — `npm run typecheck` y `npm run build` en verde; 1 de 4 shapes API verificado en vivo (openai-chat), 3 verificados solo contra documentacion (sin key transitoria disponible para Anthropic/Foundry/Gemini en esta ronda); Parte 2 (CLI: context-envelope.ts/cli-agent-runtime.ts/codex-client.ts) pendiente de la investigacion de attachment storage |
| 16 | 2026-08-25 | Tool nueva search_files: busqueda de texto real en el workspace (archivo:linea:contenido por match), la brecha mas grande frente a Grep/Glob de Claude Code/Codex nativos. Dos capas "intentar lo rapido, caer a lo generico" (mismo criterio que Fase 14): git grep -n -I --untracked (respeta .gitignore, incluye archivos nuevos sin git add) via execFile (NO exec -- decision de seguridad real, search_files no pide aprobacion asi que pattern/path del modelo no pasan por revision humana antes de ejecutarse, execFile evita inyeccion de shell al no invocar ninguna). Exit codes de git grep manejados correctamente: 0=matches, 1=sin matches (valido, no error), otro=fallback manual. Fallback manual reusa ignoredDirectories/MAX_TEXT_FILE_BYTES ya exportados de workspace-tree.ts (una sola lista de exclusion). Tope explicito SEARCH_FILES_MAX_MATCHES=200. path pasa por resolveWithinWorkspace como todas las demas tools. Sumada a EXPLORE_TOOL_NAMES (Fase 4) sin necesitar tocar runReadOnlyTool(), ya filtra generico por nombre. Verificado real con git de verdad: archivos commiteados encontrados con linea correcta, archivo untracked incluido (--untracked) sin que node_modules/ignorado por .gitignore aparezca nunca, patron inexistente da ok:true "Sin resultados." (no error), workspace sin git cae al fallback manual y encuentra igual, mas case-insensitive y path traversal bloqueado. | `src/main/tool-registry.ts`, `src/main/explore-tool.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con git real (commiteado + untracked + sin git) y ToolRegistry.execute() real, no reimplementado |
| 15 | 2026-08-24 | Runtime HTTP nuevo 'openai-chat' (Chat Completions estilo OpenAI) para type:'openrouter', origen: usuario comparte openrouter.ai/stealth/ox-alpha (modelo gratis, 1M contexto, 128K output, tool-calling real) y pide analizar y decidir. Investigado ANTES de codear: el boton "Compatible" existente (type:'openai-compatible') no sirve -- resuelve a runtime:'codex-api', que corre por codex-client.ts, el cual NUNCA lee provider.endpoint (confirmado con grep, cero resultados) y siempre le pega a la OpenAI real. Los otros 3 runtimes HTTP (foundry/anthropic-api/gemini-api) tampoco encajan -- cada uno habla el formato nativo de su proveedor, ninguno es Chat Completions. Decision confirmada con el usuario via AskUserQuestion: runtime nuevo en vez de forzar un mal ajuste. sendOpenAiApi() en api-agent-runtime.ts implementa el loop completo (tool_calls en choices[0].message, resultados como mensajes role:'tool', rol system nativo sin el hack de tag [system] que usan foundry/gemini). Wiring completo: shared/types.ts, model-capabilities.ts, ipc-agent.ts, runtime-state.ts, App.tsx (boton nuevo, modelo default stealth/ox-alpha editable, endpoint default openrouter.ai/api/v1). Verificado real con una API key real de OpenRouter provista por el usuario (usada solo en memoria, nunca escrita a archivo ni committeada): turno simple real ("OK", cost:0 real) y turno CON tool-calling real completo -- el modelo llamo read_file de verdad, el resultado simulado volvio como mensaje tool, y la respuesta final cito el contenido correctamente. | `src/shared/types.ts`, `src/shared/model-capabilities.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/main/runtime-state.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; verificado end-to-end contra el servicio real de OpenRouter, turno simple y tool-calling completo, no solo contra un mock |
| 14 (seguimiento) | 2026-08-24 | Fix del hallazgo colateral de Fase 14: compactionProviderId/compactionModelId (Fase 3) nunca se agregaron a StoredSettings/loadSettings()/saveSettings() en settings-store.ts, se perdian en cada reinicio de la app. Investigado ANTES de escribir el fix (docs/_arch/verify_compaction_settings.md): confirmado que dentro de la misma sesion andaban bien (sanitizeSettings() los preserva via spread, loadSettings() se llama una sola vez al arrancar), la perdida era solo en el roundtrip a disco -- explica un error real reportado en uso activo ("explore fallo: No hay modelo de compactacion configurado") justo despues de un reinicio de la app. Fix: mismo patron exacto que turnWatchdogSeconds, sin guard de validez (son strings, resolveConfiguredCompactionModel() ya descarta ids invalidos con find()). Verificado con bundle real: sembrado, releido, cambiado, releido tras un restart simulado -- el cambio sobrevivio el roundtrip completo. | `src/main/settings-store.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con roundtrip real a disco, sin tocar el settings.json real |
| 14 | 2026-08-23 | Timeout del watchdog de turno configurable: AppSettings.turnWatchdogSeconds (segundos) reemplaza la constante fija TURN_WATCHDOG_MS=90000, mismo criterio que maxOutputTokens (Fase 6). Default 90s si no esta configurado o el valor es invalido (0/negativo/no numerico), guard en dos puntos (UI al guardar, settings-store.ts al leer/guardar) para que nunca dispare casi instantaneo. Sin techo maximo -- el boton Detener ya cubre el caso de turno colgado. UI: seccion nueva "GENERAL" en Settings, junto a "MEMORIA". Hallazgo colateral documentado y NO corregido: compactionProviderId/compactionModelId (Fase 3) nunca se persistian en settings-store.ts, se pierden en cada reinicio -- turnWatchdogSeconds se implemento evitando ese mismo error. Ademas, fix chico separado: la descripcion de la tool run_command ahora aclara que la shell real es Windows/cmd.exe, instruye usar dir/type/del/copy en vez de ls/cat/rm/cp -- solo texto, sin cambio de logica. | `src/shared/types.ts`, `src/main/settings-store.ts`, `src/main/tool-registry.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde |
| 13 | 2026-08-23 | Nivel de esfuerzo/razonamiento configurable por turno, selector nuevo en el composer (mismo patron que sandbox mode), visible solo para claude-cli (5 niveles fijos del CLI, --effort) y codex-subscription/codex-api (catalogo real ya sincronizado, model.reasoningLevels — no hardcodeado). Investigacion previa confirmo: Claude CLI headless -p soporta --effort con efecto medible en thinking_tokens; Codex app-server usa el campo real "effort" (no "reasoningEffort") en TurnStartParams de turn/start, NO existe en thread/start (property por turno, no de conexion) — confirmado contra el schema oficial del protocolo (codex app-server generate-json-schema) y el transporte real. Sin seleccion = no se manda ningun flag/campo, mismo criterio que maxOutputTokens (Fase 6). Se agrego tambien "Claude Haiku" a defaultModels(). Verificacion end-to-end real: Claude vía la clase de produccion real dio thinking_tokens 939 (sin seleccion) / 0 (low) / 1029 (high) — confirma que "sin seleccion" es una ausencia real, no un default oculto; Codex completo turnos reales sin error con y sin effort, pero el efecto conductual cuantitativo sigue sin poder medirse (sin campo de usage/thinking en la respuesta) — documentado explicito como mitigacion con soporte estructural confirmado, no como correccion medida. | `src/renderer/src/App.tsx`, `src/main/cli-agent-runtime.ts`, `src/main/codex-client.ts`, `src/main/ipc-agent.ts`, `src/preload/index.ts`, `src/preload/index.d.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado end-to-end contra las clases reales de produccion (bundle esbuild), no scripts de investigacion standalone |
| Fix | 2026-08-23 | Fallback de deteccion de CLI (detectClaude/detectGemini/detectCodex, cli-status.ts): si el intento normal por PATH falla y es Windows, reintenta contra %APPDATA%\npm\<comando>.cmd (misma ubicacion donde installClaudeCli()/installGeminiCli() ya instalan via npm install -g), con existsSync() antes de ejecutar para no gastar el timeout de 12s en una ruta inexistente. Mitigacion para una sospecha SIN CONFIRMAR EMPIRICAMENTE (Electron instalado, lanzado desde Explorer, puede heredar un PATH de usuario desactualizado) — el usuario prefirio no correr el diagnostico manual. Si ambos caminos fallan, el detail menciona los dos intentos explicitamente. Sin regresion para quien ya detecta bien por PATH (mismo shape de retorno exacto). | `src/main/cli-status.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con bundle real contra binarios instalados de verdad en la maquina de desarrollo (4 escenarios: PATH ok, PATH roto+fallback real, ambos fallan sin archivo candidato, ambos fallan con archivo candidato roto) |
| 12 | 2026-08-23 | Fix de seguridad: el sandbox mode (read-only/workspace-write/danger-full-access) se aplicaba solo en runtimes CLI, nunca en los 3 runtimes API (foundry/anthropic-api/gemini-api) — confirmado con evidencia mecánica (grep) antes de tocar código: ExecuteContext no tenía campo sandbox, ningún caso del switch lo consultaba. Los 3 modos eran indistinguibles en runtimes API (siempre se comportaban como workspace-write). Fix: resolveApproval() centralizado en tool-registry.ts (read-only bloquea sin mostrar dialogo, danger-full-access saltea el dialogo, workspace-write sin cambios), reusado por write_file/apply_patch/run_command/revert_file y por el dispatch de tools MCP en api-agent-runtime.ts. sandbox conectado hasta ExecuteContext vía ipc-agent.ts. Test end-to-end real con conteos de invocaciones a confirm por modo: read-only=0, workspace-write=1 por accion, danger-full-access=0. | `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con bundle real y conteos concretos de confirm por modo |
| 11 | 2026-08-23 | Memoria jerárquica por tema, extiende compaction-engine.ts (Fase 3/6), no crea módulo nuevo. decisions/constraints/nextSteps pasan de 3 listas planas a agrupadas por tema (Record<nombreDeTema, MemoryTopic>); summary sigue siendo un único string sin temas. Prompt instruye reusar un tema existente si un hecho encaja ahí, crear uno nuevo solo si genuinamente no encaja en ninguno. Migración automática y sin pérdida: structured_memory en formato plano viejo se lee como tema "General" la primera vez que se toca, sin reescribir la fila hasta la próxima compactación natural. Inyección agrupada con un heading por tema en context-envelope.ts y api-agent-runtime.ts. Test end-to-end real (bundle esbuild de compaction-engine.ts+chat-store.ts, SQLite real, solo fetch mockeado): pasada 1 crea 2 temas, pasada 2 fusiona un hecho nuevo en un tema existente sin duplicarlo y crea un tema nuevo aparte, migración confirmada sin pérdida de entradas. | `src/shared/types.ts`, `src/main/chat-store.ts`, `src/main/compaction-engine.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/runtime-state.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con SQLite real de producción, cero residuos tras la corrida |
| 10 | 2026-08-23 | Cliente MCP propio (solo stdio) para los 3 runtimes API — claude-cli/codex ya tienen MCP nativo, fuera de alcance. Investigación confirmó que RpcStdioClient (Fase 9) sirve de base con un solo ajuste aditivo (envelopeExtras() para jsonrpc:"2.0"). .mcp.json mismo formato que Claude Code CLI. Tools namespaced mcp__servidor__tool, aprobación sin excepción, descubrimiento cacheado al conectar, servidor roto nunca bloquea la conexión. Test end-to-end real contra @modelcontextprotocol/server-everything vía npx: 13 tools descubiertas, tools/call real exitoso. | `src/main/mcp-client.ts`, `src/main/ipc-mcp.ts` (nuevos), `src/main/rpc-stdio-client.ts`, `src/main/api-agent-runtime.ts`, `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con servidor MCP real, no simulado |
| 9 | 2026-08-23 | Housekeeping: investigación confirmó que CodexClient y CodexAccountBridge deben seguir en procesos separados (CODEX_HOME distinto, ciclos de vida independientes, uso concurrente real) — se extrajo solo el framing JSON-RPC compartido a rpc-stdio-client.ts, texto de error preservado exacto por clase vía métodos abstractos. Borrado claude-subscription-runtime.ts (stub muerto, sin referencias vivas confirmado con grep antes de borrar). | `src/main/rpc-stdio-client.ts` (nuevo), `src/main/codex-client.ts`, `src/main/codex-account-bridge.ts`, `src/main/claude-subscription-runtime.ts` (borrado) | Completa — `npm run typecheck` y `npm run build` en verde; cero cambio de comportamiento observable |
| 8 | 2026-08-23 | VCS local oculto: git como motor interno invisible, repo privado por workspace en D:\AMATISTA\data\vcs\<id> (nunca en el proyecto real). Snapshot síncrono en write_file/apply_patch (commit del original en el primer toque, sin bloquear la escritura si falla). Tools list_file_history (sin aprobación) y revert_file (con aprobación, mismo diálogo de diff). Retención ilimitada, sin UI de línea de tiempo en esta fase. | `src/main/local-vcs.ts` (nuevo), `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; test end-to-end contra el módulo real (bundleado standalone con esbuild): 3 versiones tras 2 ediciones, revert restauró el original byte a byte, 4 versiones tras el revert (nunca borra), git status del repo real del proyecto de prueba limpio sin rastro del repo oculto |
| 7 | 2026-08-23 | Soporte AGENTS.md por proyecto (estándar real, texto plano). Tarea 0 confirmó empíricamente que claude-cli NO lo lee solo (codex-cli SÍ) — inyección manual solo donde hace falta: claude-cli, gemini-cli, y los 3 runtimes API; codex-subscription/codex-api sin tocar. Lectura cacheada una vez por conexión. Aviso (nunca truncado) si supera ~200 líneas. Botón en topbar para crear/abrir en el editor del sistema. | `src/main/agents-md.ts`, `src/main/ipc-agents-md.ts` (nuevos), `src/main/runtime-state.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `src/shared/types.ts` | Completa — `npm run typecheck` y `npm run build` en verde; Tarea 0 verificada con los binarios reales (claude 2.1.226, codex-cli 0.147.0), no asumida de la documentación |
| 6 | 2026-08-19 | Extracción estructurada en compaction-engine.ts: la misma llamada de compactación (Fase 3) ahora pide/devuelve JSON con summary + decisions/constraints/nextSteps acumulativos. Fallback a texto plano si el JSON es inválido, sin vaciar las listas. Persistido en chat_sessions.structured_memory. Inyectado en el contexto de los 3 runtimes API (no solo context-envelope.ts, extensión necesaria — ver CONTRACT.md). Extiende el contrato v2, no crea v3. | `src/main/chat-store.ts`, `src/main/compaction-engine.ts`, `src/main/context-envelope.ts`, `src/main/runtime-state.ts`, `src/main/api-agent-runtime.ts`, `src/shared/types.ts` | Completa — `npm run typecheck` y `npm run build` en verde; parseo JSON/fallback verificado con script standalone |
| 5 | 2026-08-19 | Tool "apply_patch": edición search/replace exacto (old_str/new_str, ocurrencia única obligatoria, comparación normalizada \r\n↔\n, preserva estilo de salto de línea original al escribir) para bajar tokens de salida vs. regenerar el archivo completo con write_file. write_file sin cambios, coexisten. | `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; lógica de match/CRLF verificada con script standalone |
| 4 | 2026-08-19 | Tool "explore": delega búsqueda/lectura repetitiva (read_file/list_dir/git_status/git_diff, nunca write_file/run_command) al modelo barato de Fase 3, en un mini-loop propio acotado (10 iteraciones), sin paralelismo, con resultado condensado y fallback a error claro si no hay modelo configurado. | `src/main/explore-tool.ts` (nuevo), `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/main/compaction-engine.ts`, `src/main/ipc-agent.ts` | Completa — `npm run typecheck` y `npm run build` en verde |
| 3 | 2026-08-19 | Memoria/contexto real: compactación asíncrona vía LLM para runtimes API, persistida en SQLite (summary + watermark por id de mensaje), reemplaza el truncado de App.tsx. Constante de presupuesto de contexto centralizada en src/shared/. Modelo de compactación configurable en Settings. | `src/shared/context-budget.ts`, `src/shared/model-capabilities.ts` (nuevos), `src/main/compaction-engine.ts` (nuevo), `src/shared/types.ts`, `src/main/chat-store.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; ver PENDING.md por el caveat de hueco transitorio en backfill y el ítem CLI sin priorizar |
| 2 | 2026-08-19 | Refactor de index.ts en 10 módulos por responsabilidad, sin cambio de comportamiento salvo el ítem anotado en PENDING | `src/main/runtime-state.ts`, `src/main/settings-provisioning.ts`, `src/main/attachments.ts`, `src/main/workspace-tree.ts`, `src/main/ipc-window.ts`, `src/main/ipc-settings.ts`, `src/main/ipc-chats.ts`, `src/main/ipc-cli.ts`, `src/main/ipc-projects-workspace.ts`, `src/main/ipc-attachments.ts`, `src/main/ipc-agent.ts` (nuevos), `src/main/index.ts` (reducido a bootstrap) | Completa — `npm run build` (typecheck + electron-vite build) en verde; ver PENDING.md por el caso límite no 100% verificado |
| 1 (Tarea C) | 2026-08-19 | Fase 1 Tarea C: confirmado con evidencia que compactSummary no genera resumen real, pérdida de contexto más allá de ~26 mensajes en runtimes API | `docs/_arch/PENDING.md`, `docs/_arch/CONTRACT.md`, `docs/_arch/HISTORY.md` | hallazgo documentado, fix pendiente de diseño |
