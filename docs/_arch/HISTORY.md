# HISTORY.md — Bitácora de Fases

> Log append-only. Una entrada por fase ejecutada.

| Fase | Fecha | Resumen | Archivos tocados | Estado |
|---|---|---|---|---|
| 13 | 2026-08-23 | Nivel de esfuerzo/razonamiento configurable por turno, selector nuevo en el composer (mismo patron que sandbox mode), visible solo para claude-cli (5 niveles fijos del CLI, --effort) y codex-subscription/codex-api (catalogo real ya sincronizado, model.reasoningLevels — no hardcodeado). Investigacion previa confirmo: Claude CLI headless -p soporta --effort con efecto medible en thinking_tokens; Codex app-server usa el campo real "effort" (no "reasoningEffort") en TurnStartParams de turn/start, NO existe en thread/start (property por turno, no de conexion) — confirmado contra el schema oficial del protocolo (codex app-server generate-json-schema) y el transporte real. Sin seleccion = no se manda ningun flag/campo, mismo criterio que maxOutputTokens (Fase 6). Se agrego tambien "Claude Haiku" a defaultModels(). Verificacion end-to-end real: Claude vía la clase de produccion real dio thinking_tokens 939 (sin seleccion) / 0 (low) / 1029 (high) — confirma que "sin seleccion" es una ausencia real, no un default oculto; Codex completo turnos reales sin error con y sin effort, pero el efecto conductual cuantitativo sigue sin poder medirse (sin campo de usage/thinking en la respuesta) — documentado explicito como mitigacion con soporte estructural confirmado, no como correccion medida. | `src/renderer/src/App.tsx`, `src/main/cli-agent-runtime.ts`, `src/main/codex-client.ts`, `src/main/ipc-agent.ts`, `src/preload/index.ts`, `src/preload/index.d.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado end-to-end contra las clases reales de produccion (bundle esbuild), no scripts de investigacion standalone |
| Fix | 2026-08-23 | Fallback de deteccion de CLI (detectClaude/detectGemini/detectCodex, cli-status.ts): si el intento normal por PATH falla y es Windows, reintenta contra %APPDATA%\npm\<comando>.cmd (misma ubicacion donde installClaudeCli()/installGeminiCli() ya instalan via npm install -g), con existsSync() antes de ejecutar para no gastar el timeout de 12s en una ruta inexistente. Mitigacion para una sospecha SIN CONFIRMAR EMPIRICAMENTE (Electron instalado, lanzado desde Explorer, puede heredar un PATH de usuario desactualizado) — el usuario prefirio no correr el diagnostico manual. Si ambos caminos fallan, el detail menciona los dos intentos explicitamente. Sin regresion para quien ya detecta bien por PATH (mismo shape de retorno exacto). | `src/main/cli-status.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con bundle real contra binarios instalados de verdad en la maquina de desarrollo (4 escenarios: PATH ok, PATH roto+fallback real, ambos fallan sin archivo candidato, ambos fallan con archivo candidato roto) |
| 12 | 2026-08-23 | Fix de seguridad: el sandbox mode (read-only/workspace-write/danger-full-access) se aplicaba solo en runtimes CLI, nunca en los 3 runtimes API (foundry/anthropic-api/gemini-api) — confirmado con evidencia mecánica (grep) antes de tocar código: ExecuteContext no tenía campo sandbox, ningún caso del switch lo consultaba. Los 3 modos eran indistinguibles en runtimes API (siempre se comportaban como workspace-write). Fix: resolveApproval() centralizado en tool-registry.ts (read-only bloquea sin mostrar dialogo, danger-full-access saltea el dialogo, workspace-write sin cambios), reusado por write_file/apply_patch/run_command/revert_file y por el dispatch de tools MCP en api-agent-runtime.ts. sandbox conectado hasta ExecuteContext vía ipc-agent.ts. Test end-to-end real con conteos de invocaciones a confirm por modo: read-only=0, workspace-write=1 por accion, danger-full-access=0. | `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con bundle real y conteos concretos de confirm por modo |
| 11 | 2026-08-23 | Memoria jerárquica por tema, extiende compaction-engine.ts (Fase 3/6), no crea módulo nuevo. decisions/constraints/nextSteps pasan de 3 listas planas a agrupadas por tema (Record<nombreDeTema, MemoryTopic>); summary sigue siendo un único string sin temas. Prompt instruye reusar un tema existente si un hecho encaja ahí, crear uno nuevo solo si genuinamente no encaja en ninguno. Migración automática y sin pérdida: structured_memory en formato plano viejo se lee como tema "General" la primera vez que se toca, sin reescribir la fila hasta la próxima compactación natural. Inyección agrupada con un heading por tema en context-envelope.ts y api-agent-runtime.ts. Test end-to-end real (bundle esbuild de compaction-engine.ts+chat-store.ts, SQLite real, solo fetch mockeado): pasada 1 crea 2 temas, pasada 2 fusiona un hecho nuevo en un tema existente sin duplicarlo y crea un tema nuevo aparte, migración confirmada sin pérdida de entradas. | `src/shared/types.ts`, `src/main/chat-store.ts`, `src/main/compaction-engine.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/runtime-state.ts` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con SQLite real de producción, cero residuos tras la corrida |
| 10 | 2026-08-23 | Cliente MCP propio (solo stdio) para los 3 runtimes API — claude-cli/codex ya tienen MCP nativo, fuera de alcance. Investigación confirmó que RpcStdioClient (Fase 9) sirve de base con un solo ajuste aditivo (envelopeExtras() para jsonrpc:"2.0"). .mcp.json mismo formato que Claude Code CLI. Tools namespaced mcp__servidor__tool, aprobación sin excepción, descubrimiento cacheado al conectar, servidor roto nunca bloquea la conexión. Test end-to-end real contra @modelcontextprotocol/server-everything vía npx: 13 tools descubiertas, tools/call real exitoso. | `src/main/mcp-client.ts`, `src/main/ipc-mcp.ts` (nuevos), `src/main/rpc-stdio-client.ts`, `src/main/api-agent-runtime.ts`, `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx` | Completa — `npm run typecheck` y `npm run build` en verde; verificado con servidor MCP real, no simulado |
| 9 | 2026-08-23 | Housekeeping: investigación confirmó que CodexClient y CodexAccountBridge deben seguir en procesos separados (CODEX_HOME distinto, ciclos de vida independientes, uso concurrente real) — se extrajo solo el framing JSON-RPC compartido a rpc-stdio-client.ts, texto de error preservado exacto por clase vía métodos abstractos. Borrado claude-subscription-runtime.ts (stub muerto, sin referencias vivas confirmado con grep antes de borrar). | `src/main/rpc-stdio-client.ts` (nuevo), `src/main/codex-client.ts`, `src/main/codex-account-bridge.ts`, `src/main/claude-subscription-runtime.ts` (borrado) | Completa — `npm run typecheck` y `npm run build` en verde; cero cambio de comportamiento observable |
| 8 | 2026-08-23 | VCS local oculto: git como motor interno invisible, repo privado por workspace en D:\AMATISTA\data\vcs\<id> (nunca en el proyecto real). Snapshot síncrono en write_file/apply_patch (commit del original en el primer toque, sin bloquear la escritura si falla). Tools list_file_history (sin aprobación) y revert_file (con aprobación, mismo diálogo de diff). Retención ilimitada, sin UI de línea de tiempo en esta fase. | `src/main/local-vcs.ts` (nuevo), `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; test end-to-end contra el módulo real (bundleado standalone con esbuild): 3 versiones tras 2 ediciones, revert restauró el original byte a byte, 4 versiones tras el revert (nunca borra), git status del repo real del proyecto de prueba limpio sin rastro del repo oculto |
| 7 | 2026-08-23 | Soporte AGENTS.md por proyecto (estándar real, texto plano). Tarea 0 confirmó empíricamente que claude-cli NO lo lee solo (codex-cli SÍ) — inyección manual solo donde hace falta: claude-cli, gemini-cli, y los 3 runtimes API; codex-subscription/codex-api sin tocar. Lectura cacheada una vez por conexión. Aviso (nunca truncado) si supera ~200 líneas. Botón en topbar para crear/abrir en el editor del sistema. | `src/main/agents-md.ts`, `src/main/ipc-agents-md.ts` (nuevos), `src/main/runtime-state.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/ipc-agent.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `src/shared/types.ts` | Completa — `npm run typecheck` y `npm run build` en verde; Tarea 0 verificada con los binarios reales (claude 2.1.226, codex-cli 0.147.0), no asumida de la documentación |
| 6 | 2026-08-19 | Extracción estructurada en compaction-engine.ts: la misma llamada de compactación (Fase 3) ahora pide/devuelve JSON con summary + decisions/constraints/nextSteps acumulativos. Fallback a texto plano si el JSON es inválido, sin vaciar las listas. Persistido en chat_sessions.structured_memory. Inyectado en el contexto de los 3 runtimes API (no solo context-envelope.ts, extensión necesaria — ver CONTRACT.md). Extiende el contrato v2, no crea v3. | `src/main/chat-store.ts`, `src/main/compaction-engine.ts`, `src/main/context-envelope.ts`, `src/main/runtime-state.ts`, `src/main/api-agent-runtime.ts`, `src/shared/types.ts` | Completa — `npm run typecheck` y `npm run build` en verde; parseo JSON/fallback verificado con script standalone |
| 5 | 2026-08-19 | Tool "apply_patch": edición search/replace exacto (old_str/new_str, ocurrencia única obligatoria, comparación normalizada \r\n↔\n, preserva estilo de salto de línea original al escribir) para bajar tokens de salida vs. regenerar el archivo completo con write_file. write_file sin cambios, coexisten. | `src/main/tool-registry.ts` | Completa — `npm run typecheck` y `npm run build` en verde; lógica de match/CRLF verificada con script standalone |
| 4 | 2026-08-19 | Tool "explore": delega búsqueda/lectura repetitiva (read_file/list_dir/git_status/git_diff, nunca write_file/run_command) al modelo barato de Fase 3, en un mini-loop propio acotado (10 iteraciones), sin paralelismo, con resultado condensado y fallback a error claro si no hay modelo configurado. | `src/main/explore-tool.ts` (nuevo), `src/main/tool-registry.ts`, `src/main/api-agent-runtime.ts`, `src/main/compaction-engine.ts`, `src/main/ipc-agent.ts` | Completa — `npm run typecheck` y `npm run build` en verde |
| 3 | 2026-08-19 | Memoria/contexto real: compactación asíncrona vía LLM para runtimes API, persistida en SQLite (summary + watermark por id de mensaje), reemplaza el truncado de App.tsx. Constante de presupuesto de contexto centralizada en src/shared/. Modelo de compactación configurable en Settings. | `src/shared/context-budget.ts`, `src/shared/model-capabilities.ts` (nuevos), `src/main/compaction-engine.ts` (nuevo), `src/shared/types.ts`, `src/main/chat-store.ts`, `src/main/context-envelope.ts`, `src/main/api-agent-runtime.ts`, `src/main/runtime-state.ts`, `src/main/ipc-agent.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completa — `npm run typecheck` y `npm run build` en verde; ver PENDING.md por el caveat de hueco transitorio en backfill y el ítem CLI sin priorizar |
| 2 | 2026-08-19 | Refactor de index.ts en 10 módulos por responsabilidad, sin cambio de comportamiento salvo el ítem anotado en PENDING | `src/main/runtime-state.ts`, `src/main/settings-provisioning.ts`, `src/main/attachments.ts`, `src/main/workspace-tree.ts`, `src/main/ipc-window.ts`, `src/main/ipc-settings.ts`, `src/main/ipc-chats.ts`, `src/main/ipc-cli.ts`, `src/main/ipc-projects-workspace.ts`, `src/main/ipc-attachments.ts`, `src/main/ipc-agent.ts` (nuevos), `src/main/index.ts` (reducido a bootstrap) | Completa — `npm run build` (typecheck + electron-vite build) en verde; ver PENDING.md por el caso límite no 100% verificado |
| 1 (Tarea C) | 2026-08-19 | Fase 1 Tarea C: confirmado con evidencia que compactSummary no genera resumen real, pérdida de contexto más allá de ~26 mensajes en runtimes API | `docs/_arch/PENDING.md`, `docs/_arch/CONTRACT.md`, `docs/_arch/HISTORY.md` | hallazgo documentado, fix pendiente de diseño |
