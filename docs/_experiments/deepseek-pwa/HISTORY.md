# HISTORY.md — Bitácora del experimento DeepSeek PWA

> Riesgo real de violar ToS de DeepSeek, decisión consciente del usuario. Nació como experimento aislado; integrado a master el 2026-09-23 (paso 7). Log append-only, entrada más reciente arriba.

| Paso | Fecha | Resumen | Archivos | Estado |
|---|---|---|---|---|
| 7 | 2026-09-23 | **Integración a master.** Rama rebasada sobre master (trajo `39a5b25`, sin conflictos; la implementación sin commitear quedó intacta, misma huella del diff antes y después; typecheck, build y tests en el mismo estado: 47/51, las 4 fallas de F0). Hueco de permisos cerrado en `persist:deepseek-pwa` (mismos handlers que deniegan todo en `39a5b25`). **Verificado real:** control `granted`→`denied`; login real del usuario con permisos denegados; dentro de la vista real conectada notificaciones y geolocalización `denied`; streaming intacto. Docs del experimento actualizados al estado integrado. Commit de integración + `git merge --ff-only` a master (sin push). | `src/main/deepseek-pwa-runtime.ts`, `CONTRACT.md`, `HISTORY.md`, `PENDING.md` | Integrado en local; push pendiente de ok |
| 6 | 2026-09-23 | **Ajuste de UI (solo presentación; la lógica de seguridad no cambia).** La advertencia completa ya vivía en Configuración desde el paso 5; lo que sobraba era el aviso del chat, que repetía "(experimental, no oficial)". Quedó reducido a un recordatorio mínimo de una línea ("DeepSeek PWA · sin herramientas") + el botón "Ver DeepSeek" (necesario para login/captcha, opción b). El guard doble de `deepseekPwaAcknowledged` sigue exactamente igual. **Verificado real 5/5** (build real, carpeta temporal nueva, sin conectar a DeepSeek): sin aceptar no hay botón; la advertencia completa aparece en Configuración antes de aceptar; tras aceptar se crea la conexión; en el chat ya no hay texto de advertencia, solo el recordatorio y el botón. | `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css` | Completo; sin commit (va en el commit de integración) |
| 5 | 2026-09-23 | **Puente implementado** (opción A, vista-con-sesión, panel opción b). Primer borrador con pool de vistas (tope 3) descartado por corrección de diseño del usuario antes de conectarlo; el parser se reusó tal cual. Runtime `'deepseek-pwa'`: vista por sesión (mismo ciclo que el navegador embebido), escritura por DOM, lectura por red (CDP), camino propio en `dispatchTurnForWindow()`, `remote_session_id` persistido, DeepThink en el selector de esfuerzo, aviso "sin herramientas" + "Ver DeepSeek". `deepseekPwaAcknowledged` con guard doble (UI + main). **Verificación real 7/7** con build real, login manual del usuario en el panel y 5 reinicios reales: streaming en vivo (125→1.369 chars con el turno activo), cancelación real en 574 ms (`INCOMPLETE`, sin duplicado), 2 paneles simultáneos sin mezcla, DeepThink separado (`thinking_enabled=true`, respuesta "1081" sola), sesión y conversación que sobreviven el reinicio (DeepSeek recordó "Océanos"), errores honestos (formato irreconocible / HTTP 500 simulados), guard doble (UI sin botón; main rechaza el IPC directo sin crear vista). **5 bugs reales encontrados y corregidos:** falso positivo de captcha (script AWS WAF en todas las páginas), riesgo de recursión, etiquetas vacías en vista oculta (`innerText` → `textContent`), el botón "Detener" desaparecía con el primer delta (renderer compartido: `endsTurn`), contenido de respuestas no-2xx transmitido en vivo. Carpeta temporal y sesión de prueba borradas al terminar. | `src/main/deepseek-pwa-runtime.ts` (nuevo), `src/main/deepseek-pwa-stream.ts` (nuevo), `src/main/{ipc-agent,runtime-state,chat-store,settings-store,ipc-settings}.ts`, `src/shared/{types,runtime-for,model-capabilities}.ts`, `src/preload/index{.ts,.d.ts}`, `src/renderer/src/App.tsx`, `src/renderer/src/assets/main.css`, `tests/regression/deepseek-pwa-stream.test.ts` + fixtures | Completo, verificado real (7/7); sin commit |
| 4 | 2026-09-23 | **Los 5 pendientes del diseño**, probados real (harnesses `t5_pending/`, vistas ocultas desde el inicio, stream por CDP, detector de cuelgue, cero procesos colgados). **P1 ✅** 2 vistas ocultas simultáneas: 86 y 60 fragmentos, `FINISHED`, sin contaminación cruzada. **P2 ✅** el stream distingue `THINK`/`RESPONSE` por tipo de fragmento (1.388/754 chars en el prompt largo, 0 fuera de fragmento); quedaron especificadas las reglas del parser; toggle funcional oculto y restaurado a apagado (verificado). Hallazgo: la UI cambió de español a inglés entre sesiones de la misma partición, así que los toggles se eligen por posición (regla obligatoria). **P3 ✅** Detener oculto → `POST /api/v0/chat/stop_stream` 200, `INCOMPLETE` en 767 ms, respuesta parcial guardada. **P4 ⚠️** 8 envíos seguidos (~29/min) sin límite ni ninguna respuesta que no fuera 200; se cortó a propósito en 8 por riesgo de la cuenta. **P5 ⚠️** cuelgue de 60 s no reproducido (0 en 12 generaciones, ~20 en total). | `CONTRACT.md`, `HISTORY.md`, `PENDING.md` | Completo; el diseño queda listo para implementar, con 2 residuales no bloqueantes |
| 3 | 2026-09-23 | **Diseño del puente, opción A** (sin implementar). T1: runtime `'deepseek-pwa'` con branch propio en `dispatchTurnForWindow()`, sin `wire*`; reusa F0/F1/backstop/cancelación; chat puro sin tools. T2: la UI ya soporta streaming (varios `item/agentMessage/delta` con el mismo `itemId` se concatenan; `item/completed` reemplaza), así que no hay caso especial; trampa real: los deltas que son solo espacios se descartan en el renderer. T3: "Pensamiento Profundo" va al selector de esfuerzo existente; búsqueda fija en apagado; los toggles son globales a la partición, así que se fija el estado antes de cada envío, con mutex. **T4, probado real con 5 harnesses:** cambio de conversación ✅ (`loadURL` ~1,7 s, click SPA 0,45 s); 2 generaciones simultáneas ✅ con 2 vistas visibles. **En vista oculta el servidor genera y guarda, pero el DOM no se pinta**, así que leer por DOM queda descartado. **Stream leído en vivo por CDP en una vista nunca visible ✅** (123 fragmentos, `FINISHED`), por eso la decisión de T2 cambió a "escribir por DOM, leer por red". Un harness colgado resultó ser un bug propio (`await Network.enable` sin página cargada), corregido. T5: la verdad sale del stream (200 + `FINISHED` + contenido); login/captcha → se muestra la vista para intervención humana; sin reintentos. | `CONTRACT.md`, `HISTORY.md`, `PENDING.md` | Diseño completo; 5 puntos a confirmar en `PENDING.md` |
| 2 | 2026-09-23 | Prueba 2 (2º prompt, con permiso explícito): `Enumerá del 1 al 40 con una frase cada uno`, con "Pensamiento Profundo" encendido solo para la prueba y restaurado después (verificado). `MutationObserver` real + muestreo cada 150 ms. El razonamiento se transmite primero (0→2838 chars, ~4,8 s, con la respuesta vacía), después la respuesta (171→1389 chars, 40 líneas). Todas las mutaciones de la respuesta son `childList` (81) y ninguna `characterData`, así que un observer `childList` avisa cada fragmento. Append-only: 0 violaciones. El bloque de razonamiento es un hijo aparte del `.ds-message` (clase hasheada) y se excluye leyendo solo `.ds-assistant-message-main-content`. La señal de fin por ícono se confirmó como la única robusta: un detector por texto estable habría fallado durante el razonamiento. | `CONTRACT.md`, `HISTORY.md`, `PENDING.md` | Completo |
| 1 | 2026-09-23 | Viabilidad de las Tareas 1-4. **T1:** harness Electron standalone, 3 particiones × cookies persistentes/de sesión/`localStorage`/IndexedDB, 2 procesos. `persist:` conserva todo menos las cookies de sesión. Después, el login real del usuario (Google) sobrevivió un reinicio real: 39 chats cargados del servidor, sin re-login, gracias a `userToken` en `localStorage`. **T2:** DOM mapeado por CDP y 1 prompt de prueba inocuo anunciado. Selectores: `textarea`, `.ds-button--primary.ds-button--circle`, `.ds-assistant-message-main-content` en lista virtual. Señal de fin: el ícono del botón vuelve de "detener" a "enviar". **T3:** anti-bot real presente (AWS WAF, `smidV2`, flag hCaptcha); baneos documentados solo para HTTP crudo; ninguna fuente de reportes verificada resultó inexistente. **T4:** ToS oficiales del 2026-03-27, §3.5(3) prohíbe la automatización, §8.2 sanciones. Hallazgo lateral: el navegador embebido de Amatista usa la sesión default persistente, no una aislada. | `CONTRACT.md`, `HISTORY.md`, `PENDING.md` (harnesses en el scratchpad de la sesión, fuera del repo) | Completo; falta confirmar el streaming largo (ver `PENDING.md`) |
| 0 | 2026-09-23 | Estructura de documentación separada creada (`docs/_experiments/deepseek-pwa/`), aislada de `docs/_arch/`. | `CONTRACT.md`, `HISTORY.md`, `PENDING.md` | Completo |
