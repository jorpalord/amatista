# HISTORY.md — Bitácora del experimento deepseek-pwa-tools

> Investigación de viabilidad para ampliar el puente ya integrado (`docs/_experiments/deepseek-pwa/`) con imágenes/archivos/código reales. Log append-only, entrada más reciente arriba.

| Paso | Fecha | Resumen | Archivos | Estado |
|---|---|---|---|---|
| 4 | 2026-09-23 | **Corrección de alcance sobre el paso 3:** el catálogo pasa de 4 tools fijas a **las 47 reales de `TOOL_DEFINITIONS`** (confirmado con grep), en formato compacto (una línea por tool, primera oración real de su descripción, recortada a 160 caracteres) diseñado a medida (no existía un precedente compacto en el código, confirmado por búsqueda) -- reusa los MISMOS 3 filtros de visibilidad que `ApiAgentRuntime.toolCatalog()` ya aplica (orquestador solo en chat principal, web-search solo con Tavily, `exit_plan_mode` solo en modo plan), sin ocultar nunca Familia A/B/navegador (mismo criterio que esa función). El `ExecuteContext` armado en el branch `deepseek-pwa` de `ipc-agent.ts` pasa de minimo (`workspace/sandbox/sessionId/confirm`) a incluir los MISMOS closures reales que ya arma el camino API: `hardConfirm` (Capa 2 real de Familia B), `computerUseActive`/`computerUseBegin`/`computerUseEnd`/`computerUseAbortSignal` (Familia A), navegador embebido (4 closures), mas `generateImage`/`webSearch`/`webFetch`/`listWindows`/`writeTodos`/`exitPlanMode`/`resolveExploreModel` (cheap de wirear, sin infraestructura nueva) -- `lspManager`/`terminalExec` quedan deliberadamente sin infraestructura propia (esos managers solo se crean para runtimes API, ampliacion aparte no pedida), degradan solos con su mensaje honesto ya existente. **Verificado real, mismos 6 puntos de siempre + 2 nuevos:** (1-6) re-verificados con el catalogo ampliado -- cadena real con una tool NUEVA (`run_command` para reunir info del sistema, con 2 dialogos de aprobacion reales aprobados), confinamiento, UI en vivo, formato malformado, no-regresion, todos consistentes con el paso 3; (7) **Familia B alcanzable pero gateada** -- pedido directo de `lock_screen()` genero el dialogo real de `hardConfirm` (mismo texto exacto de `tool-registry.ts`), **rechazado a proposito**, la sesion de Windows NUNCA se bloqueo real (confirmado: la conexion CDP siguio viva), DeepSeek recibio el rechazo y no insistio; (8) **Familia A gateada en Capa 1** -- pedido directo de `screenshot()` bloqueo de raiz SIN mostrar ningun dialogo (computerUseActive nunca armado en esta conexion de prueba), DeepSeek recibio el mensaje honesto de "no activado". `npm run typecheck`/`npm run build` limpios. | `src/main/deepseek-pwa-runtime.ts`, `src/main/ipc-agent.ts`, `src/renderer/src/App.tsx` | Completo -- verificado real (6/6 + 2/2 nuevos de seguridad); sin commit |
| 3 | 2026-09-23 | **Implementación real del puente de tool-calling por texto** (sobre el diseño ya medido del paso 2). `deepseek-pwa-runtime.ts`: `buildToolProtocolInstructions()` (reusa las descripciones REALES de `TOOL_DEFINITIONS` para read_file/write_file/list_dir/run_command) + `parseTextToolCall()` (parser ya medido, con el fix real de desescapar `\n`/`\t`). `ipc-agent.ts`: el branch `deepseek-pwa` de `dispatchTurnForWindow()` pasa a un loop real (tope `settings.maxToolLoop ?? 20`) -- cada `TOOL_CALL` real se ejecuta via el MISMO `toolRegistry.execute()`/`resolveApproval()`/sandbox que ya usan todos los demás runtimes (ExecuteContext SIN `hardConfirm`, asi que Familia A/B queda bloqueada sola, sin logica nueva) -- un solo `itemId`/turno visible desde la UI sin importar cuantas rondas reales hicieron falta, con streaming en vivo "gateado" para nunca filtrar el texto crudo del protocolo. `App.tsx`: aviso actualizado ("sin herramientas" -> "tools por protocolo de texto"). **Verificado real, los 6 puntos pedidos:** (1) 2 tools reales encadenadas (list_dir->read_file) con respuesta final correcta; (2) **el mas importante** -- dialogo real de aprobacion confirmado en Aprobar (archivo real creado solo despues del click, diff real mostrado) Y en Rechazar (archivo real nunca creado, DeepSeek recibio y entendio el rechazo real); (3) confinamiento real (ruta fuera del workspace rechazada con el mismo mensaje de siempre, secreto real nunca expuesto); (4) UI en vivo real ("Pensando..." -> "X completado", resumen colapsable real de pasos); (5) formato malformado forzado a proposito -> nota honesta real, cero reintento automatico; (6) no-regresion real (pregunta sin necesidad de tools -> respuesta limpia de un mensaje, sin ningun rastro del protocolo). Decision de diseño documentada: sin toggle de "modo tools" -- el protocolo siempre viaja, DeepSeek decide solo si hace falta. `npm run typecheck`/`npm run build` limpios. | `src/main/deepseek-pwa-runtime.ts`, `src/main/ipc-agent.ts`, `src/renderer/src/App.tsx` | Completo -- verificado real (6/6 puntos, con enfasis especial en el dialogo de aprobacion real); sin commit |
| 2 | 2026-09-23 | Investigación empírica real de tool-calling por TEXTO (idea del usuario: "enseñarle" a DeepSeek un protocolo de tools por texto plano, ya que no tiene function calling nativo por PWA). Protocolo diseñado reusando read_file/write_file/list_dir/run_command reales (mismo confinamiento de workspace) con formato `TOOL_CALL: nombre(args)` / `TOOL_RESULT: <resultado>`. **8 tareas reales corridas contra la cuenta real** (workspace 100% sintético, nunca contenido real del usuario), con 1/2/3+ tools encadenadas cada una. **Resultado: 8/8 tareas con la cadena de tools EXACTA esperada, 15/15 llamadas a herramientas en el formato pedido EXACTO (re-verificado real sobre 6/15 con un lector corregido, 100% limpio), 0 desvíos de formato, 0 respuestas inventadas sin pedir la tool que hacía falta.** Manejo de error real probado (Tarea 7: archivo inexistente, recuperación correcta sin insistir ni inventar). Único fallo real (1 de 9 corridas): un corte de stream del lado de DeepSeek ajeno al protocolo, ya manejado honesto por el runtime existente, resuelto con un reintento limpio. **2 hallazgos reales de esta misma investigación, no de DeepSeek:** (a) el "formato sucio" que parecía verse al principio era un artefacto del propio lector (`.textContent` mezclaba los íconos Copiar/Regenerar de la UI de Amatista con el texto real) -- corregido clonando el nodo y removiendo esos hermanos antes de leer; (b) el harness no desconectaba un chat de prueba al terminar, chocando en vivo contra el guard real de `MAX_CONCURRENT_SESSIONS=8` (F0) -- diagnosticado con el aviso del usuario en el momento, corregido reusando `selectModel()` (ya dispara `disconnect()` real) al final de cada tarea. Riesgo (Tarea 4): extensión natural, sin superficie de detección nueva -- más turnos por tarea, pero un patrón más parecido a un humano real (mensajes cortos seguidos), no menos. ~13 conversaciones reales nuevas creadas en la cuenta (todas con prompts inocuos sobre el workspace sintético), ninguna existente tocada. | `docs/_experiments/deepseek-pwa-tools/CONTRACT.md` | Completo -- medición real (8/8 tareas, 15/15 tool calls correctas); puente completo sin implementar, decisión pendiente del usuario |
| 1 | 2026-09-23 | Investigación de viabilidad completa (4 tareas), rama creada desde `master` (ya con `39a5b25`/`071acec`/`211e66a`). Login real del usuario (Google) en la app real (`D:\AMATISTA\data`). **T1:** sin canvas/artifact -- código = markdown puro, con 2 controles reales del design system (`div[role=button]` "Copiar"/"Descargar") en la barra del bloque de código. **T2:** input de archivo real (`<input type=file multiple>`, `accept` de +600 extensiones, imágenes y código y documentos mezclados) -- automatizable con `DOM.setFileInputFiles` (CDP), mismo mecanismo ya usado para leer el stream. **T3:** el botón "Descargar" SÍ dispara un `will-download` real (Blob, nombre autogenerado `deepseek_<lenguaje>_<fecha>_<hash>.<ext>`) pero se CANCELA SOLO -- 3/3 intentos reales, con y sin intervención propia -- probablemente por conflicto con el `webContents.debugger` que el runtime ya tiene adjunto para leer el stream. Hallazgo real más allá de lo pedido: el botón "Descargar" de la PWA real está roto HOY dentro de cualquier chat manejado por Amatista, sin que el usuario lo haya notado (su reporte fue de uso fuera de Amatista). **T4:** extensión natural del riesgo ya aceptado, sin superficie de detección nueva -- el único hallazgo que cambia algo real es el bug de T3 (funcionalidad rota, no riesgo nuevo). **Incidente real documentado con transparencia:** un selector de target CDP ingenuo conectó por error a la vista real de DeepSeek en vez de a Amatista, exponiendo brevemente en la salida de una herramienta los títulos reales de las conversaciones de la cuenta -- corregido de inmediato (selección de target por URL exacta) y confirmado en vivo con el usuario antes de seguir. Se creó una única conversación de prueba nueva (prompt inocuo), sin tocar ninguna existente. Carga de imágenes NO probada en vivo (mecanismo confirmado, sin subir un archivo real a la cuenta). | `docs/_experiments/deepseek-pwa-tools/CONTRACT.md` | Completo -- investigación pura, cero cambios en `src/`; decisión de implementar pendiente del usuario |
