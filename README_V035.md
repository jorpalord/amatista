# Universal Agent Studio V0.3.5

Corrección crítica:
- el composer queda siempre visible;
- solo la lista de mensajes hace scroll;
- seleccionar un workspace ya no empuja el chat fuera del viewport;
- Claude/Gemini no abren consola si sus CLI no están instalados;
- el botón de login se desactiva y muestra que primero hay que instalar el CLI.

Prueba:
```powershell
npm run typecheck
npm run dev
```
